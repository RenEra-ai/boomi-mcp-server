I’ll review only the correction delta from `153c8aa4…` to `83eb6ff…`, then run focused read-only tests/probes around the oracle’s multi-symbol behavior and the reachability direction. The repository’s referenced `receiving-code-review` skill is not available in this session, so I’ll apply its verify-or-refute discipline directly.The core reachability premise checks out so far: production `SymbolTableV1` sorts every participant by `ref`, and profile derivation scans that whole tuple; apply re-derives with the same function. I’m now checking whether the new probe set faithfully represents every valid table shape it claims to cover and whether its structural guards can pass vacuously.I found one concrete false-positive direction to test: the oracle passes a `SimpleNamespace` instead of a real `SymbolTableV1`. A behaviorally identical lookup can legitimately use the table’s documented `build_index()` API; if that refactor changes the served revision solely because the stand-in lacks the API, callers pay a revision rotation for no compiler behavior change. I’m reproducing that now before deciding whether it is a finding.The reachability argument is correct: I compiled a valid scheduled root with a six-symbol table containing an unrelated WSS-family operation; a wrong-row lookup changed the derived profile from `scheduled` to `listener`, producing the listener option fragment, while lowering still succeeded because the actual entry operation was REST. I also found two concrete oracle issues—one remaining under-binding case and one behavior-preserving refactor that causes a needless revision rotation—and am checking the final line evidence and worktree state.1. **Critical — AR5-01 still has an unbounded-arity blind spot.** The probe shapes top out at five symbols ([contract.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/contract.py:1233)), while the structural guard requires only four ([test_issue_153_canonical_apply_e2e.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_issue_153_canonical_apply_e2e.py:5185)). Production symbol tables have no such bound. I compiled a valid six-symbol scheduled process containing an unrelated WSS operation; a lookup mutant activated only when `len(symbols) > 5` changed `scheduled` to `listener`, while `_compiler_revision()` remained exactly `sha256:abe89cd6…543b80`. This is the stated eleventh same-kind dimension, but it remains a concrete limitation requiring the recorded follow-up disposition.

2. **Standard — the oracle rejects a behavior-preserving use of the real symbol-table API.** `_classify` supplies `SimpleNamespace(symbols=...)` rather than `SymbolTableV1` ([contract.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/contract.py:1095)). Reimplementing the lookup through the documented `symbols.build_index()` API produced identical answers across real tables of arity 0–6, but the oracle’s stand-in lacks that method, rotating the revision to `sha256:7447a7a4…b7026`. The equivalence controls cover dict and binary-search implementations that directly access `.symbols`, but miss this valid implementation ([test_issue_153_canonical_apply_e2e.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_issue_153_canonical_apply_e2e.py:5058)). That imposes a revision/fingerprint rotation on callers without any behavioral change.

Therefore:

- **AR5-01: PARTIALLY RESOLVED**, not fully resolved.
- **AR4-01: PARTIALLY RESOLVED** for the same remaining under-binding, with the additional false-positive issue above.
- The reachability reasoning is **correct**. A scheduled entry can compile while an unrelated listener-family symbol occupies the same table; wrong-row lookup selects `listener`, and lowering’s listener-entry refusal does not fire because the actual entry operation remains non-listener. Apply re-derives through the same faulty function and therefore agrees with the faulty plan.

Validation: the changed test file passes completely, **100 passed**, and the worktree remained clean.

VERDICT: ISSUES FOUND
