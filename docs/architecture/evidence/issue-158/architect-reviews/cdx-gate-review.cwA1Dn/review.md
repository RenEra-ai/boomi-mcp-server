I’ll compare the commit range with the plan, check the recorded deviations and evidence, and split the compiler, deployment, and validation coverage across parallel read-only reviews. I’ll use only repository reads.
The ledger explains several departures from the original plan, including the callable-action projection and reference-only deployment fallback. I’m checking those reasons against the code, with particular attention to entry invariants, operation identity, and served contract behavior.
The seven anchor proofs and atomic routing cutover match the plan. I’ve found a contract gap: listener resolution changes do not affect the served compiler revision. I’m also checking a deployment path that may use authored metadata after an operation was reused, and whether all required listener adversarials are present.
1. **Reused operations use discarded endpoint configuration — P2.** Plan §1 requires correct reuse precedence; §3 and acceptance criterion 5 require verification of the applied listener. [`orchestration.py:1460`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/deployment/orchestration.py:1460) reads authored endpoint fields without checking whether the operation was reused. Those fields suppress the account read at [`orchestration.py:5366`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/deployment/orchestration.py:5366), even though apply discarded that configuration.

   Reproduction: extend `_REFERENCE_ONLY_WSS_OP` in `tests/test_issue_158_listener_deployment.py` with `object_name="wrongIntake158"`, retaining the stored operation whose object name is `reusedIntake158`. Deployment probes `/ws/simple/executeWrongIntake158` instead of the stored endpoint. The existing endpoint/read assertions at lines 1419–1423 should fail for this variant. QA-158-r1-02 covers absent endpoint fields; it does not resolve this precedence defect.

2. **Legacy recognition still scans beyond the actual entry — P2.** Plan §3 requires recognizing the active process entry, rather than finding any listener stage. [`process_entry.py:86`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_entry.py:86) scans every pipeline stage before considering `source`, without checking the process dialect.

   Reproduction: add `pipeline={"stages":[{"kind":"listener","config":{"operation_id":"WSSOP"}}]}` to `_process_flow_config()` in `tests/test_integration_builder.py`. The `database_to_api_sync` builder accepts the extra field and still emits its scheduled database source, but `legacy_config_entry()` returns listener. This permits incorrect ASC classification and listener verification. The plan expressly assigns this existing recognition defect to the new shared authority.

3. **Listener policy and validation are missing from the served revision authority — P2.** Plan §1 requires runtime-derived listener projection and revision changes for operation-resolution/validation changes. [`ProjectionSourcesV1:125`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_ir_projection.py:125) gains no entry-policy or listener-resolution source. `entry_form_rows()` has no production consumer, and the separate operation-only capability row is absent. The new semantic entries contain handwritten statements and source identifiers.

   At [`contract.py:1092`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/contract.py:1092), the revision oracle exercises classification only; it never exercises listener resolution. For example, making `profile_bound_input_types()` return an empty set changes bound-JSON compilation from accepted to refused without changing the compiler revision. A revision non-vacuity test for that mutation should fail. D1 justifies omitting the callable-action entry, not omitting these runtime authorities.

4. **Required listener invariant adversarials are incomplete — P2.** Plan §2 and acceptance criterion 2 explicitly require listener counterparts for the adjacent ownership and correspondence checks. [`test_issue_158_entry_invariants.py:311`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_issue_158_entry_invariants.py:311) lacks independent listener mutations for ordinal/shape-ID uniqueness, non-entry ordering, synthetic Start authored provenance, and dragpoint name/local ordinal/coordinates/edge ID.

   The provenance case at line 208 also changes `origin` to `ir`, so the earlier form check masks the synthetic-provenance check. Existing corresponding tests in [`test_process_ir_compiler_invariants.py:554`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_process_ir_compiler_invariants.py:554) remain scheduled-only. This leaves the mandated fused-entry proof incomplete; it does not establish that the current guards accept those mutations.

Reviewed files: grouped by directory, `docs/architecture/{ISSUE_158_AUDIT_LEDGER.md,M12_COMPATIBILITY_INVENTORY.md,PROCESS_IR_V1.md,PROCESS_IR_COMPILER_V1.md}`; issue-158 evidence manifests, hashes, preflip capture script/inputs/outputs, method-matrix record, and commit-review attestations; `src/boomi_mcp/authoring/{connector_resolution_snapshot.py,contract.py,derived_flows.py,process_entry.py,process_ir_effects.py,process_ir_projection.py,process_materialization.py,workflow.py}`; `src/boomi_mcp/categories/{integration_builder.py,deployment/orchestration.py,components/analyze_component.py,components/canonical_process_apply.py,components/wss_route_methods.py}`; `src/boomi_mcp/categories/components/builders/{_api_service_paths.py,api_service_builder.py,connector_builder.py,process_flow_builder.py}`; `src/boomi_mcp/compiler/process_ir/{body_capabilities.py,connector_capabilities.py,connector_resolution.py,contracts.py,emitter_registry.py,entry_policy.py,execution_profile.py,invariants.py,lowering.py,pipeline.py,semantic_validation/flow.py,legacy_adapters/emission.py,legacy_adapters/registry.py,legacy_adapters/sync_pipeline.py}`; `src/boomi_mcp/models/process_ir.py`; `src/boomi_mcp/patterns/primitives/wss_listen.py`; `src/boomi_mcp/recipes/materialization.py`; `tests/{_process_ir_capability_witnesses.py,_wave_gate_golden_corpus.py,test_integration_builder.py,test_issue_151_reachability_freeze.py,test_issue_153_canonical_apply_e2e.py,test_issue_158_audit_ledger.py,test_issue_158_entry_invariants.py,test_issue_158_listener_compile.py,test_issue_158_listener_deployment.py,test_issue_158_listener_model.py,test_issue_158_wss_identity.py,test_legacy_process_ir_adapters.py,test_orchestration_listener_verify.py,test_process_emitter_parity.py,test_process_emitter_registry.py,test_process_execution_profile.py,test_process_ir_compiler_invariants.py,test_process_ir_entrypoint_diagnostic_parity.py,test_sync_pipeline_adapter_cutover.py,test_wave_gate.py}`; wave-gate manifests, sync-pipeline parity corpus, M12.12 inventory fixture, and seven listener XML anchors; `scripts/rebaseline_m12_12_inventory.py`; `agents/reports/{2026-09-10-issue-158-r1.md,2026-09-10-issue-158-s2r7.md}`.

VERDICT: ISSUES FOUND
