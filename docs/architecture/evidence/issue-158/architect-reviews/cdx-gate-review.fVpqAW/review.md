I’ll review only the correction delta and its audit evidence against the plan. I’ll keep the review read-only and use no network or Boomi MCP tools.I’m applying the repository’s receiving-code-review skill to verify the recorded findings and fixes against the code. The review is split between deployment recognition, invariant coverage, and the served revision oracle.The new oracle covers every member of the resolver’s profile-type set. I’m checking whether it also resolves the broader round-1 contract finding, and whether the added adversarials exercise each missing guard independently.I’ve reproduced several gaps in the corrections: conflict-policy reuse can still use discarded endpoint fields, a registered wrapper builder can still be mistaken for a listener, and removing an accepted WSS alias leaves the served revision unchanged. The invariant review also found two missing mutations that survive the new tests.1. **Conflict-policy reuse still reads discarded endpoint fields — P2.** Plan §1 reuse precedence; §3 and acceptance criterion 5 require verification of the applied listener. The check at [orchestration.py:1473](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/deployment/orchestration.py:1473) considers declared materialization mode, but actual apply also reuses an existing create under `conflict_policy="reuse"`.

   Reproduced offline: an operation with `action="create"`, an existing component ID, no `reference_only`, and `object_name="authoredWrongPath"` satisfies `_will_reuse_at_apply`. Despite its recorded `status="reused"`, `_listen_operation_facts_in_build` returns the authored endpoint. This still suppresses account readback and probes configuration apply discarded. The new test’s “created” control even retains the reused result. Ledger row ARCH-158-r1-01 therefore marks an incomplete correction fixed.

2. **Registered wrappers still classify from an ignored source — P2.** Plan §3 requires recognizing the actual entry. [process_entry.py:101](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_entry.py:101) excludes other registered builders from pipeline recognition, but then treats their `source` as authoritative.

   Reproduction: a valid `wrapper_subprocess` containing one `process_calls` entry plus a stray `source={"connector_type":"wss","action_type":"Listen","operation_id":"WSSOP"}`. Its builder accepts the configuration and emits a no-action start with no Listen; both `legacy_config_entry` and `_process_config_has_wss_listen` nevertheless classify it as listener. Scheduled wrappers can consequently receive incorrect ASC classification. Ledger row ARCH-158-r1-02’s structural-fix claim remains incomplete.

3. **The revision oracle still samples listener recognition — P2.** Plan §1 requires runtime-derived authority and revision changes when operation resolution changes. Although profile types are now enumerated, [contract.py:990](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/contract.py:990) fixes every positive operation’s family to `"wss"`; the recognition cases at line 1031 omit the other accepted aliases.

   Reproduced with an in-memory resolver mutation that refuses only `web_services`: a listener using that alias changes from successful compilation to `PROCESS_IR_REFERENCE_LISTENER_OPERATION_INVALID`, while all **103 oracle verdicts and `_compiler_revision()` remain identical**. Callers still cannot detect this acceptance change. CDX-158-r9-01 fixes profile sampling, but the recorded sampling mechanism persists in the recognition half of ARCH-158-r1-03.

4. **Required listener adversarials remain incomplete — P2.** Plan §2 and acceptance criterion 2 explicitly require independent geometry and provenance mutations. [test_issue_158_entry_invariants.py:453](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_issue_158_entry_invariants.py:453) tests dragpoint `y` only; the test at line 471 tests synthetic `source_path` only.

   In-memory experiments confirmed that all **23 listener cases still pass** after either removing the dragpoint `x` guard or removing the synthetic `cfg_node_id` guard. Corresponding forged plans—body-wire `x += 999`, or synthetic Start `cfg_node_id = cfg.entry_node_id`—then become accepted; the unmodified checker rejects both. Ledger row ARCH-158-r1-04’s whole-checker non-vacuity result does not establish these missing proofs.

Reviewed files: `src/boomi_mcp/authoring/{contract,process_entry,process_ir_projection}.py`; `src/boomi_mcp/categories/{integration_builder,deployment/orchestration,components/builders/connector_builder,components/builders/process_flow_builder}.py`; `src/boomi_mcp/compiler/process_ir/{connector_resolution,connector_capabilities,entry_policy,execution_profile,invariants}.py`; `src/boomi_mcp/models/process_ir.py`; `src/boomi_mcp/recipes/materialization.py`; `tests/{test_issue_153_canonical_apply_e2e,test_issue_158_entry_invariants,test_issue_158_listener_compile,test_issue_158_listener_deployment,test_wave_gate}.py`; the changed inventory and test-node fixtures; `docs/architecture/{ISSUE_158_AUDIT_LEDGER,M12_COMPATIBILITY_INVENTORY,COMPLETION_WORKFLOW_RULES}.md`; issue-158 evidence index and newly archived review evidence; support-QA reports `s2r8` and `s2r9`; `AGENTS.md`.

VERDICT: ISSUES FOUND
