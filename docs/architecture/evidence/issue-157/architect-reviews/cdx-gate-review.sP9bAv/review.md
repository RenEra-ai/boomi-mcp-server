I’ll compare the baseline-to-HEAD delta with all 14 plan items and check deviations against the ledger. I’ll keep this focused on plan conformance and evidence, without changing the tree.
The ledger records substantive plan adjustments, including a new governance diagnostic family, separate hash and preview inputs, and explicit pending flows obligations for #159. I’m checking whether those choices preserve the plan’s guarantees, especially selected-artifact evidence and the required test coverage.
I’m using the repository’s receiving-code-review skill to assess findings against the implementation before accepting them. The ledger’s older closing section is stale; I’ll use its later evidence and the current code to assess status.
I’ve found a gap in the retirement evidence: the records compare emitted XML and plan verdicts, but I haven’t found the separately required mutation-trace and execution/materialization-fingerprint comparisons. I’m also checking the six-entry-point coverage proof against the actual calls in its sentinel test.
The 625 focused tests passed, the legacy inventory check reports no drift, and all 11,948 collected tests match the manifest. The plan audit still found gaps: some coverage and projection paths use candidate configuration instead of the selected artifact, and the connection XOR check misses a binding form that apply resolves to reuse.
The evidence now supports separating implemented features from unmet guarantees. The main gaps are selected-artifact handling, complete output-only refusal, and retirement proof coverage. The transport-level folder injection and the separate governance diagnostic family are justified adaptations. I’ll mark the trading-partner placement limit as a disclosed exception.
The implementation substantially realizes the plan, but several guarantees remain incomplete. I reviewed `ba1be9f34317d720d3b012484622ab7e2742143b..140f5e77b880329f6e2db2b45626feb71411de06` and the ledger’s P1–P9/D1–D19 decisions. The working tree is unchanged.

The material conformance issues are:

1. **Required-target coverage does not consistently follow the selected artifact.** The typed gate drops the conflict policy when resolving profile indexes. A reproduced `conflict_policy="clone"` case selects `create_clone` but validates the existing namesake’s required fields instead of the authored profile. The same gate inspects candidate mappings even when the map itself is selected for reuse, contrary to the plan’s explicit reused-map opacity rule. D4 supports selected-artifact authority; it does not justify either behavior. See [selection handling](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:1833) and the [reproduction](/tmp/issue157_plan_selection_repro.py).

   The later apply refresh also calls `validate_transform_map`, which excludes required-leaf coverage. Consequently, the refreshed selected index is not checked for newly required unmapped leaves. D3 reasonably protects legacy behavior, but does not establish this guarantee for canonical applies. See [apply refresh](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:9530).

2. **Derived flows can describe discarded candidate content, and normalization removes profile names.** The projection regenerates profiles from `component.config` without selected-artifact context. In a successful reproduced plan, the selected profile contained `Root/platform_only`, while the served flow index omitted it. See [projection inputs](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/derived_flows.py:351).

   Separately, normalization unconditionally removes `*_profile_generation.component_name`. R3 argues that the emitted component carries the real name, but that does not preserve the name in the promised rich flow payload or its equality check. P6 justifies replacing script bodies with digests; it does not justify dropping profile names. This weakens items 7 and 8’s prohibition on discarding content to manufacture parity. See [name removal](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/derived_flows.py:108).

3. **The connection XOR contract misses an actual reuse encoding.** Identity-conflict and reuse-plus-inline checks run only when `reference_only=true`. A connection with `action="create"`, top-level `component_id="existing-A"`, conflicting `config.component_id="existing-B"`, and inline settings bypasses the refusal; apply can then reuse A under the reuse policy. D1 justifies limiting this contract to connections, not skipping contradictory identities in this form. See [binding checks](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/governance.py:648).

4. **Watermark validation cannot reach selected existing-profile evidence.** Governance validates watermark fields during normalization, without supplying selected or literal profile indexes. A valid declaration over a reused source profile is rejected before the selected-profile resolver runs. Item 10 requires validation against the selected source field; P7’s preservation of both consistency rules does not justify this restriction. See [watermark resolution](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/governance.py:827).

5. **The named output-only refusal misses competing locations.** `intent.flows` receives `GOVERNANCE_FLOWS_OUTPUT_ONLY`, but `config.flows` beside a valid typed request is silently ignored and the request succeeds. `authoring_request.flows` receives generic `INVALID_INPUT`. Caller rows do not enter execution, but item 7 explicitly requires named rejection across competing locations. P1 explains the diagnostic rename, not these omissions. See [competing-root guard](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:11336).

6. **Retirement proofs are genuine but narrower than required.** Independent replay against a pristine baseline extraction reproduced all ten records’ XML and plan-verdict digests, including the wired control. However, the harness collapses actual creates into per-component XML digests and does not preserve/compare the ordered mutation trace or execution/materialization fingerprints. The separate spec digest correctly records historical echo churn, but does not supply those missing comparisons. Neither D5 nor the retirement discussion justifies omitting them. This is an evidence gap, not proof that the retired fields affect execution. See [measurement harness](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_issue_157_retirements.py:178).

7. **Validator consolidation and its route proof are incomplete.** The required-leaf comparison is shared, but advisory review retains its own direct/function/script destination interpretation; its import of the new normalizer is unused. See [advisory normalization](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/transformation_review.py:678).

   The purported six-route sentinel measures five paths, calls the private recipe validator instead of `run_recipes`, never constructs a recipe intent, and includes setup compilation in its apply count. Its mutant replaces the algorithm with a no-op rather than removing a route invocation. The real recipe connections exist; the requested adversarial proof does not. See [sentinel test](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_issue_157_required_target_coverage.py:361).

Coverage of every numbered plan item:

| Item | Status | Assessment |
|---|---|---|
| **1. Audit and historical evidence** | **Partial** | Baseline, roster, isolated capture, seven-producer census, ordered payloads and digest guards exist. I could not find the requested explicit consolidation of shipped #153–#156 contracts and the final #153 handoff; references are scattered. |
| **2. Authored defaults and envelopes** | **Implemented** | Strict resolved name/action, authored name/prefix intake, normalization, equivalent-default fingerprints, and key-addressed recipe envelopes are present. The interim lift remains. |
| **3. Dependency-derived governance** | **Implemented otherwise** | Ownership follows dependency/reference authority, includes extensions, stops at roots/reuse bindings, and handles explicit placement and ambiguity. D8/D19’s transport injection is justified and preserves goldens. D15/D17 honestly disclose the trading-partner exception; they do not fulfill universal supporting-component placement for that route. |
| **4. Secrets and connection binding** | **Partial** | Shared scanner, REST policy and strict shared header merger are implemented. The identity/XOR bypass above remains. D2’s transfer of connection defaults into operation headers is justified. |
| **5. One required-leaf validator** | **Partial** | The sole comparison implementation replaces all six legacy consumer sites, including composition. JSON/DB authorities, sorted gaps, empty-index deferral and diagnostic identity are retained. Destination normalization remains duplicated. |
| **6. Routes and selected artifacts** | **Partial** | Direct plan/compile/apply preflight, raw recipes, recipe intents and advisory review reach coverage. Selection, reused-map opacity and final refresh gaps remain. P2’s separation from compiler-layer findings is justified. |
| **7. Typed derived flows** | **Partial** | Separate typed canonical preview and legacy authored flows are preserved; the projection stays outside executable hashes. Selected-content, name-preservation and competing-location refusal gaps remain. P4’s hash boundary is justified. |
| **8. Persistent accounting** | **Implemented otherwise, with a limitation** | Ordered two-sided checks, mutants, pinned corpus, nine canonical replay fixtures and generative tests exist. P5’s explicit pending state is justified: nine rows discharged, 132 flow/endpoint rows pending for #159. Name removal weakens complete-payload accounting. |
| **9. Recorded-not-wired channel** | **Implemented** | Fifth typed contribution, stable identity/ownership, deterministic results, direct output and persisted provenance exist outside executable hashes and constraints. D6’s omission from unused legacy responses is reasonable. |
| **10. Watermark/scheduling and retirements** | **Partial** | Typed declarations, persisted-DPP separation, obsolete-spelling refusals, eight RETIRE records, one RETAIN and one SPLIT exist. Selected-source validation and complete retirement proof axes are missing. |
| **11. Served contracts and artifacts** | **Implemented otherwise** | Registries, schemas, capabilities, taxonomy and contract fixture are updated. P1–P3’s diagnostic ownership, layering and regeneration adjustments are reasonable. ProcessIR schema, process XML goldens, golden manifest, dependencies and CI workflows remain unchanged. |
| **12. Test matrix** | **Partial** | Broad governance, binding, coverage, flows, retirement and placement tests exist; connection tests are consolidated into governance tests. The six-route/removal proof and complete opposed-selection matrix are not established. |
| **13. Live governance QA** | **Implemented** | The eleven-round evidence covers both intents, supporting placement, extensions, preservation, multi-root behavior, attestations, verification and cleanup. I did not repeat account mutations. |
| **14. Completion and scope boundaries** | **Partial / remaining gates** | Four commit-review rounds are archived, ending clean. I found no completed final composite wave gate with `--require-plan-fingerprint`, nor evidence of the required full Python 3.11 run; recorded local runs use 3.12. This review is the architect assessment. The stated #159/#160 and later scope boundaries are otherwise preserved. |

**Governance is on the typed per-root envelope, not `ProcessIRV1`.** Both the IR implementation and its committed schema are unchanged. **Flows are genuinely computed and excluded from execution authority**, but their selected-artifact fidelity and complete output-only rejection contract are not yet satisfied.

Verification in this review: **625 focused tests passed**, the legacy inventory check reported **no drift**, and **all 11,948 collected tests matched the active manifest**, with existing IDs preserved.

VERDICT: ISSUES FOUND
