I’m reviewing the full `6d2205a..HEAD` slice against §5.B, §10, and §11, with special focus on darkness, fail-closed behavior, and whether ledger “structural fixes” are genuinely structural. I’ll split the inspection across the registry/digest core, capture/serving paths, and ledger/rebaseline/packaging evidence, then reconcile against the actual whole-slice shape. The repository-mandated `receiving-code-review` skill is not available in this session, so I’ll apply its core discipline directly: validate each finding from code and tests, and omit speculative or already-refuted points.The first full-slice pass has exposed an important contract-shape question: the packaged file is dark, but its schema is much smaller than the revised Unit 4 model listed in the plan. I’m now checking the slice-specific §5.B/§10/§11 text and the actual consumers before treating that as a finding, because a broad plan mismatch is only reportable if the slice-specific amendments did not intentionally narrow it.The fail-closed seam is now concretely disproven for injected/future data: an archived REST `HEAD` capture can be ingested as an affirmative `database/HEAD` verdict because the ingest path takes `family` from its caller and never reconciles it to the execution-connector vocabulary. I’m also checking the mandated final-tree rebaseline now; the delta adds many pytest nodes but does not touch `test_nodes.jsonl`, which §10 explicitly requires.## Findings

1. **[P1] Capture ingestion can mint evidence for an unobserved connector/action.** The summarizer consumes only execution records, readback deltas, and access logs ([capture.py:43](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:43), [capture.py:275](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:275)); it never interprets `ExecutionConnector` or `GenericConnectorRecord`. `ingest()` instead trusts caller-supplied `family` and `actions` and copies them into the evidence row ([ingest.py:192](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ingest.py:192), [ingest.py:221](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ingest.py:221)). A checksummed REST HEAD capture ingested as `family="database"` returned `database/HEAD/idempotent`; a DELETE/204 no-op becomes `read/idempotent` because classification ignores the observed method ([ingest.py:150](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ingest.py:150)). Thus departures 1 and 3 are defensible premises, but their replacement authority was not implemented.

2. **[P1] The registry returns affirmative verdicts for unmapped rows and does not enforce strict records.** Evidence is indexed solely by `(family, action)` and returned without vocabulary resolution ([registry.py:66](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:66), [registry.py:102](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:102)). A registry with empty vocabulary and an `unmapped/DELETE` evidence row returns `idempotent`. The loader also strips arbitrary underscore-prefixed fields and accepts untyped operation-record dictionaries ([registry.py:138](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:138), [registry.py:165](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:165)); consequently `_secret` metadata and arbitrary operation records load successfully.

3. **[P1] Most of §5.B’s strict registry schema is absent.** The package defines only a simplified connector-type mapping and capability row ([models.py:99](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:99), [models.py:142](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:142)). It lacks the semantics definition, projection allowlist, capture reference, live component identity, route-coverage variants, operation-contract record, and typed registry-root models. The vocabulary has no explicit raw-action-to-`action_id` mapping. The report has no parser and ignores operation records ([report.py:22](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/report.py:22), [report.py:41](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/report.py:41)). The packaged JSON’s narrative `_comment`, `_provenance`, and `_known_unmapped` fields also contradict the closed, narrative-free contract ([registry_v1.json:3](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry_v1.json:3)).

4. **[P1] Component Config Digest V1 is fail-open and is a relocated enumeration.** Projection fields are hard-coded in Python ([digests.py:59](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:59)), namespace URIs are discarded, and unknown elements/attributes are silently ignored ([digests.py:128](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:128)). The implementation hashes a sorted JSON projection rather than a registry-owned, QName-aware `ElementTree.canonicalize` projection ([digests.py:245](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:245)). Adding an unknown credential-bearing element or attribute left the digest unchanged, and `<O/>` received a digest. The separator-collision correction is real locally, but it did not structurally replace the projection enumeration or provide the required unknown-content refusal.

5. **[P1] Route Digest V1 is not the specified algorithm.** It does not join and normalize one effective path; it hashes JSON containing separate `base` and `template` values ([digests.py:197](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:197)). The normalizer lacks percent/unreserved and dot-segment processing, removes trailing slashes, and accepts relative or non-HTTP connection URLs ([digests.py:174](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:174)). Both digest functions also return bare hex rather than their required versioned prefixes.

6. **[P1] The claimed action-enumeration structural fix remains incomplete.** The router has 18 actions ([monitoring.py:2100](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/monitoring.py:2100)), while the served monitoring overview still hand-lists 10 and its template router rejects `execution_connectors` ([meta_tools.py:5327](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/meta_tools.py:5327), [meta_tools.py:9120](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/meta_tools.py:9120)). This directly contradicts the ledger’s “swept properly” disposition ([ISSUE_155_AUDIT_LEDGER.md:162](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:162)). Additionally, `connector_documents` still omits the required `operation_id`, `connection_id`, and `execution_connector_id` ([monitoring.py:1169](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/monitoring.py:1169)).

7. **[P2] The exact A16 identifier grammar is not implemented.** Component and execution IDs accept uppercase hexadecimal, and execution dates receive no calendar validation ([ids.py:55](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ids.py:55), [ids.py:73](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ids.py:73)). Uppercase IDs and `2026.02.30` are therefore accepted.

8. **[P2] Docker parity does not exercise digest parity.** The workflow builds the image and loads the registry, but computes neither digest nor compares image results with host fixtures ([connector-replay-image-parity.yml:62](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.github/workflows/connector-replay-image-parity.yml:62)).

9. **[P2] The required §10/§11 closure records are incomplete.** `test_nodes.jsonl` remains at 10,781 active nodes while the current tree collects 10,899 tests ([test_nodes.jsonl:1](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/fixtures/wave_gate/test_nodes.jsonl:1)). The ledger promises a separate step-0 block for every slice but contains only Slice A’s block; Slice B has only an abbreviated map entry ([ISSUE_155_AUDIT_LEDGER.md:18](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:18), [ISSUE_155_AUDIT_LEDGER.md:35](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:35)). Its DC-155-F count is also zero despite the packaged-secret test being a literal deny-list ([test_connector_replay_registry.py:86](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_connector_replay_registry.py:86)).

The packaged/runtime state is currently dark: `evidence_records` and `operation_records` are empty, and no replay-authorization path consumes the loader or ingestor. There is no served production verdict today. The internal ingest seam can construct evidence rows, but findings 1–2 show that those future rows are not fail-closed.

The contract-reference diagnostic deferral is defensible: its producing surface belongs to Slice D.

Validation run: 168 focused tests passed. Those tests do not cover the whole-plan gaps above.

VERDICT: ISSUES FOUND
