I’m using the repository’s architect-review guidance because this is a capped, bounded review: I’ll inspect the actual Slice B delta, test the replay-authorization paths adversarially, and separate implementation defects from final-ledger closure issues. I’ll keep the review read-only.The Slice B baseline is `6d2205ad836846517f522f8a0ac3e6baf64c2626`; the worktree is clean at `400ada0c6bd5dfa625647e881fe2270fa3ef4243`. I’ve split the read-only audit across ingestion/authorization, digest semantics, and packaging/closure while I trace the complete delta and run independent probes.The first adversarial probes already exposed a gap the new controls do not exercise: the loader accepts a hand-authored affirmative evidence row with a fabricated digest, and `retry_safety()` returns `idempotent`. I’m tracing whether that is isolated or part of a broader missing capture-to-verdict binding before deciding how to state it.The focused suites are green, but the adversarial controls are not: I’ve reproduced multiple paths where the code can derive a positive replay verdict from evidence it never tied to both executions, plus digest collisions on behavior-changing XML. I’m finishing the packaging/closure checks and consolidating only independently reproducible findings.All 143 Slice-B-focused tests pass on the clean `400ada0c` tree. The failures are therefore in the controls themselves: mixed-execution files, one-log/two-run captures, absent-vs-null readbacks, and behavior-changing XML all pass paths that the existing tests do not exercise. The packaged evidence and operation arrays are still empty, so “dark today” is accurate; it does not make those future authorization paths safe.## Findings

1. Critical — authoritative evidence rows are disconnected from the A9 observations. [`CapabilityEvidenceRecordV1`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:184) directly carries `retry_safety` with only a digest and execution IDs. The A9 [`ClosedCaptureObservationsV1`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:400) and `CaptureReferenceV1` are not used by [`ingest()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ingest.py:305). Registry validation checks only family/action resolution at [`registry.py:241`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:241), then returns the declared verdict at [`registry.py:127`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:127). A fabricated REST PATCH row containing `a…a` as its digest and one grammar-valid execution ID loaded and returned `idempotent`. The supposed positive control constructs essentially that same row at [`test_connector_replay_registry.py:264`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_connector_replay_registry.py:264). This violates revised Unit 4’s nonempty typed-capture contract and the summary’s “no unevidenced row may authorize retry.”

2. Critical — ExecutionConnector correlation remains file-wide, not record-wide. [`capture.py:286`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:286) only checks whether any capture execution ID occurs anywhere in the file; [`capture.py:295`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:295) then trusts every nested `connectorType`. A mixed file containing an own-execution sentinel and a foreign-execution REST row ingested as REST GET/idempotent. The control at [`test_connector_replay_architect_round1.py:145`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_connector_replay_architect_round1.py:145) tests only a wholly foreign file. This directly refutes the claimed record-level refusal.

3. Critical — one observed request can certify a two-execution replay. [`capture.py:364`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:364) reads only the first counterparty log, then [`capture.py:389`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:389) copies its status and method onto every execution run. [`classify()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ingest.py:151) requires merely a nonempty status set and two execution IDs. An end-to-end checksummed capture with two executions/readback stages but only one PATCH-200 log minted `conditionally_idempotent`. The second request was never attested.

4. Critical — replay convergence collapses absent and present-null fields. [`_differing_keys()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:231) compares `a.get(k)` with `b.get(k)`, so `{}` and `{"field": null}` are equal. That result feeds convergence at [`capture.py:255`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:255). A probe where replay added a null-valued field reported no replay difference and classified the write as conditionally idempotent.

5. Critical — Component Config Digest V1 is not a closed, family-scoped, QName-aware projection. [`_refuse_unknown_fields()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:395) checks only unknown `<field id>` values, while [`_project_tree()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:443) silently drops unknown elements, attributes, wrappers, field type, and component subtype. Adding a behavior-bearing element or root attribute left the digest unchanged; malformed/missing configuration also received a digest. The allowlist model cannot declare QName-aware fields at [`models.py:379`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:379), canonicalization supplies no QName-aware arguments at [`digests.py:513`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:513), and projection lookup ignores `family` at [`registry.py:101`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:101). I reproduced QName-value and cross-family projection collisions. A later grant could therefore match configuration it did not capture.

6. Standard, blocking contract — cross-record uniqueness is incomplete. [`registry.py:238`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:238) collapses semantics definitions into a set, accepting two contradictory definitions for the same ID/revision. Operation duplicates are keyed by `(family, action, contract_ref)` at [`registry.py:251`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:251), so one contract reference may occur under multiple actions. Both cases loaded in direct probes, contrary to “exactly one semantics definition” and “duplicate records for one contract refuse.”

7. Standard, capability reachability — the banked PATCH control is vacuous and the intended positive path cannot mint. The real capture currently ingests as `unknown/unverified`. If positive statuses are supplied, [`_operation_component_id()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ingest.py:319) still filters filenames with `*operation*.xml`, missing the actual `component_op_patch.xml`; it then refuses the conditional row. The control at [`test_connector_replay_architect_round1.py:133`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_connector_replay_architect_round1.py:133) asserts only `(family, action)`, not conditional safety or operation identity. Slice F cannot use the advertised capture without changing Slice-B code.

8. Standard, blocking contract — Route Digest V1 still accepts forbidden operation query/fragment text. Connection URL query/fragment validation exists at [`digests.py:263`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:263), but [`_effective_path()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:284) treats the operation resource as unparsed text. `/x?query=1` and `/x#fragment` both receive versioned digests, contrary to Route Digest V1’s refusal rule.

9. Standard, served-contract — `connector_documents` still drops the three provenance IDs Slice B owns. [`_convert_connector_records()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/monitoring.py:1169) omits `operation_id`, `connection_id`, and `execution_connector_id`, although the SDK supplies them. The explicit Slice-B contract is at [`issue-155-strategy.md:240`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/plans/issue-155-strategy.md:240) and QA-B1 at [`issue-155-strategy.md:443`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/plans/issue-155-strategy.md:443). Per-document evidence therefore cannot be served with its exact operation/connection/execution-connector provenance.

10. Standard, packaging — the Docker parity job is structurally red. Its first container command supplies `PYTHONPATH=/app/src` at [`connector-replay-image-parity.yml:71`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.github/workflows/connector-replay-image-parity.yml:71); the package-resource import at [`connector-replay-image-parity.yml:75`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.github/workflows/connector-replay-image-parity.yml:75) does not. The image copies the repository without installing it at [`Dockerfile:52`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/Dockerfile:52), and its runtime environment does not add `/app/src` at [`Dockerfile:145`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/Dockerfile:145). The second command consequently raises `ModuleNotFoundError`; the required in-image resource proof is unavailable.

11. Closure blocker — the previous two Slice-B architect evaluations are absent from the durable ledger/archive. The roster requires this L3 loop at [`ISSUE_155_AUDIT_LEDGER.md:169`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:169), but Slice B has only L2 checkpoint rows at [`ISSUE_155_AUDIT_LEDGER.md:475`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:475). The archive index’s Slice-B entries begin with L2 commit reviews at [`index.jsonl:27`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/evidence/issue-155/index.jsonl:27); all indexed L3 rounds belong to Slice A. This third evaluation itself can only be archived afterward, but evaluations 1–2 should already be durable.

The registry package is dark for replay authorization today: both production evidence and operation-record arrays are empty. The node-manifest delay is also correct—the strategy says regenerate it last after L3. Those facts do not refute the findings because Slice F activates these exact authority seams.

Validation on clean HEAD `400ada0c6bd5dfa625647e881fe2270fa3ef4243`: 143 focused tests passed, `git diff --check` passed, and the host digest-parity checker passed. The adversarial probes above demonstrate that the green controls miss the failures.

Because this final evaluation contains unresolved replay-authority findings, Slice B cannot close on the current tree. The architect loop reaches its hard cap here; these findings require a batched correction and the owed fix-delta QA/Stage-2 review, not a fourth architect evaluation.

VERDICT: ISSUES FOUND

