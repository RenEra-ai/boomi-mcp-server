I’m treating this as the bounded slice-C architect gate, evaluation 2—not a general issue-155 review. I’ll inspect the actual correction delta, replay each of A–F against the current tree and controlling plan, and separate “the fix is real” from “the round can pass.” I’m also using the repo’s stored architect-review guidance because this exact review lineage has prior scope and evidence conventions.I found the exact prior gate artifact and correction boundary: evaluation 1 judged clean tip `9930535`; the response is the three-commit delta `9930535..ee011a7`. I’m splitting the audit now: authority/call-path coverage, XML family/scoping semantics, and snapshot/no-client/contradiction regressions, while I independently inspect the whole correction and its tests.Two things are already clear from the call graph, and I’m testing them rather than accepting the new guards at face value: the recipe engine still compiles during normalization before the new workflow comparison, and the XML “family config” scope currently means “any child under `Operation/Configuration`,” not the family-specific child. I’m also checking whether the newly carried live version/account facts survive their real production types and call sites.The correction is not closing cleanly. Two adversarial probes now show gaps the green suite misses: recipe compilation still runs before snapshot comparison, and malformed/ambiguous SOAP or database operation XML can leave the resolved action unknown and thereby preserve the caller’s declared action. I’m completing the snapshot/failed-read audit before giving the landing judgment.The remaining distinction is now clear: the new `IDENTITY_UNAVAILABLE` arm is real for transport exceptions, but it is not a complete fail-closed reuse contract. A fetched document that the stricter reader rejects, an ambiguous live action, or a non-explicit version still becomes an all-unknown identity and is accepted; the declaration and blank-path checks then skip it.Slice C does not clear evaluation 2. I reviewed the clean correction range `9930535..ee011a7`. Fixes 1–3 remain materially incomplete; Fix 4 closes only transport-level read failures.

## Findings

1. **High — critical tier: caller metadata still reaches recipe compilation as authority.**

   The typed validation route now compares before its local symbol build ([workflow.py:1407](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:1407)), but recipe normalization invokes `run_recipes` first ([workflow.py:518](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:518)). That engine accepts no snapshot ([engine.py:1574](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/recipes/engine.py:1574)), builds symbols from `connector_metadata` ([engine.py:1741](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/recipes/engine.py:1741)), and compiles them ([engine.py:1804](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/recipes/engine.py:1804)). `build_symbol_table` still copies metadata directly into `connector_type` and `action_type` ([materialization.py:193](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/recipes/materialization.py:193)). The public recipe bridge reaches that path directly ([recipe_bridge.py:142](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/patterns/recipe_bridge.py:142)).

   A runtime probe returned a recipe artifact for declared database/Get over submitted REST/POST XML; applying the omitted comparison to the same facts produced `CONNECTOR_REPLAY_IDENTITY_MISMATCH`.

   The new guard inventories modules that build snapshots, not symbol-table or compile sinks ([test_issue_155_identity_projection.py:2510](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_issue_155_identity_projection.py:2510)); it therefore excludes `recipes/engine.py` precisely because that path never builds the required snapshot. This violates the four-sink, compare-before-classification contract ([plan](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/plans/issue-155-strategy.md:252)).

   Separately, the planning surface does not actually report every mismatch: `assert_declared_matches_resolved` raises on the first one ([snapshot.py:684](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:684)), and `_validate_processes` catches it only once.

2. **High — critical tier: per-family verb extraction is correct, but XML authority still fails open.**

   The normal mappings are right: REST reads `customOperationType`, SOAP reads `operationType`, and database maps `DatabaseGetAction`/`DatabaseSendAction` ([snapshot.py:304](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:304)). They agree with the REST, SOAP, and database builders.

   However, the reader treats every direct child of `Operation/Configuration` as the family config ([snapshot.py:250](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:250)) and accepts `field id="path"` at any descendant depth ([snapshot.py:273](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:273)). It never requires REST/SOAP `GenericOperationConfig` or the corresponding database action element. Direct probes showed an arbitrary `<Other customOperationType="GET">` becoming authoritative and a nested decoy path making a missing operation path appear static.

   Submitted-XML settledness is also still REST-only ([snapshot.py:555](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:555), [snapshot.py:575](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:575)). Ambiguous or missing database/SOAP actions become `None`, after which comparison skips them ([snapshot.py:711](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:711)). Database Get+Send and conflicting SOAP configurations were both accepted under caller declarations.

3. **High — critical tier: the trusted snapshot contract remains unbuilt, and the preserved version is type-invalid.**

   The governing Slice-C contract requires closed mode/authority, component kind, actual identity/version, route facts, optional digest, extension flags, account scope, collision decisions, and root projection ([plan](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:968)). Current mode, account, version, and authority remain open optional strings ([snapshot.py:102](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:102)); the snapshot contains only `identities` and `lookup` ([snapshot.py:166](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:166)).

   Account-aware production paths omit `account_id` when constructing it ([workflow.py:1372](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:1372), [integration_builder.py:8949](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:8949)); it is built after normalization and is not retained in `_PlanInternals` ([workflow.py:1805](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:1805)).

   The correction also introduced a concrete model violation: `component_get_xml` returns integer versions ([components/_shared.py:243](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/components/_shared.py:243)), while the snapshot declares `Optional[str]` and inserts the integer through unvalidated `model_copy` ([snapshot.py:115](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:115), [snapshot.py:598](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:598)). The resulting frozen model emits a serializer warning and fails strict round-trip validation. The new test masks this by injecting `"7"` as a string.

4. **Medium — standard tier: unreadable reuse is only partially closed.**

   The new `read_failed=True` refusal is real ([snapshot.py:605](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:605)). But a truthy fetched document is accepted without checking `document_parsed`, settled family/action, actual version, or positive version. A document accepted by the preliminary ElementTree fetch but rejected by the entity-aware snapshot reader produced an all-unknown reused identity and was accepted. Missing/zero versions also pass. That contradicts the explicit parseable-XML/version requirement ([plan](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:207)) and makes the served taxonomy promise broader than the reachable refusal ([errors.py:649](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/errors.py:649)).

## Explicit answers

- **A — No.** `recipes/engine.py`, including the public recipe bridge, still compiles before any trusted comparison. The guard does not cover compile/symbol sinks, and planning reports only the first mismatch.

- **B — The three normal derivations are right.** No supported compiler family is missing. What remains wrong is selecting arbitrary configuration children and applying settledness only to REST.

- **C — No.** Root component type/subtype and fetch identity/version legitimately live outside the operation config and are already partly read. Connection route facts live under separate connection structures—REST `GenericConnectionConfig/field[@id="url"]`, SOAP connection fields, and database connection settings. Extension-bound state also comes from separate structured/extension authority. These require component-kind-specific readers and snapshot fields.

- **D — The omission is not defensible for a completed Slice C.** Digest and extension facts are the authoritative inputs Slice D’s sole minter must consume ([plan](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:1033)); Slice E independently compares ID/version/digest ([plan](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:1101)). `for_root` is Slice C’s private isolation/transport contract, not a premature served surface. Deferring these forces later slices to reopen or independently rederive the authority snapshot.

- **E — Delete the current set/split heuristic, not fail-closed ambiguity handling.** With exact family-element selection, unrelated decoys disappear. Zero, multiple, or blank relevant family configs should refuse submitted XML and produce identity-unavailable for consulted live reuse. The current test that chooses the nonblank verb from two sibling `GenericOperationConfig` elements ([test_issue_155_identity_projection.py:2427](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_issue_155_identity_projection.py:2427)) is pinning a guess over genuinely ambiguous XML.

- **F — The concrete correction regression is the integer-version/string-model mismatch.** The error code registration itself is coherent. Focused suites remained green—173 core tests and 217 with builder coverage—but the recipe, XML-decoy, non-REST ambiguity, entity-bearing readback, and version probes all reproduced outside that coverage.

The current ledger’s “fixed” dispositions for `ARCH-155-C-e1-01` through `-04` are therefore unsupported ([ledger](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:923)). Evaluation 2 cannot close Slice C.

VERDICT: ISSUES FOUND

