I’m treating this as a bounded, decisive Slice B architect gate: I’ll inspect the actual delta and ledger, then attack the two verdict-authority paths and fail-closed model/loader behavior. I’m also applying the repository’s prior architect-review discipline so findings stay tied to exact plan clauses and caller impact.The reviewed baseline is `79e1aaf`; the corrective delta is the four commits `0546058..18c1d02`, on a clean worktree. I’ve split the adversarial checks so the authority paths, typed-loader path, and digest/parity path are examined independently.The first pass already shows a key distinction: several fixes are real locally, but the config-digest rewrite still does not implement the plan’s canonical XML algorithm. I’m probing whether the remaining gaps are merely dark infrastructure or can admit an authoritative record, and I’m validating the exact malformed-input arms before writing findings.The authority probes found a sharper failure than the original mismatch case: a checksummed two-run PATCH archive with only one counterparty log is promoted into a two-run replay verdict. I’m finishing the digest/schema checks now, then I’ll reconcile which fixes are genuine versus only adjacent.The targeted Slice-B suite is clean (`129 passed`), but the adversarial probes expose authority gaps the suite does not cover. The genuinely closed items are the lowercase/calendar ID grammar, host/image parity mechanism, derived monitoring action overview, and the ledger baseline; I’m now writing the final finding set around the remaining permissive paths.Slice B is not clean. The narrow REST/database mismatch and unmapped-family cases now refuse, but affirmative retry authority can still be minted from evidence the capture did not establish.

### Findings

1. **P1 — Capture labels are not causally tied to an execution.**  
   [capture.py:263](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:263) accepts any recursively nested `connectorType`, without validating an ExecutionConnector row or matching its `executionId`. [capture.py:288](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:288) regexes an XML file selected by filename, without parsing its component identity or joining it to `GenericConnectorRecord.operationId`; [ingest.py:235](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ingest.py:235) then treats both observations as trusted. Checksum-valid probes ingested affirmative rows using a foreign execution ID, a bare `{"connectorType": ...}` object, and malformed operation XML. This violates Revised Unit 4’s joined ExecutionRecord/ExecutionConnector/GenericConnectorRecord evidence contract and lets unrelated artifacts lend authority to an execution.

2. **P1 — Missing replay evidence is promoted into a positive verdict.**  
   [capture.py:341](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:341) reads only `logs[0]`, then [capture.py:366](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:366) copies that one outcome onto every execution run. A two-execution PATCH archive with only one `PATCH 200` log ingested as `conditionally_idempotent`. Separately, [ingest.py:156](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ingest.py:156) falls through to `READ/IDEMPOTENT` whenever observed `state_changed` values are present but all false; a successful no-op PATCH therefore becomes an idempotent read without replay proof. Both paths violate the plan’s “no unevidenced row may authorize retry” rule.

3. **P1 — Vocabulary resolution remains family-only.**  
   [models.py:111](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:111) has no raw-action/action-ID mapping, [models.py:162](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:162) accepts any nonempty evidence action, and [registry.py:134](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:134) checks only whether the family appears in vocabulary. A typed registry with mapped REST and an evidence row for `BREW_COFFEE` returned `idempotent`. The exact unmapped-family case is fixed; the plan’s total/injective family-and-action vocabulary is not.

4. **P1 — The new model names do not implement the planned schemas.**  
   The semantics enums at [models.py:235](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:235) use different vocabularies from A9. [CapabilityEvidenceRecordV1](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:154) cannot encode `accepts_input`, `produces_output`, or typed capture references; [CaptureReferenceV1](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:300) has no summary; and [CaptureSummaryV1](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:115) lacks A9’s closed placement/input/output/effect/replay/source observations. Slice F therefore cannot express or validate the three capture-backed behavioral columns without changing Slice B’s purportedly finished schema.

5. **P1 — Typed operation records remain syntactic, not authoritative.**  
   [OperationContractRecordV1](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:368) applies only local field checks, while [registry.py:203](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:203) performs no contract-ID, vocabulary, semantics-resolution, account-consistency, or operation-record collision validation. `_parse` accepted an invalid contract ref, unmapped family/action, nonexistent semantics revision, invalid timestamp, mismatched record/capture account scopes, and duplicate operation records. The injected constructor also stores its record tuples unvalidated at [registry.py:52](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:52). Untyped packaged dictionaries are rejected, but the invariants that make a typed record safe to consume are still decorative.

6. **P1 — Component Config Digest V1 remains fail-open and is not the specified algorithm.**  
   [digests.py:314](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:314) falls back to hard-coded projections when registry loading fails or a projection is absent; [registry.py:101](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:101) selects only by component kind, ignoring family/version. Unknown checking covers only field IDs at [digests.py:344](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:344), while the digest remains a hard-coded, local-name-only, sorted-JSON projection at [digests.py:378](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:378) and [digests.py:421](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:421), not namespace-aware `ElementTree.canonicalize`. Unknown elements/attributes/namespaces, reordered children, truncated component roots, and an injected newly-included field either retained the old digest or still produced one. A changed live component can therefore match captured configuration evidence.

7. **P1 — Route Digest V1 still assigns identities to malformed inputs.**  
   The core slash/percent/dot-segment normalization is now correct, but [digests.py:243](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:243) does not require an absolute HTTP(S) URL with authority, and [digests.py:264](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:264) accepts matching fields in arbitrary XML. Relative bases, `ftp://`, missing authorities, malformed `%2G`, operation query/fragment text, and `<Bogus>` component roots all produced route digests. A10 and Route Digest V1 require those cases to refuse.

8. **P2 — The tracked report omits the records that can eventually mint grants.**  
   [report.py:41](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/report.py:41) renders only vocabulary and capability evidence; it ignores semantics definitions, projection allowlists, and operation records, and [report.py:22](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/report.py:22) exposes no strict parser. Adding an operation record leaves the report unchanged, contrary to the deterministic typed-report equality requirement.

9. **P2 — Method reconciliation rejects the actual banked PATCH captures.**  
   [capture.py:297](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/capture.py:297) only examines XML filenames containing `operation`; both archived PATCH captures use `component_op_patch.xml`. Their method becomes `None`, blocking the required Slice-F PATCH ingestion path.

### Fix disposition

| Fix | Judgment |
|---|---|
| 1. Trusted labels | Partial: caller mismatch refuses, but observations are uncorrelated and replay outcomes are synthesized. |
| 2. Vocabulary-first | Partial: unmapped families refuse; unmapped actions can still affirm. |
| 3. Typed model set | Not closed: names and local shape checks landed, but schemas and cross-record authority do not match the plan. |
| 4. Config digest | Not closed: tri-state field IDs landed, but projection authority and canonical XML algorithm did not. |
| 5. Route digest | Partial: central normalization closed; malformed authority inputs remain permissive. |
| 6. Action sweep | Closed. |
| 7. Identifier grammar | Closed. |
| 8. Docker parity | Closed as parity machinery, though it currently pins defective digest outputs. |
| 9. Ledger records | Closed; final-tree node-manifest regeneration is correctly still pending. |

I reviewed corrective delta `79e1aaf..18c1d0268ffdbb8a91cb9881e37f0c7eda2615d1`. The focused Slice-B suites pass with `BOOMI_LOCAL=true` (`129 passed`); the failures above come from direct malformed, truncated, cross-execution, and cross-record probes missing from those tests. These are implementation blockers in capability authority and machine-served contracts, not remaining bookkeeping.

VERDICT: ISSUES FOUND

