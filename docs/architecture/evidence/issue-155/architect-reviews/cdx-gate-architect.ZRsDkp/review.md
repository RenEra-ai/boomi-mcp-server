I’m grounding this as the bounded §6 architecture gate, not another commit review. I’ll verify the six prior dispositions, the moved served contracts, the two recorded LIMITS, and ledger claims against `9812b98..HEAD`, with parallel passes to keep the checks independent.The correction arc is broader than the six direct fixes: it also changes path identity semantics, discovery explanations, semantic revisioning, and the public attestation shape. I’m treating those as contract changes that need their own plan authority, not assuming a clean commit review makes them acceptable.One important discrepancy has already surfaced: the ledger’s “plan performs no declared-vs-live comparison” limit is contradicted by the current plan path itself—it builds a live snapshot and returns `CONNECTOR_REPLAY_IDENTITY_MISMATCH` inside the plan report. I’m now checking whether the remaining older conformance gaps (ID grammar, discovery shape, registry report parser, semantic revision, and snapshot lifecycle) were actually closed or merely left outside the six headline dispositions.The current tip has advanced during/around the review to `c16e92e` (a ledger/evidence collection commit after `3e9a834`), so I’m using the actual current `HEAD` as requested. That change does not alter the source-behavior checks, but it matters for any closure/ledger claim I cite.The first “fixed” disposition does not survive an adversarial source-provenance check. The registry now binds the digest and the replay enum to the semantics, but it still accepts a replay claim after its only replay-observing source (`endpoint_readback`) is removed and the record is legitimately re-digested; the production grant projector then mints. I’m tracing that through the exact model and loader lines now, because it keeps finding 1 Critical rather than merely leaving a documentation gap.A second headline disposition is also incomplete: the attestation now carries the four newly added fields, but still records only `route_coverage_kind`; the concrete static route digest that was checked is deliberately dropped. That is the same mutation-accounting defect from finding 5—an audit record still cannot reconstruct which route coverage authorized the write.

The “sort/dedup cannot be exercised publicly” limit is also failing under a direct multi-call process-scope shape; I’m validating that probe before treating the ledger statement as contradicted.I’ve hit two contradictions that matter to the verdict: the deeper call-chain trace challenges the rootless-grant refutation, while the ledger’s two LIMIT claims conflict with direct public-path probes. I’m resolving those against the actual caller flow and archived QA evidence before I lock the findings.The dispositions are now clear. Findings 2 and 3 are genuinely fixed; 1 and 5 are only partially fixed; 4’s refutation relies on a test-manufactured state that the production caller does not supply; and 6 still lacks final-tree live proof. I also confirmed that both recorded LIMITS are false at the current tip—one was never exhaustive, and the other became stale after later corrections.ISSUES FOUND. I reviewed the clean current tip `c16e92e22ec5e3473bf16c0101fe10fd96ffa632`; the prompt’s commit count is stale—this is 35 commits after `9812b98`, though the source delta remains 19 files and 1,188 insertions.

## Disposition audit

| Finding | Judgment |
|---|---|
| 1 | **Not fixed** — digest and observation equality are fixed, but evidence-source authority remains open. |
| 2 | **Fixed** |
| 3 | **Fixed** |
| 4 | **Not refuted** — the recorded refutation relies on a manufactured state. |
| 5 | **Partially fixed** — sequencing, retention, and sort/dedup are fixed; concrete route coverage is still omitted. |
| 6 | **Not fixed** — the slice map is corrected, but final-tree live positive QA is absent. |

## Blocking findings

1. **Critical — a record can still authorize replay using sources that did not observe replay.** The model itself states that only endpoint readback backs replay claims ([models.py:411](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:411)), but its validator requires readback only for state-effect claims ([models.py:517](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:517)). The new registry checks correctly recompute the digest and compare `replay` with `duplicate_guarantee` ([registry.py:314](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:314)), but never check whether a named source could observe that replay. A direct probe changed the operation capture to `effect="read_only"`, retained `replay="same_effect"`, removed `endpoint_readback`, recomputed the digest through the published minter, and still loaded and minted one grant through [connector_resolution.py:955](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/connector_resolution.py:955). That contradicts the plan’s source mapping ([plan:737](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:737)) and no-unevidenced-authority rule ([plan:5](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:5)).

2. **Finding 4’s refutation is invalid.** The production evidenced path projects a root and mints its grant at [workflow.py:2304](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:2304), then passes that grant-bearing table into materialization at [workflow.py:2417](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:2417). The refutation test instead manually deletes grants from a valid projected table ([test_issue_155_contract_projection.py:1049](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_issue_155_contract_projection.py:1049)) and calls that synthetic state “what actually arrives.” Materialization then explicitly clears both root and grants ([process_materialization.py:560](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_materialization.py:560)), disabling enforcement because [error_handling.py:287](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/error_handling.py:287) checks grants only for rooted tables. Account independence does not require this: the transient symbol table is absent from the canonical plan material ([process_materialization.py:466](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_materialization.py:466)). No final-write bypass was demonstrated, but the compiler still operates under weaker rules than [Unit 6 requires](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:1038).

3. **Critical — Finding 5 is only partially fixed because the durable attestation still omits concrete route coverage.** Static coverage safely carries hashed route digests ([models.py:572](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:572)), and recheck computes and verifies the live digest ([recheck.py:312](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/recheck.py:312)), but then records only `"route_coverage_kind"` ([recheck.py:383](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/recheck.py:383)). The served model likewise has no route-digest/coverage field ([authoring_workflow.py:918](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/models/authoring_workflow.py:918)). An auditor therefore cannot determine which static route authorized the mutation once the registry changes. Unit 8 expressly requires route coverage in the concrete binding ([plan:1095](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:1095)).

4. **Critical — Finding 6’s live-QA disposition is false.** The ledger itself still says this half is unresolved ([ISSUE_155_AUDIT_LEDGER.md:1351](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:1351)). The positive public write pass ran at `42ec5977` and was declared current only through docs-only `e943999` ([slice-f-r5.md:3](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/agents/reports/2026-09-03-issue-155-slice-f-r5.md:3)). The later live round records that the packaged record names operation v1 while live is v2, making the end-to-end path unreachable and explicitly not recaptured ([slice-f-r6.md:305](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/agents/reports/2026-09-03-issue-155-slice-f-r6.md:305)). Relevant source changed afterward. The required final-tree public safe-write gate at [plan:1231](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:1231) is not earned.

5. **Critical by retained source label — Unit 4’s QName-aware digest mechanism remains incomplete.** The ledger claims this was closed because QName-aware canonicalization arguments exist ([ISSUE_155_AUDIT_LEDGER.md:894](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:894)). But parsing still uses plain `ElementTree.fromstring` ([digests.py:145](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:145)), and projection copies QName-valued content into a fresh tree without the source namespace bindings ([digests.py:600](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:600)) before canonicalization ([digests.py:667](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:667)). A valid `xmlns:q` / `q:GET` probe fails with `Prefix q ... not declared`, exactly what [the plan forbids](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:177).

6. **Units 4–7 remain materially partial.**

   - Unit 4’s prescribed `icv1` language is not implemented. The server instead accepts broad `$ref:[A-Za-z0-9_.-]+` tokens ([ids.py:159](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ids.py:159)); the shipped record uses `$ref:rest.patch...` ([registry_v1.json:229](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry_v1.json:229)); and the mapping model lacks the required raw/action and canonical family/action IDs ([models.py:119](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:119)). `report.py` also has render/write but no required strict parser ([report.py:22](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/report.py:22)).
   - Unit 5’s lifecycle is still reversed: workflow normalizes—and recipe-compiles—before constructing the live snapshot ([workflow.py:1882](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:1882)), while `run_recipes` receives no snapshot ([workflow.py:523](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:523)).
   - Unit 6’s sole-minter and typed-grant contracts are absent. `build_symbol_table` remains symbol-only ([materialization.py:142](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/recipes/materialization.py:142)); minting happens separately at [connector_resolution.py:753](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/connector_resolution.py:753); and the grant type omits account scope, component IDs/versions/digests, route coverage, and capture digest ([contracts.py:293](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/contracts.py:293)), contrary to [plan:1020–1038](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:1020).
   - Unit 7 discovery omits both configuration digests ([discovery.py:27](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/discovery.py:27)); its live reader derives only ID/version, not current digest or actual account scope ([query_components.py:477](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/components/query_components.py:477)). The new `superseded_records` envelope is safe but is unsanctioned served-contract growth ([discovery.py:208](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/discovery.py:208)). The semantic revision still omits explicit vocabulary mappings ([contract.py:849](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/contract.py:849)).

The new attestation identity fields and authoring-contract rotation are sanctioned. The separate `connector_path_equivalence` revision row is also appropriate account-independent compiler provenance; its problem is not inclusion, but that the required vocabulary mapping remains absent.

## LIMIT and ledger audit

Both load-bearing LIMITS are false at this tip:

- The one-binding ceiling at [ledger:1369](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:1369) missed process scope with `source_replay_policy="allow_duplicates"`. A public compile/apply probe with one GET followed by two evidenced PATCH calls succeeded and produced one attestation with two bindings. Process scope constrains only the first step ([process_ir.py:2428](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/models/process_ir.py:2428)), and the minter visits every call ([connector_resolution.py:798](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/connector_resolution.py:798)). Thus the sort key is publicly exercisable; exact-duplicate dedup may remain structurally unreachable.
- The no-plan-comparison row at [ledger:1390](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:1390) was historically true at its recorded SHA but is stale now. A current public `plan` probe with wrong declared path returns `validation_report.is_valid=false` and `CONNECTOR_REPLAY_IDENTITY_MISMATCH`: plan passes its client into validation ([workflow.py:1868](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:1868)), reads named live components ([workflow.py:1384](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:1384)), and compares during snapshot construction ([connector_resolution_snapshot.py:917](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:917)). The contrary source comment at [connector_resolution_snapshot.py:963](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:963) is stale too.

The slice map at ledger lines 23–30 is corrected. The unsupported ledger claims are the “fixed” dispositions for findings 1 and 5, the refutation of finding 4, the QName closure, and the two current LIMIT statements above.

VERDICT: ISSUES FOUND

