I’m grounding this in the exact baseline-to-tip tree, the audit ledger, and the durable evidence—not the prior slice review conclusions. I’ll use the existing #155 review memory only to recover known risk areas, then independently inspect this issue-level arc and answer A–E with exact code/plan evidence.The stored #155 review checklist reinforces two things I’m applying here: inspect the actual issue delta before trusting the ledger, and probe authority-bearing paths directly. I’m now splitting the review into independent slice-arc, replay-chain, and apply-boundary checks while I audit the issue-level evidence myself.I’ve narrowed the main risk to the account-scope link in the grant chain. I’m now probing the public authoring witness with the actual account identity deliberately unavailable, while the parallel audits finish checking slices A–E and the closure record.The focused mechanism suite is green except for one server-import test that requires the repository’s local-mode environment flag; I’m rerunning that boundary test correctly. The adversarial probe itself is decisive: the shipped “supported” witness reports `client_account_id=None` yet still mints one production grant and compiles the retried PATCH.A second, more direct bypass has surfaced in the trusted-snapshot path: a live operation XML document accepted by `ElementTree` but rejected by the stricter identity reader is retained as an “unparsed” reuse identity; the declaration/live comparison then skips it, while symbol construction falls back to caller-declared `GET`. That can classify an actual PATCH as read-only, so I’m tracing which public compile routes can reach it before fixing the severity and verdict.The implementation arc is mostly present, but the adversarial pass has now separated three things that the green suites blur together: the live PATCH witness is genuine, the authorization perimeter has fail-open arms, and the apply/attestation layer is not plan-complete. I’m tightening each finding to an exact plan clause and reachable caller impact before issuing the verdict.ISSUES FOUND. I reviewed the exact clean target `9812b98bdb85fc5619128fdfd31408535eedba16` against baseline `9860842e5932c2e091a115e4697722bd8429953c`. The PATCH capture is genuine, but the implementation is not fail-closed end to end and the issue cannot be signed off.

## Blocking findings

1. **Critical — an operation contract can authorize replay its capture did not observe.** The plan forbids any unevidenced row from authorizing retry ([plan:5](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:5)). `CapabilityEvidenceRecordV1` correctly relates replay observations to retry safety, but `OperationContractRecordV1` is fields-only ([models.py:619](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/models.py:619)); registry checks cover mapping, semantics existence, account equality and uniqueness, not capture replay versus `duplicate_guarantee` or recomputation of `record_digest` ([registry.py:281](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/registry.py:281)). Ingestion verifies the archive, then copies `operation_record.json` verbatim ([ingest_connector_replay_evidence.py:98](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/ingest_connector_replay_evidence.py:98)). A direct probe changed the record’s replay observation to `not_exercised`, retained its `same_effect` semantics and stale digest: the loader accepted it, one contract and grant were minted, and compilation succeeded.

2. **Blocking runtime defect — fetched-but-unreadable live identity falls back to caller authority.** A strict parse failure returns `document_parsed=False` ([connector_resolution_snapshot.py:400](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:400)), but a truthy fetched document is still appended as a live identity ([connector_resolution_snapshot.py:764](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:764)); unknown family/action is then skipped during comparison ([connector_resolution_snapshot.py:897](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/connector_resolution_snapshot.py:897)), while symbols retain caller `connector_metadata` ([materialization.py:193](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/recipes/materialization.py:193)). A public compile probe supplied fetched XML accepted by the outer parser but rejected by the strict reader: actual PATCH declared as GET compiled in a retried region with no grant. The plan expressly requires parseable reuse XML and trusted classification before even read-only capability decisions ([plan:203](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:203)). The ledger already calls this real, unfixed residue ([ledger:985](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:985)).

3. **Critical — missing actual account identity is treated as “skip the check.”** The plan says missing actual account ID means no grant ([plan:209](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:209)). The minter compares scope only when `account_id` is truthy ([connector_resolution.py:927](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/connector_resolution.py:927), [connector_resolution.py:978](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/connector_resolution.py:978)); apply rechecks likewise compare only when a scope was derived ([recheck.py:247](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/recheck.py:247)). The boundary helper returns `None` when the client exposes no account ([integration_builder.py:10354](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:10354)). Probes produced: matching account → grant; foreign account → refusal; missing account → grant, with the recheck also passing. The capability’s own admission witness uses a `MagicMock` client with no actual account, so it exercises this fail-open arm.

4. **Blocking compiler-contract defect — rootless compilation deliberately disables the per-call grant requirement.** `validate_error_handling` makes `grants=None` when `process_root_ref` is absent, then tests grant membership only when that value is not `None` ([error_handling.py:285](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/error_handling.py:285), [error_handling.py:378](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/error_handling.py:378)). Materialization explicitly clears root and grants, then recompiles ([process_materialization.py:560](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_materialization.py:560), [process_materialization.py:691](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_materialization.py:691)). The same conditional PATCH table refuses when projected-but-grantless and succeeds when rootless-and-grantless. That contradicts the revised contract’s unqualified requirement that conditional retry requires a per-call grant ([plan:1040](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:1040)). Normal wet apply later reconstructs a grant, so this is not by itself a demonstrated final-write bypass; it is nevertheless a public materialization artifact compiled under weaker rules than the plan permits.

5. **Critical — Unit 8 violates three mutation-accounting requirements.**

   - Create-side JIT runs before late materialization at [integration_builder.py:9400](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:9400); materialization follows at [integration_builder.py:8160](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:8160), then submission at [integration_builder.py:8287](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:8287). The plan requires recheck after late binding, immediately before submission ([plan:300](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:300)). A public probe changed the operation version during apply materialization: the process was written and drift was detected only afterward.
   - A mutation-uncertain update exception drops the already-computed submitted digest/XML and component result ([integration_builder.py:3266](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:3266), [integration_builder.py:3283](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:3283)); the caller then returns `mutation=None, readback=None` ([integration_builder.py:8378](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:8378)). A simulated transport loss after submission returned `mutation_status="performed"` but no mutation, readback, write note or replay attestation, contrary to [plan:305–310](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:305).
   - The served evidence binding omits process root, connection ref, both configuration digests and concrete route coverage ([authoring_workflow.py:856](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/models/authoring_workflow.py:856)); construction sorts only three fields and does not deduplicate ([canonical_process_apply.py:393](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/components/canonical_process_apply.py:393)). It also accepts duplicate bindings and nested account scope unequal to the parent, contrary to the required tuple and equality invariant ([plan:293](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:293)).

6. **Closure evidence is not current or internally coherent.** The last live Slice-F QA explicitly found the positive public path unreachable; subsequent reachability corrections are covered only by mocked-network tests, not a later live public safe-write pass. That live pass is an explicit closure gate ([plan:1227](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:1227), [Slice-F QA report:20](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/agents/reports/2026-09-02-issue-155-slice-f-r3.md:20)). Separately, the ledger’s authoritative slice map still records B and C with escalated-open residue, E in flight, and F not started ([ledger:23](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:23)), contradicting its later E/F clean reports ([ledger:2214](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:2214), [ledger:2285](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_155_AUDIT_LEDGER.md:2285)). Archive checksums themselves verified; this is disposition and closure-record dishonesty, not missing bytes.

## A. Plan conformance

| Slice | Judgment |
|---|---|
| A | **Built, with a justified substitution.** Canonical bindings, semantic/emitter mirrors, family capability, lineage checks, blank-path net, publication and source replay policy exist. The true source golden uses a profile rather than the planned DDP case because the legacy DDP oracle reads an unestablished property; the recorded safe replacement is justified ([PROVENANCE.md:57](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/fixtures/process_ir/issue155/PROVENANCE.md:57)). The #160 re-lowering is accepted as the stated out-of-scope residue. |
| B | **Built differently and incomplete.** Registry package, capture/ingest, models, digests, report, monitor action and image parity exist. Reading the verb from operation XML is justified by the measured generic execution action. Drift remains: the prescribed `icv1` grammar/mapping ([plan:136](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.codex/plans/issue-155.md:136)) became broad `$ref:[A-Za-z0-9_.-]+` with no bare canonical ID ([ids.py:159](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/ids.py:159)); `report.py` has no strict parser ([report.py:20](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/report.py:20)); and the required source namespace/QName-value mechanism is absent from the plain `ElementTree` projection ([digests.py:139](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/digests.py:139)). The QName gap is presently dormant because packaged QName-aware sets are empty. |
| C | **Partially built, with blocking drift.** Snapshot types and structured/submitted/live readers exist, but live unreadability fails open as finding 2. Workflow also still normalizes and recipe-compiles before constructing its live snapshot ([workflow.py:485](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:485), [workflow.py:1882](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/workflow.py:1882)); `run_recipes` has no snapshot input ([engine.py:1574](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/recipes/engine.py:1574)). |
| D | **Built differently and incomplete.** Per-root projection, grants, discovery and semantic revision exist, but authority is split across proposal, minting, rootless compilation and apply-time registry reconstruction rather than the sole-minter design. Discovery omits both configuration digests and derives no current account scope ([discovery.py:27](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/connector_replay/discovery.py:27)); the compiler revision includes rows, grammar and semantics but omits explicit vocabulary mappings ([contract.py:849](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/contract.py:849)). |
| E | **Built but not plan-conformant.** The global recheck and normal identity comparisons are sound. Create JIT placement, mutation-uncertain retention and the durable tuple are not. |
| F | **Evidence ingestion, derivation and the manifest flip are built.** `lookup_capability` derives PATCH safety through the registry rather than changing the handwritten baseline row ([connector_capabilities.py:414](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/connector_capabilities.py:414)). The current capture is credible, but the missing live gate and authority defects mean production enablement is not complete. |

## B. Replay-evidence chain

The chain is long, but not sound.

| Path | Result |
|---|---|
| Public authoring/typed compile | Fetched-but-strictly-unreadable actual PATCH can be accepted under caller-declared GET, with no grant. |
| Typed materialization compile | Per-root validation sees a grant; the subsequent rootless artifact compile clears it and accepts contract-only evidence. |
| Recipe | A normal conditional production retry currently refuses for lack of contract/grant; I found no positive recipe bypass. Its pre-snapshot identity ordering still drifts from the plan. |
| Raw apply | The base raw table lacks the contract, so the positive conditional path refuses rather than bypasses. |
| Dry apply | It traverses the rootless artifact hole, but performs no mutation and later restores root projection. |
| Wet typed apply | With a readable identity and actual account, the current packaged row obtains a grant and later rechecks it. With account identity missing, scope enforcement disappears; create JIT is also staleable after its check. |
| Ingestion/registry | A grant can be traceable to a capture that says replay was never exercised. That is provenance-shaped authorization, not replay evidence. |

So the direct answer is **yes**: there are grantless compiler/materialization authorizations, and there is a grant-minting path whose cited capture does not prove replay. I did not find a normal account-present, parseable, current-packaged-record wet submission that skips every later grant check.

## C. Capability flip

The underlying capture is honest. It records durable operation/connection version stability, two complete PATCH executions, two counterparty 200 responses, `R0 != R1`, business-state `R1 == R2`, a stable control, and movement in `modifiedOn` proving the replay actually executed ([PROVENANCE.md:20](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/evidence/issue-155/captures/cap155-e7-patch-operation-record/PROVENANCE.md:20), [PROVENANCE.md:36](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/evidence/issue-155/captures/cap155-e7-patch-operation-record/PROVENANCE.md:36)).

The reference-only `action="create"` document is genuinely caller-authorable ([test_issue_155_contract_projection.py:619](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_issue_155_contract_projection.py:619)). But its wet witness fakes the network ([test_issue_155_contract_projection.py:776](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_issue_155_contract_projection.py:776)), and the manifest witness runs with no actual account identity. Therefore:

- The evidence does support **one static REST PATCH operation as conditionally idempotent**.
- The manifest does not semantically claim general PATCH idempotence or unsupported verbs.
- The current `supported` claim nevertheless outruns demonstrated production reachability and strict account enforcement.

Evidence breadth is honest; enablement truth is not yet honest enough for closure.

## D. Next issue’s inherited regret

The specific regret is the already-served, under-specified mutation-attestation schema. It omits the configuration and route identities that were actually checked and uses an account-scope representation incompatible with its parent. Repairing that later means rotating a public contract, while historic attestations cannot reliably be reconstructed after registry or live-component state changes.

## E. Sign-off and acceptable residue

I would refuse sign-off until the operation-record semantic/digest binding, unreadable-live-identity refusal, missing-account refusal, rootless grant bypass, three Unit-8 mutation defects, live public safe-write QA, and contradictory/escalated ledger state are resolved. The served ID/discovery/fingerprint gaps are Standard blocking contract drift and require either correction or an already-filed, properly checkpointed follow-up.

I would accept as recorded residue:

- The explicitly evidence-absent verb rows.
- The #160 dynamic-path lowering/map cutover limitation.
- The pre-existing typed-root `orchestrate_deploy` limitation.
- The source-golden substitution justified by the unsafe legacy DDP oracle.
- No live evidence reads during a non-mutating dry run.
- Sequential partial mutation before a later JIT refusal, but only when all prior mutations, readbacks and attestations are retained truthfully.

Focused validation was green—594 passed and one skipped—but the adversarial authority and mutation probes above still produced the fail-open results.

VERDICT: ISSUES FOUND

