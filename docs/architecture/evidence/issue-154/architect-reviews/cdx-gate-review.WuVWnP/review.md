I’ll audit this as a plan-conformance gate, not another general code-quality review. I’ll keep the tree untouched, reconcile every claimed item and acceptance criterion against production code/tests/evidence, and pay special attention to the narrower subprocess analysis, map authority, reachability, and the unattested golden.The working tree is clean at `031c57c`, and the stated baseline is a valid ancestor. One bookkeeping mismatch is already visible: the range contains 20 commits, not 19; I’m treating that as evidence hygiene unless the extra commit changed shipped behavior beyond the plan.The repository calls for the `receiving-code-review` discipline, but that skill is not exposed in this session’s available-skill catalog. I found the local instruction file and will use it as read-only review guidance: reproduce and verify claims technically, rejecting both false positives and compliance-only fixes.The effect subsystem is where the plan-to-code gap is concentrating. I’m now checking two concrete cases the shipped tests do not cover: a valid map-level document-cache join, and an authored subprocess whose guaranteed effects arise through control flow rather than only on the root spine.The implementation does realize the six grammar behaviors, but the audit is finding several substantive plan gaps in the effect-authority half—especially public-path reachability and subprocess summary semantics. I’m validating those against focused tests and the served contract now, while treating the filed #179 limitation separately rather than double-counting it.The reconciliation is complete. Two especially important corrections to the existing audit record emerged: legacy `source → map_ref` requests do reach the real public compiler (so the ledger’s blanket reachability claim is false), and HEAD contains a twentieth corrective commit that the archived Stage‑2 review does not cover. I’m doing the final clean-tree/attestation check and then I’ll give the prioritized verdict.Nine plan-conformance issues remain; two are P1 behavioral authority failures.

1. **[P1] Map effects omit document-cache joins.** [`derive_map_effect`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_ir_effects.py:386) returns an empty replay-safe effect for direct maps without inspecting `document_cache_joins`. A public typed request containing a valid cache join, no writer, and an empty effect declaration planned and compiled successfully. This is independent of #179 because the probe used indexed `$ref` profiles.

2. **[P1] Subprocess summaries are not the planned path-sensitive analysis.** The flat CFG scan in [process_ir_effects.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_ir_effects.py:457):

   - reports reads even after an in-child write;
   - misses writes made in every Decision arm;
   - marks `cache_put` and persisted-property children replay-safe;
   - trusts cycles and opaque descendants instead of making them inert.

   Recorded deviation 7 therefore delivers materially incorrect authority, not merely a narrower implementation.

3. **[P2] External-writer identity incorrectly depends on the authored flag.** Resolution binds only flagged `cache_get` occurrences at [process_ir_effects.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_ir_effects.py:650). Under the plan, an unflagged matching declaration remains valid but cannot downgrade the missing-writer error. Shipped behavior instead rejects it as an invalid/unbound declaration, violating the four-case truth table.

4. **[P2] The shared control union remains duplicated.** [`LinearNodeV1`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/models/process_ir.py:1119) and [`ControlBodyStepV1`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/models/process_ir.py:1184) separately repeat the linear members. Current sets are equal and the ten placement rows are genuinely derived, but a future linear kind can still silently fail to propagate into every control body—the duplicate-authority defect the plan intended to remove.

5. **[P2] Served-contract item 10 is incomplete and internally contradictory.**

   - `target` still says it must be followed by `stop`, excluding the shipped return terminal ([projection.py:316](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_ir_projection.py:316)).
   - The new generalized connector-root ordering and entry-role rules are absent.
   - `cache_get` says its flag alone legitimizes a writerless read ([projection.py:462](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_ir_projection.py:462)), while a later rule correctly requires both flag and declaration.
   - The effect rules are hand-written rather than generated from [`effect_authority_rows()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_ir_effects.py:687), and the planned generated INERT row is absent.
   - `errors.py` and semantic remediation still tell callers merely to supply a typed contract, omitting the server-authority requirement.

6. **[P2] Additional public-schema deviations were not recorded.** Declaration-family tuples reject duplicates but are not canonicalized at [authoring_workflow.py:397](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/models/authoring_workflow.py:397); reordering equivalent declarations changes the normalized payload and hashes. The planned script-language alias is also absent, and the public script declaration accepts any non-empty language.

7. **[P2] Required public integration coverage is missing.** The ledger’s assertion that no public `map_ref` compiles is false: legacy `source → map_ref` flows intentionally bypass call-pair profile checking ([connector_resolution.py:534](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/connector_resolution.py:534)), and a real declared map request plans and compiles successfully. However, checked-in tests exercise resolvers directly or substitute a fake resolver ([test:882](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_process_ir_effect_declarations.py:882)); none supplies real map/registered-script declarations through plan→compile→emit, and no effect goldens shipped. Live QA’s blanket reachability rationale tested the wrong topology.

8. **[P2] The connector-prefix golden is not an independent oracle.** Its provenance explicitly records the prefix as UNATTESTED ([PROVENANCE.md:35](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/fixtures/process_ir/issue154/PROVENANCE.md:35)). It may remain as a clearly non-authoritative regression snapshot, but it cannot satisfy the clean-room golden requirement until backed by a UI-authored or executed-green reference.

9. **Completion validation is stale on HEAD.** The last archived Stage-2 review covers only through `b6f4838` ([ledger:128](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_154_AUDIT_LEDGER.md:128)). HEAD `031c57c` then corrected that review’s findings, including a test-guard change, without the unconditionally required affected validation and fix-only review. The range contains 20 commits, not 19.

The current runtime grammar for items 1–5 otherwise behaves correctly, the six probe shapes compile, adversarial neighbors retain their codes, root-local fresh internal capabilities are used, and no new legacy spelling or unexplained scope creep was found. #179’s substantive deferral and the recorded technical refutation are valid; neither covers the findings above. The focused review suite passed 319 tests, and the worktree remained unchanged.

VERDICT: ISSUES FOUND
