I’ll review only `c2ebc43..05f9b96`, with special attention to placement parity, every intentional yield, and the ledger/prose-guard claims. I’ll keep the tree unchanged and report only reproducible contradictions. The referenced `receiving-code-review` skill is not available in this session, so I’ll apply its stated verify-before-accepting discipline directly.The delta is cleanly scoped to the five named commits. One potentially important boundary is already visible: the new root verdict intentionally yields for `[control, process_call]`, so I’m checking the full compiler diagnostic—not just whether some later invariant happens to fail.I’m also using the GitHub review skill to verify that #177 was actually filed at reconciliation with the required scope and placement; the ledger alone cannot establish that external fact.The placement authority still has untested precedence rings. I’ve reproduced mismatches for root source ordering, ancestor-connector nesting, Try/Catch continuation, and a `cache_put` prefix. I’m now checking the new regression and prose-guard tests themselves, because two currently pass without exercising the behavior their comments claim.I’ve completed the delta review and am tightening the report to the smallest set of independently reproducible contradictions. The malformed placement cases still fail closed; the defects are in diagnostic authority/precedence plus several claimed validation and record fixes.I found six concrete contradictions.

1. **Root placement precedence still diverges.** In [process_ir.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/models/process_ir.py:1251), the shared verdict runs under different surrounding rules:

   - For `body.steps=[process_call, source]`, parsing returns `PROCESS_IR_SCHEMA_INVALID_CARDINALITY` at `/body` (“source may appear only as the first step”), while compiling the equivalent mutated model returns `PROCESS_IR_CAPABILITY_UNSUPPORTED` at `/body` for connector mixing.
   - For the exact `[branch, process_call]` document constructed in [test_process_ir_rich_control_bodies.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_process_ir_rich_control_bodies.py:1610), parsing returns `PROCESS_IR_SEMANTIC_CONTROL_CONTINUATION_UNSUPPORTED` at `/body`; full compilation returns `PROCESS_IR_SEMANTIC_AMBIGUOUS_FLOW` at `/body/steps/0`. The added fail-closed test only asserts that some nonempty code exists, so it does not prove the claimed owning rule or diagnostic parity.

2. **Body and ancestor placement remain separate authorities.** The independent branches in [body_capabilities.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/body_capabilities.py:215) still disagree with model validation:

   - Root connector → Branch leg terminal Process Call: both paths return the same code and pointer, but the parser message includes “a connector runs upstream of this body”; the compiler message omits it.
   - Replacing the first step of `scoped_try_catch_process_retry0_exception.json`’s process-scoped `try_body` with a Process Call gives parser `PROCESS_IR_CAPABILITY_NODE_NOT_ALLOWED_IN_BODY` at `/try_body/steps/0`, versus compiler `PROCESS_IR_CAPABILITY_ERROR_SCOPE_UNSUPPORTED` at `/try_body/steps`.
   - Branch leg `steps=[{"kind":"cache_put","cache_ref":"cache"}]`, `terminal=process_call` gives parser `PROCESS_IR_SCHEMA_INVALID_CARDINALITY` at the leg object, versus compiler’s dedicated return-path code at `/terminal`. This also violates the plan’s explicit dedicated-code requirement for every prefixed terminal Process Call.

3. **The “update-repair pin” never exercises an update.** [test_wrapper_subprocess_builder.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_wrapper_subprocess_builder.py:432) separately inspects the frozen before-image and performs a fresh `WrapperSubprocessBuilder.build()`. It never passes the old XML through `merge_for_update` or `_apply_structured_update`, which is the real update path in [integration_builder.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:3184). A regression that retained the old Stop or edge during merging would still pass this test. Current merging works, but the claimed permanent update regression does not exist.

4. **Known DC-175-D instances remain stale, so the deviation cannot truthfully say every instance is fixed.**

   - [M12_COMPATIBILITY_INVENTORY.md](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/M12_COMPATIBILITY_INVENTORY.md:108) still publishes nonempty plural `process_calls`, optional Return Documents, and `start → processcall(s) → stop/return`; line 651 repeats “ordered process_calls + Stop/Return Documents.”
   - [process_flow_builder.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/components/builders/process_flow_builder.py:22) still documents catch routing to Stop and a wrapper `start → process call(s) → stop`; the later canonical-chain comment repeats it.
   - The machine-served `emittable_today` entry in [design_doctrine.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/kb/design_doctrine.py:232) still advertises a parent referencing each child and child documents returning to that parent.

   Issue #177 may legitimately own a general consistency mechanism, but it does not make these plan-listed, already-known instances fixed.

5. **The defect-class table is already stale again.** [ISSUE_175_AUDIT_LEDGER.md](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_175_AUDIT_LEDGER.md:69) says counts are mechanically derived, yet DC-175-E remains 5 while later rows explicitly identify its sixth and seventh instances and then add `L2-r8-01`/`L2-r8-02`; DC-175-G remains 4 despite later fifth and sixth instances; DC-175-D’s seven-item list omits `A-175-01`, whose own class cell is DC-175-D. The claimed recomputation does not cover the final delta rows.

6. **The prose guard still mishandles polarity and cannot serve as the recorded structural fix.** [test_wave_gate.py](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_wave_gate.py:6353) searches for negation only before the equality verb. Consequently the accurate, in-scope sentence `Submitted bytes match no live readback.` is classified as forbidden. More fundamentally, lines 6309–6317 explicitly disclaim complete coverage and instruct future reviewers not to treat another missed phrasing as a defect. That is honest as a best-effort lint, but it contradicts the ledger’s claim that this is DC-175-G’s structural invariant: [CLAUDE.md](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/CLAUDE.md:475) requires coverage derived from the authority’s full case set, otherwise a recorded deviation is required. The recorded #177 deviation is for DC-175-D, not DC-175-G.

The malformed placement cases above remain fail-closed; I found diagnostic-authority and validation-record defects, not a reachable connected Process Call.

VERDICT: ISSUES FOUND
