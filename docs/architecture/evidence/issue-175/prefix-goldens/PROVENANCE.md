# Pre-baseline frozen bytes — NEGATIVE-ONLY witnesses

Captured at baseline `3fd5027e16d4f9fe5377884a0140909a3b4d1e67`, **before the first source
edit of issue #175**, by byte-for-byte copy from the working tree.

## What these are

Three generated artifacts that, at the baseline, pin the defect #175 exists to fix: a Process
Call shape carrying an EMPTY return-path element together with an outgoing dragpoint to a
trailing Stop. Under the platform model recorded in `ISSUE_175_AUDIT_LEDGER.md`, that pairing
is invalid — the Boomi UI declines to draw the connection and the Stop is left orphaned on the
canvas.

| File | Copied from |
| --- | --- |
| `processcall_standalone_parent.xml` | `tests/fixtures/golden_xml/processcall_standalone_parent.xml` |
| `process_ir_rich_branch_process_call.xml` | `tests/fixtures/golden_xml/process_ir_rich_branch_process_call.xml` |
| `wrapper_flow.process.xml` | `tests/fixtures/process_ir/emitter_parity/wrapper_flow.process.xml` |

## What they may be used for

**Negative controls only.** Their whole evidential value is that they were captured before the
fix and can never be regenerated afterwards: once the grammar and emitters change, the case
definitions that own these goldens produce the corrected bytes, and the pre-fix form ceases to
exist anywhere in the tree.

They demonstrate exactly one thing: that the new graph-verifier continuation invariant is
NON-VACUOUS — it rejects artifacts this repository actually produced.

## What they may NOT be used for

These are **generated by the implementation under test**. Under the clean-room fixture rule in
`CLAUDE.md` Stage 1 step 2, they are therefore disqualified as positive oracles. They may never
be used to establish what the platform accepts, what a valid Process Call looks like, or what
the corrected emission should be.

The positive oracles for this slice are, and remain:

- the UI-built captures under `tests/fixtures/live_xml/m11/` (frozen long before this baseline);
- the platform's own Process Call and Return Documents documentation;
- components built through the Boomi UI during the Stage-1 QA engagement;
- live readback of components proven operable.
