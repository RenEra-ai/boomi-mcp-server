I’ll treat this as the architect-conformance gate only. I’m splitting the inspection across the three invariants, while I independently verify the full diff, audit evidence, scope boundaries, and run the relevant guards/mutants where safe.The current tree is clean at `03aadd7`. I’ve confirmed the major deviations are explicitly recorded, including keeping `diagnostic()` total and narrowing the AST/data-flow mechanism. I’m now stress-testing the guards rather than relying on the archived green summaries; one parser edge already looks worth probing because its key regex is only left-anchored.The stress probes have now produced two acceptance-level escapes: the exact A6-01 default mutant is killed, but an explicit forwarded code override and a keyword-form sink call both leave all 11 served-text guards green. Separately, §8 can be hidden inside a Markdown HTML comment while the parser still reports 27/27. I’m checking whether the capability witness “kind” is similarly declarative rather than behavior-binding.The clean suite is masking several structural gaps: I’ve reproduced green guards against real authority changes in all three invariants. I’m consolidating only the deviations that materially break the plan’s pins, plus the audit-evidence gap.## Findings

- [P2] [tests/_process_ir_diagnostic_emissions.py:456](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_process_ir_diagnostic_emissions.py:456) — A6-01’s exact default-expression mutant is now caught, but deviation 4 remains unsound. Runtime inspection covers defaults only, while collection examines only a sink’s first positional argument at [line 536](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_process_ir_diagnostic_emissions.py:536). Changing the real explicit forwarded code at [invariants.py:616](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/invariants.py:616) to a constructed unregistered value left all 11 served-text tests green while changing the emitted diagnostic. Keyword sink calls likewise disappear, and direct `CompilerDiagnostic` construction at [pipeline.py:124](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/pipeline.py:124) is not pinned. The claimed full emitter case set and restructuring pin therefore remain incomplete.

- [P2] [tests/test_process_ir_served_text_enforcement.py:318](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_process_ir_served_text_enforcement.py:318) — the reverse pin merges all three registries into one code set, losing producer ownership. Adding a compiler-owned delegated code to the semantic registry passed every relevant guard and caused projection to advertise bogus semantic-validator attribution. This violates the plan’s producer-specific equations and requirement that delegated codes resolve exclusively to their owning compiler specs.

- [P2] [tests/_process_ir_capability_witnesses.py:373](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_process_ir_capability_witnesses.py:373) — `mixed_connector_execution` only counts two `connectoraction_*` nodes; it does not prove both source and target emitter forms. A mutant emitting the database target as another `connectoraction_source` left all seven capability-enforcement tests green. The implementation also unrecordedly substitutes REST→DB with no map for the planned REST→map→SOAP case at [line 360](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_process_ir_capability_witnesses.py:360).

- [P2] [tests/_process_ir_capability_witnesses.py:582](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_process_ir_capability_witnesses.py:582) — the typed-idempotency observer proves only that evidence is non-null, not that the semantic record preserves the exact authored `{"kind":"verified_action"}` evidence. Injecting an invented `contract_ref` during lowering left all seven capability guards green.

- [P2] [tests/test_process_ir_authoring_contract_parity.py:1050](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_process_ir_authoring_contract_parity.py:1050) — the §8 guard parses raw lines without proving they form rendered Markdown authority. Wrapping the real §8 heading and table in an HTML comment still returned the exact 27/27 mapping, while rendered Markdown contained neither the heading nor the table. A structurally removed authority therefore leaves invariant 3 green.

- [P3] [tests/_process_ir_entrypoint_differential.py:79](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_process_ir_entrypoint_differential.py:79) — the extracted API does not implement the plan-required structured internal-failure outcome. Unexpected parser/compiler exceptions escape raw; substituting a compiler that raises `RuntimeError` demonstrated this. The deviation is not recorded.

- [P3] [tests/test_issue_177_ledger_class_table_is_derived.py:69](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_issue_177_ledger_class_table_is_derived.py:69) — A6-07 is not structurally closed. `_finding_rows()` ignores valid compact Markdown rows such as `|A6-10|...|` because it requires `| `; such a row renders in the table but leaves the derived-class guard green.

- [P3] [docs/architecture/evidence/issue-177/index.jsonl:1](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/evidence/issue-177/index.jsonl:1) — completed Stage-1 QA/live evidence is not durably archived as designed. The ledger cites reports under `agents/reports/`, but that directory is ignored at [.gitignore:131](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/.gitignore:131) and has no tracked files. The archive contains non-vacuity and review artifacts, but not the promised targeted/full QA summaries or live MCP evidence. This finding excludes the still-owed evaluation-2 and wave artifacts.

Current HEAD `03aadd76e1b03f1f57277f1d13e8436401b7d0b6` is clean, and the combined targeted suite passed 556 tests. I confirmed A6-02, A6-04, A6-05/06, and A6-08/09; the exact A6-01 default mutant and historical L2-r6-01 mutant now fail as recorded. No additional inert non-vacuity record, withdrawn prose scanner, or work owned by #147/#154/#155/#156/#158/#176 was found.

Complexity judgment: invariant 1 is now more intricate than its property justifies and still fails ordinary Python restructuring. A total canonical construction path plus a small ban on direct diagnostic constructors would be simpler and stronger. The provenance machinery is intricate but currently sound; the capability registry remains worthwhile once its two underspecified observers are made exact.

VERDICT: ISSUES FOUND
