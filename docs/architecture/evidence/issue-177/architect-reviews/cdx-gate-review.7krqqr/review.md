I’m treating this as a design-conformance audit, not reopening the repo’s general code-review gate. I’ll inspect the exact baseline delta, split the three invariants into parallel checks, and independently reconcile the ledger, call graph, mutants, and acceptance criteria against the current tree.The repository is clean at `3c0e6eb` on the requested branch, and the implementation delta matches the stated inventory. I’m now stress-testing the “authority can’t be renamed out from under the guard” property—the place where a nominally exhaustive test most often becomes vacuous.I found several reproducible conformance gaps despite the clean ordinary review: most importantly, a dynamically constructed diagnostic code can still reach production while every new served-text guard stays green. I’m consolidating those mutants with the plan deviations and audit evidence now.## Findings

- [P2] The invariant-1 scanner is not fail-closed over the full emitter set. It recognizes only first-parameter wrappers at [tests/_process_ir_diagnostic_emissions.py:210](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_process_ir_diagnostic_emissions.py:210), while its census sees only complete code-shaped literals/constants at [line 398](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_process_ir_diagnostic_emissions.py:398). Changing the real sixth-parameter default at [invariants.py:676](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/invariants.py:676) to `"PROCESS_IR_" + "SEMANTIC_TOTALLY_NEW_UNREGISTERED"` created a runtime-emittable unregistered code, yet all ten served-text tests passed. Direct `CompilerDiagnostic` constructors are also absent from the pinned sinks at [line 91](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_process_ir_diagnostic_emissions.py:91), and the planned `legacy_adapters/**` boundary assertion is missing. Thus the pinned table plus census does not recover what deleting the resolver lost.

- [P2] Injected projection sources bypass the planned completeness validation. [process_ir_projection.py:1143](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_ir_projection.py:1143) accepts blank diagnostic fields, and [line 1602](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_ir_projection.py:1602) routes injected sources directly into that merge. Blanking an injected compiler message still built all 182 entries and served its remediation as the summary. This contradicts the explicit file plan and makes the “fallback unreachable by construction” claim false.

- [P2] Seven of eight supported capability witnesses discard the emission plan and inspect only CFG/model facts, beginning at [tests/_process_ir_capability_witnesses.py:218](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_process_ir_capability_witnesses.py:218). A compiler substitute returning the real CFG but no emission plan left all seven green; only `bounded_retry` failed. Consequently, the planned connector-action, process-call transition, nested/bare-false emission, `catcherrors`, and exact idempotency observations are not enforced.

- [P2] Capability-fixture provenance is weaker than recorded. [tests/_process_ir_capability_witnesses.py:27](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_process_ir_capability_witnesses.py:27) says inline documents are refusal inputs, but five supported admission witnesses are inline. The two listed frozen error-handling fixtures are never loaded, while [test_process_ir_capability_enforcement.py:189](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_process_ir_capability_enforcement.py:189) accepts any nonblank provenance string.

- [P2] The §8 heading pin is substring-based. [test_process_ir_authoring_contract_parity.py:983](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_process_ir_authoring_contract_parity.py:983) remained green after rewording the authority to `## 8. Capability states (normative)`. This violates acceptance criterion 1’s rewording requirement.

- [P2] The §8 parser silently excludes valid Markdown table rows without optional outer pipes. [test_process_ir_authoring_contract_parity.py:1012](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_process_ir_authoring_contract_parity.py:1012) considers only lines starting with `|`. Adding `` `invented_doc_only` | gated | #999 `` produced another table row under Python-Markdown but was ignored, leaving 27/27 equality green. The claimed full table case set is therefore incomplete.

- [P3] The audit ledger’s derived defect-class table is stale. [ISSUE_177_AUDIT_LEDGER.md:95](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_177_AUDIT_LEDGER.md:95) lists only DC-177-A with two rows; the 21 finding rows actually classify as A=4, B=7, C=3, D=7.

- [P3] The recorded call-graph rationale is overstated. [ISSUE_177_AUDIT_LEDGER.md:67](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ISSUE_177_AUDIT_LEDGER.md:67) says every production handler catches only `ProcessIRCompileError`, but broad handlers exist, including [engine.py:1756](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/recipes/engine.py:1756) and [integration_builder.py:7913](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/categories/integration_builder.py:7913). Enough pre-`try` and typed-only paths exist to justify keeping `diagnostic()` total, but the universal claim is false.

- [P3] QA-177-r1-01’s replacement prose remains overbroad at [process_ir_projection.py:1385](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/authoring/process_ir_projection.py:1385): native graph-verifier codes deliberately remain outside the three registries, and injected rows can still reach the fallback. QA-177-r1-02’s correction is accurate.

## Recorded deviations

1. Keeping `diagnostic()` total is sound, but its call-graph explanation needs qualification and its claimed guard-based unreachability is disproved by the mutant above.
2. `_complete_spec_rows` and its lazy private import are acceptable; the missing injected-source validation is the actual gap.
3. Compiler-owned served text is sound. Producer attribution makes cross-producer text identity inappropriate.
4. Deleting the resolver is not adequately compensated by the census and pins; this deviation is unsound.
5. Avoiding duplicate #178 vector equality is sound: the live registry genuinely covers 27/27 keys, and reachable field-level gates have compile halves. The admission witnesses nevertheless omit planned emission observations and independent fixture provenance.

The real L3-04, L2-r6-01, state-drift, and header mutants are accurately recorded and fail their intended guards. The one historical false-pass witness is candidly documented and corrected; evidence hashes validate. No withdrawn English-prose predicate or work owned by #147, #154, #155, #156, #158, or #176 was introduced. The focused suite passed 113 tests, but the additional mutants above expose the acceptance gaps.

VERDICT: ISSUES FOUND
