I’ll review the literal `ca5d778… → 6ac2b5d…` delta, then trace both fixes into the parser/model behavior and run focused tests or adversarial probes as needed. I’ll treat only concrete blocking or substantive defects as findings.The container rebuild has a narrower claim than its docstring: it normalizes dict/list shells, but deliberately leaves scalar objects—and currently dict keys—unchanged. I’m testing whether any such surviving object can still execute inside the parser, and separately checking that case IDs correspond to the documents they label rather than only to the expected vocabulary.Two concrete defects are now reproduced: a surviving scalar subclass still forges parser provenance, and the “exact product” check proves only labels, not the documents behind them. The current focused suite passes, but both adversarial probes pass through its protections, so I’m doing one final plan-compliance scan before the verdict.Findings:

- **CRITICAL — the payload is not fully inert.** [`_inert_payload`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/compiler/process_ir/pipeline.py:161) preserves scalar objects and dict keys. A `str` subclass placed in `version` can override `__ne__`; the parser invokes it during the version comparison at [process_ir.py:2835](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/src/boomi_mcp/models/process_ir.py:2835). I reproduced it raising a forged `ProcessIRValidationError` with a real parser code: the caller-chosen path, message, remediation, and canary were served verbatim. Thus this is another instance patch, not a root fix. That said, it requires malicious in-process Python code, which can also monkeypatch the compiler. If such callers are outside the trust boundary, that limitation should be explicitly accepted and this hardening loop should stop; under the currently claimed contract, it remains Critical.

- **STANDARD — the exact-product assertion validates labels, not constructed documents.** `_generate` builds the ID from `neighbor` separately from the model returned by `_build` at [test_process_ir_entrypoint_diagnostic_parity.py:297](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_process_ir_entrypoint_diagnostic_parity.py:297), while the new assertion compares only those IDs at [test_process_ir_entrypoint_diagnostic_parity.py:461](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_process_ir_entrypoint_diagnostic_parity.py:461). I substituted the actual Branch terminal `target` with `exception` inside `_build` while retaining the original IDs: 40 cases labeled `opp=target` contained `exception`, yet the exact-product, partition, denied-cell, diagnostic-parity, and safety tests all passed. The missing plan requirement is an independent projection from each constructed document back to its claimed `(context, slot, candidate, neighbor, mode)` signature.

No rebuild regression was otherwise found: all 31 focused tests passed, `datetime` remained refused, `bytes` remained parser-accepted, and all 682 parser-accepted generated documents survived the actual inert rebuild with identical canonical JSON.

VERDICT: ISSUES FOUND
