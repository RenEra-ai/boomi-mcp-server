I’ll review only `f4235b9..HEAD`, trace each of the eight corrections to its tests and fail-closed behavior, and treat substitutions 3 and 8 as explicit design decisions rather than reopening cleared code. I’ll report only actionable delta findings.The first pass has isolated two contracts that need adversarial checks rather than just reading the happy-path tests: local bootstrap reuse from an independently landed ref, and the “worktree unchanged” fingerprint on already-dirty files. I’m reproducing both before deciding severity, and I’m separately validating the CI-environment refutation.The adversarial checks confirmed residual exit‑0 paths, not just documentation disagreements: a provider can terminate the wave with `SystemExit(0)`, an independently replayed bootstrap is accepted after the real introduction has landed elsewhere, and same-count rewrites of dirty files are invisible to hygiene. I’m doing the final reconciliation now, including severity and the two requested substitution judgments.## Findings

- [P1] [scripts/wave_gate.py:954](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:954) — substitution 3 remains replayable. The code checks whether another ref contains the current branch’s exact introduction commit. A stale branch can independently recreate both manifests after another commit already landed them on `dev`; because the SHAs differ, `--bootstrap` exits 0 and skips every transition check. Check other refs’ history for either manifest path, independent of commit identity.

- [P1] [scripts/wave_gate.py:210](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:210) — ambiguous baseline detection covers only `heads`, `tags`, and `remotes`. I reproduced `refs/ambiguous` plus `refs/heads/ambiguous`; `resolve_baseline(..., base="ambiguous")` silently accepted the former. Revision expressions such as `ambiguous~0` also bypass the check. This can validate the wrong transition range.

- [P1] [scripts/wave_gate.py:252](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:252) — HEAD, porcelain, and numstat are not a content fingerprint. Same-line-count rewrites of an already-dirty tracked file—and any rewrite of an existing untracked file—produce identical before/after values. Local `wave` can therefore mutate user bytes and still pass hygiene.

- [P2] [scripts/wave_gate.py:1466](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:1466) — provider failures can still exit green. `_provider_call` catches only `Exception`; a provider or generator raising `SystemExit(0)` terminates the wave with status 0 and bypasses final hygiene. I reproduced this. `cases`/`mutations` attribute access also occurs outside the guard.

- [P2] [tests/_wave_gate_golden_corpus.py:561](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/_wave_gate_golden_corpus.py:561) — `unittest.SkipTest` is re-raised rather than converted to `RendererMismatch`. Pytest treats it as a skip, so ordinary CI can leave a golden unrendered while remaining below the 30-skip cap.

- [P2] [scripts/wave_gate.py:315](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:315) — PR checkout binding tests parent membership, not exact parentage. An octopus commit containing the head and target among additional parents is accepted despite not being a `refs/pull/N/merge` commit. Require exactly the expected two parents, preferably target then head.

- [P2] [ENDGAME_VERIFICATION_GATE.md:437](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/docs/architecture/ENDGAME_VERIFICATION_GATE.md:437) — substitution 8’s `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` refutation is incorrect. `anyio.run()` does not depend on AnyIO’s pytest plugin. With autoload disabled, all 9,712 nodes still collected and the two cited modules passed 50/50 tests. Omitting the flag leaves CI exposed to plugins from drifting transitive dependencies.

- [P2] [tests/test_wave_gate.py:203](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_wave_gate.py:203) — the bare-`wave` negative asserts only exit status 2, not `BASELINE_EVENT_INVALID`. Reverting to argparse’s uncoded failure would leave this test green, so the promised diagnostic regression proof is absent.

Substitution 3 is rejected as implemented. Substitution 8 is partially accepted: the three `BOOMI_*` omissions and two diagnostic aliases remain fail-closed, but the pytest-plugin omission is not technically refuted.

The digest/material checks, four mutation classes, exact floor equality, tombstone reporting, PR bootstrap anchoring, push binding, and rollout-status corrections otherwise match the plan. Targeted result: `167 passed`; worktree remained clean.

VERDICT: ISSUES FOUND
