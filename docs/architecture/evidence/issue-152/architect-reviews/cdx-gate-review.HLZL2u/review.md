I’ll review only `1627a96..HEAD`, tracing each of the three corrected paths and their focused tests. I’ll treat the inlined plan as context, not as review scope, and I’ll specifically look for fail-open behavior and a prose-independent alternative to Git stderr matching.The three intended paths are present. I’m now stress-testing the exception boundary and the Git-accounting assumption at their actual trust boundaries; one subtle distinction matters here: catching `BaseException` is only fail-closed if provider-controlled magic methods cannot reintroduce a status-bearing `GateFailure` outside the guarded call.A concrete Git case breaks the narrow classifier: tracked-path `lstat()` failures are printed as `<path>: <errno text>` and Git continues with status 0. I’m verifying that against the installed Git and then I’ll consolidate it with the fingerprint-boundary result; the tombstone correction itself is holding up.Two blocking issues remain.

1. **[P1 — mutation accounting] The stderr enumeration still fails open.** [`_refuse_unreadable()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:372) accepts access errors outside seven phrases. With installed Git 2.50.1, a tracked-path symlink loop produced status 0 and `dir/file: Too many levels of symbolic links` from both `git status` and `git diff`; `_status()` returned normally. Git deliberately prints arbitrary `lstat()` errno text and continues, and its directory iterator can treat `readdir()` errors as EOF without any diagnostic. [Git source: tracked paths](https://github.com/git/git/blob/v2.50.1/diff-lib.c#L229-L233), [directory iteration](https://github.com/git/git/blob/v2.50.1/dir.c#L2399-L2408).

   Locale pinning cannot make that complete. A prose-independent resolution is to build/reconcile the inventory using NUL-delimited index data plus a byte-path `os.scandir` walk, directly `lstat`/read/hash relevant entries, and fail on errors while opening or advancing directories. Ignored paths can be classified through structured `git check-ignore` exit/output rather than stderr prose.

2. **[P1 — fail-open exit] The fingerprint boundary still trusts provider-controlled `GateFailure`.** [`except GateFailure: raise`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:1909) passes through any embedded status. Provider-returned objects are exercised after `_provider_call`, including `len(result)` at [`_fingerprint()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:1734). A tuple subclass whose `__len__` raises `GateFailure(..., status=0)` reaches [`main()`](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:1997), which returns 0; I reproduced the full orchestrated path with `main_status=0`. The handler also formats `{exc!r}` inside itself, so a failing `__repr__` can escape it with `SystemExit(0)`.

   Normalize every non-exact, non-status-1 phase exception into a fresh status-1 failure without formatting the exception object, and coerce/validate provider outputs as exact built-in types inside the guard. The new [SystemExit test](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/tests/test_wave_gate.py:2083) exercises the pre-existing direct-call guard, not this outer boundary.

The born-tombstone reporting and floor-neutral accounting are faithful; I found no issue there.

VERDICT: ISSUES FOUND
