I’ll audit `d90a315..HEAD` read-only, with separate passes on the streaming-summary fix, the deferral/rollout ledger, and any remaining fail-open paths. I’ll reproduce concrete edge cases before treating them as findings.The round-13 code now counts every summary candidate during streaming, so the 400-line ring can no longer erase the genuine pytest outcome. I’m running the focused regression plus the broader gate unit suite, and I’m checking whether the summary grammar itself leaves another reproducible false-green case.I reproduced one remaining false-green against real pytest 9.0.2: a normal local pytest hook can place a forged outcome fragment immediately before pytest’s genuine stats on the same physical line. The gate counts that as one candidate and reads the first counts, so a wholly skipped run returned green even with `maximum_skipped=0`. I’m finishing the independent whole-gate pass before deciding whether there are additional findings.A second independent path is under verification: the parser ignores `deselected`/`xfailed` outcomes and only requires the pass count to stay within the skip-cap headroom. That can let a required test remain collected but be removed only from the full run. I’m checking it with an actual pytest hook, not just synthetic text.- [P1] Count outcome summaries, not newline records — [scripts/wave_gate.py:1470](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:1470). With pytest 9.0.2, a local hook can append forged and genuine outcomes to one physical line: `100 passed, 0 skipped in 0.01s 100 skipped in 0.01s`. The parser treats this as one candidate and reads the first counts; `run_suite` returned green with all 100 tests skipped and a zero skip cap. Require a strict full-line grammar and reject repeated outcome/duration clauses.

- [P1] Reconcile every execution outcome with collection — [scripts/wave_gate.py:1503](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:1503). A hook can retain a required node during collection but deselect it only during execution. I reproduced `229 passed, 1 deselected`, exit 0, with 230 collected; the gate returned success because `deselected` is ignored and the missing pass fits inside skip-cap headroom. A required golden test can therefore avoid execution. Reject unapproved outcomes and require the accounted test total to equal collection.

- [P2] Validate the complete render envelope — [scripts/wave_gate.py:1658](/Users/gleb/Documents/Projects/Renera/boomi-mcp-server/scripts/wave_gate.py:1658). `_render_pass` ignores `len` and `sha256`, accepts unknown fields, and uses permissive base64 decoding. It accepted `{"len":0,"sha256":"not-the-payload","b64":"PHgvPgo=!!!"}` as `b"<x/>\n"`; two such envelopes can pass determinism and expected-byte checks despite violating the required child protocol. Enforce the exact schema, canonical base64, length, and digest.

The #165 lineage correction and full rollout-owed record are now consistent, and #152 remains open. The code is not ready to land while these paths remain.

VERDICT: ISSUES FOUND
