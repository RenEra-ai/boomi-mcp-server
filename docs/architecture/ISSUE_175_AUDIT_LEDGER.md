# Audit ledger — issue #175 (M12 defect slice)

Instantiated at Stage-1 step 0 per `docs/architecture/templates/AUDIT_LEDGER_TEMPLATE.md`.
Conventions inherited from #152's end state apply from row one; platform-behaviour claims
carry a provenance marker (`measured here` / `documented, not measured` / `assumption`).

## Baseline (Stage-1 step 0)

- Issue: #175 — Process Call outbound connectivity contradicts the platform's return-path model
- Step-0 baseline: `3fd5027e16d4f9fe5377884a0140909a3b4d1e67`
- Measured green baseline at that tree: **10096 passed, 17 skipped** (full non-KB suite,
  `.venv` 3.12, `PYTHONPATH=src`, plugin autoload disabled) — *measured here*.
- Slice kind: behaviour-affecting (emitted process XML changes on every Process Call surface)
- Artifact trust boundary: the slice CREATES AND OWNS the terminal Process Call grammar, the
  new capability code and its remediation, the graph-verifier continuation invariant, the
  legacy catch-leg composition rules, the wrapper singleton rules, this ledger, and its
  archive. It CONSUMES, unchanged and as the positive oracle it may never rewrite: the two
  M11 UI-built live captures under `tests/fixtures/live_xml/m11/`, the platform's own Process
  Call and Return Documents documentation, and the pre-baseline frozen bytes recorded in the
  archive. Generated goldens are NEGATIVE-ONLY witnesses in this slice; they are never
  promoted to platform evidence.
- Expected defect classes (pre-enumerated so a second instance triggers structurally ON
  ARRIVAL): a hand-model of a fact whose authority lives in another component; a guard that
  permits a condition without rejecting its inconsistent inverse; served prose that outlives
  the enforcement it describes; a generated artifact used as its own oracle.

## Platform model (the authority this slice is derived from)

A Process Call's outbound connection is **not** a free graph edge. The parent shape carries a
return-path element naming the CALLED process's Return Documents shapes; only those make a
forward connection valid, and a call whose child returns nothing is a path terminal.
*Documented* (help.boomi.com, "Process Call step" / "Return Documents step") and *measured
here* against the in-tree UI-built captures: in
`tests/fixtures/live_xml/m11/process_doccacheretrieve_loadalldoc_variant.xml`, shapes
`shape4`, `shape33` and `shape34` each carry an empty return-path element and NO outgoing
dragpoint, while `shape10` — the only one with a populated entry — is also the only one with a
forward edge, and that edge carries an `identifier` attribute equal to the entry's
`childShapeName`. `process_dpp_profile_decision_flow.xml` shape `shape61` is a fifth terminal
instance. Four terminal to one connected, and the connected one needs TWO coordinated bindings.

## Deferred capability (recorded at instantiation; the target issue already exists)

Returning-child return-path binding is transferred to **#176** ("Process Call return-path late
binding for returning subprocesses", filed 2026-08-20 under epic #134, placed immediately
after #175 and before #156). Reason class: `out-of-scope-by-design`. Two measured facts size
it and are recorded in its body: the connected form needs a return-path entry AND a matching
dragpoint identifier keyed the same; and `render_dragpoints` has exactly two byte-exact forms
today (labeled = identifier and text; plain = neither), so the live returning form — identifier
without text — is a THIRD form this repo cannot currently emit at all (*measured here*). This
is a gated capability transfer, not blocking residue, once every connected form is unreachable.

## Loop roster (fixed BEFORE the first correction; a gate not listed here cannot mint a loop
## mid-run — a roster addition is itself a recorded checkpoint decision)

1. Stage-1 QA — one live `boomi-qa-tester` engagement through the public MCP tool boundary,
   slice-scoped, with the pre-fix characterization probes as its first task.
2. Stage-2 repo Codex review — detached, `--base 3fd5027e16d4f9fe5377884a0140909a3b4d1e67`,
   then delta-scoped fix rounds. Every round COLLECTED, never read from `wait`.
3. Architect implementation review — the additive gate this run's wrapping pipeline declares,
   judging implementation against the attested design plan.
4. Composite wave gate — full suite + every active golden-manifest entry + deterministic
   compile/fingerprint checks + one integration-level review of the wave delta + one live
   scenario per changed capability class.
5. Terminal correction loop — ONLY via a recorded roster-addition checkpoint.

## Defect-class ledger (a class is a (mechanism, runtime-authority) pair, assigned at
## reconciliation; counts derived from the rows' class cells, never typed)

Counts are derived mechanically from the rows' defect-class cells by the rule CLAUDE.md
states: *distinct post-reconciliation defects* — a revision row (`X-a`) is the same defect as
the row it supersedes and never counts twice, a `finding-refuted` row never counts, and one
defect reported by two gates (or by the same gate in two rounds) is ONE instance. Where a
count differs from the raw row tally, the rows folded together are named in the cell, so the
derivation is checkable rather than asserted. The counts recorded at instantiation were
EXPECTATIONS and were superseded here at close.

| Class | Mechanism | Runtime authority | Instances (derived from rows) | Resolution |
| --- | --- | --- | --- | --- |
| **DC-175-A** | assert that a Process Call continues, and emit the outgoing edge, without consulting the called process | the called process's Return Documents shapes | **8 rows / 7 distinct defects** — SRC-175-1, SRC-175-2, SRC-175-4, L2-r1-01, L2-r2-01, L2-r3-01, L3-01+L3R-02 *(one defect, reported by the same gate in two rounds — the scope question, raised then resolved by reverting)* | **Structurally answered, twice.** At instantiation the second instance triggered the graph-verifier invariant instead of patching each emission site. The three L2 rows were successively weaker forms of that one guard, which CP-1 named explicitly as the instance-patch pattern and answered by restating the rule as ONE universal statement rather than adding a fourth condition; it then survived two further rounds untouched. L3R-02 narrowed its SCOPE on evidence (one live sample cannot carry a platform rule) without reopening the mechanism. Sibling sweep and non-vacuity witness recorded on the rows. |
| **DC-175-B** | a grammar rule asserting a fact about another component's graph that the model has no field to carry and no check to verify | the called process's Return Documents shapes | **1** — SRC-175-3 | Same authority as DC-175-A but a different mechanism (grammar, not emission), so it is recorded as its own class rather than a third instance. Resolved by making the terminal form the supported one and every connected form a typed refusal pointing at #176. |
| **DC-175-C** | a generated artifact used as evidence for the behaviour that generated it | the platform's own UI-built export | **0** — none arose; the prevention held | Structurally prevented rather than detected: the pre-fix bytes are frozen into the archive BEFORE the first source edit, the clean-room provenance rule binds every QA fixture row, and generated goldens are declared negative-only in the trust boundary above. |
| **DC-175-D** | served prose describing a capability the enforcement no longer grants | the enforcement registries and the graph verifier | **7** — L3-02, L3-04, L3-07, L2-r6-01, L3R-04, L3R-05, L3R2-04 | **RECORDED DEVIATION from the structural-fix rule → follow-up #177, reason class `blocked-by-mechanism`, filed at this reconciliation.** Standard tier only, which is the sole tier a deviation is available to; every instance here is `fixed`, so nothing is left unresolved in this slice. Grounds: the invariant is not writable inside a defect slice — it needs a general mechanism tying served text to the enforcement that backs it across capability entries, diagnostic registries, architecture docs and the compatibility inventory. Two bites DID ship here and are the pattern #177 generalises: L3-04 pinned a capability code to its served message and remediation, and L3R2-03 shipped an EXECUTABLE prose guard deriving its rule from the contract text and failing closed if that contract is reworded. Recorded honestly: this class recurred on the SAME files twice (L3-07 → L3R-05), which is exactly the evidence that instance-fixing it does not work. |
| **DC-175-E** | a #175 consequence mirrored into ONE of two sibling protocol surfaces — `wrapper_subprocess` got the treatment at every layer, `database_to_api_sync` at none | the shared catch-leg emitter's actual composition behaviour | **5** — QA-175-r1-01, QA-175-r1-03, L3-03, L3R-01, L3R2-01 | **Structural fix landed at the fifth instance (L3R2-01), with the mandatory sibling sweep — and the sweep changed the answer.** Enumerating every same-body placement found FOUR divergences where the reviewer had named one. `process_call_placement_verdict()` in `models/process_ir.py` is now the single authority deciding `(reason, at, message)`; both entry points RENDER it. Non-vacuity witness: a parametrised parity test over all seven placements asserting agreement on code AND pointer, plus a positive row so a verdict that refused everything could not satisfy it; the mutant restoring the old pointer fails 2 of 7. Coverage claim derived from the compiler's published body-context set. Landing it only at the fifth instance is the recorded cost of treating the earlier four as unrelated diagnostics. |
| **DC-175-F** | an unpinned hand-copy of a step-status derivation whose authority is another code path in the same module | the canonical executor's outcome-derived status | **4** — QA-175-r1-02, plus L3-06 + L3R-03 + L3R2-02 *(one defect — the missing permanent regression, reported across three consecutive rounds of the same gate)* | Two distinct defects, not four: the live status-derivation defect (QA-175-r1-02, fixed and live-validated), and ONE coverage defect that three successive rounds kept finding under-discharged. The second is now closed with permanent pins rather than another partial: a `_build_plan` refusal assertion naming the exact code and field, an `_apply_plan` executor tripwire asserting zero calls, and an update-repair pin whose before-image is the pre-baseline frozen golden. Both L3-06 and L3R-03 were re-derived **Critical** at round 3 via revision rows L3-06a/L3R-03a — the class anchor fires on mutation accounting regardless of the P2 source label — and discharged, never deferred. |
| **DC-175-G** | a served diagnostic asserting a consequence the check itself did not establish | the graph's actual edge set | **4** — L2-r4-01, L3-05, L3R2-03, A-175-02 | **Structural fix shipped at the third instance (L3R2-03) and immediately proved necessary by a fourth.** The fix is executable, per this repo's #171 lesson that prose structural fixes recur: a scanner refuses any durable doc claiming submitted bytes match a live readback, deriving its rule from `models/authoring_workflow.py` and failing closed if that contract is reworded. Its first version was VACUOUS — it excluded newlines, so it passed its own single-line witness AND the restored real defect, which is hard-wrapped; caught by hand-running the mutant rather than trusting the green. A-175-02 is a fourth instance of the CLASS but not a recurrence past the fix: it is the L2-r4-01 message REGRESSING when L3R-02 reverted that code wholesale, and the materially different action available — a witness constructing the concrete graph the claim excludes — was taken rather than escalating. |
| **DC-175-H** | a retirement recorded for an identity that never existed — a manifest hand-edited to mirror a revert instead of being reconciled against the range endpoints | the manifest's own base/tip row set, and pytest collection | **1** — W-175-01 | First instance, so no structural trigger. Fixed by reconciling against the endpoints rather than by editing the tombstones: rows added AND removed within the range need no row at all, so the three were removed and the appended region renumbered, with the base-region positional ids verified unchanged one by one before writing. The floor needed no change, which is the tell that the header had always been computed for the corrected row set. |

## Finding rows (one per raw finding; append-only; exactly one disposition each)

| ID | Source gate + run dir + attestation | Verbatim summary | Original label | Blocking class | Defect class | Derived tier (anchor inline) | SHA/delta | Disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SRC-175-1 | originating report — issue #175 body, §3(a); reporter Hlib (UI observation) + repo forensics; no run dir (pre-gate intake) | "Our emitter hardcodes an empty return-path element in every case and *still* draws a dragpoint to a mandatory trailing Stop … The UI resolves the contradiction by not rendering the connection — leaving an orphaned Stop floating on the canvas." | **P1** (source label, immutable) | emitted XML or graph validity | DC-175-A | **Critical** — anchor: source label P1 derives to Critical per the tier rule ("any finding its source gate/reviewer labeled P0/P1/Critical/High"). Never deferred, never closed over. | baseline `3fd5027e16d4f9fe5377884a0140909a3b4d1e67` | `fixed` — every emission surface now produces the TERMINAL form. Validated three ways: the regenerated goldens are structurally byte-identical to the live UI-built terminal shape (3/3 emitted calls match the single skeleton the m11 capture's three terminal calls collapse to); live QA built a wrapper and a branch through the public MCP boundary and read back `['start','processcall']` / `['start','branch','processcall','processcall']` with zero `stop` shapes; and `verify_process_graph` returns clean with a non-zero `shapes_checked`. |
| SRC-175-2 | originating report — issue #175 body, §4 blast radius; extended during implementation planning to the legacy catch leg (*measured here*: the catch composition wires the DLQ Process Call to a downstream terminal) | "Every surface that emits `processcall` produces the pattern today" — the canonical PATH MODE placements, the wrapper thin parents, the emitter parity fixtures, and (found in planning) the legacy catch leg's error-subprocess composition. | **P1** (inherits the report's label; same defect, second site) | emitted XML or graph validity | DC-175-A | **Critical** — anchor: same source P1 label as SRC-175-1; this is the SECOND instance of DC-175-A, which is what makes the structural fix mandatory in this batch. | baseline `3fd5027e16d4f9fe5377884a0140909a3b4d1e67` | `fixed` — the second site, the legacy catch leg, is closed too: `catch_notify + error_subprocess_ref` now emits notify → TERMINAL call with no trailing Stop, and the two `catch_exception` compositions are refused typed and pre-mutation. The `document_cache_ref` route is the over-firing control and is unchanged in all three compositions. |
| SRC-175-3 | originating report — issue #175 body, §3(b) | "for a non-returning child the grammar **forces** authoring the exact shape the platform forbids" — the PATH MODE rule requires a stop terminal, and the node model has no field, and no cross-component check, that could distinguish a returning child from a non-returning one. | **P1** (inherits the report's label) | capability reachability | DC-175-B | **Critical** — anchor: same source P1 label; the grammar is what makes the invalid shape the ONLY authorable one. | baseline `3fd5027e16d4f9fe5377884a0140909a3b4d1e67` | `fixed` — `process_call` moved from the body STEP unions to the TERMINAL unions and the root became the exact singleton, so the shape the platform forbids is no longer authorable; every connected form raises `PROCESS_IR_CAPABILITY_PROCESS_CALL_RETURN_PATH_BINDING_UNSUPPORTED`. Live QA confirmed all three direct-ProcessIR forms serve that code at compile with `mutation_performed: false`. |
| SRC-175-4 | originating report — issue #175 body, §3(c) | "the check only *permits* terminal-ness — it never flags the inconsistent inverse (empty `returnpaths` **plus** an outgoing dragpoint), so our contradictory output passes `verify_process_graph` clean." | **P1** (inherits the report's label) | emitted XML or graph validity | DC-175-A | **Critical** — anchor: same source P1 label. Closing this row IS the DC-175-A structural fix; it is recorded separately from SRC-175-1/2 because it names the one-sided guard rather than an emission site. | baseline `3fd5027e16d4f9fe5377884a0140909a3b4d1e67` | `fixed` — this row IS the DC-175-A structural fix. The invariant (`no declared return path ⇒ no outgoing connection`) is enforced at the one place every emitted graph passes through, and is proven non-vacuous in BOTH directions against a UI-built capture: it stays clean on the live connected call, fires when only that call's return path is cleared, and goes clean again on a byte-identical restore. Live QA independently reached the code at the public boundary. |
| QA-175-r1-01 | Stage-1 QA — `agents/reports/2026-08-20-issue-175-r1.md`, live `renera`, one engagement | "the `catch_exception` + `error_subprocess_ref` refusal has no plan-time gate … `plan` returns `planned_action: \"create\"`, and apply really creates the preceding component (inventory 26→27) before refusing" | **High** | machine-served contracts + mutation accounting (a refusal that fires after a write) | DC-175-E | **Critical** — anchor: source label High derives to Critical per the tier rule. | slice tip (uncommitted) | `fixed` — the rule now lives in ONE function, `_process_call_catch_composition_error`, which BOTH the plan-time validator and the emit-time guard call. Verified closed by re-measuring: `validate_config` now returns `PROCESS_CALL_CONFIG_INVALID` on `reliability.catch_exception`. |
| QA-175-r1-02 | Stage-1 QA — same engagement; reproduced on four distinct failure modes | "`results[key][\"status\"]` is derived from the requested action, so a failed step reads `\"status\": \"created\", \"component_id\": null`" | **Medium** | **mutation accounting** | DC-175-F | **Critical** — anchor: the tier rule makes ANY validated finding in mutation accounting critical, regardless of the source label. Not deferrable. | slice tip (uncommitted) | `fixed` — the apply loop now derives status from the OUTCOME (`refused` when the step provably wrote nothing, else `failed`), matching the derivation that already existed in `_execute_canonical_process`. PRE-EXISTING and outside the slice diff; fixed rather than deferred because a mutation-accounting misreport is critical by class and #175 increases its reachability. |
| QA-175-r1-03 | Stage-1 QA — same engagement | "the `database_to_api_sync` schema template never publishes `PROCESS_CALL_CONFIG_INVALID`, and its #108 note still advertises `[notify ->] [dlq route ->] exception` (now refused) while its #89 note still promises a catch-row Stop that is no longer emitted" | **High** | machine-served schemas/contracts | DC-175-E | **Critical** — anchor: source label High. | slice tip (uncommitted) | `fixed` — the sync protocol now publishes the code, and both composition notes state the error-subprocess exclusion. Covered structurally by a check that DERIVES the served-code expectation by driving each builder, rather than re-asserting a hand-listed one. |
| L2-r1-01 | Stage-2 repo Codex review, run dir `cdx-review.bMSYKi`, base `3fd5027`, head `3c07ad2`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Require a real Process Call return-path declaration — when raw or escape-hatch XML contains `<returnpaths><returnpaths/></returnpaths>` (or any unrelated child) plus an outgoing dragpoint, this returns true even though no non-empty `childShapeName` is declared … `verify_process_graph` reports no errors for a continuation the platform cannot bind" | **P2** | emitted XML or graph validity | DC-175-A | Standard — anchor: source label P2; not in a critical class and no critical anchor. | `3c07ad2` + this fix | `fixed` — the check asked "does the element have any children?" rather than "is any return path BINDABLE". Verified by reproduction before the fix (three malformed shapes escaped) and after (all four are flagged), with the real platform form and the live m11 capture staying clean in both runs — the discriminator that stops the tightening from degenerating into "flag every call with an edge". The bindability test is derived from the capture, which is why `childShapeName` is required and `returnLabel`, legitimately empty there, is not. |
| L2-r2-01 | Stage-2 repo Codex review (fix-only, auto scope), run dir `cdx-review.tAoZSM`, scope `working tree diff` on head `3c07ad2` (dirty), `STATUS: completed`, teardown `confirmed stopped` | "[P2] Match return paths to outgoing dragpoint identifiers — when a declared return path does not match an outgoing dragpoint `identifier` (or the dragpoint omits it), this predicate still returns true … the newly added positive test is actually malformed because its base fixture's dragpoint has no identifier" | **P2** | emitted XML or graph validity | DC-175-A | Standard — anchor: source label P2; no critical class or anchor. | this fix (uncommitted at review time) | `fixed` — BOTH halves were correct. A declared return path is not a BOUND one: the platform attributes an edge to a branch through `dragpoint/@identifier` carrying that branch's `childShapeName`, which the live capture pairs exactly. Pass 2a′ now checks declaration and correspondence as SEPARATE conditions, leaving `_is_terminal` (a question about the declaration, not the edge) unchanged. The test half was the sharper catch: my "valid connected call" fixture set the declaration and left the base fixture's identifier-less dragpoint, asserting clean for a shape the live evidence says is invalid — a fixture written from memory, which is the #146 trap. Measured after: no-identifier and wrong-identifier are flagged, matching-identifier is clean, and both live captures stay clean. |
| L2-r3-01 | Stage-2 repo Codex review (fix-only), run dir `cdx-review.mVWUMU`, base `3c07ad2`, head `45ceecb`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Validate every Process Call dragpoint binding — when a Process Call has multiple outgoing dragpoints and only one identifier matches a declared `childShapeName`, this `any(...)` suppresses the error for the entire shape … a mismatched or identifier-less branch is treated as reachable even though the platform drops it" | **P2** | emitted XML or graph validity | DC-175-A | Standard — anchor: source label P2; no critical class or anchor. | this fix | `fixed` — reproduced first: a mixed set (one attributed edge, one not) passed the check entirely. The response is the CP-1 named correction rather than a third condition: the guard is now ONE universal statement — every outgoing connection must be attributed to a return path the call declares — and the no-declaration case falls out of it (the declared set is empty, so every edge is unbound) instead of needing its own branch. Measured: all five shapes from rounds 1–3 are flagged by the single rule, the valid form is clean, a legitimate multi-return fan-out with every branch attributed is clean, and both live captures are clean. The message still discriminates the two remedies and now names the unattributed target. |
| L2-r4-01 | Stage-2 repo Codex review (fix-only), run dir `cdx-review.ALvWTM`, base `45ceecb`, head `a4d62c9`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Identify the unbound dragpoint rather than the shared target — when a valid and an invalid dragpoint both point to the same downstream shape … `targets` collapses them to `shape3`, so this message says the connections to `shape3` are unattributed and that the shape is unreachable. The valid `shape233` dragpoint still reaches it" | **P2** | machine-served schemas/contracts | DC-175-G | Standard — anchor: source label P2; no critical class or anchor. The RULE was not disputed and did not change; this is the served text only. | this fix | `fixed` — measured before: the message named `shape3` and claimed it unreachable while the bound dragpoint still reached it, and never named `shape2.dragpoint2`. Now names the connection and its identifier state, and claims only that the platform drops an unattributed connection. The test that induced the wrong message was corrected too, and now pins the inverse: the bound connection must NOT be named, and the shared target must NOT appear. |
| L3-01 | §6 architect implementation review, run dir `cdx-gate-review.fh8t5s`, gate `review`, verdict `ISSUES FOUND`, attested against the plan bytes | "The verifier exceeds authorized scope … The plan authorized `PROCESS_CALL_ORPHAN_CONTINUATION` only for absent/empty `returnpaths` plus an outgoing dragpoint and deferred binding-shape validation to B. This quietly broadens C into #176's territory." | **P2** | emitted XML or graph validity | DC-175-A | Standard — anchor: source label P2. | `753b46f` | `severity-refuted` — the SCOPE claim is accurate and the finding is kept in the record as a genuine expansion; what is refuted is that it should be reverted. Disputed claim: that binding-shape VALIDATION belongs to #176. #176 owns EMITTING a binding; verifying a graph the platform would drop is verification, which is what this module exists to do for escape-hatch and live-readback XML. Concrete evidence: the repo's own Stage-2 gate demanded exactly this widening twice — rounds 2 and 3 both found the narrower rule certifying continuations the platform drops — so reverting would reintroduce two findings this loop already validated as real. It cannot regress our own output either: this server emits no connected call at all while #176 is gated, so the check is reachable only on hand-authored or live-readback XML, and it fails toward reporting. Recorded as deviation **D11** rather than silently exceeding the plan. |
| L3-02 | §6 architect implementation review, same run dir | "The dedicated continuation diagnostic is incomplete … `[process_call, stop]` returns `/body`, and a prefixed terminal call returns its leg container, not the promised offending `/steps/1` or `/terminal`." | **P2** | machine-served schemas/contracts | DC-175-D | Standard — anchor: source label P2. | this fix | `fixed` — measured before: `/body`, `/body`, `/body/steps/0/legs/0`. A model-level validator's error is located at the MODEL, so the promised precision was not deliverable by raising alone. The validator now passes the offending position through the error CONTEXT and the translator appends it; the segments are our own literals and integers, so no authored value can reach a pointer. Measured after: `/body/steps/1`, `/body/steps/2`, `/body/steps/0/legs/0/steps/0`, `/body/steps/0/legs/0/terminal`. |
| L3-03 | §6 architect implementation review, same run dir | "a mutable Branch model containing `process_call` in `steps` falls through … to `PROCESS_IR_CAPABILITY_NODE_NOT_ALLOWED_IN_BODY`, rather than the dedicated return-path-binding code required for every continuation attempt" | **P2** | machine-served schemas/contracts | DC-175-E | Standard — anchor: source label P2. This is a THIRD instance of DC-175-E (one rule, two enforcement paths disagreeing), and the structural response from that class already applies: the compiler path now mirrors the parser's translation exactly rather than carrying its own answer. | this fix | `fixed` — reproduced: the same authored mistake returned the dedicated code through `parse_process_ir_v1` and the generic code through `compile_process_ir_v1`. Now both agree, scoped by whether the body admits a terminal call — so a Decision FALSE arm, which admits none in any slot, correctly KEEPS the generic code (measured both ways). |
| L3-04 | §6 architect implementation review, same run dir | "The compiler-side diagnostic registry was omitted … neither a message nor remediation for `PROCESS_IR_CAPABILITY_PROCESS_CALL_RETURN_PATH_BINDING_UNSUPPORTED` … Its missing-terminal remediation also still omits terminal Process Calls." | **P2** | machine-served schemas/contracts | DC-175-D | Standard — anchor: source label P2. | this fix | `fixed` — measured: zero occurrences of the code in the compiler diagnostics module, so `compiler_diagnostic_specs()` omitted it entirely and direct compiler errors served the generic fallback remediation. Message and remediation registered; the missing-terminal remediation now lists the process-call terminal. Verified the code is served by `compiler_diagnostic_specs()`. This was a named deliverable in the plan that I simply did not do. |
| L3-05 | §6 architect implementation review, same run dir | "ADR-001's exception asserts facts the implementation/evidence does not establish … says the UI suppresses the connection, while the durable QA record says UI behavior was inconclusive"; "claims every refusal names `process_call_return_path_binding`, but the catch-composition error … omits that capability"; "the §8 matrix additionally still lists unqualified 'wrapper ProcessCall' as emittable" | **P2** | machine-served schemas/contracts | DC-175-G | Standard — anchor: source label P2. | this fix | `fixed`, all three. The UI claim is the one that mattered: I asserted a rendering behaviour QA explicitly could not reproduce. The ADR now attributes the canvas observation to the report, records the UI leg as inconclusive, and rests the exception on the RUNTIME measurement instead — which is the stronger evidence and was available all along. The catch hint now names the gated capability, so prerequisite 4 is true rather than aspirational; the §8 row is qualified to the terminal form. |
| L3-06 | §6 architect implementation review, same run dir | "[permanent integration coverage missing] the test only duplicates the status formula and inspects source text; it does not exercise `_build_plan`/`_apply_plan`, zero-mutation catch refusal, or pre-fix update repair" | **P2** | mutation accounting | DC-175-F | Standard — anchor: source label P2; the underlying QA-175-r1-02 defect is already `fixed`, so this is coverage depth, not live residue. | this fix | `fixed` for the reachable half — a `_build_plan` regression now drives the PLANNER and asserts the catch composition is refused there with zero planned mutation, which is the surface QA measured going wrong (inventory 26→27). The pre-fix update-repair half is inherently live and was measured in the Stage-1 engagement (orphan Stop and dead edge both removed); it is recorded there rather than simulated offline. |
| L3-07 | §6 architect implementation review, same run dir | "Several explicitly required documentation corrections were skipped" — the Return Documents primitive lacks the caller-binding caveat and still names `WrapperSubprocessBuilder` as its emitter; the wrapper docstring documents `start → process call(s) → stop` with a two-call example; `effects.py` still labels root Process Call chains and step-form leg calls as allowed | **P2** | machine-served schemas/contracts | DC-175-D | Standard — anchor: source label P2. | this fix | `fixed`, all three. `effects.py` was the sharpest: its reachability note listed `root [pc, pc, stop] -> ALLOWED` and `leg [pc] -> ALLOWED` while citing a test whose corrected measurements say the opposite — a note contradicting its own witness. All three now state what the enforcement does. |
| L2-r6-01 | Stage-2 repo Codex review over the full unreviewed correction delta, run dir `cdx-review.V8Doeb`, base `8162351`, head `90625b0`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Handle all-call roots before selecting a non-call neighbor — when a root contains only two or more `process_call` steps, no element satisfies this predicate, so `next(...)` raises `StopIteration`; `parse_process_ir_v1` only catches `ValidationError`, causing unsupported caller input to escape instead of returning the dedicated diagnostic. The same selection also points `[pc, pc, stop]` at the stop rather than the first unsupported continuation at `/body/steps/1`." | **P2** | runtime behavior + capability reachability (an unhandled exception on the public parse entry point, with the typed refusal unreachable for that input) | DC-175-D | Standard — anchor: source label P2; no source critical label and no critical class (not secrets, data loss, or mutation accounting). | this fix | `fixed` — a CRASH, and one introduced by the previous round's own fix for L3-02: the offending index was written as "the first step that is not a process_call", which has no solution for an all-call chain. Reproduced before the fix (`[pc, pc]` and `[pc, pc, pc]` both escaped as `StopIteration`), and the suite had stayed green because no test exercised a call chain WITHOUT a trailing terminal — the gap is now a parametrised witness. Rewritten against the first call's INDEX, which is total by construction. The pointer half was correct too: `[pc, pc, stop]` pointed at the stop, but removing the stop would not fix the document — the second CALL is the first node that may not follow a call. |
| L3R-01 | §6 architect review REPLAY over the correction delta, run dir `cdx-gate-review.xhnai8`, base `753b46f`, head `574d541`, verdict `ISSUES FOUND` | "the mutable-model diagnostic fix mishandles Decision FALSE with a prefix: the new prefix check runs before slot admissibility, so a mutated FALSE arm … returns `PROCESS_IR_CAPABILITY_PROCESS_CALL_RETURN_PATH_BINDING_UNSUPPORTED` at `/steps/0` while equivalent parsed JSON correctly returns generic `PROCESS_IR_CAPABILITY_NODE_NOT_ALLOWED_IN_BODY` at `/terminal` — contradicts both the plan and the ledger's claim that FALSE arms retain the generic code" | **P2** | machine-served schemas/contracts | DC-175-E | Standard — anchor: source label P2. FOURTH instance of DC-175-E (one rule, two enforcement paths disagreeing); the class's structural response already applies and was extended rather than re-derived. | this fix | `fixed` — reproduced exactly as described, and the ledger row it contradicts was MINE, which is the sharper half of the finding: I asserted the FALSE-arm behaviour without measuring it. The prefix rule now carries the same admissibility guard the step check already had, so where a slot admits no call the slot check wins. Measured after: FALSE arm reports the generic code at `/terminal` on BOTH paths; the TRUE arm still reports the dedicated code. |
| L3R-02 | §6 architect review REPLAY, same run dir | "D11 is not a sufficient refutation of the verifier-scope finding … the cited repo reviews establish internal consistency only, not platform semantics, and the relevant UI probe is unmeasured — revert identifier-correspondence and per-branch binding validation for populated declarations to #176, while preserving semantic empty-declaration detection (including malformed empty children) and raw dragpoint coverage" | **P2** | emitted XML or graph validity | DC-175-A | Standard — anchor: source label P2. | this fix | `fixed` — **my earlier refutation (D11) is WITHDRAWN, and the wider rule reverted.** The reviewer is right on the point that decided it: the identifier↔childShapeName requirement rests on exactly ONE live sample — measured, there is a single connected Process Call in the entire in-tree live corpus — and the two Stage-2 rounds that demanded the wider rule reasoned from that same sample. Internal agreement is not platform evidence; this repo already recorded that lesson as "two gates can agree on the wrong answer" (#144). The risk is asymmetric: if any valid platform form omits the identifier, the check would reject a customer's legitimate process through `build_integration(action="verify")`. #175 now judges only what is evidenced — a call declaring NO bindable return path beside an outgoing connection, which four captures and a runtime canary measurement support — and the populated case is left to #176. The reverted tests were converted into a scope record rather than deleted, so the boundary stays visible and is the test to invert if #176 proves the pairing required. |
| L3R-03 | §6 architect review REPLAY, same run dir | "the validation/coverage finding is only partially discharged … the new regression exercises only `_build_plan`, `planned_action != \"create\"` does not prove zero planned mutation, `_apply_plan` is never invoked, and no permanent pre-fix update-repair regression was added" plus "Probe 2 and the mandatory post-fix clean-canvas check remain unmeasured (the latter validates #175's terminal form and cannot transfer to #176)" | **P2** | mutation accounting | DC-175-F | Standard — anchor: source label P2. | this fix, and one item OPEN | `fixed` for the offline half; the UI half is **not-validated** and is stated as such at closure. The weak assertion is replaced (`planned_action == "error_process_validation"` plus the exact code and field — "not create" would also be satisfied by `reuse`), and an `_apply_plan` regression now patches `_execute_component` as a TRIPWIRE and asserts `call_count == 0`, which is the property the live defect violated. The reviewer is CORRECT that the post-fix canvas check belongs to #175 and cannot transfer to #176; it is blocked by an unavailable browser extension on three independent attempts and is recorded as the one unverified acceptance criterion rather than claimed. **REVISION 2026-08-20 (original disposition above retained):** the extension became available and the check was MEASURED, so this half moves `not-validated` → `fixed` and #175 no longer closes with an unverified acceptance criterion. Measured in BOTH directions, because a post-fix canvas alone cannot tell "the fix works" from "this shape never orphaned anything": a pre-fix control component (id `230906ef`…, the pre-baseline golden bytes planted through the DIRECT Component API — the fixed MCP boundary refuses them, which is the fix) renders three shapes and ONE edge, leaving the trailing Stop with no inbound connection, against the post-fix component (id `5ff25f37`…, authored through the public MCP boundary) rendering two shapes both attached. Both components are named in full in the archived evidence. The same readback answers the report's open §6 probe 1: Boomi **stores** the contradictory `shape2 → shape3` dragpoint and declines to render it — store-not-strip, so submitted and stored bytes agree and the report's conditional P0 escalation does not trigger. Two ways this check reads false were hit and are recorded in the evidence: the shape PALETTE answers to the same element names on every canvas (all at 0×0 under `shape_palette_widget` — a first pass nearly reported a surviving orphan from one), and a URL-hash navigation does not re-render the Boomi SPA, so a post-fix reading returned the pre-fix canvas until a hard reload plus visible-header attribution was added. Archived at `docs/architecture/evidence/issue-175/live-ui/` (both platform readbacks + geometry). ADR-001's §9 amendment is corrected in the same batch: it recorded the canvas leg as INCONCLUSIVE, which is no longer true. The runtime canary measurement remains the stronger evidence and still carries the exception. |
| L3R-04 | §6 architect review REPLAY, same run dir | "one original finding was omitted from the reconciliation: original finding 6 required updates to GitHub #134, #141 and #156 … a read-only check confirms those issues remain stale … yet splitting original finding 2 into two ledger rows displaced it while index.jsonl records all seven as reconciled" | **P2** | *(audit-record integrity — NOT one of the eight blocking classes)* | DC-175-D | Standard — anchor: source label P2; audit-record integrity is non-blocking by the repo's own convention, but the STALE SERVED RECORDS it names are not. | this fix | `fixed` — both halves. The three issue descriptions are now updated (#141 stated a placement the grammar no longer admits, which was the live hazard); the reconciliation record is corrected here. Worth recording that the block I had assumed — auto-mode refusing cross-issue `gh` edits — did NOT apply: I asserted it from memory instead of testing it, and prepared a manual hand-off for work I could simply do. |
| L3R-05 | §6 architect review REPLAY, same run dir | "the documentation correction remains incomplete: the wrapper docstring says exactly one call but its example still shows two calls; `effects.py:49` still records incorrect diagnostic codes; and `test_process_ir_semantic_effects.py:188` commentary still claims root call chains and step-form leg calls are allowed" | **P2** | machine-served schemas/contracts | DC-175-D | Standard — anchor: source label P2. | this fix | `fixed`, all three, and the codes were RE-MEASURED at this tree rather than reasoned about: `root [pc, set_dpp, stop]`, `root [pc, pc, stop]` and `leg steps=[pc, set_dpp]` all now report the dedicated return-path code, not the generic placement code the old rows recorded. The test-file commentary had never moved with the source file it cites. |
| L3R2-01 | §6 architect implementation review, round 3, run dir `cdx-gate-review.xEZ510`, base `574d541`, head `c2ebc43`, verdict `ISSUES FOUND`, attested against the plan bytes | "[body_capabilities.py] still breaks parser/compiler pointer parity. For a prefixed terminal Process Call, Branch and Decision TRUE parsing reports the dedicated code at `/terminal`, while mutable-model compilation reports it at `/steps/0`. Decision FALSE is corrected, but L3R-01 only moved the asymmetry." | **P2** | machine-served schemas/contracts | DC-175-E | Standard — anchor: source label P2; no critical class or anchor. | this fix | `fixed`, and this is the FIFTH instance of DC-175-E, so the response is the STRUCTURAL fix the rule demands rather than a sixth pointer patch. Reproduced first, exactly as described. **Sibling sweep** (mandatory at a structural fix, and it changed the answer): enumerating every same-body placement a Branch leg can express found FOUR divergences, not one — the prefix pointer; the connector-with-call-terminal pointer (`` `` vs `/terminal`); and TWO orderings where one path reported mixing and the other the return-path rule for the same document. The whole suite was green throughout, because nothing pinned the agreement. **The fix:** `process_call_placement_verdict()` in `models/process_ir.py` is now the single authority deciding `(reason, at, message)`; both entry points RENDER it, and the compiler's admissibility guard and its cross-nesting checks (which have no same-body verdict to render) are preserved. The label table moved with it, so the two cannot drift to "decision true-arm" and "decision true arm". **The order is derived, not chosen** — and my first attempt got it wrong: I ordered mixing first on a lifetime argument, which left two cases still diverging. The real authority is the type union — `process_call` is in no step-slot union, so the parser never reaches a mixing question for a call in `steps`. Step form therefore comes first, because that is what the type system already decides. **Non-vacuity witness:** a parametrised parity test over all seven placements asserting both paths agree on CODE AND POINTER, plus a positive row proving the legal terminal form is still accepted (without it a verdict that refused everything would satisfy every parity assertion). Hand-run the mutant: restoring the old `/steps/0` pointer fails 2 of 7, and the restore is byte-identical. **Coverage claim derived from the authority's full case set:** every published body context has a placement label, asserted against the compiler's published body-context set rather than restated. |
| L3R2-02 | §6 architect implementation review, round 3, same run dir | "L3R-03 remains incomplete … no permanent regression updates pre-fix `Start → Process Call → Stop` XML and asserts removal of both the dead edge and Stop. The ledger quotes that omission and then marks the offline half fixed without addressing it. That row also assigns the blocking class `mutation accounting` but derives Standard severity; repository policy makes any validated mutation-accounting finding Critical." | **P2** | mutation accounting | DC-175-F | **Critical** — anchor: the blocking class is mutation accounting, and the policy anchor fires on the class regardless of the P2 source label. Derived, not chosen. | this fix | `fixed`, both halves, and BOTH were correct. The severity half is the sharper one: it is an audit-record defect of exactly the kind that invalidates a closure, and the row it names was mine — I had derived Standard from the source label while the row's own class is mutation accounting. L3-06 and L3R-03 now carry dated severity revisions to Critical with the originals retained; the tier is DISCHARGED rather than deferred, which is the only legal outcome for critical. The coverage half is now a permanent pin in `test_wrapper_subprocess_builder.py`: a rebuild must contain neither the orphaned Stop nor the dragpoint that never bound, must keep the call, and must not silently repoint it. Its before-image is the pre-baseline frozen golden in the evidence archive — causally independent of the code under test — and a companion test asserts that before-image really carries the defect, without which the repair assertion would be satisfied by a build that was never broken. |
| L3R2-03 | §6 architect implementation review, round 3, same run dir | "ADR-001 and the ledger overstate the UI evidence as \"submitted and stored bytes agree\" with \"no attestation drift.\" The archive contains readbacks, not the exact submitted control payload, and readback XML contains server-added metadata … The repository's own attestation contract explicitly says submitted bytes cannot be compared directly with readback bytes at authoring_workflow.py." | **P2** | machine-served schemas/contracts | DC-175-G | Standard — anchor: source label P2; no critical class or anchor. | this fix | `fixed`, and the claim was MINE, written this session. It is worse than unproven: `models/authoring_workflow.py` states that `submitted_xml_digest` *"is never compared against a live readback: a readback carries server-assigned attributes and would mismatch on every healthy apply"* — so I asserted an equality the shipped model says cannot be observed. What was actually measured is at the GRAPH level: the stored component still carries the `shape2 → shape3` dragpoint, so the platform does not STRIP the contradiction, which is the question the report asked and all the P0 condition turns on. Corrected in all four places that carried it. **SECOND instance of DC-175-G as durable prose outrunning its evidence** (L3-05 was the first, also in ADR-001), so the structural fix is mandatory and — per this repo's own #171 lesson that prose structural fixes recur and executable ones hold — it is EXECUTABLE: a scanner asserts no durable doc claims submitted bytes match a readback, deriving its rule from the contract text and failing closed if that contract is reworded. Recorded honestly: my first version of that scanner was VACUOUS — it excluded newlines from a sentence, so it passed its own single-line witness AND passed the restored real defect, which is hard-wrapped across two lines. Caught by running the mutant instead of trusting the green. It now scans whitespace-flowed text, its witness is the real wrapped sentence, and the mutant fails with the offending sentence named. |
| L3R2-04 | §6 architect implementation review, round 3, same run dir | "The executable L3R-02 revert is complete, but its documentation still asserts the withdrawn rule. process_graph_verifier.py calls every-edge identifier binding a universal invariant immediately before deliberately skipping every populated declaration. The tests likewise still claim an identifier-less populated declaration is proven invalid, and later call a synthetic multi-return fixture \"the platform's own\", although that proof is deferred to #176." | **P2** | machine-served schemas/contracts | DC-175-D | Standard — anchor: source label P2; no critical class or anchor. | this fix | `fixed`, all three, and the reviewer's framing is the right one: the CODE revert was complete and its PROSE was not, so the module asserted a universal every-edge invariant in one paragraph and disclaimed it in the next. The invariant is now restated as what it actually is — a property of the DECLARATION alone, which is why it has no per-edge case to get weaker. The test that reproduces the m11 pairing now asserts only that the attested shape verifies clean, which is a fact about the shape rather than a claim that unpaired shapes are invalid. The multi-return fixture carries an explicit PROVENANCE note: it is SYNTHETIC, hand-extended from the single-return capture, no multi-return parent has been captured live (that is #176's probe 4), and it is a negative guard only. |
| W-175-01 | Composite wave gate, `scripts/wave_gate.py wave --base 3fd5027`, run at tip `c2ebc43` | `MANIFEST_TRANSITION_ILLEGAL pytest-nodes: rows ['pytest-010144', 'pytest-010145', 'pytest-010146'] are appended already tombstoned; a tombstone records the retirement of a row that existed, so an identity that was never active cannot be introduced retired. A row added and removed within the same range needs no row at all.` | **gate refusal** | machine-served schemas/contracts | DC-175-H (new class: a retirement recorded for an identity that never existed — the manifest hand-edited to mirror a revert instead of being reconciled against the range endpoints) | Standard — anchor: no source severity label; the gate refuses rather than grading, and the class is not a critical one. | this fix | `fixed` — caused by the L3R-02 revert: three identifier-correspondence tests were added earlier in this slice and removed again by the revert, and the manifest recorded the removal as tombstones. From the range's endpoints those identities never existed, so they need no rows at all. Removed, and the appended region renumbered — legal because the positional-id invariant binds only rows present at the BASE, which were verified unchanged one by one before writing. The floor needed no change (10020 + 157 appended − 7 tombstoned = 10170), which is itself the tell: the header had always been computed for the corrected row set, so the rows were the error. Also recorded: my first wave-gate invocation piped the runner to `tail`, so the shell reported `tail`'s exit code and the run LOOKED clean — the gate's verdict is the printed contract token, never the pipeline status. |
| L3-06a | revision of L3-06 (§6 round 3, per L3R2-02) | supersedes L3-06's DERIVED TIER only; L3-06's original text, original P2 source label and evidence are retained above unedited | *(inherits L3-06 — the source label is immutable)* | mutation accounting | *(inherits DC-175-F)* | **Critical** — re-derived from the anchor, not chosen: repo policy makes *any* validated finding in secrets/security, data loss, or mutation accounting Critical, and this row's own blocking class is mutation accounting. The superseded derivation reasoned from the P2 SOURCE label and from the underlying live defect already being fixed; neither displaces the class anchor, and a derived tier that mismatches its critical anchor is one of the stated conditions that invalidates a closure. | `c2ebc43` | `fixed` — disposition unchanged, and the critical tier is **DISCHARGED, never deferred** (critical residue may not be deferred at any checkpoint). The live half was measured in the Stage-1 engagement — component inventory 26→27 before the fix, 22→22 after, with a non-vacuity control observing 22→23 and 24→25 on genuine creates in the same run — and the permanent offline coverage this row said was missing now exists (`_build_plan` refusal, an `_apply_plan` executor tripwire asserting zero calls, and the update-repair pin). |
| L3R-03a | revision of L3R-03 (§6 round 3, per L3R2-02 and L3R2-03) | supersedes L3R-03's DERIVED TIER and its store-not-strip wording; L3R-03's original text and original P2 source label are retained above unedited | *(inherits L3R-03 — the source label is immutable)* | mutation accounting | *(inherits DC-175-F)* | **Critical** — re-derived from the anchor exactly as for L3-06a: the row's blocking class is mutation accounting, so the class anchor fires regardless of the P2 source label. | `c2ebc43` | `fixed`. Two corrections. (1) TIER, as above — discharged, not deferred. (2) The superseded row said the UI readback showed "submitted and stored bytes agree"; that claim is WITHDRAWN as an overstatement of my own. What was measured is at the GRAPH level — the stored component still carries the `shape2 → shape3` dragpoint, so the platform does not STRIP the contradiction, which is the question the report asked and the only thing its conditional P0 turns on. Byte agreement was never measured and cannot be: `models/authoring_workflow.py` records that a submitted digest *"is never compared against a live readback"* because a readback carries server-assigned attributes and would mismatch on every healthy apply. The UI measurement itself, the two false-reading traps, and the archived evidence are unchanged. |
| A-175-01 | Pre-closure audit (main-thread adversarial re-verification of the slice against its own closure bar; NOT a roster gate, so it mints no loop — its findings are reconciled into the round-3 correction batch) | "the defect-class table is stale, and two classes are past their second instance with neither a structural fix nor a recorded deviation" — typed cells read 2/1/0/0/2/1/1 while the rows derive 8/1/0/7/5/4/3, and DC-175-H is absent from the table entirely | *(no source severity label — an internal audit)* | *(audit-record integrity — NOT one of the eight blocking classes)* | DC-175-D | Standard — anchor: no source label, no critical class. Audit-record integrity is non-blocking by this repo's convention, but a stale count in a table whose own header says the counts are DERIVED is a stated closure-invalidating condition, so it is fixed rather than noted. | this fix | `fixed` — CONFIRMED by re-deriving the counts myself rather than trusting the audit: a script reading the rows' class cells reproduces 8/1/0/7/5/4/3/1. The table is recomputed, the derivation RULE is now stated above it (revision rows and refuted rows never count; one defect reported twice is one instance), and every folded pair is named in its cell so the arithmetic is checkable. DC-175-H added. The substantive half: **DC-175-D reached seven instances with no structural fix and no deviation**, which is the condition that invalidates a closure — now a recorded deviation to **#177** (`blocked-by-mechanism`), filed at this reconciliation as the rule requires. DC-175-F's raw count of four is three rounds of ONE coverage defect plus one live defect; that is stated in the cell rather than left to the reader. |
| A-175-02 | Pre-closure audit, same pass | "the served `PROCESS_CALL_ORPHAN_CONTINUATION` message asserts an unreachability it does not establish" — the payload says "the shapes it points at are left unreachable" while the verifier emits no unreachability error for that shape, and a sibling branch leg reaches it | *(no source severity label — an internal audit)* | machine-served schemas/contracts | DC-175-G | Standard — anchor: no source label; served contract text is a blocking class but carries no critical anchor (not secrets, data loss, or mutation accounting). | this fix | `fixed` — REPRODUCED before touching it: on `start → branch → {leg1: processcall(empty returnpaths) → shape5, leg2: message → shape5}` the verifier returns exactly one error, the orphan on `shape3`, and NO unreachability error for `shape5` — so one payload certified the shape reachable and told the caller it was not. **This is a REGRESSION, not a new defect:** L2-r4-01 fixed this same sentence, and the L3R-02 scope revert restored the older message wholesale along with the code. That is the sharper lesson — reverting a rule reverted its served text with it, and nothing pinned the corrected wording. The message now claims only what the check computes: the platform does not bind this connection, so it does not exist in the emitted graph. Witnessed by the concrete graph above plus a companion on a genuine dead end, so the wording is pinned as correct rather than merely absent. |
| L2-r2-01a | revision of L2-r2-01 (round 4, per the pre-closure audit) | supersedes L2-r2-01's DISPOSITION TEXT only; the original row, its P2 source label and its evidence are retained above unedited | *(inherits L2-r2-01)* | *(inherits)* | *(inherits DC-175-A)* | *(inherits Standard)* | `2e03be1` | `fixed` **— with its measured claim now false and corrected here.** The original said "Measured after: no-identifier and wrong-identifier are flagged". That was true when written and is not true of the shipped code: L3R-02 reverted identifier correspondence to #176, so a POPULATED declaration is no longer judged however its dragpoint is spelled. Re-measured at this tip: empty declaration + edge → flagged; declared + no identifier → clean; declared + wrong identifier → clean; declared + matching identifier → clean. The rule that ships is the empty-declaration one, pinned by `test_a_populated_declaration_is_not_judged_here` as an explicit scope record. |
| L2-r3-01a | revision of L2-r3-01 (round 4, per the pre-closure audit) | supersedes L2-r3-01's DISPOSITION TEXT only; the original row and label are retained above unedited | *(inherits L2-r3-01)* | *(inherits)* | *(inherits DC-175-A)* | *(inherits Standard)* | `2e03be1` | `fixed` — same correction as L2-r2-01a. The original claimed "all five shapes from rounds 1–3 are flagged by the single rule"; after the L3R-02 revert only the empty-declaration spellings are flagged, which is two of them. The universal restatement CP-1 called for did happen and did end the weaker-form sequence — that part of the original stands — but its SCOPE was subsequently narrowed on evidence, and the disposition text had not moved with it. |
| L2-r4-01a | revision of L2-r4-01 (round 4, per A-175-02) | supersedes L2-r4-01's DISPOSITION TEXT only; the original row and label are retained above unedited | *(inherits L2-r4-01)* | *(inherits)* | *(inherits DC-175-G)* | *(inherits Standard)* | `2e03be1` | `fixed` — **and then REGRESSED and re-fixed.** The original's claim ("Now names the connection and its identifier state") described a message the L3R-02 revert removed: reverting the rule reverted its served text with it, restoring the pre-fix wording including the unreachability assertion. Recorded as A-175-02 above and re-fixed there, this time with a witness that constructs the graph the claim excludes — the wording is now pinned, which is what was missing the first time. |
| L3-01a | revision of L3-01 (round 4, per the pre-closure audit) | supersedes L3-01's DISPOSITION only; the original row, its P2 source label and its evidence are retained above unedited | *(inherits L3-01)* | *(inherits)* | *(inherits DC-175-A)* | *(inherits Standard)* | `2e03be1` | **`fixed`, superseding the original `severity-refuted`.** The original disposition rested entirely on deviation **D11**, and L3R-02 explicitly WITHDREW D11 — leaving the row's only disposition resting on a retracted argument, which is a row without a valid disposition. The finding itself (that the verifier exceeded the plan's authorized scope) was correct and is now ACCEPTED rather than refuted: the wider rule was reverted to #176 and the verifier judges only the empty-declaration case the evidence supports. Measured at this tip — declared + no identifier, declared + wrong identifier and declared + matching identifier all verify clean, while both empty-declaration spellings are flagged — and pinned by the scope-record test `test_a_populated_declaration_is_not_judged_here`, which is the test to invert if #176 proves the pairing required. |
| L2-r7-01 | Stage-2 repo Codex review (support review billed to the §6 correction batch), run dir `cdx-review.urxVfk`, base `82b58cb`, head `11ab615`, `STATUS: completed`, teardown `confirmed stopped` | "[P1] Preserve existing pytest manifest identities — Against the requested merge base, `pytest-010144` was already assigned to a tombstoned verifier test, but this line repoints it to a different test. `scripts/wave_gate.py manifests --base 82b58cb…` therefore exits with `MANIFEST_TRANSITION_ILLEGAL`, blocking the required gate; retain the existing rows and append the new tests with fresh IDs." | **P1** | machine-served schemas/contracts | DC-175-H | Critical by anchor on the raw **P1** source label — recorded as such before reconciliation, and the refutation below is the documented severity-specific technical refutation the rule requires, not a re-grading. | `11ab615` | `finding-refuted` — the OBSERVATION is reproduced and true; the CONCLUSION and the remedy are both false, each disproved by measurement. (1) *"blocking the required gate"* — the required gate's baseline on a push to `dev` is `github.event.before`, which is `origin/dev`'s tip; measured, that is `3fd5027` — this slice's own Stage-1 baseline — and the wave baseline is the same commit. At that base the manifest passes (`manifests ok (10185 required nodes, 61 active goldens)`). `82b58cb` is an intermediate commit on this branch that arrives in the SAME fast-forward push and is never anyone's baseline. (2) A baseline must itself be a legal manifest state, and `82b58cb` is not: measured against `3fd5027` it fails with the born-tombstone error — that failure IS row W-175-01, the defect this change fixes. (3) The prescribed remedy does not work, and I built it to check rather than arguing from the text: retaining the three tombstoned rows with their ids and appending the new tests with fresh ids still fails against `3fd5027` with the identical born-tombstone error. No manifest state is legal against both commits — the constraints are mutually unsatisfiable — so the one legal against the actual gate baseline is the only available choice. Recorded as a limitation rather than hidden: a run explicitly based at an intermediate commit of this branch will refuse, and that is a consequence of `82b58cb` having been committed in an illegal state, which history-preserving policy forbids me to rewrite. |
| L2-r7-02 | Stage-2 repo Codex review, same run dir | "[P2] Defer mixing errors when process calls are not admitted — When a validated mutable model is changed so a Decision FALSE or Try/Catch body has a `process_call` terminal while retaining a legal connector step, this unconditional branch reports mixing at `/steps/0`. Those contexts never admit `process_call`, so parsing the equivalent payload correctly reports the illegal `/terminal`; the compiler now points callers at the legal connector instead." | **P2** | machine-served schemas/contracts | DC-175-E | Standard — anchor: source label P2; no critical class or anchor. Sixth instance of DC-175-E, and a HOLE IN THE ROUND-3 STRUCTURAL FIX rather than a new mechanism. | this fix | `fixed` — reproduced exactly: FALSE arm with a connector step and a call terminal gave parser `/false_arm/terminal` against compiler `/false_arm/steps/0`, and the compiler's pointer named the LEGAL connector. My round-3 fix guarded the return-path reasons by admissibility and left the mixing reason unguarded — the same guard, applied to two of three branches. The whole verdict is now guarded at the call site, which is where the knowledge lives: the verdict is body-local and cannot know the slot was closed. Measured after: all three FALSE-arm placements agree on code, pointer and message. Permanent witness added over every placement in a slot that admits no call, and the mutant removing the guard fails 2 of 3 with a byte-identical restore. |
| L2-r7-03 | Stage-2 repo Codex review, same run dir | "[P2] Reuse the shared message for step-form parser errors — For a `process_call` placed in a Branch/TRUE-arm `steps` list, the discriminated union rejects the node before the model validator can invoke this verdict. Consequently `parse_process_ir_v1` still serves `_translate_pydantic_error`'s static message, while compiling an equivalent mutated model serves this new context-specific message, so `(reason, at, message)` is not actually shared by both entry points." | **P2** | machine-served schemas/contracts | DC-175-E | Standard — anchor: source label P2. Same class as L2-r7-02, same round, same structural fix — counted with it as ONE further instance rather than two. | this fix | `fixed`, and the finding lands on my WITNESS as much as on the code: the round-3 parity test compared code and pointer only, so two entry points could agree on both and still hand the caller different words — which is exactly what they did. Measured before: parser served the translator's static text, compiler served a context-specific message naming the step index. The step-form wording is now ONE constant served by both, and the step index is deliberately dropped from it because the pointer already carries that fact — duplicating a fact into a message is the mechanism that produced the shared-target defect earlier in this slice. The parity witness now compares the FULL diagnostic. |
| L2-r7-04 | Stage-2 repo Codex review, same run dir | "[P2] Make the prose guard distinguish assertion polarity — This regex flags compliant text such as `Submitted bytes do not match a live readback` because it ignores negation, while missing equivalent forbidden claims such as `The submitted payload matches the stored bytes` because `match` precedes `bytes`. Since the scanner gates every architecture Markdown/JSON file, it can both block accurate documentation and allow the claim it is intended to prevent." | **P2** | machine-served schemas/contracts | DC-175-G | Standard — anchor: source label P2. Fifth instance of DC-175-G — and, pointedly, a defect IN the structural fix that class received two rounds ago. | this fix | `fixed`, both halves measured before touching it: the compliant negation flagged True, the reordered claim flagged False. This is the SECOND time this one guard was wrong — it shipped vacuous (newline-blind) in round 3 and order-and-polarity-blind in round 4 — which is the honest headline: a guard is code, and code written without adversarial cases is as wrong as the prose it polices. The regex is replaced by an order-insensitive token test plus an explicit polarity check that rejects a negation appearing ahead of the equality verb. Witness table now runs in BOTH directions: three sentences that MUST flag (including the real defect with its real line break, and the reviewer's reordered variant) and five that MUST NOT (three correct statements of the contract, golden-corpus byte equality, and an ordinary store-not-strip observation). A guard that pressures an author to delete an accurate sentence is worse than no guard. |
| L2-r7-01a | revision of L2-r7-01 (round 5, self-caught by the ledger's own critical-lowering scanner) | supersedes L2-r7-01's DERIVED TIER CELL only; the original row, its **P1** source label, its evidence and its disposition are retained above unedited | *(inherits L2-r7-01 — the source label is immutable)* | machine-served schemas/contracts | *(inherits DC-175-H)* | **Critical** — anchor: the raw **P1** source label, and NOT lowered. The superseded cell phrased the tier as "Critical by anchor …", which the in-tree scanner reads as a lowering that names no refutation — and it was right to: the phrasing invited exactly the reading that a severity argument had been made. None was. The tier STANDS at Critical; what is refuted is the FINDING, in full and on measurement, which removes it rather than re-grading it. | `702f4c8` | `finding-refuted` — unchanged from L2-r7-01, whose three measurements carry it: the required gate's baseline is `origin/dev` = `3fd5027`, at which this tree passes; `82b58cb` is not a legal manifest state to be compatible with; and the prescribed remedy, built and run rather than argued about, still fails. |
| QA-175-r4pa-01 | Stage-1 QA round 4 — `agents/reports/2026-08-20-issue-175-r4-placement-authority-r1.md`, live `renera`, through `build_integration` | "the connector-mixing verdict is rendered UNGUARDED in bodies that admit no `process_call`, so the two entry points disagree on the JSON pointer" — Decision FALSE arm `steps=[connector_call]`, `terminal=process_call`: parser generic at `/false_arm/terminal`, compiler generic at `/false_arm/steps/0` | **Medium** | machine-served schemas/contracts | DC-175-E | Standard — anchor: source label Medium; no critical class or anchor. | `2e03be1`, re-measured at `11ab615` | `fixed` — the SAME defect the Stage-2 review reported as L2-r7-02, found independently and in the same round by a different gate, so it counts ONCE (one defect reported by two gates is one instance). QA's arm C11 is the exact case: 10 of its 11 named arms already agreed, and the 11th is the one my round-3 guard missed. Already discharged by the L2-r7-02 fix; recorded here because the ledger records raw findings by source, and because two independent gates converging on one hole is the evidence that the hole was real rather than a reviewer's model of the code. |
| QA-175-r4pa-02 | Stage-1 QA round 4, same engagement, from QA's OWN sibling sweep beyond the dispatched cases | "the ROOT-sequence process-call rule is still two independent copies — the two entry points disagree on the pointer, and on the CODE when a connector is present" — `[pc, stop]`, `[set, pc]`, `[pc, set]` diverge on pointer; `[conn, pc]`, `[pc, conn]` serve `PROCESS_IR_CAPABILITY_UNSUPPORTED` at `/body` versus the return-path code at a step index | **Medium** | machine-served schemas/contracts | DC-175-E | Standard — anchor: source label Medium; no critical class or anchor. Seventh instance of DC-175-E. | this fix | `fixed`, and this one is a genuine EXTENSION rather than a duplicate — it found what my round-3 sibling sweep missed. That sweep enumerated every placement a BODY can express and stopped there; the root sequence is a sibling instance of the same mechanism and was left as two hand-written copies. Reproduced independently before fixing: all five illegal root shapes diverge, two of them on the CODE, so a machine consumer branching on the code took a different branch depending on which entry point it called. `process_call_root_verdict()` now decides the root exactly as the body verdict decides a body, and both entry points render it — the parser's pointer convention (the first step that is not THE call, written against the first call's index so an all-call chain still has an answer) wins, because it is the one an earlier round already validated. Measured after: all six root shapes agree on code, pointer AND message. Permanent witness over every root shape including the two-call chain and the legal singleton; the mutant restoring the compiler's own root code fails 2 of 7, restore byte-identical. **The lesson is about my sweep, not the code:** a sibling sweep bounded by the artifact I happened to be editing is not a sweep of the mechanism. |
| L2-r8-01 | Stage-2 repo Codex review (support review billed to the §6 correction batch), run dir `cdx-review.nptjyj`, base `11ab615`, head `a3d21e9`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Check slot admissibility before ancestor mixing — For a valid root `connector_call` feeding a Decision whose FALSE-arm terminal is mutated to `process_call`, parsing reports the generic slot diagnostic at `/terminal`, while compiler validation reports ancestor mixing there instead." | **P2** | machine-served schemas/contracts | DC-175-E | Standard — anchor: source label P2; no critical class or anchor. | this fix | `fixed` — and it did NOT reproduce on my first measurement, which is the part worth recording: code and pointer were identical, so a code+pointer comparison said "agree". Only comparing the MESSAGE showed the divergence — ancestor mixing versus generic slot admission for one document. That is exactly the gap L2-r7-03 taught two rounds ago, and I had already widened the parity assertions to whole diagnostics; the probe I wrote by hand had not caught up. The cross-nesting branch now carries the same admissibility guard as everything else: where the slot never admitted a call, the slot check is the true diagnosis. |
| L2-r8-02 | Stage-2 repo Codex review, same run dir | "[P2] Align root-rule precedence across both entry points — The parser reaches this new shared verdict only after source-order and control-continuation checks, while compiler validation invokes the verdict without those same preempting rules. Consequently `[branch, process_call]` parses as a continuation error at `/body` but compiles as a return-path error at `/body/steps/0`." | **P2** | machine-served schemas/contracts | DC-175-E | Standard — anchor: source label P2; no critical class or anchor. | this fix | `fixed` — reproduced, and the fix is a PRECEDENCE decision rather than more shared code, which is the finding's actual point: sharing a verdict implementation is not the same as sharing the rule ordering around it. Measured the discriminator before choosing: `[branch, set_dpp]` — the same defect with a different kind — produces NO diagnostic from `validate_body_capabilities` either, because control-continuation is not a rule that stage owns. The root verdict was therefore answering for a document it does not own, and now yields when a control node is followed by any step. **Fail-closed verified, not assumed:** the full pipeline still refuses the mutated `[branch, process_call]` model (`PROCESS_IR_SEMANTIC_AMBIGUOUS_FLOW`), so yielding closed no hole — a root call now behaves exactly like any other kind after a control node. |
| L2-r8-03 | Stage-2 repo Codex review, same run dir | "[P2] Scope negation to the equality clause — Any negation anywhere before the first equality verb suppresses the whole sentence. For example, `No live verification is performed, but submitted bytes match the stored readback` returns false and lets the forbidden second clause pass." | **P2** | machine-served schemas/contracts | DC-175-G | Standard — anchor: source label P2. | this fix | `fixed` — measured False before the fix, and the guard is now clause-scoped rather than sentence-scoped. **THIRD consecutive round in which this one guard was wrong**: vacuous (newline-blind) in round 3, order-and-polarity-blind in round 4, clause-blind here. Recorded plainly because the pattern matters more than the fix — I wrote a guard to stop prose asserting more than it can support, and wrote it three times without adversarial inputs of my own. Every version passed its own witnesses; each next reviewer supplied the case I had not thought of. The witness table now carries the mixed-clause sentence verbatim. |
| L2-r8-04 | Stage-2 repo Codex review, same run dir | "[P2] Match submitted as a complete word — The substring check also matches explicitly different subjects such as `unsubmitted`. Thus `An unsubmitted payload matches the stored bytes` satisfies every later token check and incorrectly fails the documentation gate." | **P2** | machine-served schemas/contracts | DC-175-G | Standard — anchor: source label P2. Same guard and same round as L2-r8-03 — counted with it as ONE instance, not two. | this fix | `fixed` — measured True (a false positive) before the fix. A word-boundary test I had used in the FIRST version of this guard and dropped when rewriting it around clause splitting; the rewrite lost a property the original had, which is its own small lesson about replacing a predicate wholesale. `\\bsubmitted\\b` restored, and `unsubmitted` is now a MUST-NOT-FLAG witness so the property cannot be dropped silently again. |

**Supersession map** — every revision row and the row it supersedes; a revision ADDS a row, and a committed row is never edited.
- `L3-06a → L3-06`
- `L3R-03a → L3R-03`
- `L2-r2-01a → L2-r2-01`
- `L2-r3-01a → L2-r3-01`
- `L2-r4-01a → L2-r4-01`
- `L3-01a → L3-01`
- `L2-r7-01a → L2-r7-01`

Dispositions: `fixed` · `finding-refuted` · `severity-refuted` · `not-validated` ·
`deferred` (issue, reason class, placement). A refutation names the disputed claim and the
concrete evidence. An original label is never edited — a revision is a new dated line with the
original retained.

## Checkpoints (a row is written IN FLIGHT at every third evaluation of each loop — 3, 6, 9, …
## — in the batch it governs, never reconstructed at close)

| Loop | Evaluation (window / cumulative) | SHA (+dirty) | Outcome | Rationale |
| --- | --- | --- | --- | --- |
| L1 (Stage-1 QA) | 3 / 3 | `6e2a7ff` (clean) | **CONTINUE** | Third L1 evaluation, so a checkpoint is forced. Round 3 was a single-scenario affected re-run for the L2-r6-01 crash fix and returned ZERO findings. **Per-tier counts:** zero Critical, zero Standard, zero unresolved at any tier in L1. **Breadth:** one scenario, narrowing from round 2's four and round 1's full matrix. **Classes:** no new class; nothing recurring in L1. **Trend vector:** findings 3 → 0 → 0; severity Critical-anchored → none → none; breadth full matrix → 4 items → 1 item. Every axis improving or flat at zero. **Why CONTINUE and not CLOSE-CLEAN:** L1 is clean, but the run is not over — the §6 architect gate must be replayed over the correction delta and the ENTIRE wave set re-run, because corrections mutated the tree after both. CLOSE-CLEAN would assert a completeness the owed gates have not established. **Named finite next correction:** none owed in L1; the next actions are the two replays above, not a fix. **Rule-outs:** DEFER-STANDARD-AND-* is inapplicable — there is no residue of any tier to defer. ESCALATE-OPEN is unwarranted: validation is available and nothing is unresolvable. The two items carried to #176 (UI-built Probe 2, post-fix canvas check) are UNMEASURED rather than deferred findings, blocked twice by an unavailable browser extension and not load-bearing given the runtime evidence. |
| L1 (Stage-1 QA) | 2 / 2 | `f8b56ef` (clean) | **CLOSE-CLEAN (L1 only)** | Round 2 returned ZERO findings. Recorded as a checkpoint because it closes the loop, not because it is a third evaluation. All three round-1 findings are confirmed closed by LIVE measurement, each re-taken the way it was originally found: the catch composition now refuses at plan with `PROCESS_CALL_CONFIG_INVALID` and apply performs no operations — component inventory 22 → 22, against 26 → 27 in round 1 — with a non-vacuity control (the same probe observed 22→23 and 24→25 on genuine creates in the same run) and four precision controls proving the new gate does not over-fire; the served sync-protocol text carries the exclusion and publishes the code, with all three stale strings scanning to `[]` against a positive control; and the apply-status fix was exercised on BOTH arms, including the `refused` arm round 1 never reached. Zero unresolved findings of any tier in L1, no deferrals. Two items remain UNMEASURED and are carried to #176 rather than claimed: the UI-built Probe 2 and the post-fix canvas check, both blocked twice by an unavailable browser extension. Neither is load-bearing here — the in-tree UI-built captures answered the pairing contract, and the runtime evidence (a canary-bearing control proving the downstream shape does not execute) is stronger than a canvas observation. **This closes L1 only**; the composite wave gate is still owed. |
| L2 (Stage-2 repo Codex review) | 3 / 3 | `45ceecb` (clean) | **CONTINUE** | **Per-tier counts:** zero Critical, one Standard (L2-r3-01, P2, emitted XML/graph validity). Every Critical row in this slice is already `fixed`. **Breadth:** NARROWING, not spreading — all three L2 findings land in one defect class (DC-175-A), one file, one function; rounds 2 and 3 are the same condition. **Classes:** no NEW class in three rounds; DC-175-A recurring, each instance resolved. **Trend vector:** highest unrefuted severity flat at P2 (never critical); unresolved count flat at 1; breadth narrowing. **The honest risk, named:** three successive strengthenings of one predicate — declaration validity, then correspondence existence, then correspondence universality — IS the instance-patch pattern, and a fourth condition would confirm it. Each round found a WEAKER form of one rule rather than a different defect, which is the "count fix KINDS, not findings" signal. **Named finite next correction:** restate the guard as ONE universal invariant — every outgoing connection of a Process Call must be attributed to a return path that call declares — which makes all three round-findings unwritable instead of adding a third condition. Not a widening of samples; a single statement that subsumes them. **Rule-outs:** CLOSE-CLEAN is unavailable (validated blocking residue exists). DEFER-STANDARD-AND-PROCEED is legal here (zero critical residue, not the last owed gate) but refused: the residue is a hole in the very guard this slice exists to add, and deferring it would ship a guard that certifies what it was built to reject. ESCALATE-OPEN is unwarranted — validation is available, severity is unambiguous, and the next action is concrete. |
| L2 (Stage-2 repo Codex review) | 2 / 5 | `8162351` (clean) | **CLOSE-CLEAN (L2 only)** | Round 5 returned NO findings — the first clean L2 round: *"The patch accurately identifies only the unbound connections and avoids falsely claiming that a shared target is unreachable. The verifier and audit-evidence tests pass."* Recorded as a checkpoint even though round 5 is not a third evaluation, because it CLOSES the loop and the closing decision belongs in the record. Zero unresolved findings of any tier in L2: r1-01, r2-01, r3-01 and r4-01 are all `fixed`, each with its own affected validation (full non-KB suite) and its own delta-scoped re-review. No deferral is recorded in this loop, so nothing is carried to the final bar from L2. The CP-1 named correction was applied and held: the rule survived rounds 4 and 5 untouched, which is the evidence that restating it universally ended the weaker-form sequence rather than merely postponing the next instance. **This closes L2 only** — the architect implementation review and the composite wave gate are still owed, and the slice does not close until they are current on the final tree. |
| L3 (§6 architect implementation review) | 3 / 3 | `c2ebc43` (clean at review; corrections uncommitted when this decision was recorded) | **CONTINUE** | Third evaluation of this loop, so a checkpoint is forced. **The owed validation ran first and this decision comes after it:** the round-3 corrections were applied, the affected scenarios re-run, and the full non-KB suite run, before this row was written. **Per-tier counts:** one **Critical** (L3R2-02, mutation accounting by anchor) and three Standard, all four `fixed`; zero unresolved at any tier; zero deferrals in this loop. **Breadth:** four findings across three artifacts — one compiler/model rule, one test-coverage gap plus an audit-record defect, and two prose classes. Narrower than round 1 (seven findings, six artifacts) but NOT yet collapsed to record-only. **Classes:** no NEW class from the reviewer; DC-175-E recurring (fifth instance) and DC-175-G recurring (second as durable-prose-outruns-evidence); DC-175-H is new and came from the WAVE GATE, not this loop. **Trend vector:** findings 7 → 5 → 4; highest unrefuted severity P2 → P2 → P2 at source, with one row re-derived Critical on an anchor I had previously mis-applied; artifacts touched 6 → 5 → 3. Improving on count and breadth, flat on severity. **Why CONTINUE and not CLOSE-CLEAN:** two recurring classes were answered STRUCTURALLY this round for the first time — one authority for placement, one executable scanner for prose claims — and a structural fix earns its confidence by surviving a round, not by being written. Round 3 also proved the point twice over: L3R-01 was a round-2 fix that only MOVED an asymmetry, and my own first scanner was vacuous against the very sentence it was written for. Closing now would assert that this round's fixes hold on exactly the evidence that failed last round. **Named finite next correction:** none outstanding — the next actions are re-running the wave set on the corrected tip and re-running this gate scoped to the correction delta, not a fix. **Rule-outs:** CLOSE-CLEAN is unavailable while owed gates are stale after mutation. DEFER-STANDARD-AND-PROCEED and -AND-CLOSE are inapplicable: there is no residue of any tier to defer, and the critical row may not be deferred in any case. ESCALATE-OPEN is unwarranted — validation is available, severity is now unambiguous, and every finding had a concrete corrective action that was taken. **On the instance-patch test:** DC-175-E is at five instances, which is the pattern the structural-fix rule exists to stop; the response this round was the invariant rather than a sixth patch, with a sibling sweep that found three further divergences the reviewer had not named, so this is not a repeated class being instance-patched. |

Each rationale records: per-tier counts and breadth, new/resolved/recurring defect classes
(derived from the rows), the trend vector, explicit rule-outs of the other outcomes, and a
NAMED finite next correction. The decision comes after the owed validation and before the next
mutation.

## Deferrals

Pointer-only — reason class, placement, and lineage live on the finding row and in the filed
issue. The one deferral this slice records at instantiation (returning-child return-path
binding → #176, `out-of-scope-by-design`) is described in the Deferred-capability section
above; it carries no finding row because it is a capability transfer decided at design time,
not a validated finding set aside. Any finding later deferred gets its own row and its own
lineage. `window-exhausted` is single-use per finding.

## Evidence index

Collected run directories are archived (byte-verified, allowlisted sidecars) under
`docs/architecture/evidence/issue-175/` with `index.jsonl` + `SHA256SUMS`, in the batch that
collects them. Run citations always use the COMPLETE run-dir name, backticked. The archive is
seeded at instantiation with the pre-baseline frozen bytes described in the trust boundary —
the negative-only witnesses whose whole value is that they were captured BEFORE the first
source edit and can never be regenerated afterwards.

## Final-tree validation (filled at close; every roster gate current on the FINAL sha)

| Gate | Evidence (quoted output / run URL / archived round) | SHA |
| --- | --- | --- |
