# Audit ledger — issue #177 (M12 — served-text ↔ enforcement consistency gate, DC-175-D structural fix)

Instantiated at Stage-1 step 0 per `docs/architecture/templates/AUDIT_LEDGER_TEMPLATE.md`, committed
in the Stage-1.5 baseline commit. Conventions inherited from #175/#178's end state apply from row
one: no hand-maintained totals or instance lists — counts are derived from the rows; every derived
field carries its deriving anchor inline; platform-behaviour claims carry a provenance marker
(`measured here` / `documented, not measured` / `assumption`); measured output is quoted, never
re-keyed from memory; audit-record-integrity findings are NON-blocking from round one; the
defect-class table is derived LAST, after the final finding row of the final batch.

## Baseline (Stage-1 step 0)

- Issue: #177 — Served-text ↔ enforcement consistency gate (DC-175-D structural fix)
- Step-0 baseline: `6f26caff7481356119fee5b36a1730cec0fb5df2`
- Measured green baseline at that tree: **10236 passed, 17 skipped** in 863.35s — *measured here*,
  quoted verbatim from the run's own summary line, full non-KB suite (`.venv` 3.12, `PYTHONPATH=src`,
  `pytest tests --ignore=tests/kb -p no:cacheprovider`). 10253 collected, which equals the current
  `minimum_collected` floor in `tests/fixtures/wave_gate/test_nodes.jsonl`.
- Slice kind: **behaviour-affecting**. The served static text of seven compiler diagnostic codes
  changes, and three of them change the summary a caller receives through the machine-facing
  `process_ir_authoring` projection (today those three serve their remediation in the summary slot).
  The ProcessIR authoring / compiler revision digests therefore move. Not dark.
- Artifact trust boundary. The slice **CREATES AND OWNS**: the served-text/enforcement guard modules
  and their AST emission collector; the manifest-keyed capability witness registry; the extracted
  dual-entry-point differential helper; the strict §8 capability-table parser; the seven canonical
  compiler messages; this ledger and its evidence archive under
  `docs/architecture/evidence/issue-177/`. It **CONSUMES**, unchanged and as authorities it may
  never rewrite: `PROCESS_IR_V1_CAPABILITIES` in `src/boomi_mcp/models/process_ir.py` (values and
  ordering); the §8 table in `docs/architecture/PROCESS_IR_V1.md`; `BODY_CAPABILITIES_V1`; the
  parser's own diagnostic tables and `parse_process_ir_v1`; #178's derived parity case generator and
  its corpus; `docs/architecture/ISSUE_175_AUDIT_LEDGER.md`, which is append-only and is never edited
  by this slice.
- Expected defect classes, pre-enumerated so a second instance triggers structurally ON ARRIVAL:
  1. **DC-177-A** (inherits **DC-175-D**) — served prose/text describing a capability or diagnostic
     the enforcement no longer grants; runtime authority: the capability manifest, the diagnostic
     registries, and the graph verifier. This slice exists to ship this class's invariant, so a fresh
     instance inside it is a direct signal the invariant is too narrow.
  2. A hand-enumeration shadowing a derivable authority — the standing class. This slice's whole
     deliverable is authority-derived case sets, so any hand-listed code set, capability set, or
     count is an instance.
  3. A guard that enumerates nothing and therefore passes everything — recorded in this repo five
     separate times (#149, #151, #162, #175, and #175's own prose scanner). Every invariant here
     carries a non-vacuity witness for exactly this class.
  4. A derived value re-derived by hand at the wrong moment (inherits **DC-175-D**) — this slice
     maintains manifest floors, a defect-class table, and three coverage claims, all derived.
  5. Hand-modelled platform behaviour in served text.

## Loop roster (fixed BEFORE the first correction; a gate not listed here cannot mint a loop
## mid-run — a roster addition is itself a recorded checkpoint decision)

1. **Stage-1 QA** — live `boomi-qa-tester` engagement through the public MCP tool boundary, scoped to
   the served `process_ir_authoring` diagnostic and capability projections and the ProcessIR
   plan/compile refusal paths this slice changes.
2. **Stage-2 repo Codex review** — detached, `--base 6f26caff7481356119fee5b36a1730cec0fb5df2`, then
   delta-scoped fix rounds. Every round COLLECTED via step 5d, never read from `wait`.
3. **§6 architect implementation review** — declared additive gate, run under
   `/codex-claude:codex-issue`, judging implementation-vs-design-plan. Fresh checkpoint window.
4. **Composite wave gate** — full non-KB suite + every active golden-manifest entry + deterministic
   compile/fingerprint checks + one integration-level review of the wave delta + one live scenario
   per changed capability class. ONE composite evaluation, not five loops.
5. **Terminal correction loop** — ONLY via a recorded roster-addition checkpoint.

## Recorded design decisions (made at Stage-1 step 0, before the first correction)

- **`diagnostic()` in `src/boomi_mcp/compiler/process_ir/diagnostics.py` stays TOTAL.** The design
  plan proposed making it raise for an unregistered code. It is called from inside the handlers that
  exist to stop a raw escape (`pipeline._guarded`, `emitter_registry`), and every production handler
  catches only `ProcessIRCompileError` — a raise there converts a diagnostic-registration gap into an
  unhandled error on a live MCP tool. `pipeline._compile_error_from_validation`'s own docstring
  records that exact failure mode being fixed once already. Fail-closed lives in the guard instead,
  and the fallback branch is retained as a degradation path, not as a silent default.
  *Deviation from the architect design plan, recorded.*

  **Revision 2026-08-22, after the §6 architect review (A6-08).** The sentence above originally
  read "every production handler catches only `ProcessIRCompileError`". That is OVERSTATED and the
  original wording is retained here rather than edited away: broad `except Exception` handlers do
  exist downstream, at `src/boomi_mcp/recipes/engine.py:1756` and
  `src/boomi_mcp/categories/integration_builder.py:7913` — verified by reading both. The DECISION
  stands on the narrower true claim: the `diagnostic()` call sites inside
  `pipeline._guarded` and `emitter_registry` are reached on paths whose own handlers are typed, and
  several are outside any `try` at all, so a raise there still escapes as an unhandled error rather
  than a served diagnostic. The same review also disproved the companion claim that the guard makes
  the fallback "provably unreachable" — see A6-01 — so that phrasing is withdrawn too.
- **The compiler's served text is the compiler layer's OWN authority, not a copy of the parser's.**
  Four of the seven codes with an empty compiler message also carry a parser message. Hand-writing
  compiler text for them is NOT a DC-177-A instance: the mechanism of that class is a hand-written
  description of a fact whose runtime authority lives *elsewhere*, and the compiler table is itself
  the authority for compiler-served text. The repo's shipped design already keeps per-layer
  remediations deliberately distinct and the projection attributes each to its producer. Consequence
  recorded here so it is not re-litigated: **no cross-producer text-identity assertion is added** —
  it would contradict the shipped design and
  `test_every_producer_s_message_AND_remediation_is_findable_in_its_entry`.
- **Case sets are never narrowed; only mechanisms are.** Acceptance criterion 3 requires coverage
  derived from the authority's full case set, so all 27 manifest keys, the whole §8 table, and the
  full served diagnostic union are in scope. Two mechanisms are narrowed against non-convergence
  risk: the AST collector pins an unresolved-SITE table instead of modelling Python control flow, and
  the capability witnesses do not restate #178's cross-entry vector equality (already gated by a
  derived product over the whole placement matrix). *Deviations from the architect design plan,
  recorded.*
- **Issue scope item 4 is WITHDRAWN by the issue itself** and is not built here. No general
  English-semantics checker. #175's prose scanner stays exactly where it is, as a best-effort lint.

## Defect-class ledger (empty at instantiation; a class is a (mechanism,
## runtime-authority) pair, assigned at reconciliation, revisable with original retained)

| Class | Mechanism | Runtime authority | Instances (derived from rows) | Resolution |
| --- | --- | --- | --- | --- |
| **DC-177-A** | served or durable text describing a capability, diagnostic or guard behaviour the code does not have | the capability manifest, the diagnostic registries, the graph verifier, and — for a guard's own comments — the guard's code | **6 rows** — QA-177-r1-01, QA-177-r1-02, A6-04, A6-07, A6-08, A6-09 | **Not structurally fixable in this slice, and the issue says so.** Scope item 4 — a predicate over English prose — was WITHDRAWN by the issue because prose has no closed case set. Every instance here is in the non-served region (docstrings, comments, a ledger table) and each is fixed individually in its batch; the served half is what invariants 1-3 close. Doc-prose ownership transfers to #147 (`M12_CAPABILITY_DOC_DRIFT`), recorded on INH-177-G. |
| **DC-177-B** | an identity key coarser than the property the check claims to pin, so a second instance collapses onto the first and escapes | the emission sites and the §8 table as they actually appear in the tree | **8 rows** — L2-r1-01, L2-r1-02, L2-r2-03, L2-r5-02, L2-r6-01, L2-r6-02, L2-r7-01, A6-05 | **STRUCTURAL FIX SHIPPED** at Stage-2 r2 and extended as later instances arrived: site identity is `(path, sink, full-expression)` with a per-site occurrence count, the census is keyed by the referencing path's producer and by allowed source path, and the §8 authority is matched as an exact LINE with rows selected by position. Each carries a hand-run witness proving the previous, coarser key passed the escape. |
| **DC-177-C** | a witness that does not reach the enforcement it claims to pin | the compile entry point and the emission plan | **5 rows** — L2-r1-03, L2-r1-04, L2-r2-01, A6-02, A6-03 | **STRUCTURAL FIX SHIPPED:** a witness must observe the ENFORCEMENT, not an echo of its own input. Every gated capability reachable on a validated model now asserts the compile-path refusal, and every admission witness asserts an emitter-kind fact against the emission plan — verified by re-running the reviewer's own substitute (real CFG, emptied plan): 7 of 8 formerly green, 8 of 8 now caught. |
| **DC-177-D** | an AST reader modelling an open-ended Python form space and failing OPEN on the forms it does not model | the emitting modules' actual runtime values | **8 rows** — L2-r2-02, L2-r3-01, L2-r3-02, L2-r3-03, L2-r4-01, L2-r4-02, L2-r5-01, A6-01 | **STRUCTURAL FIX SHIPPED, by REMOVING the mechanism rather than extending it.** Six instances across four rounds, each a different Python form (defaults, unpacking, rebinding, unreachable defaults, aliased calls, bindings with no `Name(Store)` node). The reader no longer resolves forwarded parameters at all; the one remaining hole — a code assembled by concatenation, A6-01 — is closed by reading the EVALUATED default through `inspect.signature`, which has no syntax to enumerate. |

**Derivation rule, stated so the arithmetic is checkable.** Counts are computed from the rows'
defect-class cells by a script, never typed — a stale hand-count in this table was itself finding
A6-07. `INH-177-D` and `INH-177-G` are EXCLUDED: they are the inherited class-level deviations this
slice exists to discharge, not defects found inside it. `finding-refuted` rows would never count;
there are none. One defect reported by two gates counts once.

Second-instance check: run against this table AT ROW-WRITE TIME, and also when a second finding lands
in the same file/subsystem within a loop. On the second instance the structural fix is mandatory in
that batch (or the immediately-next dedicated batch for dispatch/compiler/materialization/shared-apply
code), with sibling sweep, non-vacuity witness, and an authority-derived coverage claim.

## Finding rows (one per raw finding; append-only; exactly one disposition each)

| ID | Source gate + run dir + attestation | Verbatim summary | Original label | Blocking class | Defect class | Derived tier (anchor inline) | SHA/delta | Disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| INH-177-D | Inherited — `docs/architecture/ISSUE_175_AUDIT_LEDGER.md` DC-175-D class row, recorded deviation filed at that reconciliation | "**RECORDED DEVIATION → #177, reason class `blocked-by-mechanism`, filed at this reconciliation.** Standard tier only, which is the sole tier a deviation is available to. Grounds: the invariant needs a general mechanism tying served text to the enforcement behind it, across capability entries, diagnostic registries, architecture docs and the compatibility inventory — repo-wide machinery, not a Process Call change." | *(no source severity label — a class-level deviation, Standard tier by the rule quoted)* | machine-served schemas/contracts | DC-177-A | Standard — anchor: the deviation is recorded as Standard tier only; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | this slice | `fixed` — the class invariant SHIPPED for the served region: invariant 1 (every emittable code carries complete served text, derived from an AST read of the emitting modules), invariant 2 (a manifest-keyed capability witness registry, 27/27 keys), invariant 3 (§8 key-AND-state equality against the manifest). Each carries a bidirectional pin and a hand-run non-vacuity witness against the real historical defect; evidence in `docs/architecture/evidence/issue-177/non-vacuity.md`. |
| INH-177-G | Inherited — `docs/architecture/ISSUE_175_AUDIT_LEDGER.md` DC-175-G class row, recorded deviation filed at that reconciliation | "**DEVIATED → #177** (`blocked-by-mechanism`, filed at this reconciliation) … English has no closed case set, so a predicate over prose cannot meet that bar … It is RETAINED as a best-effort lint, pinned by the real historical defect and by every counter-example a reviewer supplied, and it is explicitly not the class invariant. #177 owns the invariant, where checks derive from runtime authorities instead of from reading English." | *(no source severity label — a class-level deviation, Standard tier by the rule quoted)* | machine-served schemas/contracts | DC-177-A | Standard — anchor: same as INH-177-D; the deviation is Standard tier only. | this slice | `deferred` — **#147**, reason class `out-of-scope-by-design`, placement: #147 is the post-#160 M12 closeout that already owns `M12_CAPABILITY_DOC_DRIFT` and consumes this gate. Grounds, quoted from the issue's own withdrawal of scope item 4: "English has no closed case set, so a predicate over prose cannot make the coverage claim the structural-fix rule requires." The BOUNDED half — served machine text — is discharged here by invariants 1-3. #175's prose scanner stays exactly where it is, a best-effort lint, explicitly not this class's invariant. This is `out-of-scope-by-design`, not `window-exhausted`: no lineage is consumed. |
| QA-177-r1-01 | Stage-1 QA, live, `agents/reports/2026-08-22-issue-177-served-text-gate-r1.md`, round 1 | "`_diagnostic_summary`'s docstring in `process_ir_projection.py` still says \"Seven compiler codes have no entry in the static `_MESSAGES` table\" and \"Only THREE reach this fallback\" (measured: 0 and 0), and cites `test_exactly_the_expected_codes_fall_back_to_their_remediation` as its measurement authority — a node **this same slice tombstoned**. Its fallback loop is now dead code." | **Low** | *(none — durable prose, PROVEN NOT SERVED by a 405 KB sweep across list_capabilities, the root template, all ten category pages and 45 tool descriptions, with two control phrases firing)* | DC-177-A | Standard — anchor: source label Low; no critical class and no critical anchor. Outside the eight blocking classes, so it takes the ONE batched correction pass. | this fix | `fixed` — the docstring now states what is true (all three counts are zero, measured), explains the historical shape because that is why the pin exists, and cites the LIVE authority `test_no_compiler_diagnostic_falls_back_to_its_remediation` instead of the tombstoned node. The fallback loop is RETAINED as an honest degradation path rather than deleted, and is pinned empty by that test — deleting it would remove the only behaviour a future regression could land on. |
| QA-177-r1-02 | Stage-1 QA, same report and round | "the identical three-place comment about serving an empty string \"so a caller can see the gap\" was corrected in `diagnostics.py` and removed in `models/process_ir.py`, but is unchanged in `findings.py`, three lines above the `finding_specs()` docstring that says \"Fails closed since #177\". A sibling-sweep miss." | **Low** | *(none — module comment, proven not served by the same sweep)* | DC-177-A | Standard — anchor: source label Low. Outside the blocking classes. | this fix | `fixed` — and the finding is exactly right: I corrected two of three siblings and left the third contradicting the docstring immediately below it. The comment now records that the blank-emitting rationale was MEASURED false (seven rows served for four slices, no caller looked) rather than merely reversed. Swept afterwards for the old strings across `src/`, `tests/` and `docs/`: the only remaining occurrences are the new text quoting the old rationale as history. |
| L2-r1-01 | Stage-2 repo Codex review, round 1, run dir `cdx-review.CIb4L8`, base `6f26caf`, head `7aa79b3`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Preserve full identity for unresolved emission sites — when another unresolved `finding(...)` call is added anywhere in `semantic_validation/flow.py` or `validation_policy.py`, this projection collapses it with the existing delegation to the same `(path, sink)` tuple. The equality therefore stays green even if the new expression can supply an unregistered code" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-B | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `7aa79b3`→`a206252` | `fixed` — CONFIRMED by planting the exact escape: a second unresolved `finding(...)` in `flow.py`. Measured, the old key returned `OLD guard equality holds on the mutated tree: True`. Site identity is now `(path, sink, full-expression)` with a per-site occurrence count. |
| L2-r1-02 | Stage-2 repo Codex review, round 1, run dir `cdx-review.CIb4L8`, base `6f26caf`, head `7aa79b3`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Restrict forwarding skips to the enclosing wrapper — when a sink's dynamic code is held in a local variable whose name also appears as a parameter anywhere in the module—especially `code`—the module-wide `self.parameters` set makes this branch skip it as though it were the enclosing wrapper's forwarded parameter. That call is neither resolved nor reported as unresolved" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-B | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `7aa79b3`→`a206252` | `fixed` — CONFIRMED by planting a local named `code` in a non-wrapper function; the module-wide set dropped it silently. The skip was scoped to the enclosing function, which immediately surfaced two further real sites the old rule had been dropping. |
| L2-r1-03 | Stage-2 repo Codex review, round 1, run dir `cdx-review.CIb4L8`, base `6f26caf`, head `7aa79b3`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Exercise parser-gated fields through direct compilation — for field-level gates such as `catch_all=false`, listener scope, `load_all_documents=false`, and the `definedparameter` source, this helper invokes only the parser. The #178 product cited here varies node kinds and placements, not these field values" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-C | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `7aa79b3`→`a206252` | `fixed` for the reachable cases and REFUTED-with-evidence for one. Measured per field: `scope` and `load_all_documents` are real mutable fields whose assignment is not re-validated, so both gained a compile-path half on a carrier proven to compile clean. `catch_all` is not a model field at all — `ValueError: "TryCatchNodeV1" object has no field "catch_all"` — so no compile case exists. My claim that `definedparameter` was equally unreachable was WRONG and is corrected at L2-r2-01. |
| L2-r1-04 | Stage-2 repo Codex review, round 1, run dir `cdx-review.CIb4L8`, base `6f26caf`, head `7aa79b3`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Verify the retry count after lowering — if retry lowering hardcodes zero or drops the authored value, this witness still passes because it discards the emission plan and inspects only the CFG semantic record derived from the input" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-C | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `7aa79b3`→`a206252` | `fixed` — the witness now retains the plan and asserts `CatchErrorsInputV1.retry_count == 5` as well as the CFG semantic. The capability the manifest advertises is the retry the compiler EMITS, not the one the caller typed. |
| L2-r2-01 | Stage-2 repo Codex review, round 2, run dir `cdx-review.rsFiQz`, base `7aa79b3`, head `a206252`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Add a compile witness for definedparameter sources — the claim that no mutable model can carry this construct overlooks that `source_values` is a normal mutable list: after parsing a legal `set_dpp` with a static source, a caller can assign the `definedparameter` dict to `source_values[0]` and pass the model to `compile_process_ir_v1`" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-C | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `a206252`→`b07c9cb` | `fixed`, and my justification was simply wrong: no union member class is needed to put the shape on a validated model. Measured — the compiler refuses with `PROCESS_IR_CAPABILITY_UNSUPPORTED` at `/body/steps/1/source_values/0`. That enforcement was real and unpinned; it now has its own `mutate`. |
| L2-r2-02 | Stage-2 repo Codex review, round 2, run dir `cdx-review.rsFiQz`, base `7aa79b3`, head `a206252`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Include the helper's default diagnostic code — `_check_region_containment` is called without its `code` argument at two sites, so it can emit its default `PROCESS_IR_SEMANTIC_AMBIGUOUS_FLOW`; contrary to this exemption's reason, not every caller supplies a literal" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-D | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `a206252`→`b07c9cb` | `fixed` — the pinned reason was false, which is this slice's own defect class landing in my own guard. First fixed by resolving defaults and call sites (measured: parameter index 5, resolving to `PROCESS_IR_SEMANTIC_AMBIGUOUS_FLOW` from the default plus `PROCESS_IR_COMPILE_ERROR_REGION_INVALID` from the one explicit call site). That resolver was removed at L2-r4; the site is now pinned with an accurate reason and backed by `test_every_code_named_in_the_emitting_modules_is_served`, whose mutant evidence is recorded in `docs/architecture/evidence/issue-177/non-vacuity.md`. |
| L2-r2-03 | Stage-2 repo Codex review, round 2, run dir `cdx-review.rsFiQz`, base `7aa79b3`, head `a206252`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Preserve the full expression in site identities — the new counter treats `dump` as the full code-expression identity, but `collect_emissions()` still truncates `ast.dump(argument)` to 80 characters" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-B | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `a206252`→`b07c9cb` | `fixed` — the truncation was left in the producer while the consumer was hardened, so the identity was still coarser than the claim. Full dump stored. |
| L2-r3-01 | Stage-2 repo Codex review, round 3, run dir `cdx-review.WBkpzf`, base `a206252`, head `b07c9cb`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Fail closed when forwarded arguments use unpacking — when a helper is called with `**{"code": "NEW_CODE"}` or positional values supplied through `*args`, this branch treats the value as omitted and retains only the default" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-D | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `b07c9cb`→`f1ebd0d` | `fixed` at r3 by rejecting unpacking, then made moot at r4 when the resolver was deleted. |
| L2-r3-02 | Stage-2 repo Codex review, round 3, run dir `cdx-review.WBkpzf`, base `a206252`, head `b07c9cb`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Reject rebound parameters as one-hop forwarding — when the enclosing function reassigns the parameter, such as `code = choose_code()`, matching the sink argument's identifier to the original parameter is insufficient" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-D | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `b07c9cb`→`f1ebd0d` | `fixed` at r3 by rejecting rebinding, then made moot at r4 when the resolver was deleted. |
| L2-r3-03 | Stage-2 repo Codex review, round 3, run dir `cdx-review.WBkpzf`, base `a206252`, head `b07c9cb`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Count defaults only when a call actually omits them — when the helper has no call sites, or every call explicitly overrides this parameter, these lines still add its default code before call analysis" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-D | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `b07c9cb`→`f1ebd0d` | `fixed` at r3, then made moot at r4. Worth recording that writing the control for this one caught an error in my own expectation: the every-call-overrides case SHOULD resolve, to the supplied code alone. |
| L2-r4-01 | Stage-2 repo Codex review, round 4, run dir `cdx-review.vl7VZx`, base `b07c9cb`, head `f1ebd0d`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Reject indirect calls before dropping the default — with `alias = helper`, a direct `helper("OVERRIDE")`, and `alias()`, `seen_call` is true but `omitted` remains false, so the resolver reports only `OVERRIDE` even though runtime can emit the default" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-D | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `f1ebd0d`→`a1c7860` | `fixed` by DELETING the resolver, not by adding alias detection. Fourth consecutive round finding a further Python form: the class could not converge because Python's binding and call syntax has no closed case set, which is the coverage claim the structural-fix rule requires. Forwards are now REPORTED, never resolved. |
| L2-r4-02 | Stage-2 repo Codex review, round 4, run dir `cdx-review.vl7VZx`, base `b07c9cb`, head `f1ebd0d`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Detect rebinding forms without Name(Store) nodes — for a forwarded `code` parameter rebound through pattern capture (`case code`), `import ... as code`, or a local `def/class code`, Python records the binding in a string field rather than an `ast.Name(Store)`" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-D | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `f1ebd0d`→`a1c7860` | `fixed` by the same deletion — with no binding analysis left, there is no binding form to miss. |
| L2-r5-01 | Stage-2 repo Codex review, round 5, run dir `cdx-review.h8qNBO`, base `f1ebd0d`, head `a1c7860`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Do not skip rebound first parameters — when a sink wrapper rebinds its first parameter (for example, `code = choose()`) before calling `finding(code, ...)`, this name-only check still classifies the argument as forwarded" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-D | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `a1c7860`→`6f4512c` | `fixed` — a regression I introduced by deleting `_is_rebound` alongside the resolver. Resolved by dropping the SKIP: the pinned table grew from 3 sites to 7 (the sink definitions now appear there), the emitted code counts were unchanged at 31/15/16/19 across parser, compiler, semantic and verifier, and `_is_rebound` no longer exists, so there is no binding form left for the reader to miss. |
| L2-r5-02 | Stage-2 repo Codex review, round 5, run dir `cdx-review.h8qNBO`, base `f1ebd0d`, head `a1c7860`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Tie pinned codes to their emitting source — this only verifies that the hand-listed strings exist in the merged served registry; it never verifies that they are the helper default and call-site values" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-B | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `a1c7860`→`6f4512c` | `fixed` — and the hand-listed tuple was deleted rather than tied. Replaced by a census requiring every code NAMED in the scanned modules to be served, which catches a changed default without tracing a value. Non-vacuity hand-run and recorded: the first census, matching only KNOWN constants, was green on the mutant; matching on code SHAPE (families derived from the real code set) catches it. |
| L2-r6-01 | Stage-2 repo Codex review, round 6, run dir `cdx-review.tlshb8`, base `a1c7860`, head `6f4512c`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Check census entries against their producer tables — using the merged `_served()` union recreates the cross-layer masking this file is intended to prevent" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-B | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `6f4512c`→`7518dd9` | `fixed` — the census is keyed on the referencing path's producer via `_SATISFYING_TABLES`. Witnessed: a default changed to the parser-only `PROCESS_IR_SCHEMA_UNKNOWN_NODE` now fails with the exact code and path. |
| L2-r6-02 | Stage-2 repo Codex review, round 6, run dir `cdx-review.tlshb8`, base `a1c7860`, head `6f4512c`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Scope unserved exceptions to their intended modules — the allowlist is applied globally by code rather than to the documented source boundary" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-B | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `6f4512c`→`7518dd9` | `fixed` — `UNSERVED_BY_DESIGN` is now `code -> allowed path prefix`. Witnessed: a compiler forward using `LEGACY_ADAPTER_SEMANTIC_LOSS` now fails with the exact code and path. |
| L2-r7-01 | Stage-2 repo Codex review, round 7, run dir `cdx-review.bZjszs`, base `6f4512c`, head `7518dd9`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Retain served-state validation for exemptions — when an `UNSERVED_BY_DESIGN` code is later added to a served table while remaining named at its allowed path, `_allowed_unserved` excludes it from `unserved`, while this stale check still passes because it only verifies path presence" | **P2** | capability reachability + machine-served schemas/contracts (these guards are the mechanism protecting both) | DC-177-B | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `7518dd9`→`93f81df` | `fixed` — my r6 rewrite dropped the served half while the comment above it still claimed both directions. Restored producer-aware. The witness for it initially reported a FALSE PASS because the mutant anchor `_MESSAGES = {` did not match the real annotated declaration; the live mutant was confirmed to have taken effect before the guard's result was believed. |
| A6-01 | §6 architect implementation review, run dir `cdx-gate-review.7krqqr`, `parsedVerdict: ISSUES FOUND`, attestation `a662126631cee4d1`, daemon `stopped:true` | "invariant-1 scanner is not fail-closed over the full emitter set: it recognizes only first-parameter wrappers and only complete code-shaped literals/constants; mutating the real sixth-parameter default at `src/boomi_mcp/compiler/process_ir/invariants.py:676` to `"PROCESS_IR_" + "SEMANTIC_TOTALLY_NEW_UNREGISTERED"` produced a runtime-emittable unregistered code while all ten served-text tests passed" | **P2** | capability reachability + machine-served schemas/contracts | DC-177-D | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `3c0e6eb`→this fix | `fixed`, and the finding is correct against my own recorded reasoning: my r4 claim that the census plus the pinned table recover what deleting the resolver lost is FALSE for a code the source never spells as one token. REPRODUCED before fixing — the mutant yields the real default `PROCESS_IR_SEMANTIC_TOTALLY_NEW_UNREGISTERED` with 10 passed. I did NOT take the suggested repair (recognise dynamically constructed codes): concatenation, f-strings, `.format` and `.join` are the same unbounded space that produced a finding in four consecutive Stage-2 rounds. Instead `runtime_forward_defaults()` imports the module and reads the EVALUATED default through `inspect.signature`, so how the expression was written stops mattering. The same mutant now fails naming file, function, parameter and value. |
| A6-02 | §6 architect implementation review, run dir `cdx-gate-review.7krqqr`, `parsedVerdict: ISSUES FOUND`, attestation `a662126631cee4d1`, daemon `stopped:true` | "injected projection sources bypass the planned completeness validation: blank diagnostic fields are accepted, so blanking an injected compiler message still built all 182 entries and served its remediation as the summary — the "fallback unreachable by construction" claim is currently false" | **P2** | machine-served schemas/contracts | DC-177-C | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `3c0e6eb`→this fix | `fixed` — and this was a deliverable in my own implementation plan (step 4) that I simply did not write. Validation now runs at the merge in `_diagnostic_entries`, so it covers injected and production sources alike and names CODES only. Witnessed: blanking an injected compiler message now raises `compiler diagnostic source serves a blank message for PROCESS_IR_COMPILE_CONTROL_WIRING_INVALID` instead of building 182 entries. |
| A6-03 | §6 architect implementation review, run dir `cdx-gate-review.7krqqr`, `parsedVerdict: ISSUES FOUND`, attestation `a662126631cee4d1`, daemon `stopped:true` | "seven of eight supported capability witnesses discard the emission plan and inspect only CFG/model facts; a compiler substitute returning the real CFG but no emission plan left all seven green (only `bounded_retry` failed)" | **P2** | capability reachability | DC-177-C | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `3c0e6eb`→this fix | `fixed` — every admission witness now asserts an emitter-kind fact against the plan. Re-ran the reviewer's own substitute (real CFG, emptied plan): before, 7 of 8 stayed green; after, **8 of 8 are caught**. |
| A6-04 | §6 architect implementation review, run dir `cdx-gate-review.7krqqr`, `parsedVerdict: ISSUES FOUND`, attestation `a662126631cee4d1`, daemon `stopped:true` | "capability-fixture provenance weaker than recorded: the header says inline documents are refusal inputs, yet five supported admission witnesses are inline; the two listed frozen error-handling fixtures are never loaded, and the enforcement test accepts any nonblank provenance string" | **P2** | *(audit-record integrity — NOT one of the eight blocking classes)* | DC-177-A | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `3c0e6eb`→this fix | `fixed`, all three halves. This is my own file claiming something its code did not do — DC-177-A landing inside the slice built to close it. `scoped_try_catch` and `bounded_retry` now LOAD the frozen anchors that were listed but unused; the header states honestly which three admission inputs remain inline and why; and provenance is declared from a closed `PROVENANCE_KINDS` set that the gate compares against, with a floor asserting the strongest kind is actually in use. |
| A6-05 | §6 architect implementation review, run dir `cdx-gate-review.7krqqr`, `parsedVerdict: ISSUES FOUND`, attestation `a662126631cee4d1`, daemon `stopped:true` | "the §8 heading pin is substring-based and stayed green after rewording the authority to `## 8. Capability states (normative)`, violating acceptance criterion 1's rewording requirement" | **P2** | machine-served schemas/contracts | DC-177-B | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `3c0e6eb`→this fix | `fixed` — REPRODUCED (`reworded heading still contains the pin: True`). The heading is now matched as an exact LINE; the reworded authority fails with `expected exactly one line equal to '## 8. Capability states', found 0`. |
| A6-06 | §6 architect implementation review, run dir `cdx-gate-review.7krqqr`, `parsedVerdict: ISSUES FOUND`, attestation `a662126631cee4d1`, daemon `stopped:true` | "the §8 table parser only considers lines starting with `|`, so a valid pipe-less Markdown row (`` `invented_doc_only` | gated | #999 ``) was ignored while 27/27 equality stayed green" | **P2** | machine-served schemas/contracts | DC-177-B | Standard — anchor: source label P2; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `3c0e6eb`→this fix | `fixed` — rows are now selected by POSITION (header, delimiter, then every non-blank line to the first blank), outer pipes optional, the way Python-Markdown reads a table; a stray row elsewhere in the section fails too. The reviewer's exact row now reports `{'in the doc only': ['invented_doc_only']}` — the accurate diagnosis rather than 'malformed row', which the first cut produced. |
| A6-07 | §6 architect implementation review, run dir `cdx-gate-review.7krqqr`, `parsedVerdict: ISSUES FOUND`, attestation `a662126631cee4d1`, daemon `stopped:true` | "derived defect-class table is stale: lists only DC-177-A with two rows, while the 21 finding rows classify as A=4, B=7, C=3, D=7" | **P3** | *(audit-record integrity — NOT one of the eight blocking classes)* | DC-177-A | Standard — anchor: source label P3; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `3c0e6eb`→this fix | `fixed` — the table is recomputed FROM the rows' defect-class cells by a script rather than re-typed, which is the property its own header claims. A stale derived table in a slice whose subject is derived-versus-restated facts is the sharpest possible instance of the class. |
| A6-08 | §6 architect implementation review, run dir `cdx-gate-review.7krqqr`, `parsedVerdict: ISSUES FOUND`, attestation `a662126631cee4d1`, daemon `stopped:true` | "recorded call-graph rationale overstated ("every production handler catches only `ProcessIRCompileError`"): broad handlers exist at `src/boomi_mcp/recipes/engine.py:1756` and `src/boomi_mcp/categories/integration_builder.py:7913`" | **P3** | *(audit-record integrity)* | DC-177-A | Standard — anchor: source label P3; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `3c0e6eb`→this fix | `fixed` — verified both handlers by reading them; the claim was wrong as written. The original wording is RETAINED with a dated revision beside it rather than edited away, and the decision is restated on the narrower claim that is true. |
| A6-09 | §6 architect implementation review, run dir `cdx-gate-review.7krqqr`, `parsedVerdict: ISSUES FOUND`, attestation `a662126631cee4d1`, daemon `stopped:true` | "QA-177-r1-01's replacement prose remains overbroad: native graph-verifier codes deliberately stay outside the three registries and injected rows can still reach the fallback" | **P3** | machine-served schemas/contracts | DC-177-A | Standard — anchor: source label P3; no critical class (not secrets, data loss, or mutation accounting) and no critical anchor. | `3c0e6eb`→this fix | `fixed` — the docstring now scopes its claim to the three registries, names the graph verifier as deliberately outside them, and states the two limits that made the older phrasing false: the native graph verifier serves its own result outside the three registries, and an injected `ProjectionSourcesV1` could carry a blank field past the accessors until A6-02 closed that at the merge — witnessed by the refusal `compiler diagnostic source serves a blank message for PROCESS_IR_COMPILE_CONTROL_WIRING_INVALID`. The prose I wrote to correct a QA finding was itself overbroad, which is the same class again. |
| L2-r9-01 | Stage-2 repo Codex review, round 9 (§6-correction delta), run dir `cdx-review.xjo2Um`, base `93f81df`, head `371c533`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Inspect keyword-only forwarded defaults — when a forwarded code parameter is keyword-only or positional-only, this pass silently skips it because `_ModuleScan.forwarded_parameter()` examines only `owner.args.args`" | **P2** | capability reachability + machine-served schemas/contracts | DC-177-B | Standard — anchor: source label P2; no critical class and no critical anchor. | `371c533`→this fix | `fixed` — Python's parameter KINDS are a closed set of three (positional-only, positional-or-keyword, keyword-only), unlike the call and binding syntax this reader deliberately refuses to model, so enumerating them converges. All three are now walked. |
| L2-r9-02 | Stage-2 repo Codex review, round 9 (§6-correction delta), run dir `cdx-review.xjo2Um`, base `93f81df`, head `371c533`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Pin unreadable defaults by exact forwarding site — for nested owners such as the existing `collect_lineage_findings._report`, runtime inspection reports an unreadable default, but this check accepts it merely because some delegation in the same file is pinned" | **P2** | capability reachability | DC-177-B | Standard — anchor: source label P2; no critical class and no critical anchor. | `371c533`→this fix | `fixed` — dispositioned by exact `(path, function, parameter)` in `UNREADABLE_DEFAULTS`, compared in both directions. The one member's reason is VERIFIED rather than asserted: `_report(code: str, node, severity="error", evidence=())` takes `code` with NO default, so there is no default value to read. |
| L2-r9-03 | Stage-2 repo Codex review, round 9 (§6-correction delta), run dir `cdx-review.xjo2Um`, base `93f81df`, head `371c533`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Tie provenance tags to the actual witness source — when an admission witness is mislabeled as `inline refusal document`, or an inline document claims `frozen fixture nonexistent.json`, this check still passes because it validates only an accepted prefix" | **P2** | *(audit-record integrity — NOT one of the eight blocking classes)* | DC-177-C | Standard — anchor: source label P2; no critical class and no critical anchor. | `371c533`→this fix | `fixed` — provenance is validated against the witness's own MODE and, for a frozen claim, against the `FIXTURE_PROVENANCE` inventory AND the file's existence on disk. Both escapes hand-run: the mislabel and the nonexistent fixture are each caught. |
| L2-r9-04 | Stage-2 repo Codex review, round 9 (§6-correction delta), run dir `cdx-review.xjo2Um`, base `93f81df`, head `371c533`, `STATUS: completed`, teardown `confirmed stopped` | "[P2] Match the capability heading without stripping indentation — if the real heading is indented by four spaces, Python-Markdown renders it as a code block rather than a section heading, but `line.strip()` still matches" | **P2** | machine-served schemas/contracts | DC-177-B | Standard — anchor: source label P2; no critical class and no critical anchor. | `371c533`→this fix | `fixed` — the heading, table header and delimiter are matched as RAW lines. Witnessed: indenting §8 by four spaces now fails with `expected exactly one line equal to '## 8. Capability states', found 0`. A structurally removed authority no longer leaves the guard green. |
| QA-177-r2-01 | Stage-1 QA round 2, live, `agents/reports/2026-08-22-issue-177-served-text-gate-r2.md`, tree `371c533` | "the blank-field guard has no test at all: five surgical mutants scored 0/5 KILLED — delete all 7 guard lines, drop the `remediation` half, `is None` only (accepts `""` / `"   "`), interpolate the whole authored row into the error — all SURVIVED, both controls green. Sharpened by reachability: `_complete_spec_rows` already refuses blank fields for all three production accessors, so no production input can reach this guard; its entire domain is injected snapshots, i.e. dead code that a "remove unused branch" cleanup has nothing to stop" | **Medium** | machine-served schemas/contracts | DC-177-C | Standard — anchor: source label Medium; no critical class and no critical anchor. | `371c533`→this fix | `fixed` — I added a production guard and no test pinning it, which is the shape a later cleanup deletes with nothing failing. Two nodes now pin both halves of its promise (every blank form from every producer is refused; the error names the CODE and no authored content), with the producer codes DERIVED from each table rather than typed. QA's mutants re-graded: **4/4 now KILLED**, control 64 passed. The reported `AttributeError` on a non-string is fixed too — and my first attempt, wrapping the value in `str()`, was WORSE: `str(object())` is a non-empty string, so the row passed the guard and failed downstream. It now checks the type. |

Dispositions: `fixed` · `finding-refuted` · `severity-refuted` · `not-validated` · `deferred` (issue,
reason class, placement). A refutation names the disputed claim and the concrete evidence. An original
label is never edited — a revision is a new dated line with the original retained.

## Checkpoints (a row is written IN FLIGHT at every third evaluation of each loop —
## 3, 6, 9, … — in the batch it governs, never reconstructed at close)

| Loop | Evaluation (window / cumulative) | SHA (+dirty) | Outcome | Rationale |
| --- | --- | --- | --- | --- |
| Stage-2 repo Codex review | 3 / 3 | `f1ebd0d1074607a5d703c9d7562fa4d97c32475c` (clean) | `CONTINUE` | Recorded AFTER the round-3 correction's owed validation (round 4), per the rule that reaching a checkpoint never cancels validation already owed. **Per-tier counts:** 0 critical in every round; Standard 4 (r1) → 3 (r2) → 3 (r3) → 2 (r4). **Breadth:** r1 spanned three artifacts (collector, witness registry, guard); r2 two; r3 and r4 are entirely inside ONE function, `resolve_forward`, which r2 caused me to add. **Defect classes** (derived from the rows): DC-177-B *(a guard whose identity key is coarser than the property it claims to pin)* — instances r1-01, r1-02, r2-03; structural fix applied at r2 (full site identity + per-site counts, with a hand-run witness proving the old key passed both escape shapes); **no instance since → resolved**. DC-177-C *(a witness that does not reach the enforcement it claims to pin)* — instances r1-03, r1-04, r2-01; fixed by adding the compile-path halves and the lowered-retry assertion; **no instance since → resolved**. DC-177-D *(an AST reader modelling an open-ended Python form space and failing OPEN on unmodelled forms)* — instances r2-02, r3-01, r3-02, r3-03, r4-01, r4-02; **recurring**. **Trend:** none worsening; two classes resolved; breadth collapsed from three artifacts to one function. **Rule-outs.** `CLOSE-CLEAN` is illegal — r4 carries validated findings. `DEFER-STANDARD-AND-PROCEED`/`AND-CLOSE` rejected: the residue is not blocked by mechanism or out of scope by design, and a fix exists that costs one batch. `ESCALATE-OPEN` rejected: the structural-fix rule escalates only when a class recurs after its structural fix **with no materially different action left**, and there is one — DC-177-D's r3 fix EXTENDED the resolver, whereas the named correction below DELETES it. **Named finite next correction:** remove `resolve_forward`/`_is_rebound` entirely; report any forwarded parameter that is not a sink's own first parameter as unresolved; restore `invariants.py` `_fail` to the pinned table with a human-stated two-code authority that the guard asserts is served; replace the resolver's control with one over the reporting property. Applied in this batch. |
| Stage-2 repo Codex review | 3 / 6 | `6f4512c018f381365898be1c0725543399dbd832` (clean) | `CONTINUE` | Second window's third evaluation, recorded after r6's findings were reconciled and before the next mutation. **Per-tier counts:** 0 critical in every round; Standard 4 / 3 / 3 / 2 / 2 / 2. **Breadth:** r6's two findings are both inside ONE test function, the census added at r5. **Defect classes:** DC-177-D *(an AST reader modelling an open-ended Python form space)* — the class that could not converge — is **ELIMINATED, not patched**: r4's decision deleted `resolve_forward`/`_is_rebound` outright rather than adding a sixth form, and r5 and r6 contain no instance of it. DC-177-B *(an identity key coarser than the property it pins)* **recurred** at r6 in new code: the census compared against the MERGED served union and applied its by-design allowlist by code alone. Both are bounded — they are scoping errors in a check, not an open-ended domain — and both are fixed in this batch by keying the census on the referencing path's producer and on the allowed path prefix, with the two escape scenarios the reviewer described hand-run RED and recorded. **Trend:** none worsening; the non-convergent class is gone, which is the materially-better dimension — a flat count of 2 that no longer contains an unbounded class is not the same residue as before. **Rule-outs.** `CLOSE-CLEAN` illegal: r6 carried validated findings. `DEFER-*` rejected: the residue is neither `blocked-by-mechanism` nor `out-of-scope-by-design`, and the correction costs one batch — deferring a one-batch fix into a freshly-minted issue is exactly the debt-minting the default warns against. `ESCALATE-OPEN` rejected: no critical residue, and a concrete corrective action existed and is applied. **Named finite next correction:** scope the census by producer table and by allowed source path (applied here); r7 is its owed delta-scoped validation. |

Each rationale records: per-tier counts and breadth, new/resolved/recurring defect classes (derived
from the rows), the trend vector, explicit rule-outs of the other outcomes, and a NAMED finite next
correction.

## Deferrals

Pointer-only — reason class, placement, and lineage live on the finding row and in the filed issue.
RULE: a deferral's issue body QUOTES the commit SHA of the already-committed checkpoint row it cites.
`window-exhausted` is single-use per finding.

*(none at instantiation)*

## Evidence index

Collected run directories are archived (byte-verified, allowlisted sidecars) under
`docs/architecture/evidence/issue-177/` with `index.jsonl` + `SHA256SUMS`, in the batch that collects
them. Run citations always use the COMPLETE run-dir name (`cdx-review.<suffix>` /
`cdx-gate-review.<suffix>`, backticked).

Non-vacuity mutant evidence lives in `docs/architecture/evidence/issue-177/non-vacuity.md`: for each
invariant, the exact file and edit, the guard that went red with its first assertion line, and the
restored-green confirmation.

## Final-tree validation (filled at close; every roster gate current on the FINAL sha)

| Gate | Evidence (quoted output / run URL / archived round) | SHA |
| --- | --- | --- |
| Stage-2 repo Codex review — CLOSING round | Round 8, run dir `cdx-review.GY2uVx`, `STATUS: completed`, teardown `confirmed stopped`, **no findings**: "The restored staleness check correctly detects exemptions that become fully served for their producer while preserving path scoping. The targeted test suite passes, and the evidence checksum matches the updated document." A clean round carries no finding row, so it is cited HERE — `test_every_completed_run_is_cited_by_its_ledger_outside_the_frozen_legacy_set` failed the wave gate on exactly that gap, which is the check earning its keep: an archived round nothing cites is an archived round nobody can place. | `93f81df5204dabfb8e9be9fe4385b197f64cb5b5` |
| §6 architect implementation review — evaluation 1 | Run dir `cdx-gate-review.7krqqr`, `parsedVerdict: ISSUES FOUND`, daemon `stopped:true`, archived under `architect-reviews/`. Nine findings (6×P2, 3×P3), all dispositioned `fixed` in the rows above. Its delta-scoped replay is OWED and is recorded as outstanding below. | `3c0e6eb46a999beb967f7c2a5203c170bcf1b1ab` |
