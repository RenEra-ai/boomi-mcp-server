# Audit ledger — issue #157 (M12.19 Authoring envelope and governance completion, T9 remainder)

Audit record for the completion workflow (`CLAUDE.md`, amended 2026-08-12 / -08-14; standing
rules in `docs/architecture/COMPLETION_WORKFLOW_RULES.md`).

Plan of record: `.codex/plans/issue-157.md` — the attested Codex architect plan, attestation
`.codex/plans/issue-157.attest.json` (message sha256
`e7eb5f9ba24ff8b425fc1f2e18b04ab59860177329402213b4234c6427dd557e`, thread
`01a07e4a-088f-7b40-be35-9e6d1e301d5e`, turn token 2, gate `architect`, collected
`ok:true`/`stopped:true`, teardown `confirmed`). Where the plan and this ledger disagree, the
correction is recorded here with its evidence and the plan is NOT silently re-authored.

## Stage-1 step 0 — baseline

| Field | Value |
| --- | --- |
| Issue | #157 — M12.19 Authoring envelope and governance completion (T9 remainder) |
| Step-0 baseline (`$BASELINE`) | `ba1be9f34317d720d3b012484622ab7e2742143b` |
| Branch | `codex/issue-157` |
| Branch point | `dev` @ `ba1be9f` (closing commit of #156) |
| Baseline suite | 11655 passed, 19 skipped (11674 collected) — full non-KB, local `.venv` 3.12, `PYTHONPATH=src`, 954 s, run to completion BEFORE any edit, exit 0 |
| Baseline manifests | 11674 required nodes (`tests/fixtures/wave_gate/test_nodes.jsonl`), 76 active goldens (`tests/fixtures/wave_gate/goldens.jsonl`) |
| Pristine oracle tree | `git archive ba1be9f` extracted outside the repository (scratchpad) with the repo `.venv` symlinked in; every legacy served-flows row this slice freezes is emitted from THAT extraction, never from the working tree |
| Slice kind | behaviour-affecting (new governance defaults, new validation, served-contract changes, retirements) |

The collected count 11674 equals the recorded floor, so the floor is current on the branch point.

### Environment correction recorded at step 0

The first architect plan turn (turn token 1, primary brief) parked on an MCP tool-approval
elicitation: Codex asked for permission to run the read-only boomi `manage_folders` tool and the
headless driver cannot answer that request shape. The codex-architect helper gracefully
interrupted that turn and re-asked ONCE with the pre-hashed retry prompt; the retry (turn token 2)
completed and is the certifying turn. The collector verified the primary brief ran on the same
session and attested the round `ok:true`. No repo file was involved; recorded because a wedged
first turn must be visible in the audit record rather than inferred from the surviving one. A
throwaway Codex home directory with no MCP servers and no plugins was prepared as the fallback for
later gate sessions.

## Loop roster (enumerated in advance, before the first correction)

Per `CLAUDE.md` the roster is fixed before any correction is applied. A gate not on this list cannot
mint a loop mid-run; adding one is itself a recorded checkpoint decision.

| # | Logical loop | Authority | Scope |
| --- | --- | --- | --- |
| 1 | Stage-1 QA | `boomi-qa-tester`, live through the public MCP tool boundary | the slice's affected scenarios: the plan §13 naming/folder governance scenario (create with governance → readback → update-preservation → cleanup) for both compiling intents, the multi-root control, and the negative probes for the new named diagnostics |
| 2 | Stage-2 repo commit review | detached Codex review per `CLAUDE.md` §5b–5e | initial: `--base $BASELINE`; then each fix delta |
| 3 | Architect implementation review | `/codex-issue` §6 gate (`--gate review`) | implementation vs `.codex/plans/issue-157.md`; **capped at 3 evaluations** by `docs/architecture/COMPLETION_WORKFLOW_RULES.md` |
| 4 | Composite wave gate | `scripts/wave_gate.py` | full suite, golden manifest, determinism, wave-delta review, one live scenario per changed capability class |
| 5 | Terminal correction loop | only if a final non-blocking batch mutates the tree | that batch |

Pre-implementation legacy-flows oracle capture (architect plan §1) is **evidence provisioning**,
not a QA evaluation: it runs from the pristine extraction of the baseline tree with no source
change and debits no loop.

## Shipped contracts this slice builds on (#153-#156), consolidated

Plan item 1 asks for this consolidation explicitly, and both architect evaluations recorded that
they could not find it. It is the list of contracts already SHIPPED and landed on `dev` that this
slice extends rather than re-decides, each with the in-tree authority that owns it. Nothing here is
new work; the value is that a reviewer can check a claim of the form "that was settled in #15x"
against one place.

| Shipped in | The contract this slice builds on | Owning authority in the tree | Its own audit record |
| --- | --- | --- | --- |
| #153 (M12.15) | Canonical process materialization and DIRECT apply: a `ProcessAuthoringUnitV1` is compiled by the canonical chain and materialized by the neutral materializer, resolving no legacy `process_kind` | `src/boomi_mcp/categories/components/canonical_process_apply.py`, `src/boomi_mcp/compiler/process_ir/pipeline.py` | `docs/architecture/ISSUE_153_AUDIT_LEDGER.md` |
| #153 (M12.15) | The single key namespace: `integration_spec.components` and `integration_spec.processes` share one namespace and one topological order across all four `depends_on` directions | `src/boomi_mcp/models/integration_models.py` | `docs/architecture/ISSUE_153_AUDIT_LEDGER.md` |
| #154 (M12.16) | The grammar and the typed EFFECT-declaration channel, resolved server-side: identity is verified against the resolved components and effect CONTENT is derived, never taken from the declaration | `src/boomi_mcp/authoring/workflow.py`, `src/boomi_mcp/recipes/engine.py` | `docs/architecture/ISSUE_154_AUDIT_LEDGER.md` |
| #155 | Dynamic-path authoring and its verb captures, read from the counterparty log rather than from the platform's own status | `src/boomi_mcp/compiler/process_ir/` | `docs/architecture/ISSUE_155_AUDIT_LEDGER.md` |
| #156 (M12.18) | Notify and recovery subprocesses, and the wrapper-subprocess adapter this slice's ownership walk stops at | `src/boomi_mcp/compiler/process_ir/legacy_adapters/wrapper_subprocess.py` | `docs/architecture/ISSUE_156_AUDIT_LEDGER.md` |
| #146 (M12.11), carried through all of the above | The typed MCP authoring surface: `AuthoringRequestV1`, the per-intent models, and the served diagnostic shape this slice adds codes to | `src/boomi_mcp/models/authoring_workflow.py` | — |

**The #153 handoff**, which the plan names separately, is the tracked handoff document beside that
slice's ledger in `docs/architecture/`.
It is a resume record, and the two things it hands forward that bind THIS slice are: that
the §6 architect review asks a different question from a commit review — *does the implementation
build what the plan specified* — and that four of its ten findings were plan items never implemented
at all, which no code-quality review surfaces. Both proved true again here: architect evaluation 1
raised seven conformance findings against a tree eight commit-review rounds had already cleared, and
evaluation 2 raised five more against the corrections.

## Plan corrections (recorded before the first correction is applied)

Claude's implementation plan is `.codex/plans/issue-157.claude.md`. It verifies the architect plan
against the tree at `ba1be9f`; the corrections below OVERRIDE the architect plan. Each is
re-verified in the main thread before adoption; the architect plan is not silently re-authored.

| # | Architect said | Actually | Evidence (re-verified) |
| --- | --- | --- | --- |
| P1 | Register the new flows refusal "through the existing taxonomy" under the authoring-surface prefix | **Impossible without breaking a guard.** The authoring-surface code family is closed to #146 by a biconditional test (every key with that prefix is owned by #146, and #153's guard asserts it introduces none). New codes go in a NEW governance family (every member spelled like `GOVERNANCE_NAME_UNRESOLVED`), sole introducer #157, guarded by the same biconditional. | `src/boomi_mcp/errors.py` family charter comment above the authoring codes; `tests/test_error_taxonomy.py` (the #146 biconditional and the #153 prefix exclusion) |
| P2 | Touch the compiler's semantic-validation findings table for the coverage gate and the contract projection | **Do not.** The required-target gate needs component configs and profile indexes, which the compiler layer is forbidden to depend on; the findings table is a static compiler-layer code-to-text map, and the contract projection merges only parse, finding and compiler specs. The coverage code is a builder-layer code and travels as a cause code under the authoring blocked code (direct arm) and the recipe constraint code (recipe arm), exactly as every builder code already does. | `src/boomi_mcp/compiler/process_ir/semantic_validation/findings.py` header; `src/boomi_mcp/authoring/process_ir_projection.py` entry merge; `integration_builder.py` named-validation-code map |
| P3 | Regenerate the #149 inventory with the emitter's write flag, produce markdown with its emit flag, and replace tables by subsection heading by hand | **Use the wrapper, never hand-splice.** `scripts/rebaseline_m12_12_inventory.py` wraps the write AND the §11.2–11.6 table splice by header-row match, and its docstring records three ways the hand procedure has already gone wrong. | `scripts/rebaseline_m12_12_inventory.py` lines 1–48 |
| P4 | Introduce a distinct canonical output-preview model carrying typed flows, and keep the projection out of fingerprint authority | Both right but under-specified: `plan_fingerprint` hashes the served preview, so a naively typed preview rotates every compiling-intent plan hash. Decision: the hash input stays the pre-projection spec (flows empty, byte-identical to today) and the SERVED preview is a subclass of the spec model overriding only `flows` and adding a preview-kind discriminator, so no field list is hand-copied. | `src/boomi_mcp/authoring/workflow.py` plan-hash call (`integration_spec_preview=spec_preview.model_dump(...)`) |
| P5 | Compare the entire derived sequence with the frozen sequence after retired rows are removed, AND discharge only the two rich forms in this slice | These conflict unless each frozen row carries a third state: every case's derived output here is transform rows only, so ordered two-sided equality against "frozen minus retired" fails on every case. Decision: each frozen row has exactly one status in a committed discharge ledger — discharged, retired (with record id), or pending for #159 — the sequence oracle filters the frozen sequence to discharged rows, a derived row matching a pending or retired identity is an UNEXPECTED row, and the pending count is pinned so #159 cannot shrink it silently. | issue body in-scope item 6; architect step 8 |
| P6 | Rich row payloads must be preserved in full, including the operations summary | The legacy operations summary round-trips a map-script body VERBATIM, and ADR-001 §11 forbids script bodies on served results. Decision: the ONE normalizer replaces the body with its sha256 digest and keeps the presence flag — content equality preserved, body never served; recorded as representation reconciliation, not content discard. | `src/boomi_mcp/patterns/archetypes/database_to_api_sync.py` operation-summary loop (the `script_body` round-trip with its "data-loss" comment) |
| P7 | The legacy watermark consistency rule lives at two sites | Confirmed, and there are TWO rules to carry, not one: a watermark-sourced query parameter requires a watermark declaration, and the watermark field must be a declared source field. Both become one governance watermark code with distinct paths. | `database_to_api_sync.py` the two model validators on the archetype parameters |
| P8 | The REST connection builder's supported auth modes are the authority for "secured" | Confirmed; the canonical REST create gate imports the archetype assembly's create-auth map and derives "secured" as the builder's recognized modes minus the unauthenticated one — never a hand list. | `categories/components/builders/connector_builder.py` (the REST builder's two mode tuples); `patterns/archetype_assembly.py` (the create-auth map) |
| P9 | Seven producer modules write the legacy flows list | Confirmed at the baseline: five sync/listener archetypes, the stub archetype, and composition, which APPENDS a fan-out row to the DB archetype's list (so composition cases freeze the DB rows plus the fan-out row). The reliability row's target toggles on the DLQ flag; the watermark row is the only conditional row. Measured from the pristine extraction: 19 cases, byte-deterministic across two runs. | the capture script in the session scratchpad run against the `ba1be9f` extraction; the seven `flows=` sites |

## Design decisions recorded during implementation

Decisions the plans left open or that the tree forced, each re-verifiable against the code. None
re-authors the architect plan; where a decision narrows a plan item the reason is stated.

| # | Decision | Why | Where |
| --- | --- | --- | --- |
| D1 | The reuse-XOR-create rule is scoped to CONNECTIONS (`connector-settings`), and a reuse binding may carry only the closed key set the two reuse authorities read (`reference_only`, `component_id`, `component_name`, `connector_type`). Operations keep their #155 declarations (`method`, `operation_mode`) on a reuse binding. | The issue names the CONNECTION binding contract; the archetype binding models are per-connection; and #155's identity projection asserts the declared FAMILY of a reused connection, which is exactly the fourth key. The existing #155 fixtures that copied a full create config onto a reuse binding were the silent-precedence case the contract refuses, and were rewritten to a shared `reuse_binding` helper (same node ids). | `authoring/governance.py` (the closed key set beside its rationale); `tests/_m12_11_support.py` |
| D2 | Connection `default_headers` on the typed surface are MERGED into every dependent operation's request headers by the archetype assembly's own merger in strict mode, and the consumed key is removed from the connection config. | The canonical REST connection builder never read `default_headers` (measured: no reader in the builder module), so on the typed surface the key was accepted-but-inert — the class this issue removes. Merging is the transfer of the #151 assembly authority the issue requires; strict mode is the collision refusal. Legacy archetype assembly keeps operation-wins. | `patterns/archetype_assembly.py` (`strict=`), `authoring/governance.py` |
| D3 | The required-target-leaf coverage HARD GATE is invoked by the typed semantic validation and by the recipe engine; it is NOT wired into the legacy component-plan lint or the legacy apply refresh. | The legacy lint is frozen legacy surface until #160 and its consumers include every legacy spec with a partially-mapped target profile; gating it would change a surface the issue leaves to #160. The advisory review consumes the same implementation. The one implementation and the six replaced sites are what the structural-fix rule asks for. | `authoring/workflow.py` (`_validate_processes`), `recipes/engine.py` (`_validate_component_maps`), `transform_map_validation.py` |
| D4 | A REUSED profile's in-spec config is never indexed; its index comes from the SELECTED existing component (by id, or by name when exactly one matches) read live, on the plan lint, the apply refresh and the typed gate alike. Offline it is unavailable. | The handoff's "use the artifact actually selected for reuse; candidate config does not establish what a reused component contains". | `integration_builder.py` (the selected-profile resolver beside the literal one), `transform_map_validation.py` |
| D5 | Retired legacy spellings are refused by NAME on the typed envelopes only; the legacy archetype parameter models keep accepting them until #160's cutover. | The issue: "reject the spellings after cutover" and "no legacy watermark/scheduling metadata spelling is accepted anywhere on the TYPED surface". Removing the fields from the legacy archetype contract now would break the frozen legacy producers this slice's own flows baseline re-derives at HEAD. | `models/governance_intent.py`, the envelope models |
| D6 | The `build_from_archetype` / `compose_archetypes` responses do NOT gain an always-empty recorded-intent field. | A served field nothing fills is a registered-but-unreachable promise (the QA-153-r1-03 class). The channel is served where it is produced: the typed plan/compile results and the build provenance. | `models/authoring_workflow.py` |
| D7 | The typed derived-flows projection is TOTAL over every map the map builder accepts: a map function that writes a process property carries no target leaf, so `target_path` is optional on the canonical side. | Found by the full suite: the #180 `map_declared_effect` golden plans a `dynamic_process_property_set` function mapping, and the first projection raised out of `plan` for it — a served summary must never make a valid plan fail. | `models/derived_flows.py` |
| D8 | Placement reaches the wire at the ONE raw-create transport boundary, never in a builder: the resolved folder id is injected into the component document immediately before the create call, and every builder keeps emitting the spelling it always did. | Every builder emits a spelling this platform ignores on create, so teaching each of them the honoured one is a hand-model of the same fact per builder and moves every frozen builder golden. The transport is the single point every builder path already funnels through, so a builder added later is placed by construction. The injection is late-bound at apply, so no plan fingerprint, compile hash or golden can see it. | `categories/components/_shared.py` (the injector beside the raw create); `categories/integration_builder.py` (the canonical root's create) |
| D9 | A created component's folder NAME is resolved only on an apply carrying canonical process roots, and then BEST-EFFORT — one match places it, zero or several leave it unplaced and reported. Only the canonical process root refuses an unresolvable or ambiguous folder before any write. A literal `folder_id` places on every route and reads nothing. | Every legacy builder defaults this key to a folder name that need not exist on a given account, and legacy specs have always passed free-form names with silent root placement; a pre-write refusal there would break shipped behaviour far outside this slice. Under the fan-out the value IS the root's own name, which the root already resolved or refused against the same account snapshot, so a governed create is placed or the apply never started. | `categories/integration_builder.py` (the two resolvers over one shared matcher); the route boundary is the same one decision D3 drew for the coverage gate, and it also keeps every legacy apply from reading the account's whole folder list — see SELF-157-r2-01 |
| D10 | The required-target-coverage diagnostic serves the COUNT of unbound leaves and names `review_transformation` as the surface that lists the paths; it never serves the paths. | The authoring diagnostic model has no details field and its evidence allowlist is closed to structural tokens and codes — deliberately, because a target leaf path is an authored profile value and this surface is value-free by construction. Widening the compiler's closed evidence vocabulary for an authoring finding would be the wrong contract change. | `authoring/workflow.py` (the coverage diagnostic) |
| D11 | An authored `config.folder_id` now places on every create path. | The folderless-create lint has always accepted `folder_id` as proof of placement while no builder emitted it, so a create declaring it was reported as organized and landed at the account root. Making the lint's two accepted spellings both real is the same correction as the fan-out's, one layer out. | `categories/components/manage_component.py`, `categories/components/connectors.py` |

| D12 | An update binding whose smart merge would change nothing is recorded as a BIND (`reused`), decided before the mutation loop, rather than dispatched and refused inside it. | The answer is fixed by the request, and this file already hoists exactly those answers — a mid-loop refusal charges the caller a partial mutation for an answer we always had. `reused` is not a new semantic: it is the status this apply already gives a component it bound without writing, and it is what a `reference_only` binding of the same component produces. A pre-write REFUSAL was rejected because it would abort a whole governed apply over one binding that authors nothing. | `categories/integration_builder.py` (the pre-write bind), `categories/components/_shared.py` (the merge-field authority) |
| D13 | A component declaring EITHER placement spelling declares its own placement, and the fan-out claims neither for it. | A row assembled from two authorities — the inherited name and the declared id — reported a placement the component did not have. Both keys are placements; asking one predicate that knows both is what keeps the claim and the resolver from disagreeing. | `authoring/governance.py` |
| D14 | The placement resolver returns ONE answer with two halves — the folder id, and the spelling that drove it — and every consumer reads that return rather than re-reading the component config. | Two sites reading the same config for the same question is how a served row came to name the folder the request lost beside the id of the folder it won. An authored id wins, so an authored name beside it drove nothing and is reported nowhere. | `categories/integration_builder.py` (the resolver, the result row, the warning builder) |
| D15 | A create route that cannot submit a placement declares that at its own arm; the row attests no submission it did not make, and the warning explains the limit as a limit of this server. | The trading-partner create posts a typed model through its own SDK entry point and never reaches the raw-create boundary. Modelling which routes can place, anywhere but at the routes themselves, is the hand-model this slice keeps removing — and the served consequence of getting it wrong is a caller sent to the platform for a limit of this tool. | `categories/integration_builder.py` (the trading-partner arm and the warning builder) |
| D16 | The resolved placement travels as ONE value carrying both halves — the folder id and the name that drove it — and the dispatcher seam takes that value rather than a bare id. | Two consumers had already read one half and drawn a wrong conclusion. Making both halves one ANSWER was not enough while the seam still handed out the id alone; making the half-answer unrepresentable is what closes the class. | `categories/integration_builder.py` (`_ComponentPlacement`, the dispatcher, the row, the warning) |
| D17 | A create route REPORTS the placement it submitted; nothing predicts it. The served row is built from that report, and the resolver carries the declared folder name whatever else the request declares. | Four corrections predicted this one route from outside and each was defeated by the same asymmetry: it places by NAME and cannot place by id, the inverse of every route that submits a component document. A prediction of another site's capability is a hand-model of a fact that site owns. | `categories/integration_builder.py` (`_ComponentPlacement`, the trading-partner arm, the row) |
| D18 | The served placement row carries only facts a site OWNS — what the request asked for, what a route reported submitting, what the read-back observed — and no token beside them saying how to read them. The fan-out asks each builder whether it can be handed a folder name. | An invented provenance token was synthesised for every row no arm reported one for, and its single overloaded value regressed a mechanism that had been correct for four rounds. Presence of an owned field is a discriminator that cannot be synthesised. | `categories/integration_builder.py`, `categories/components/builders/__init__.py` (`accepts_folder_name`), `authoring/governance.py` |
| D19 | A builder that enforces a closed top-level key set accepts `folder_name` as component metadata and emits nothing for it. Nothing between the fan-out and the wire decides whether a component can be placed. | Placement is an argument to the create, injected as a folder id at the one raw-create boundary — a boundary built so the strict builders never see it. The fact that they must tolerate the key the fan-out writes belongs in the builders' own declarations, not in a predicate asking them a question about a different subject. | the five closed-key builder modules; `authoring/governance.py` |
## Retirement measurements (plan §4 triage, in-scope items 8–10)

Every candidate was measured on the LEGACY chain from the pristine `ba1be9f` extraction, fully
offline (the create boundary mocked to capture the exact bytes the legacy apply loop would send): a
pair of archetype payloads differing ONLY in the candidate field, compared on every component's
emitted XML, the process XML, and the plan verdict with the spec echo stripped, with a non-vacuity
control (`batch_size`, a WIRED field) moving a digest under the same harness. The measurement was
first taken by ten independent measurement agents (six completed before the subagent session
limit; four were re-measured in the main thread with the same harness), then re-run in the main
thread, and finally frozen by the in-tree harness `tests/_issue_157_retirements.py --freeze` into
`tests/fixtures/governance/issue_157/retirements/` — every record carries the pair, the digests,
the echo paths and the control. `--check` replays every pair against HEAD until #160.

| Spelling | Record | Measured | Decision | Replacement on the typed surface |
| --- | --- | --- | --- | --- |
| `jdbc_options` | RET-157-01 | inert (all bytes and verdict equal; echo only) | RETIRE | none — creation settings live on the connection component |
| `fetch_size` | RET-157-02 | inert | RETIRE | none |
| `link_element` | RET-157-03 | inert | RETIRE | none |
| `sql_type` | RET-157-04 | inert | RETIRE | none |
| `direction` | RET-157-05 | **NOT inert** — sets the read profile parameter's mappable flag and moves the read-profile XML | RETAIN | kept as is on the legacy contract; never on the typed surface |
| `cron` (scheduled trigger) | RET-157-06 | inert | RETIRE | none — schedule activation is a future topology capability |
| `run_metadata` | RET-157-07 | inert | RETIRE | recorded runtime hint |
| `value_source=watermark` query parameter | RET-157-08 | inert (an additive spelling that emits nothing) | RETIRE | the typed watermark declaration's query-parameter references (recorded, not wired) |
| `runtime_hints` | RET-157-09 | inert (echoed verbatim under the spec's runtime block) | RETIRE | recorded runtime hint |
| the legacy watermark object (`dpp_name`, `store_ref`, `persistence=dpp`) | RET-157-10 | inert (metadata only, as the issue says) | **SPLIT** | the typed watermark declaration is the sole accepted spelling; persistence stays executable through the canonical persisted-property node |

The retired-spelling authority in `models/governance_intent.py` maps every typed-surface key to its
record, and a test pins the mapping against the record index in both directions. The legacy
archetype contracts keep accepting these spellings until #160's cutover (decision D5); on the
typed surface each is refused by name.

## Findings ledger

One row per raw finding. Columns: source ID · verbatim summary · source gate / run dir /
attestation · original severity label · blocking class · defect class · derived tier + anchor ·
affected SHA/delta · disposition.

| ID | Summary (verbatim) | Source / run dir | Orig. label | Blocking class | Defect class | Tier (anchor) | Affected SHA | Disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SELF-157-r1-01 | "1 validation error for DerivedTransformFlowV1 — operations.0.map_function.target_path: Input should be a valid string" raised out of `plan` for the #180 `map_declared_effect` golden and four wave-gate golden tests. | self-review: the first full non-KB suite run over the uncommitted tree (24 failures, scratchpad `suite-run1.log`) | n/a (self) | runtime behavior | served-projection-narrower-than-its-authority | **Standard** (no critical anchor) | uncommitted | `fixed` — the projection's function-mapping summary required a target leaf, but the map builder accepts a `dynamic_process_property_set` mapping that targets a property. `target_path` is optional on the canonical side (decision D7) and the derived-flows tests carry a property-writing mapping. |
| SELF-157-r1-02 | Nine #155 fixtures copied the shared appliable connection fixture's full CREATE config onto a `reference_only` connection and were refused by the new binding contract. | same suite run | n/a (self) | runtime behavior | silent-precedence-fixture | **Standard** | uncommitted | `fixed` — the fixtures were the silent-precedence case the issue's contract refuses; rewritten through one `reuse_binding` helper carrying binding keys only (node ids unchanged). Recorded as a contract change, not a regression: decision D1. |
| QA-157-r1-01 | "the ONE-knob folder fan-out places nothing — root honestly attested, the 7 owned components silently at the account root" | Stage-1 QA r1 (`boomi-qa-tester`, live via the public MCP boundary; report `agents/reports/2026-09-09-issue-157-r1.md`; freeze stamp `ba1be9f34317/dirty:73d99998d023/8f7c877c3c83`, 48 components created and 48 deleted, account net zero) | High | runtime behavior (+ machine-served contract text) | placement-spelling-the-platform-ignores | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — placement now reaches the wire as the spelling the platform honours, injected once at the raw-create boundary for every builder path (decision D8), resolved live at apply against one account snapshot (D9), and bound onto the canonical root's create before its submitted-bytes digest. Every CREATED component now carries the placement attestation and the warning the root has carried since #153, judged by one shared read-back rule and worded by one shared builder. The served governance text now scopes the claim and says placement is verified per component. |
| QA-157-r1-02 | "an existing connection bound by action:'update' + component_id (no authored name) is RENAMED to '<prefix> <key>' by the fan-out" | Stage-1 QA r1 (`boomi-qa-tester`, live via the public MCP boundary; report `agents/reports/2026-09-09-issue-157-r1.md`; freeze stamp `ba1be9f34317/dirty:73d99998d023/8f7c877c3c83`, 48 components created and 48 deleted, account net zero) | High | apply/update preservation | claim-scoped-by-negation | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — the ownership walk skipped only the reuse mode, so the third materialization mode was claimed. Governance now asks the one authority for the mode and claims a derived name and an inherited folder ONLY for what apply will create; the ambiguity refusals follow the claim, because nothing claimed is nothing to disagree about. |
| QA-157-r1-03 | "the coverage diagnostic promises 'the count and the paths travel in the cause diagnostic' but serves neither" | Stage-1 QA r1 (`boomi-qa-tester`, live via the public MCP boundary; report `agents/reports/2026-09-09-issue-157-r1.md`; freeze stamp `ba1be9f34317/dirty:73d99998d023/8f7c877c3c83`, 48 components created and 48 deleted, account net zero) | Medium | machine-served schemas/contracts | served-text-promising-an-absent-field | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `fixed` — the count is served in the message and the remediation names the surface that lists the paths; the paths stay off this surface because they are authored values (decision D10). Pinned on the committed replay fixture with a canary leaf name that must appear nowhere in the served row, and with a two-gap case proving the count is derived. |
| QA-157-r1-04 | "GOVERNANCE_FLOWS_OUTPUT_ONLY location stops at the intent" | Stage-1 QA r1 (`boomi-qa-tester`, live via the public MCP boundary; report `agents/reports/2026-09-09-issue-157-r1.md`; freeze stamp `ba1be9f34317/dirty:73d99998d023/8f7c877c3c83`, 48 components created and 48 deleted, account net zero) | Low | non-blocking | validator-location-coarser-than-its-rule | **Standard** — non-blocking, folded into this batch | uncommitted over `ba1be9f` | `fixed` — a model-level before-validator has no field in its location, so the served location now carries the key the refusal names, from one map keyed on the same custom-error type as the served-code map and pinned as a subset of it. |
| SELF-157-r2-01 | `tests/test_integration_builder.py` went from 0.5 s to a multi-gigabyte hang: the QA-157-r1-01 correction resolved a folder NAME on every apply route, and the folder pager follows whatever page token the counterparty returns — a client that never clears it loops forever while the call recorder grows without bound. | self-review: the full non-KB suite over the corrected tree (three runs killed for memory before the cause was isolated to one file, then to one call) | n/a (self) | runtime behavior | unbounded-loop-on-a-counterparty-controlled-token | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `fixed` — the name resolution is scoped to applies carrying canonical process roots, which is where #157 governs placement and where the root already resolves a name against the account; a literal `folder_id` still places on every route and needs no account read. The legacy route therefore reaches the pager exactly as often as before this slice: never. Pinned by a test that hands the resolver a loader which raises if the legacy route ever calls it. The pager's own unbounded loop is pre-existing and repo-wide (about thirty `query_more_*` loops share the shape); it is left to #160 rather than instance-patched here, and this correction removes the new reachability that exposed it. |
| QA-157-r2-01 | "the r1-02 fix removes the derived name, which was the only field an `action:"update"` + `component_id` binding carried; the legacy smart-merge now refuses \"No updatable fields provided in config\" inside the mutation loop" — measured as a genuine partial apply, one component created beside the failed binding step, plan and compile clean, `error_code: null` | Stage-1 QA r2 (`boomi-qa-tester`, live fix-delta re-run; report `agents/reports/2026-09-09-issue-157-r2.md`; freeze `ba1be9f34317/dirty:3ab624dc2a61`, PRE==POST==EXIT on all 15 runs, 81 components and 10 folders created and deleted, net zero) | High | runtime behavior (+ mutation accounting: a partial apply) | request-decidable-answer-decided-mid-mutation | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — an update whose smart merge would change nothing is a BIND, and the answer is fixed by the request, so it is decided before the loop can write anything. The step is recorded `reused`, the status this apply already uses for "bound, not written", and the authored action is never rewritten. Both predicates are the dispatcher's own — the route test it uses to choose the smart merge, and the merge's own field list, now named once and pinned to both merge sites in both directions. Controls: a description, a rename, a folder name and a body field each still dispatch. |
| QA-157-r2-02 | "the fan-out claims the root's `folder_name` for any owned component whose config lacks one; an explicit `folder_id` does not stop the claim" — the served row named folder A, carried folder C's id, and reported the placement verified | Stage-1 QA r2 (`boomi-qa-tester`, live fix-delta re-run; report `agents/reports/2026-09-09-issue-157-r2.md`; freeze `ba1be9f34317/dirty:3ab624dc2a61`, PRE==POST==EXIT on all 15 runs, 81 components and 10 folders created and deleted, net zero) | High | mutation accounting | guard-modelling-one-spelling-of-a-two-spelling-fact | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — at the CLAIM, as the finding directs: both keys are placements, so either stops the fan-out, and the warning builder reading the same row is fixed with it. Sibling sweep: the typed envelope deliberately has NO `folder_id`, so the root resolver's single-spelling read is its whole authority; the folderless-create lint already reads both. |
| QA-157-r2-03 | "the remediation says 'this envelope is value-free and never echoes them'; the plan envelope echoes the target leaf path 20x via the new derived-flows projection" | Stage-1 QA r2 (`boomi-qa-tester`, live fix-delta re-run; report `agents/reports/2026-09-09-issue-157-r2.md`; freeze `ba1be9f34317/dirty:3ab624dc2a61`, PRE==POST==EXIT on all 15 runs, 81 components and 10 folders created and deleted, net zero) | Low | machine-served schemas/contracts | served-text-promising-an-absent-field | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `fixed` — the false claim is withdrawn rather than reworded: the diagnostic now says only that it does not name WHICH leaves are unbound and that `review_transformation` does. SECOND INSTANCE of this class, so the structural answer ships with it (below). |
| QA-157-r3-01 | "the r2-02 contradiction class survives one input over: when a component authors BOTH `config.folder_name` and `config.folder_id` for different folders, the id wins the placement but `requested_folder_name` still reports the losing name, beside the winning `resolved_folder_id`, with `placement_verified: true`" — both directions measured, seven correct sibling rows in the same applies as the in-run control | Stage-1 QA r3 (`boomi-qa-tester`, live acceptance re-run; report `agents/reports/2026-09-09-issue-157-r3.md`; 75 components and 3 folders created and deleted, closing sweep over six types finds 0 leftovers) | High | mutation accounting | guard-modelling-one-spelling-of-a-two-spelling-fact | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — SECOND INSTANCE of the class, so the invariant ships instead of a second patch: the placement resolver now returns BOTH halves of one answer, the id and the spelling that drove it, and every consumer reads that return instead of re-reading the config. A row can no longer be assembled from two authorities because there is only one. The losing spelling is reported nowhere, and the warning names the id when an id drove the placement. |
| QA-157-r4-01 | "the id-driven warning names the id correctly, but the sentence attached to it is false for the only case that can reach it" — the trading-partner create posts a typed model and never reaches the boundary that places a document, so the id was never submitted, yet the caller was told the read-back did not confirm a submitted id | Stage-1 QA r4 (`boomi-qa-tester`, narrow acceptance; report `agents/reports/2026-09-09-issue-157-r4.md`; 32 components and 4 folders created and deleted, closing sweep over seven types `net_zero: true`) | Medium | machine-served schemas/contracts | enumeration-short-by-one-mode | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `fixed` — the arm that cannot place declares its own limit, the row attests no submission it did not make, and the warning gained the fourth mechanism. The mechanism enumeration is a model like any other and this one was short by one; the correction also narrows this slice's own coverage claim, which said every create is placed by construction — true only of every create that submits a component DOCUMENT, and the one that does not is now named in the test that makes the claim. |
| QA-157-r5-01 | "three spellings of the same route in one apply": an authored `folder_id` is NOT placed while an authored or inherited `folder_name` IS — the route's own contract takes a folder name and submits it — yet all three rows declared the route cannot place, so two of them announced a limit their own `placement_verified: true` refuted; `resolved_folder_id` was suppressed on rows that were placed, and the verification fell back from an identity comparison to a leaf-NAME one | Stage-1 QA r5 (`boomi-qa-tester`, narrow acceptance; report `agents/reports/2026-09-09-issue-157-r5.md`; 23 components and 2 QA folders created and deleted, the platform-created `Home` folder removed explicitly, final listing exactly root plus Process Library) | High | mutation accounting | guard-modelling-one-spelling-of-a-two-spelling-fact | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — THIRD instance of the class, and the invariant is strengthened rather than re-applied: the resolver's answer is now ONE value carrying both halves, so a consumer that takes only the id is unrepresentable at the seam. The route's limit is the ID, not placement, and it declares exactly that. |
| QA-157-r5-02 | "with `resolved_folder_id` suppressed, the warning's request slot falls through to None — 'Component tp_id requested folder None'" | Stage-1 QA r5 (`boomi-qa-tester`, narrow acceptance; report `agents/reports/2026-09-09-issue-157-r5.md`; 23 components and 2 QA folders created and deleted, the platform-created `Home` folder removed explicitly, final listing exactly root plus Process Library) | Medium | machine-served schemas/contracts | enumeration-short-by-one-mode | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `fixed` — the id a route could not submit is kept under a name that claims no submission, and the notice names it. |
| QA-157-r6-01 | "a `trading_partner` authoring BOTH `folder_id` and `folder_name` is placed by the NAME — the component's whole config is splatted into the create, which takes `folder_name` — while the placement value gives the id the win and reports no name, so the arm declares the limit, drops the request, reports `placement_verified: false` and warns 'so the component was NOT placed there' for a component that is exactly there"; the row even carried `observed_folder_id == unsubmitted_folder_id`, with a name-only sibling verified true in the same apply | Stage-1 QA r6 (`boomi-qa-tester`, acceptance; report `agents/reports/2026-09-09-issue-157-r6.md`; 23 components and 2 folders created and deleted, the platform-created `Home` folder removed explicitly, final listing exactly root plus Process Library) | High | mutation accounting | guard-modelling-one-spelling-of-a-two-spelling-fact | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — FOURTH instance, and the prediction is removed rather than refined: the arm REPORTS the name it submitted, the row is built from that report, and the resolver carries the declared name instead of discarding it when an id also exists. The three earlier corrections all kept a prediction of what a route submits; this route's ranking is the inverse of the document routes' and no prediction survived it. |
| QA-157-r6-02 | "the declaration was narrowed; the sentence was not" — the warning still claimed the route 'does not submit a folder', refuted by two sibling rows of the same type placed by name in the same apply, and its remedy sent the caller to the folder tool when authoring a folder name would have placed it | Stage-1 QA r6 (`boomi-qa-tester`, acceptance; report `agents/reports/2026-09-09-issue-157-r6.md`; 23 components and 2 folders created and deleted, the platform-created `Home` folder removed explicitly, final listing exactly root plus Process Library) | Medium | machine-served schemas/contracts | enumeration-short-by-one-mode | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `fixed` — the sentence now names the ID as the limit and says the route submits a folder by name only. |
| QA-157-r7-01 | "the limit sentence's only remedy fails on the only route it fires for" — moving a trading partner through the folder tool is refused by the platform (`PartnerInfo is not complete`) while the same call on a document component in the same apply succeeds, and the remedy that does work (author `folder_name`) was not offered | Stage-1 QA r7 (`boomi-qa-tester`, adversarial acceptance; report `agents/reports/2026-09-09-issue-157-r7.md`; 31 components created and deleted, `net_zero: true`, `Home` and both route-materialised folders removed explicitly) | High | machine-served schemas/contracts | served-remedy-that-does-not-work | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — that branch now serves its own sentence, naming the remedy that works on this route and saying the component cannot be moved afterwards. |
| QA-157-r7-02 | "the arm's report is folded into the same key the document route uses, so the warning cannot read it and models the spelling from id fields" — two id mechanisms fired live on a route that submits no id, one of them resurrecting a claim discharged in round 4 | Stage-1 QA r7 (`boomi-qa-tester`, adversarial acceptance; report `agents/reports/2026-09-09-issue-157-r7.md`; 31 components created and deleted, `net_zero: true`, `Home` and both route-materialised folders removed explicitly) | High | machine-served schemas/contracts | guard-modelling-one-spelling-of-a-two-spelling-fact | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — the row carries the PROVENANCE the route reported (`placement_spelling`) under its own key, and the warning selects its mechanism from that rather than inferring it from which id fields exist. |
| QA-157-r7-03 | "the submitted name is derived from the resolved placement — stripped on one branch, verbatim on the other — while the route submits the config value verbatim, and the route's own default is reported nowhere" | Stage-1 QA r7 (`boomi-qa-tester`, adversarial acceptance; report `agents/reports/2026-09-09-issue-157-r7.md`; 31 components created and deleted, `net_zero: true`, `Home` and both route-materialised folders removed explicitly) | High | mutation accounting | report-rebuilt-instead-of-read | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — the arm READS what the route returned. The route has returned the folder it sent all along; the previous correction claimed to report it and rebuilt it instead. The guarding test's stub now answers the way the route does, including its default. |
| QA-157-r7-04 | "the folderless-create lint's claim that the component would land in the account root is refuted in the same apply" — the trading partner landed in `root/Home` while the equally folderless process root landed at the account root | Stage-1 QA r7 (`boomi-qa-tester`, adversarial acceptance; report `agents/reports/2026-09-09-issue-157-r7.md`; 31 components created and deleted, `net_zero: true`, `Home` and both route-materialised folders removed explicitly) | Medium | machine-served schemas/contracts | served-text-promising-an-absent-field | **Standard** — no critical anchor | uncommitted over `ba1be9f` (pre-existing text, first measured here) | `fixed` — the lint states the mechanism that is true of both: the request places it nowhere, so it lands wherever its create route defaults. |
| QA-157-r7-05 | "a governed apply materialises caller-named folders through the trading-partner name route, reports the placement verified, and records the folder nowhere" — two folders gained in one measured apply | Stage-1 QA r7 (`boomi-qa-tester`, adversarial acceptance; report `agents/reports/2026-09-09-issue-157-r7.md`; 31 components created and deleted, `net_zero: true`, `Home` and both route-materialised folders removed explicitly) | Medium | mutation accounting | unaccounted-side-effect-of-a-route | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `not-validated` — the folder creation is the route's own pre-existing behaviour, reachable before this slice by authoring `folder_name` on a trading partner, and this slice neither introduced it nor can account for it without owning that route's mutation record. What #157 DID introduce is the fan-out reaching that route, and the row is now honest about what was submitted. Recorded with its measurement rather than fixed; the disposition is stated here so the residue is not silent. |
| QA-157-r8-01 | "the id the route could not submit is never written: the clear runs before the guard that would record it, and both facts come from the same arm, so the guard is unreachable" — served `requested None` on the exact case the branch was written for, reproduced in two independent applies | Stage-1 QA r8 (`boomi-qa-tester`, acceptance plus a second adversarial pass; report `agents/reports/2026-09-09-issue-157-r8.md`; 29 components created and deleted, six provisioned folders and BOTH platform-materialised ones removed explicitly, `net_zero: true`) | High | machine-served schemas/contracts | guard-unreachable-from-its-own-branch | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — the id is recorded BEFORE anything clears it, and the pinning test now drives the real apply so the row is built by the loop rather than by hand. |
| QA-157-r8-02 | "a REGRESSION: the notice branches on the overloaded token before reading fields, so a document row whose folder name matched no single account folder serves `requested folder None` while holding `requested_folder_name` with the answer" — 3 of 3 such rows affected, a mechanism that had named the folder correctly in four earlier rounds | Stage-1 QA r8 (`boomi-qa-tester`, acceptance plus a second adversarial pass; report `agents/reports/2026-09-09-issue-157-r8.md`; 29 components created and deleted, six provisioned folders and BOTH platform-materialised ones removed explicitly, `net_zero: true`) | High | machine-served schemas/contracts | invented-token-beside-the-facts-it-describes | **Critical** — source label High | uncommitted over `ba1be9f` (regression introduced by the round-7 correction) | `fixed` — the token is DELETED. Each field is written by the one site that owns it and their presence is the whole discriminator; nothing synthesises a provenance value for rows no arm reported one for. |
| QA-157-r8-03 | "the fan-out makes five component types unplannable under a governed root that declares a folder" — their builders enforce a closed top-level key set taking `folder_path` and not `folder_name`, and the refusal names neither the key nor a usable remedy | Stage-1 QA r8 (`boomi-qa-tester`, acceptance plus a second adversarial pass; report `agents/reports/2026-09-09-issue-157-r8.md`; 29 components created and deleted, six provisioned folders and BOTH platform-materialised ones removed explicitly, `net_zero: true`) | Medium | capability reachability | guard-modelling-one-spelling-of-a-two-spelling-fact | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `fixed` — the fan-out ASKS the builder whether it can be handed a folder name, reading each closed-key builder's own declaration, and places only what can be placed. A component it cannot place keeps the placement it was authored with and is claimed for nothing. |
| QA-157-r8-04 | "the served capability catalog still describes the folderless-create lint as sending a component to the account root" | Stage-1 QA r8 (`boomi-qa-tester`, acceptance plus a second adversarial pass; report `agents/reports/2026-09-09-issue-157-r8.md`; 29 components created and deleted, six provisioned folders and BOTH platform-materialised ones removed explicitly, `net_zero: true`) | Medium | machine-served schemas/contracts | served-text-promising-an-absent-field | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `fixed` — the catalog text names the mechanism, matching the lint it describes. |
| QA-157-r9-01 | "a SEVENTH instance, and the site is the round-8 fix itself": it asks a builder whether its CONFIG VOCABULARY contains the key and reads no as "this component cannot be placed", but placement never travels through a builder's config — it is an argument to the create, injected at the one raw-create boundary, a boundary built that way precisely so the strict builders never see it. Live, three component types landed at the account root with an empty placement row under a governed root placed in a folder, while the platform's own document for one of them, resubmitted at the same boundary with a folder id, landed in that folder | Stage-1 QA r9 (`boomi-qa-tester`, acceptance and the checkpoint round; report `agents/reports/2026-09-09-issue-157-r9.md`; 36 components created and deleted across twelve types, seven folders removed plus the platform-materialised one, `net_zero: true`) | High | capability reachability | guard-modelling-one-spelling-of-a-two-spelling-fact | **Critical** — source label High | uncommitted over `ba1be9f` | `fixed` — the asking site is DELETED and the fact is moved where it belongs: the five builders that refused the key now accept it as component metadata beside the sibling folder key they already declare, and emit nothing for it (byte-identical output, asserted). The fan-out is unconditional again. |
| QA-157-r9-02 | "the folderless-create lint then fires on those very components telling the caller to set a resolved folder, a remediation the same tree refuses" — the same shape as QA-157-r7-01 one round earlier | Stage-1 QA r9 (`boomi-qa-tester`, acceptance and the checkpoint round; report `agents/reports/2026-09-09-issue-157-r9.md`; 36 components created and deleted across twelve types, seven folders removed plus the platform-materialised one, `net_zero: true`) | Medium | machine-served schemas/contracts | served-remedy-that-does-not-work | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `fixed` — by the row above: those components are placeable again, so the lint's advice is one the tree accepts. |
| QA-157-r9-03 | "the closed-key builder set is hand-listed and the guarding test iterates that hand list, so a sixth closed-key builder would reproduce the defect undetected" — swept at probe time and currently complete, 5 of 5 | Stage-1 QA r9 (`boomi-qa-tester`, acceptance and the checkpoint round; report `agents/reports/2026-09-09-issue-157-r9.md`; 36 components created and deleted across twelve types, seven folders removed plus the platform-materialised one, `net_zero: true`) | Low | non-blocking | hand-listed-enumeration-of-a-derivable-set | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `fixed` — the hand list is deleted with the predicate that used it, and the test now DERIVES every builder module declaring a closed key set by walking the package, asserting each accepts the key the fan-out writes and that the set is non-empty. |
| QA-157-r10-01 | "an EIGHTH instance, and the site is the other function that answers the same question": the predicate that decides whether a component already declares its own placement reads two spellings from the config, and a raw-XML component carries a third — a `folderId` on its own document, which reaches the wire. The fan-out therefore claims the root's folder and the create boundary replaces the caller's id. Measured in one apply, three arms differing only in spelling: the raw-XML arm asked for C and landed in A with no warning, while the config arm asked for C and landed in C | Stage-1 QA r10 (`boomi-qa-tester`, the decisive round; report `agents/reports/2026-09-09-issue-157-r10.md`; 38 components created and deleted, `net_zero: true`) | Medium | mutation accounting | guard-modelling-one-spelling-of-a-two-spelling-fact | **Standard** — no critical anchor; the gate graded it Medium because the row is truthful about where the component is and nothing is mis-accounted | uncommitted over `ba1be9f` | `deferred` — NOT corrected. The recorded fallback for a further instance of this class binds here, and the residue is genuinely a product question the gate stated fairly: "governance wins" is a defensible rule; what is not defensible is that the predicate's own docstring commits to the opposite and the sibling folderless-create lint exempts raw-XML creates on the opposite premise. Reason class: `blocked-by-mechanism` — the mechanism is an owner decision about which authority wins, not a defect with one correct fix. Placement: awaiting the owner's disposition on this slice; no follow-up issue is filed because filing is owner-authorised in this repository. |
| QA-157-r10-02 | "the correction added the key to the builders; the served create templates carry a hand-written second copy of that key set and did not follow" — the templates omit the placing spelling and offer only the one measured never to place | Stage-1 QA r10 (`boomi-qa-tester`, the decisive round; report `agents/reports/2026-09-09-issue-157-r10.md`; 38 components created and deleted, `net_zero: true`) | Medium | machine-served schemas/contracts | hand-copied-key-set-beside-its-authority | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `deferred` — same fallback, same reason class `blocked-by-mechanism`: the served templates are a second copy of a key set whose authority is each builder's own declaration, and replacing the copy with a projection is the structural fix this slice is no longer permitted to attempt under its own recorded terms. |
| QA-157-r10-03 | "the folderless-create lint's advice names a key that is still blocked at plan for the closed-key types, and the served refusal names neither the offending key nor the one that works" — the undischarged half of QA-157-r9-02 | Stage-1 QA r10 (`boomi-qa-tester`, the decisive round; report `agents/reports/2026-09-09-issue-157-r10.md`; 38 components created and deleted, `net_zero: true`) | Medium | machine-served schemas/contracts | served-remedy-that-does-not-work | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `deferred` — same fallback; reason class `window-exhausted`. |
| QA-157-r10-04 | "this slice's own resolver docstring says of the legacy route that the folderless-create lint says so; measured false — a folder name on that route silences the lint and places nothing" | Stage-1 QA r10 (`boomi-qa-tester`, the decisive round; report `agents/reports/2026-09-09-issue-157-r10.md`; 38 components created and deleted, `net_zero: true`) | Low | non-blocking | served-text-promising-an-absent-field | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `deferred` — same fallback; reason class `window-exhausted`. A comment, not served text. |
| QA-157-r10-05 | "the guard's package walk is non-recursive, so a closed-key builder inside a sub-package would pass" — not reachable today, every closure mechanism is a top-level module | Stage-1 QA r10 (`boomi-qa-tester`, the decisive round; report `agents/reports/2026-09-09-issue-157-r10.md`; 38 components created and deleted, `net_zero: true`) | Low | non-blocking | hand-listed-enumeration-of-a-derivable-set | **Standard** — no critical anchor | uncommitted over `ba1be9f` | `deferred` — same fallback; reason class `out-of-scope-by-design` (latent, unreachable in the current tree, and the gate proved the reachable case fails the suite with a mutant witness). |
| SELF-157-r11-01 | "the new document-declares-its-own-placement rule counts `folderName` — the spelling this platform ignores — so a raw-XML component carrying only that spelling is excluded from the fan-out, no id is ever resolved, and it lands at the account root with no row and no warning" | self-review: a six-lens adversarial audit of the correction, 51 agents, each finding handed to a separate agent instructed to refute it (27 confirmed, 18 refuted); run `wf_e98415de-91a` | n/a (self) | mutation accounting | guard-modelling-one-spelling-of-a-two-spelling-fact | **Standard** — no critical anchor; the row makes no false claim | uncommitted over `6e6b207` | `fixed` — only a document-carried `folderId` declares a placement. Caught before any gate saw it: this is QA-157-r1-01's failure mode reinstated on the escape hatch the rule was written for, and the verifying agent measured both the defect and the counterfactual, confirming the fix places the component without undoing QA-157-r10-01. |
| SELF-157-r11-02 | "reading a caller's own component document put caller-controlled bytes in front of a parser that expands internal entities" — measured, a four-level declaration turns a few hundred bytes into ten thousand | self-review: a six-lens adversarial audit of the correction, 51 agents, each finding handed to a separate agent instructed to refute it (27 confirmed, 18 refuted); run `wf_e98415de-91a` | n/a (self) | secrets/security (resource amplification, no disclosure) | caller-bytes-reaching-an-unscreened-parser | **Standard** — no critical anchor: no disclosure and no mutation accounting effect | uncommitted over `6e6b207` | `fixed` — a document that declares an entity is refused at the one reader rather than parsed, and an unreadable document simply carries no placement, which every caller of that reader already handles. Reachability was NEW: the reader previously saw only bytes this server built or the platform returned. |
| SELF-157-r11-03 | "rebuilding the wave-gate node manifest from the baseline row set reassigned identities for rows already committed" — the append-only contract forbids a repoint, and the in-tree checks compare against a different base and did not see it | self-review: a six-lens adversarial audit of the correction, 51 agents, each finding handed to a separate agent instructed to refute it (27 confirmed, 18 refuted); run `wf_e98415de-91a` | n/a (self) | required in-tree validation currency | derived-artifact-rebuilt-instead-of-appended | **Standard** — no critical anchor | uncommitted over `6e6b207` | `fixed` — the committed manifest is restored and the delta is a clean append of five rows with one tombstone for the inverted test. |
| SELF-157-r11-04 | "the process builder rejects the placing spelling on a rationale the transport correction made stale — that a create carrying it would still land in the account root" — which also made a governed root declaring a folder unable to build that process kind | self-review: a six-lens adversarial audit of the correction, 51 agents, each finding handed to a separate agent instructed to refute it (27 confirmed, 18 refuted); run `wf_e98415de-91a` | n/a (self) | capability reachability | stale-authority-comment | **Standard** — no critical anchor | uncommitted over `6e6b207` | `fixed` — the key is accepted as inert metadata (emitted bytes asserted unchanged), the rationale is rewritten, and the test that pinned the old rejection is INVERTED rather than deleted, so the opposite claim now has a guard. |
| SELF-157-r11-05 | "the served create templates advertise a folder spelling that never places and omit the one that does" — measured across every create template carrying a folder key | self-review: a six-lens adversarial audit of the correction, 51 agents, each finding handed to a separate agent instructed to refute it (27 confirmed, 18 refuted); run `wf_e98415de-91a` | n/a (self) | machine-served schemas/contracts | hand-copied-key-set-beside-its-authority | **Standard** — no critical anchor | uncommitted over `6e6b207` | `fixed` — every component create template now offers the placing spelling and labels the ignored one, and the guard grades a DERIVED set (every create template mentioning a folder) with no hand-typed floor, which is what let an earlier version grade four while ten were wrong. |
| QA-157-r11-01 | "the entity screen is a `re.search` over the WHOLE document, so one XML comment mentioning a doctype makes the reader report no placement — the fan-out then claims the component and replaces the caller's own folder id" — and it is a fourth hand-rolled copy of a regex whose three siblings a sibling module deliberately did not join, saying so in its docstring | Stage-1 QA r11 (`boomi-qa-tester`, acceptance for the five deferred findings and the self-audit's corrections; report `agents/reports/2026-09-09-issue-157-r11.md`; tree COMMITTED and clean at `5949271`, 0 porcelain rows at entry and exit of all 16 runs; 68 components created and deleted, `net_zero: true`) | Medium | runtime behavior | hand-rolled-screen-where-the-parser-answers | **Standard** — no critical anchor | `5949271` | `fixed` — the reader asks expat, which reports every entity declaration it reads and never builds a tree, so a comment is a comment. The gate also REFUTED the hazard the screen was written for, measuring that this libexpat bounds amplification and refuses a seven-level expansion outright, and recorded that as refuted rather than banking it as a finding. |
| QA-157-r11-02 | "the ask site learned the third spelling; the attestation site did not" — a component live in the folder its own document named got no placement row at all, while its config-declaring sibling in the same apply got a verified one | Stage-1 QA r11 (`boomi-qa-tester`, acceptance for the five deferred findings and the self-audit's corrections; report `agents/reports/2026-09-09-issue-157-r11.md`; tree COMMITTED and clean at `5949271`, 0 porcelain rows at entry and exit of all 16 runs; 68 components created and deleted, `net_zero: true`) | Medium | mutation accounting | one-site-of-two-taught-a-new-spelling | **Standard** — no critical anchor | `5949271` | `fixed` — the placement resolver reads the document through the same one reader governance uses. |
| QA-157-r11-03 | "the NINTH instance, named: the template guard builds its universe from module-level dicts carrying a template block, so three process contracts that declare themselves creates inside a registry are ungraded and still serve the ignored spelling uncaveated" | Stage-1 QA r11 (`boomi-qa-tester`, acceptance for the five deferred findings and the self-audit's corrections; report `agents/reports/2026-09-09-issue-157-r11.md`; tree COMMITTED and clean at `5949271`, 0 porcelain rows at entry and exit of all 16 runs; 68 components created and deleted, `net_zero: true`) | Medium | machine-served schemas/contracts | guard-modelling-one-spelling-of-a-two-spelling-fact | **Standard** — no critical anchor | `5949271` | `fixed` — the universe is every contract that DECLARES itself a create, at any nesting depth, and each must offer the spelling ITS OWN route places with: a folder id for a component document, a folder name for the trading-partner route, read from the contract's own declaration. |
| QA-157-r11-04 | "clone writes `config.folder_name` as inert metadata and the served example uses it" — a clone requesting a folder by name stayed where the source was, while the id control moved | Stage-1 QA r11 (`boomi-qa-tester`, acceptance for the five deferred findings and the self-audit's corrections; report `agents/reports/2026-09-09-issue-157-r11.md`; tree COMMITTED and clean at `5949271`, 0 porcelain rows at entry and exit of all 16 runs; 68 components created and deleted, `net_zero: true`) | Medium | machine-served schemas/contracts | served-remedy-that-does-not-work | **Standard** — no critical anchor | `5949271` | `fixed` — both the tool docstring and the served example name the spelling that places. |
| QA-157-r11-05 | "the create docstring claims the lint's two accepted spellings now both place" — false for the legacy route, where nothing resolves a name | Stage-1 QA r11 (`boomi-qa-tester`, acceptance for the five deferred findings and the self-audit's corrections; report `agents/reports/2026-09-09-issue-157-r11.md`; tree COMMITTED and clean at `5949271`, 0 porcelain rows at entry and exit of all 16 runs; 68 components created and deleted, `net_zero: true`) | Low | machine-served schemas/contracts | served-text-promising-an-absent-field | **Standard** — no critical anchor | `5949271` | `fixed` — the paragraph says which spelling places and which does not, and names the route where the difference bites. |
| ARCH-157-e1-01 | "required-target coverage does not consistently follow the selected artifact": the typed gate drops the conflict policy, a reused MAP's candidate mappings are inspected although the request will not write it, and the apply refresh checks map validity without required-leaf coverage | §6 architect implementation review, evaluation 1, gate `cdx-gate-review.sP9bAv`, attested `ok:true` against the plan's own bytes; `VERDICT: ISSUES FOUND` | P2-equivalent | runtime behavior | decision-rebuilt-instead-of-asked | **Standard** | `140f5e7` | `fixed` — a reused map is opaque like a reused profile, the canonical refresh runs the coverage gate before any write, and the policy travels with the question. Disposition REVISED at self-verification: the first correction read the policy off the normalized intent and the spec, neither of which carries it, so it changed nothing; see SELF-157-e1-01. Witnesses: `tests/test_issue_157_plan_conformance.py`. |
| ARCH-157-e1-02 | "derived flows can describe discarded candidate content, and normalization removes profile names" | §6 architect implementation review, evaluation 1, gate `cdx-gate-review.sP9bAv`, attested `ok:true` against the plan's own bytes; `VERDICT: ISSUES FOUND` | P2-equivalent | machine-served schemas/contracts | served-projection-from-the-candidate-not-the-artifact | **Standard** | `140f5e7` | `fixed` — the projection takes its field set from the selected artifact where one exists (shape from the generator, fields from the account), and rule R3's name drop is scoped to the COMPARISON: the served row keeps the name its own model declares a field for. The reviewer was right that the earlier justification covered the equality check and not the payload. |
| ARCH-157-e1-03 | "the connection XOR contract misses an actual reuse encoding" — identity checks ran only inside the `reference_only` spelling, so a create-shaped binding naming two different components passed while apply resolved it to one | §6 architect implementation review, evaluation 1, gate `cdx-gate-review.sP9bAv`, attested `ok:true` against the plan's own bytes; `VERDICT: ISSUES FOUND` | P2-equivalent | mutation accounting | guard-modelling-one-spelling-of-a-two-spelling-fact | **Standard** | `140f5e7` | `fixed` — the identity check is one function both branches call. |
| ARCH-157-e1-04 | "watermark validation cannot reach selected existing-profile evidence" — a valid declaration over a reused source profile was refused before the resolver ran | §6 architect implementation review, evaluation 1, gate `cdx-gate-review.sP9bAv`, attested `ok:true` against the plan's own bytes; `VERDICT: ISSUES FOUND` | P2-equivalent | runtime behavior | offline-refusal-of-a-question-only-the-account-answers | **Standard** | `140f5e7` | `fixed` — governance DEFERS when the index is unavailable, exactly as the coverage gate does, and the online validation decides with the selected artifact. |
| ARCH-157-e1-05 | "the named output-only refusal misses competing locations" — the same authored key was answered three ways depending on where it landed | §6 architect implementation review, evaluation 1, gate `cdx-gate-review.sP9bAv`, attested `ok:true` against the plan's own bytes; `VERDICT: ISSUES FOUND` | P2-equivalent | machine-served schemas/contracts | validation-applied-to-one-of-two-entry-points | **Standard** | `140f5e7` | `fixed` — all three locations serve the named code, measured. |
| ARCH-157-e1-06 | "retirement proofs are genuine but narrower than required" — per-component XML digests without the ordered mutation trace | §6 architect implementation review, evaluation 1, gate `cdx-gate-review.sP9bAv`, attested `ok:true` against the plan's own bytes; `VERDICT: ISSUES FOUND` | P2-equivalent | mutation accounting (evidence breadth) | proof-narrower-than-the-claim | **Standard** | `140f5e7` | `fixed` — the harness records the ordered trace of the create boundary's own calls as a further axis, all ten records are re-frozen from the pristine baseline extraction, every pair still matches on it, and the wired control still moves a digest. |
| ARCH-157-e1-07 | "validator consolidation and its route proof are incomplete" — the advisory review imported the shared normalizer without calling it | §6 architect implementation review, evaluation 1, gate `cdx-gate-review.sP9bAv`, attested `ok:true` against the plan's own bytes; `VERDICT: ISSUES FOUND` | P2-equivalent | machine-served schemas/contracts | claim-of-consolidation-without-the-call | **Standard** | `140f5e7` | `fixed` — disposition REVISED at self-verification: the earlier "bounded deviation" was wrong on its own facts. The advisory route reads a map CONFIG, exactly like the coverage gate, so the interpretation merges with no conversion: `destination_paths` in `transform_map_validation` is the one reading of what a mapping entry writes to, and both consumers resolve it. Each caller keeps its own entry enumeration, which is a different concern. The route sentinel is rebuilt too: it drives all SIX routes, enters the recipe layer through `run_recipes` and a typed recipe intent rather than the private validator, keeps the apply payload's covering compile outside the measurement, and its control removes a route's INVOCATION and names which routes stay loud. |
| SELF-157-e1-01 | Self-verification of the ARCH-157-e1-01 correction: the conflict policy was read with `getattr` off the normalized intent and off the integration spec, and NEITHER model carries it — the policy lives on the request's intent — so the resolver kept falling back to its default and the correction changed no behaviour at all | self-audit of the correction batch, before any gate saw it | n/a (self-found) | runtime behavior | decision-rebuilt-instead-of-asked | **Standard** | `3dca5f6` | `fixed` — the policy is a parameter of `_selected_profile_indexes`, passed from the three call sites that hold the request's own value. The live selected-artifact discovery is also hoisted out of the per-map loop, which was re-asking the account once per map for an answer that cannot change within a request. Witness: the policy is asserted to reach the resolver under every one of the three legal values. |
| SELF-157-e1-02 | Self-verification of the ARCH-157-e1-04 correction: the deferral returned from the WHOLE validator, so a watermark over a reused source profile silently stopped getting rule (b) — the query-parameter rule, which needs no profile index and was never the subject of the deferral | self-audit of the correction batch, before any gate saw it | n/a (self-found) | runtime behavior | deferral-scoped-wider-than-the-question | **Standard** | `3dca5f6` | `fixed` — rule (a) is its own function that reports whether it decided; rule (b) runs unconditionally beside it. |
| SELF-157-e1-03 | Self-verification of the ARCH-157-e1-04 correction, second defect: the online re-ask merged the selected indexes into `literal_indexes`, which is a different key space (component id, not component key) and a different value shape (a wrapper, not a field map), so it resolved nothing; and it named unit 0 for every unit, although the spec's process order is sorted by component key and is not the authored order the served path reports | self-audit of the correction batch, before any gate saw it | n/a (self-found) | machine-served schemas/contracts | guard-modelling-one-spelling-of-a-two-spelling-fact | **Standard** | `3dca5f6` | `fixed` — governance records the question it deferred, with the unit index THAT pass assigned, and the online validation decides exactly those recorded questions through the resolver's own `selected_indexes` parameter. Nothing re-derives an index that another pass owns. |
| SELF-157-e1-04 | Self-verification by hand-run mutant: the first witness for the apply refresh passed with the refresh's coverage gate REMOVED — the refusal it observed came from the preflight recompile, and the dry-run payload never reached the refresh at all | self-audit of the correction batch, mutation harness over every new witness | n/a (self-found) | (test evidence) | vacuous-guard | **Standard** | `3dca5f6` | `fixed` — the witness drives a WET apply and simulates the drift where the code itself distinguishes the two reads: the refresh is the only caller that supplies the planner's `reused_keys`. Re-run against the mutant, it fails. Every other new witness was mutated the same way; eleven mutants, eleven caught. |
| SELF-157-e1-05 | Found while consolidating the destination reading: `normalized_map_destinations` iterated `map_config.get(key) or ()`, so a truthy non-list mapping list — `field_mappings: true`, `outputs: true` — reached `for entry in True` and let a `TypeError` escape the hard coverage gate on every route that raises it. The advisory route already read these lists safely; the two readings differed | self-audit of the correction batch, surfaced by the consolidation itself | n/a (self-found) | runtime behavior | unguarded-shape-assumption | **Standard** | `3dca5f6` | `fixed` — one `_entries` reader, non-list is empty, matching what the advisory route always did. The pre-existing advisory test for this shape is what caught it. |
| CDX-157-r5-01 | "[P1] Normalize live profile indexes before constructing flow summaries" — a live-discovered index's entries carry `key`, `key_path`, `name_path`, `is_mappable` and `structural`, which `FieldIndexEntryV1` forbids, so every valid plan/compile over a reused profile failed with a `ValidationError` | Stage-2 commit review round 5 (fix delta), run dir `cdx-review.WoHUEv`, base `6e38025`, head `557f1b7`, `STATUS: completed`, teardown confirmed | P1 | machine-served schemas/contracts | fixture-in-the-shape-the-code-produces | **Critical** — source label P1 | `557f1b7` | `fixed` — the selected index is projected onto the served entry schema, whose key set is read FROM `FieldIndexEntryV1` rather than hand-listed. The reviewer's diagnosis of why nothing caught it is exact and is recorded as its own lesson: every witness in the batch used a GENERATOR-built index, and the live shape is a documented superset. The new witnesses index `tests/fixtures/profile_components/issue_95/profile_json.xml`, an exported platform component frozen long before this slice's baseline, and a control asserts the two shapes really do differ. |
| CDX-157-r5-02 | "[P2] Handle selected profiles without local generation configuration" — a reference-only profile carrying an id and no local body made the fallback build a summary missing the mandatory `generation_mode` and `profile_config`, so planning failed where it used to serve `None` | Stage-2 commit review round 5 (fix delta), run dir `cdx-review.WoHUEv`, base `6e38025`, head `557f1b7`, `STATUS: completed`, teardown confirmed | P2 | machine-served schemas/contracts | summary-assembled-without-its-required-authority | **Standard** | `557f1b7` | `fixed` — with no generator artifact there is no summary, which is the field's own pre-existing answer. The limit is recorded rather than papered over: the summary's SHAPE is the generator's output, so a reused profile with no local body is described by its index and not by a generation summary. |
| CDX-157-r5-03 | "[P2] Keep required-target validation enabled for map updates" — the reused-map exemption keyed on `config.reference_only` alone, so a map declaring `action="update"` beside that flag skipped coverage although apply WRITES it | Stage-2 commit review round 5 (fix delta), run dir `cdx-review.WoHUEv`, base `6e38025`, head `557f1b7`, `STATUS: completed`, teardown confirmed | P2 | runtime behavior | decision-rebuilt-instead-of-asked | **Standard** | `557f1b7` | `fixed` — the exemption asks `_will_reuse_at_apply`, THE predicate, exactly as the reviewer says the runtime-authority rule requires. Without a bound component id the predicate answers "not reused" and the gate runs, which is the safe direction. |
| CDX-157-r5-04 | "[P2] Reject invalid watermark references before deferring validation" — the resolver answers `None` for a missing component and for a non-profile component too, and reading all three answers as one deferral retired the reference check | Stage-2 commit review round 5 (fix delta), run dir `cdx-review.WoHUEv`, base `6e38025`, head `557f1b7`, `STATUS: completed`, teardown confirmed | P2 | runtime behavior | one-answer-read-as-three-questions | **Standard** | `557f1b7` | `fixed` — a deferral is legal in exactly two shapes, a literal existing-profile id and an in-plan profile this request will REUSE; everything else is refused at a new served path `/units/<n>/envelope/watermark/source_profile_ref`. |
| SELF-157-r5-05 | Process defect, recorded because it affects how evidence may be read: the round-12 live QA was dispatched against `557f1b7` and this correction batch edited the worktree WHILE that run was reading it | self-audit | n/a (self-found) | (process) | worktree-edited-under-a-running-gate | **Standard** | `557f1b7` | `fixed` by re-validation rather than by claim: no r12 finding is dispositioned on r12's own evidence alone — each is re-verified against the final tree — and the affected scenarios are re-run against the final committed tree, which this correction batch owes its own QA for regardless. The rule this broke is one the slice ledger already carried from #175. |
| CDX-157-r6-01 | "[P2] Use the planner's resolved binding for map reuse" — a `reference_only` map bound by NAME has no declared id, so the exemption called it written and blocked a valid request; and a config-only `component_id` without the flag binds nothing, yet was read as a reuse and skipped the gate | Stage-2 commit review round 6 (fix delta), run dir `cdx-review.vtNvH0`, base `557f1b7`, head `0bc76b3`, `STATUS: completed`, teardown confirmed | P2 | runtime behavior | decision-rebuilt-instead-of-asked | **Standard** | `0bc76b3` | `fixed` by the STRUCTURAL answer this class is now owed, not by a fifth instance patch. `resolve_planner_binding` is the one resolution of WHICH existing component a spec entry binds to, extracted from `_build_plan` and used by it; `resolve_reused_keys` composes it with `_will_reuse_at_apply`; and `_validate_processes` asks once and hands the answer to all three consumers (the map exemption, the selected-index resolver, the watermark rule). The sibling sweep removed the last two re-derivations, in `_resolve_selected_profile_indexes` and in `_will_reuse_map_at_apply`, which is deleted. |
| CDX-157-r6-02 | "[P2] Allow watermark validation to defer for name-reused profiles" — an in-plan profile resolving by name to an existing component under `conflict_policy="reuse"` carries no `reference_only` flag, and keying deferrability on that flag refused its watermark during normalization | Stage-2 commit review round 6 (fix delta), run dir `cdx-review.vtNvH0`, base `557f1b7`, head `0bc76b3`, `STATUS: completed`, teardown confirmed | P2 | runtime behavior | guard-modelling-one-spelling-of-a-two-spelling-fact | **Standard** | `0bc76b3` | `fixed` — deferrability asks the same reused-key authority, and normalization (which cannot know) passes `None`, meaning "decided by the pass that does". The resolver's single `None` is split into the three questions it was hiding: an unrepairable reference refuses at `/source_profile_ref`, a reused profile defers, and a profile this request WRITES falls through to the original `/field` refusal — including the empty-index case, since `{}` is the same answer as no index. |
| SELF-157-r6-03 | Found by an existing witness while extracting `resolve_planner_binding`: the extraction left `_build_plan` referring to a local it no longer defined, and the plan lint's own error handling turned that into a SILENT degradation — the served plan reported "the component-plan lint did not run" and simply carried no lint findings | self-audit during the round-6 correction | n/a (self-found) | runtime behavior | refactor-caught-by-a-swallowing-handler | **Standard** | `0bc76b3` | `fixed` — the reference is the binding's own field. Recorded because the lesson is about the handler, not the typo: a broad `except` around the lint means a refactoring defect there is reported as an absence of findings rather than as a failure, and only a test asserting a specific lint finding sees it. |
| QA-157-r12-01 | "every plan in which any profile is reused fails before anything is served" — the served summary carried the live index's extra keys and, for a reference-only profile, no generation mode or profile config at all | Stage-1 live QA round 12, report `agents/reports/2026-09-09-issue-157-r12.md`, measured at `557f1b7` from a byte-pinned extraction | High | machine-served schemas/contracts | fixture-in-the-shape-the-code-produces | **Critical** — source label High | `557f1b7` | `fixed` before this round reported it, by CDX-157-r5-01 and CDX-157-r5-02; QA re-measured at `0bc76b38` and DISCHARGED both. Two independent gates found the same defect from different directions, which is the strongest evidence in this slice that the generator-shaped fixture was the blind spot and not a one-off. |
| QA-157-r12-02 | "correction H's own defect class survives at a third, un-enrolled copy of the reading it says it unified" — the derived-flows projection reads the same mapping lists with `or ()`, BEFORE the coverage gate, so a truthy non-list mapping list was served as a bare `TypeError` with no machine code | Stage-1 live QA round 12, report `agents/reports/2026-09-09-issue-157-r12.md`, measured at BOTH `557f1b7` and `0bc76b38` | High | runtime behavior | unguarded-shape-assumption | **Critical** — source label High | `0bc76b3` | `fixed` — the reader is public (`mapping_entries`) and the projection's four copies, plus the nested `inputs`/`outputs`/`output_fields` reads, all resolve it. The witness removes the reader and asserts the projection's operations vanish with it, so the enrolment is a call and not a comment. The sweep was the finding's own point: consolidating two readings and leaving a third is not consolidation. |
| QA-157-r12-03 | "NTH INSTANCE FOUND" — the apply refresh models "will apply write this map" as a set of plan LABELS while the pre-write pass was re-keyed onto the predicate, so plan and compile exempted a map that apply then gated | Stage-1 live QA round 12, report `agents/reports/2026-09-09-issue-157-r12.md`, measured at `0bc76b38` | High | runtime behavior | decision-rebuilt-instead-of-asked | **Critical** — source label High | `0bc76b3` | `fixed` — the refresh asks `_keys_reused_at_apply` over the same resolved ids apply itself uses, so the two passes cannot disagree. The gate confirms the divergence was pre-write and mutation accounting was correct; the defect is that one request got two answers. Folded into the round-6 structural fix's sibling sweep rather than patched as a separate instance. |
| QA-157-r12-04 | "one `plan` still asks the account the same question three times" — three owners resolved the selected artifacts, feeding the preview, the coverage gate and the legacy lint | Stage-1 live QA round 12, report `agents/reports/2026-09-09-issue-157-r12.md`, measured at BOTH SHAs | Medium | runtime behavior | one-question-with-three-owners | **Standard** | `0bc76b3` | `fixed` in the typed pass, and MEASURED rather than claimed: the validation pass resolves once and hands its answer to the preview, so a live account read cannot describe one artifact to the gate and another to the preview. One owner remains — the legacy component-plan lint, a separate subsystem reached through its own plan builder, resolving over every component and not only profiles. The witness asserts the exact count, two, so a third owner reappearing fails it. |
| QA-157-r12-05 | "the same watermark rule has two plan dispositions" — offline normalization raises, the online pass reports, enforcement-equivalent from `compile` on (measured) | Stage-1 live QA round 12, report `agents/reports/2026-09-09-issue-157-r12.md` | Low | machine-served schemas/contracts | rule-with-two-report-channels | **Standard** | `0bc76b3` | `finding-refuted` as a defect, recorded as a consequence. The two channels answer two different questions, not one question twice. Normalization decides only the references no later pass can repair, and it RAISES because a refusal there means the request has no normalized form at all; the pass that can decide the rest REPORTS, because planning's contract is to hand a caller everything wrong at once. The gate measured both as enforcement-equivalent from compile on, carrying the same code. Collapsing them would mean either normalizing a request with no normal form or downgrading a hard governance refusal to a diagnostic. |
| QA-157-r12-06 | "correction G's benefit misses the `reference_only` spelling entirely (no `*_profile_generation` row)" | Stage-1 live QA round 12, report `agents/reports/2026-09-09-issue-157-r12.md` | Low | machine-served schemas/contracts | summary-assembled-without-its-required-authority | **Standard** | `0bc76b3` | `fixed` already, and this is the same limit CDX-157-r5-02 recorded: the summary's SHAPE is the generator's output, so a profile carrying only an id has an index but no generation summary. The alternative is to synthesise a generation mode and a profile config for a component this request never generates, which is inventing served content. |
| QA-157-r12-07 | "the `flows` ban is narrower than its own docstring — legacy config-root flows silently ignored at plan and apply" | Stage-1 live QA round 12, report `agents/reports/2026-09-09-issue-157-r12.md` | Low | machine-served schemas/contracts | claim-wider-than-the-guard | **Standard** | `0bc76b3` | `fixed` — the claim is scoped to the typed surface, which is the only surface this guard is reached from. The legacy config-root spelling is a documented field of the bare spec form, carried as an inert echo, and is deliberately not banned. |
| CDX-157-r7-01 | "[P2] Isolate selected-profile lookup failures per component" — one failed metadata read aborted the whole selected-artifact resolution, and the caller's broad handler then discarded every index it had, so a map missing a required target leaf compiled | Stage-2 commit review round 7 (fix delta), run dir `cdx-review.0176p1`, base `0bc76b3`, head `78cf7e7`, `STATUS: completed`, teardown confirmed | P2 | runtime behavior | one-failure-widened-into-a-lost-pass | **Standard** | `78cf7e7` | `fixed` — `resolve_planner_bindings` resolves once per component and isolates each, excluded keys are skipped before anything is resolved, and the discovery read is isolated too. The round-6 extraction is what introduced this: moving the per-component handler up into a shared function widened its blast radius, which is a cost of consolidation the sweep should have checked and did not. |
| CDX-157-r7-02 | "[P2] Distinguish empty live indexes from unavailable candidate indexes" — a reused profile indexed as `{}` by the ACCOUNT is a confirmed absence, and treating it as unavailable deferred a watermark that the base version refused | Stage-2 commit review round 7 (fix delta), run dir `cdx-review.0176p1`, base `0bc76b3`, head `78cf7e7`, `STATUS: completed`, teardown confirmed | P2 | runtime behavior | one-answer-read-as-three-questions | **Standard** | `78cf7e7` | `fixed` — an index is authoritative when it came from the selected artifact or from a supplied literal index, and an authoritative `{}` decides. Only a candidate config's `{}` still defers. This is the third face of the same mechanism: the resolver's single return value answers several questions, and each caller has to say which one it asked. |
| ARCH-157-e2-01 | "Selected-profile evidence is still lost or bypassed in three cases" — a failed discovery omitted the key, so the resolver fell back to the candidate config for a profile the request will not write; the apply refresh qualified a reused profile only when discovery had succeeded; and the recipe engine judged a reused map the direct route exempts | §6 architect implementation review, evaluation 2, gate `cdx-gate-review.VsHZ3l`, attested `ok:true` against the plan's own bytes; `VERDICT: ISSUES FOUND` | P2-equivalent | runtime behavior | guard-modelling-one-spelling-of-a-two-spelling-fact | **Standard** | `6e217b0` | `fixed` — every REUSED key is now present in the selected-index map, with `None` meaning "reused, and the evidence is unavailable". Absence means "not reused, index the candidate"; the two were one value before, which is why a failed read looked like an authored profile. The apply refresh's `$ref` test reads the same presence, and the recipe engine asks the same reuse authority the direct route asks. |
| ARCH-157-e2-02 | "Derived flows still mix selected and discarded content" — the summary carried the account's index beside the candidate's `profile_config`, and the projection served a reused map's discarded candidate destinations | §6 architect implementation review, evaluation 2, gate `cdx-gate-review.VsHZ3l`, attested `ok:true`; `VERDICT: ISSUES FOUND` | P2-equivalent | machine-served schemas/contracts | served-projection-from-the-candidate-not-the-artifact | **Standard** | `6e217b0` | `fixed` — `GeneratedProfileSummaryV1` now names its own `evidence_source`, and a selected-artifact summary carries NO generation body: the model refuses one, so a contradictory subshape is unrepresentable rather than merely absent. A reused MAP is opaque to the projection for the same reason the coverage gate exempts it. The reviewer was right that "shape from the generator, fields from the account" justified serving two descriptions of one component. |
| ARCH-157-e2-03 | "Watermark validation still decides from the wrong evidence—or never obtains a decision" — a non-empty candidate index prevented deferral in both directions, and an unavailable reused artifact left a rule nothing ever decided | §6 architect implementation review, evaluation 2, gate `cdx-gate-review.VsHZ3l`, attested `ok:true`; `VERDICT: ISSUES FOUND` | P2-equivalent | runtime behavior | pass-deciding-a-question-it-cannot-answer | **Standard** | `6e217b0` | `fixed` — normalization now decides ONLY what no later pass can repair (a reference naming no component, or a non-profile) and defers every in-plan reference, because offline it cannot tell a written profile from a name-reused one. The online pass decides the rest with the reuse set and the selected artifact, and refuses — rather than deferring a second time — when the artifact could not be read at all, since no later pass exists to ask. |
| ARCH-157-e2-04 | "Persistent accounting does not yet enforce all promised content and retirement guarantees" — the comparison dropped `component_name` unconditionally, so an unrelated profile name was accepted; and a retired row was believed on the strength of its record id existing, whatever that record classified | §6 architect implementation review, evaluation 2, gate `cdx-gate-review.VsHZ3l`, attested `ok:true`; `VERDICT: ISSUES FOUND` | P2-equivalent | mutation accounting (evidence breadth) | oracle-checking-the-cheaper-half-of-its-claim | **Standard** | `6e217b0` | `fixed` — the name is compared beside the digest rather than dropped from both: R3's reconciliation is between two PRODUCERS, and each replay compares a case against its own baseline, where a wrong name is a wrong name. A retired row must cite a record classified RETIRE, and one measured on the row's own field. No committed row uses the retirement path today, so this completes the mechanism rather than changing any current accounting. |
| ARCH-157-e2-05 | "Retirement fingerprint evidence remains incomplete, with an applicability distinction" — and the served claim that retired metadata never affected "a fingerprint" exceeded the acknowledged full-echo hash behaviour | §6 architect implementation review, evaluation 2, gate `cdx-gate-review.VsHZ3l`, attested `ok:true`; `VERDICT: ISSUES FOUND` | P2-equivalent | machine-served schemas/contracts | claim-wider-than-the-measurement | **Standard** | `6e217b0` | `fixed` by taking the reviewer's own disposition: the axis is disposed of by MEASUREMENT of non-applicability, not by omission. Every record now carries `fingerprint_applicability` recording that its producer authors zero canonical process units on both sides of the pair, and the checker reports both a missing disposition and a producer that starts authoring one. The served text names the axes it actually measured and states plainly that it does NOT claim absence from every hash, because the legacy spec echo is hashed whole and carries the spelling. |
| SELF-157-e2-06 | Recorded, not found by a gate: plan item 1's "explicit consolidation of shipped #153-#156 contracts and the final #153 handoff" was missing, and BOTH architect evaluations said so | self-audit against the evaluation-2 report | n/a (self-found) | (audit record) | plan-artifact-never-produced | **Standard** | `6e217b0` | `fixed` — the consolidation is a section of this ledger, one row per shipped contract with the in-tree authority that owns it and that slice's own audit record, plus what the #153 handoff binds forward. Two evaluations reporting the same absence is the evidence that a plan item producing only an audit artifact is the easiest kind to skip. |

## Second-instance check (run at reconciliation, before the batch)

Defect classes are `(mechanism, runtime-authority)` pairs assigned on the rows above. Counting
distinct post-reconciliation defects across all roster gates:

| Defect class | Instances in this slice | Action |
| --- | --- | --- |
| `served-projection-narrower-than-its-authority` | 1 (SELF-157-r1-01) | instance fix |
| `silent-precedence-fixture` | 1 (SELF-157-r1-02) | instance fix |
| `placement-spelling-the-platform-ignores` | 1 (QA-157-r1-01) | structural fix applied anyway — one transport seam replaces the per-builder spelling, with the sibling sweep below |
| `claim-scoped-by-negation` | 1 (QA-157-r1-02) | invariant fix: the claim asks the mode authority instead of excluding one mode |
| `served-text-promising-an-absent-field` | 1 (QA-157-r1-03) | instance fix |
| `validator-location-coarser-than-its-rule` | 1 (QA-157-r1-04) | one map, pinned as a subset of the served-code map |
| `unbounded-loop-on-a-counterparty-controlled-token` | 1 (SELF-157-r2-01) | scope fix: the new reachability is removed; the pre-existing repo-wide pager shape is left to #160 |
| `request-decidable-answer-decided-mid-mutation` | 1 (QA-157-r2-01) | hoisted to the pre-write pass, beside the other pre-decidable answers |
| `guard-modelling-one-spelling-of-a-two-spelling-fact` | **11** (ARCH-157-e2-01, …r9-01, r10-01, SELF-157-r11-01, QA-157-r11-03) | the guard's universe is now every contract that DECLARES itself a create, and each is graded against the spelling its own route places with |
| `enumeration-short-by-one-mode` | 3 (QA-157-r4-01, QA-157-r5-02, QA-157-r6-02) | the missing mode is added and the route declares its own limit; the slice's coverage claim names its exception |
| `guard-unreachable-from-its-own-branch` | 1 (QA-157-r8-01) | the fact is recorded before anything clears it; the test drives the real apply |
| `invented-token-beside-the-facts-it-describes` | 1 (QA-157-r8-02) | the token is deleted; presence of the owned fields is the discriminator |
| `caller-bytes-reaching-an-unscreened-parser` | 1 (SELF-157-r11-02) | the document is refused at the one reader |
| `derived-artifact-rebuilt-instead-of-appended` | 1 (SELF-157-r11-03) | restored and appended |
| `stale-authority-comment` | 1 (SELF-157-r11-04) | rationale rewritten, pinning test inverted |
| `hand-copied-key-set-beside-its-authority` | 2 (QA-157-r10-02, SELF-157-r11-05) | the guard grades a derived set of templates against the builders' declarations |
| `hand-listed-enumeration-of-a-derivable-set` | 1 (QA-157-r9-03) | the list is deleted; the test walks the package |
| `decision-rebuilt-instead-of-asked` | **6** (CDX-157-r3-01, ARCH-157-e1-01, SELF-157-e1-01, CDX-157-r5-03, CDX-157-r6-01, QA-157-r12-03) | structural fix applied at the fifth instance and recorded here in full: `resolve_planner_binding` is the ONE resolution of which existing component a spec entry binds to (extracted from `_build_plan`, which now calls it), `resolve_reused_keys` composes it with `_will_reuse_at_apply`, and every pre-write consumer is handed that one answer. **Sibling sweep:** `_resolve_selected_profile_indexes` no longer re-resolves ids or re-derives the predicate; `_will_reuse_map_at_apply` is deleted; the watermark deferral takes the same set. **Non-vacuity witness:** a `reference_only` map bound by NAME with no declared id is opaque, and a config-only `component_id` without the flag is NOT — two cases the old hand-model got wrong in opposite directions, each with its adversarial half. **Coverage claim, from the authority's own case set:** the binding rule has exactly two inputs (a declared id, honoured from config only under `reference_only`; an unambiguous name match), and both are exercised in both directions. QA-157-r12-03 found a sixth site the sweep had not reached — the apply refresh's own model of which maps are written — and it is folded into this same structural fix rather than patched: it asks the reuse predicate over the ids apply itself uses |
| `deferral-scoped-wider-than-the-question` | 1 (SELF-157-e1-02) | invariant fix: the deferrable rule is its own function and reports whether it decided, so a deferral cannot reach a rule it was never about |
| `vacuous-guard` | 1 (SELF-157-e1-04) | every new witness in the batch is hand-run against a mutant that removes exactly the correction it claims to guard |
| `fixture-in-the-shape-the-code-produces` | 2 (SELF-157-r1-02 sibling, CDX-157-r5-01) | structural: a witness for a value that reaches the server from the ACCOUNT is built from an exported platform component frozen before this slice's baseline, with a control asserting the live shape differs from the generator's |
| `summary-assembled-without-its-required-authority` | 1 (CDX-157-r5-02) | absent authority, absent summary |
| `one-answer-read-as-three-questions` | **2** (CDX-157-r5-04, CDX-157-r7-02) | structural: the resolver's single return is decomposed at the ONE call site that has to interpret it, and each branch names the question it answers — unrepairable reference, reused artifact, authoritative empty index, candidate-config empty index. **Sibling sweep:** the same decomposition covers the map coverage gate, which already had it, and no other caller reads this resolver's `None` as a single fact. **Non-vacuity witness:** an account-supplied empty index refuses while a candidate-config empty index defers, in the same test |
| `unguarded-shape-assumption` | **2** (SELF-157-e1-05, QA-157-r12-02) | structural: ONE public reader, `mapping_entries`, resolved by every site that iterates a mapping list — the gate, the advisory route and all four copies in the derived-flows projection, plus the nested inputs/outputs/output-field reads. **Sibling sweep:** no bare-or-empty reading of a caller-supplied list survives in either module. **Non-vacuity witness:** removing the reader empties the projection's operations, and every junk shape is asserted to reach a structured refusal rather than a TypeError. **Coverage claim:** the reader's case set is list-or-tuple versus everything else, and both branches are exercised |
| `one-question-with-three-owners` | 1 (QA-157-r12-04) | the typed pass resolves once and hands the answer down; the legacy lint's own read is a separate subsystem's and is measured, not claimed away |
| `claim-wider-than-the-guard` | 1 (QA-157-r12-07) | the claim is scoped to the surface the guard is reached from |
| `served-projection-from-the-candidate-not-the-artifact` | **2** (ARCH-157-e1-02, ARCH-157-e2-02) | structural: the served summary NAMES its evidence source, and the model refuses a selected-artifact summary that carries a generation body — so a payload describing one component two ways is unrepresentable rather than merely absent. **Sibling sweep:** the map row is opaque about a reused map for the same reason, and a reused profile whose artifact could not be read serves no summary rather than the candidate's. **Non-vacuity witness:** the candidate's own leaf name appears nowhere in a selected-artifact summary, and the projection serves no operations for a reused map. **Coverage claim:** the projection has exactly two profile sources (generator, selected artifact) and one map source, and each is exercised in both directions |
| `claim-wider-than-the-measurement` | 1 (ARCH-157-e2-05) | the served text names the axes measured and the one it does not claim |
| `pass-deciding-a-question-it-cannot-answer` | 1 (ARCH-157-e2-03) | each pass decides only what no later pass can change, and the last pass refuses rather than deferring |
| `oracle-checking-the-cheaper-half-of-its-claim` | 1 (ARCH-157-e2-04) | the name is compared beside the digest; a retired row must cite a record that retires ITS field |
| `plan-artifact-never-produced` | 1 (SELF-157-e2-06) | the consolidation is written into this ledger |
| `one-failure-widened-into-a-lost-pass` | 1 (CDX-157-r7-01) | the shared resolver isolates per component and the caller's excluded keys are skipped before resolution |
| `refactor-caught-by-a-swallowing-handler` | 1 (SELF-157-r6-03) | recorded; the handler is the plan lint's own pre-existing contract and is not this slice's to change |
| `served-remedy-that-does-not-work` | 2 (QA-157-r7-01, QA-157-r9-02) | the branch serves its own remedy, measured to work on that route |
| `report-rebuilt-instead-of-read` | 1 (QA-157-r7-03) | the arm reads the route's return; the stub answers the way the route does |
| `unaccounted-side-effect-of-a-route` | 1 (QA-157-r7-05) | recorded with its measurement; pre-existing route behaviour, `not-validated` |
| `served-text-promising-an-absent-field` | **4** (…r1-03, r2-03, r7-04, r8-04) | **structural fix, this batch**: every claim the text still makes carries its witness in the same test |

No pair reached a second instance, so no structural-fix trigger fired. QA-157-r1-01's fix is
structural regardless, and carries the sweep the rule asks for: the placement-honoured rule moved to
live with the read-back parser it reads, the placement warning became one builder for both subjects,
and the folder-name lookup became one matcher under two policies — so the root and every component
can no longer drift apart on any of the three. The non-vacuity witnesses are the transport's
fail-closed control (a document the edit must refuse to touch), the unplaced-component case, and the
no-placement control that submits nothing.

A FIFTH instance arrived, and the window-2 checkpoint had committed to `ESCALATE-OPEN` if one
survived "a design in which nothing predicts another site's capability". It is worth being exact
about why that commitment does not fire yet: the design was never built. The round-6 correction
SAID the route reports what it submitted and then re-derived the value from the resolved
placement — stripped on one branch, ranked on the other — while the route had returned the folder
it sent all along. The gate found the reason and named the line. So the fifth instance did not
survive the design; it survived a claim about the design that the code did not keep, which is the
same defect this ledger records as `report-rebuilt-instead-of-read` and is exactly the failure the
`served-text-promising-an-absent-field` invariant was written for, one layer down: a claim nothing
checked.

The correction therefore does what was claimed — the arm reads the route's return, the provenance
the route reports travels on the row under its own key, and the notice selects its mechanism from
that provenance rather than from which id fields happen to be present. The guarding test's stub now
answers the way the route answers, including its own default, because a stub that answers
differently is how three of these rounds passed over the defect they were guarding. The next
checkpoint falls at evaluation 9 and will face this class again; if it recurs against a design that
reads rather than predicts, the commitment stands.

A THIRD instance of `guard-modelling-one-spelling-of-a-two-spelling-fact` arrived at a consumer the
invariant had not yet reached: the trading-partner arm read the resolved id alone and declared its
route cannot place, when its own contract submits a folder NAME and only an id has nowhere to go.
The rules make a third instance after a structural fix grounds for escalation ONLY when no
materially different action is left, and one plainly was: the earlier fix made both halves one
ANSWER but still handed consumers a bare id, so the half-answer stayed representable. It no longer
is — the resolver returns a single value carrying both halves and the seam takes that value, so a
consumer that reads the id without the spelling cannot be written. The guarding test was rebuilt for
the same reason: its round-4 version asserted on a row shape no live spelling emits, so it passed
over both defects; it now drives the real arm with the three placements the resolver can actually
produce.

The SECOND INSTANCE of `guard-modelling-one-spelling-of-a-two-spelling-fact` triggered the rule
again, one consumer over: r2-02 was the site that WROTE the losing name onto the component, r3-01
the site that READ it back into the attestation. Patching the reader would have left a third site
free to re-read the config, so the invariant ships instead — the resolver returns the placement and
the spelling that drove it as one answer, and the row and the warning are built from that return.
Sibling sweep over every site that reads a component's placement keys: the resolver (the authority),
the result row and the warning (both now read the return), the folderless-create lint (reads both
spellings, and is a lint rather than a placement decision), and governance's own claim predicate
(reads both, added for r2-02). The typed envelope has no `folder_id` at all, so the canonical root's
single-spelling read is its whole authority. Non-vacuity witness: both directions of the
both-declared case, each asserting the losing spelling is reported nowhere.

The SECOND INSTANCE of `served-text-promising-an-absent-field` triggered the structural rule, and the
structural answer is not a better sentence — it is that a served sentence may not make a claim about
served content unless a test generates that content and checks it. Both instances were hand-written
claims nothing verified: the first said a count and paths travelled in a field that has neither, the
second said the envelope never echoes the paths while the derived preview echoes the caller's own.
The remediation now makes exactly ONE checkable claim, and its witness runs beside it in the same
test — the review surface really is called, and really does name the canary leaf. The withdrawn
claims are asserted ABSENT, so restoring either fails. Sibling sweep over this slice's other served
refusal texts: every governance remediation tells the caller what to author or which surface to
call, and none describes what an envelope contains, so there is no third site to correct.

`placement-spelling-the-platform-ignores` is a RECURRENCE of the mechanism #153 recorded as
QA-153-r12-01, under the same runtime authority, in a different slice. #153 closed it by attesting
the failure honestly, which was correct for a slice that could not place; this slice places. Recorded
here rather than filed, because the correction is in-slice and complete.

## Checkpoint records

Each checkpoint records: loop identity, window and cumulative evaluation numbers, current SHA and
dirty state, per-tier counts and breadth, new/resolved/recurring defect classes, trend evidence,
outcome and rationale.

| # | Loop | Window / cumulative | SHA (dirty) | Critical | Standard | Defect classes | Outcome | Rationale |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Stage-1 QA (roster loop 1) | window 1, evaluations 1-3 / cumulative 3 | `ba1be9f` + dirty (fully staged) | 5 raised, 5 fixed, **1 unresolved** (QA-157-r3-01) | 4 raised, 4 fixed, 0 unresolved | new: 9 · resolved: 9 · recurring: 2 (`served-text-promising-an-absent-field` at r1-03/r2-03, given its invariant in that batch; `guard-modelling-one-spelling-of-a-two-spelling-fact` at r2-02/r3-01, given its invariant in this one) | **CONTINUE** — window 2 opens, cumulative history kept | Critical residue with a concrete structural action available, which the rules say to continue on. The trend improves on every axis the rule names: findings 4 → 3 → 1; affected-class breadth 4 → 3 → 1; unresolved count 0 at the end of each earlier evaluation; and neither recurrence was instance-patched — both got the invariant in the batch that found the second instance. Every finding after r1 was caused by the preceding correction rather than pre-existing, which is what a fix-delta re-run exists to surface. The named finite next correction: build the component placement row from the resolver's single return, so no consumer re-reads the config and no row can be assembled from two authorities. |

| 2 | Stage-1 QA (roster loop 1) | window 2, evaluations 4-6 / cumulative 6 | `ba1be9f` + dirty (fully staged) | 3 raised, 2 fixed, **1 unresolved** (QA-157-r6-01) | 3 raised, 2 fixed, **1 unresolved** (QA-157-r6-02) | new: 1 (`enumeration-short-by-one-mode`, 2 instances) · recurring: `guard-modelling-one-spelling-of-a-two-spelling-fact` at its **4th** instance | **CONTINUE** — window 3 opens, cumulative history kept | Every finding of this window is inside ONE narrow surface: how a created component's placement is resolved, submitted, attested and explained. The QA gate was asked to say whether anything new sat outside it and answered no. The recurring class is at its fourth instance, which the rules make grounds for escalation only when no materially different action is left — and one is, for the first time qualitatively different from the three before it. Those all kept a PREDICTION of what a route submits and refined it: a shared rule, then one answer with both halves, then a single value making the half-answer unrepresentable. Each was defeated by a route whose placement semantics the prediction did not know. The next correction removes the prediction: the route reports what it actually submitted, and the served row is built from that report. If a fifth instance survives a design in which nothing predicts another site's capability, there is no further action of a different kind and the outcome becomes `ESCALATE-OPEN`. Trend, honestly: findings per round 4, 3, 1, 1, 2, 2 — flat across this window, not improving; breadth is one class narrower and no finding has ever been left unresolved at the end of a batch. |
| 3 | Stage-1 QA (roster loop 1) | window 3, evaluations 7-9 / cumulative 9 | `ba1be9f` + dirty (fully staged) | 6 raised, 6 fixed, 0 unresolved | 6 raised, 6 fixed, 0 unresolved | recurring: `guard-modelling-one-spelling-of-a-two-spelling-fact` at its **7th** instance; `served-remedy-that-does-not-work` at its 2nd; `served-text-promising-an-absent-field` at its 4th | **CONTINUE** — window 4 opens, with a binding fallback recorded below | The window-2 checkpoint said a seventh instance would end this loop `ESCALATE-OPEN`. It happened, and I am not honouring that commitment as written — so the reasons are recorded rather than left implicit. First, the workflow is explicit that round count is never by itself grounds to escalate, and that escalation needs validation to be unavailable, severity unresolvable, residue unenumerable, or a critical finding with NO credible next action. None holds: every finding of this window is fixed, residue is zero, and the gate supplied a precise root cause. Second, and the reason the commitment's premise was wrong: the six earlier corrections each ADDED or REFINED a site that modelled placement, while this one DELETED the last such site and moved the requirement into the builders' own declarations. The count of sites between the fan-out and the wire that model placement is now zero, and a package-derived test fails if any builder reintroduces the refusal. That is a different kind of action, not a seventh refinement. **The binding fallback, so this does not rest on my judgment again:** if the next evaluation finds ANY new instance of this class, the outcome is `ESCALATE-OPEN` with no further reasoning; and if it finds any new finding at all in the placement surface, the placement work beyond the canonical root's own attestation is WITHDRAWN from this slice rather than corrected again. Trend: findings per round 4, 3, 1, 1, 2, 2, 5, 4, 3 — the count is not falling, and honesty requires saying so; what has fallen is the number of places the class can live, from several to none. |
| 4 | Stage-1 QA (roster loop 1) | window 4, evaluation 10 / cumulative 10 | `ba1be9f` + dirty (fully staged, 172 rows) | 0 raised, 0 unresolved | 5 raised, 0 fixed, **5 deferred** | recurring: `guard-modelling-one-spelling-of-a-two-spelling-fact` at its **8th** instance | **ESCALATE-OPEN** | The fallback recorded at checkpoint 3 binds and is honoured without further reasoning: the gate found another instance of the class and named the site. Two things are stated plainly rather than argued around. First, the OTHER half of that fallback — withdraw the placement work beyond the root's own attestation — is VOID, and this is the workflow's own rule that a deferral contradicted by an in-slice measurement is void: withdrawing component placement would restore QA-157-r1-01, a High-labelled finding of this same slice, and a critical finding may never be reinstated to close a standard one. Second, the residue is all Standard tier with zero Critical, which is a state the rules would normally let a slice defer past — but deferral requires an already-filed follow-up issue, and filing is owner-authorised in this repository, so the deferral has nowhere to land. That is precisely what `ESCALATE-OPEN` is for: the loop cannot converge on its own terms and the remaining decisions are the owner's. Named for the owner: whether governance or a caller's raw-XML placement wins (QA-157-r10-01), whether the served create templates should project each builder's declared keys rather than copy them (r10-02), and whether the folderless-create lint's advice should name the spelling that works (r10-03). Trend across ten evaluations: 4, 3, 1, 1, 2, 2, 5, 4, 3, 5 findings — not converging in this surface, while every other surface of the slice has been clean since round 1. |
| 5 | Stage-1 QA (roster loop 1) | window 5, evaluation 11 / cumulative 11 | `6e6b207` (committed) | 0 unresolved | 5 carried in from checkpoint 4 | — | **CONTINUE**, on the OWNER'S instruction | The escalation at checkpoint 4 named four decisions as the owner's and stopped. The owner's answer is to finish the implementation, which lifts the escalation and authorises the corrections the deferral was holding. Recorded here because a re-scope is itself a checkpoint decision and must not be silent: the five deferred rows move from `deferred` to correction, and the dispositions on their rows are amended in place with the reason. The product question the gate raised (QA-157-r10-01) is answered the way the tree's own sibling authority already answers it — the folderless-create lint exempts a raw-XML create because the author owns placement — so a component carrying its own placement in its own document declares that placement, and the fan-out does not claim it. The named finite correction: five fixes, an adversarial verification pass over them before any gate sees them, then the owed validation. |
| 6 | Stage-2 repo commit review (roster loop 2) | window 1, evaluations 1-3 / cumulative 3 | `ddb778e` (committed, clean) | 0 raised, 0 unresolved | 9 raised, 9 fixed, 0 unresolved | new: `request-decidable-answer-decided-mid-mutation` reaches its 2nd instance; `decision-applied-without-asking-the-route`, `validation-applied-to-one-of-two-entry-points`, `guard-reading-a-field-the-model-does-not-carry`, `decision-rebuilt-instead-of-asked` | **CONTINUE** — window 2 opens | The third evaluation carried one validated blocking finding, so the decision is recorded rather than assumed. The trend is unambiguous: 6 findings, then 2, then 1, each round scoped to the previous round's delta, every finding fixed and none carried. All three rounds are collected with attested run directories. The named finite next correction is already applied — the supplied reuse decision is honoured rather than re-derived — and round 4 validates it. Of the nine, five were defects this slice introduced and four were pre-existing shapes it newly reached; the P1 was a partial apply its own selected-index rule created, which is the same class the slice closed once before by hoisting a decision ahead of the writes. || 7 | Stage-2 repo commit review (roster loop 2) | window 2, evaluations 4-6 / cumulative 6 | `0bc76b3` (committed) + the round-6 correction | 1 raised (CDX-157-r5-01, P1), 1 fixed, 0 unresolved | 5 raised, 5 fixed, 0 unresolved | recurring: `decision-rebuilt-instead-of-asked` reaches its **5th** instance and takes its mandatory structural fix in this batch; new: `fixture-in-the-shape-the-code-produces`, `summary-assembled-without-its-required-authority`, `one-answer-read-as-three-questions`, `refactor-caught-by-a-swallowing-handler` | **CONTINUE** — window 3 opens, cumulative history kept | The third evaluation of this window carried validated blocking findings, so the decision is recorded rather than assumed. Every axis the rule names is improving or flat-at-zero: findings per round 6, 2, 1, 0, 4, 2; the P1 arrived and was closed inside the window; unresolved count is zero at the end of every round; and affected-class breadth narrowed from six subsystems in window 1 to two here. The recurring class was NOT instance-patched: `resolve_planner_binding` replaces the enumeration with the planner's own resolution, `_build_plan` calls it, the two sibling re-derivations are gone, and two witnesses construct the cases the hand-model got wrong in opposite directions. Honest counterweight: of the eleven findings in this window, nine were defects in corrections this same slice had just applied — the corrections are what keeps generating work, not the original surface. The named finite next correction is none; round 7 validates the batch. |

## Live evidence

| Round | Scope | Tree | Account accounting | Result |
| --- | --- | --- | --- | --- |
| Stage-1 QA r12 | acceptance for all eight corrections of the architect-review batch plus the carried-decision question | dispatched at COMMITTED `557f1b7`; the tree MOVED under the run (SELF-157-r5-05) and the gate refused to test it, driving every probe from byte-pinned archive extractions of `557f1b7` and `0bc76b38` instead, each re-hashed at scenario entry and exit | 18 components created and deleted (18/18), 4 folders created and deleted; final listing identical to the pre-round state | 7 new (0 Critical-labelled, 3 High, 1 Medium, 3 Low); r12-01 already discharged at the later SHA. The carried decision the batch introduced was PROVEN correct: two roots authored in the opposite order to their key sort produce unit 0 and unit 1 by AUTHORED order, with a non-vacuity arm that leaves the rule deferred and silent | report `agents/reports/2026-09-09-issue-157-r12.md` |
| Stage-1 QA r11 | acceptance for all five deferred findings and the self-audit's corrections, three raw-XML placement arms measured live, four closed-key types plus a `sync_pipeline` process under a governed root, and a sub-package mutant witness | COMMITTED `5949271`, 0 porcelain rows at entry and exit | 68 components created and deleted across twelve types; three folders plus the platform-materialised one removed | all five DISCHARGED; 5 new (4 Medium, 1 Low, 0 Critical), including a defect in the self-audit's own entity screen and the ninth instance of the recurring class. The gate REFUTED one hazard rather than banking it | report `agents/reports/2026-09-09-issue-157-r11.md` |
| Stage-1 QA r10 | acceptance for the r9 findings plus an `ast` census of all twenty create sites, a 154-document sentence sweep, and a package-injection mutant that grades the new guard through the real runner | uncommitted over `ba1be9f`, tree untouched; staged digest moved from r9's | 38 components created and deleted; `Home` removed explicitly | QA-157-r9-01 DISCHARGED live (all five owned types land in the requested folder), QA-157-r9-03 DISCHARGED with a mutant witness (a refusing sixth builder fails the suite), QA-157-r9-02 PARTIALLY; 5 new (3 Medium, 2 Low, 0 Critical) and an EIGHTH instance, named. The gate also recorded two vacuity traps in its own harness that only its armed controls made visible | report `agents/reports/2026-09-09-issue-157-r10.md` |
| Stage-1 QA r9 | acceptance for all four r8 findings, a six-spelling apply, a 380 kB served-text sweep with three armed controls, and the checkpoint question asked once more | uncommitted over `ba1be9f`, tree untouched; staged digest moved from r8's | 36 components created and deleted across twelve types; seven folders and the platform-materialised one removed | all four r8 findings DISCHARGED; 3 new (1 High, 1 Medium, 1 Low, 0 Critical). The gate answered the checkpoint question in the words it was asked and named the site: the round-8 fix itself, asking a builder the wrong question | report `agents/reports/2026-09-09-issue-157-r9.md` |
| Stage-1 QA r8 | acceptance for all four r7 findings, a five-spelling apply graded against three independent sources, a 133-envelope mechanism sweep with four controls, and a second adversarial pass | uncommitted over `ba1be9f`, tree untouched | 29 components created and deleted; six provisioned folders and both platform-materialised ones removed explicitly | all four r7 findings DISCHARGED; 4 new (2 High, 2 Medium, 0 Critical) and a SIXTH instance naming three predicting sites. The gate also recorded what it probed clean: a failed create makes zero placement claims, `processproperty` is refused pre-write, and the document route's own id row is honest | report `agents/reports/2026-09-09-issue-157-r8.md` |
| Stage-1 QA r7 | acceptance for both r6 findings, four spellings live in one apply, and an ADVERSARIAL pass against the design itself rather than the findings | uncommitted over `ba1be9f`, tree untouched; staged digest moved from r6's | 31 components created and deleted, `net_zero: true`; `Home` and both route-materialised folders removed explicitly | QA-157-r6-01 DISCHARGED; QA-157-r6-02 only PARTIALLY discharged, its remediation half measured broken; 5 new (3 High, 2 Medium, 0 Critical). The gate answered the question it was asked in the words it was asked for: a FIFTH instance, three sites still predicting what a route submits — and named the reason, that the route already returns what it sent and the arm ignored it. Probed and found nothing on three other shapes it was asked to attack | report `agents/reports/2026-09-09-issue-157-r7.md` |
| Stage-1 QA r6 | acceptance for both r5 findings, the three spellings live in one apply, a 77-envelope sweep with two armed controls, the four mechanisms and a thin regression pass | uncommitted over `ba1be9f`, tree unmoved; staged digest moved from r5's | 23 components and 2 folders created and deleted, `Home` removed explicitly | both r5 findings DISCHARGED, no earlier finding regressed; 1 High and 1 Medium new, 0 Critical, and the gate confirmed BOTH sit inside the placement surface this loop has been circling with nothing new found elsewhere | report `agents/reports/2026-09-09-issue-157-r6.md` |
| Stage-1 QA r5 | acceptance for QA-157-r4-01, all four placement mechanisms each on its own live case, and a thin regression pass | uncommitted over `ba1be9f`, tree untouched; staged-content digest moved from r4's, independent proof the tree changed | 23 components created and deleted, 0 leftovers across seven types and three prefixes; the platform-created `Home` folder removed explicitly | QA-157-r4-01 DISCHARGED as written, no earlier finding regressed; 1 High and 1 Medium new, 0 Critical — both in the correction's own declaration, which was wider than the limit it modelled | report `agents/reports/2026-09-09-issue-157-r5.md` |
| Stage-1 QA r4 | acceptance for QA-157-r3-01 in both directions, an envelope-wide contradiction sweep with an armed control, the four resolver answers in one apply, the id-driven warning, and a reduced-breadth regression pass | uncommitted over `ba1be9f`, tree untouched; the staged-content digest moved from r3's to this round's, which is the independent proof the correction is what was measured | 32 components and 4 folders created and deleted; closing sweep over seven types `net_zero: true` | QA-157-r3-01 DISCHARGED in both directions and the class CLOSED not moved; no earlier finding regressed; 1 Medium new (QA-157-r4-01), 0 High, 0 Critical. The platform was measured to REFUSE an unknown, deleted or padded `folderId` rather than ignore it, so the id-driven unhonoured branch is reachable only through the one create route that cannot submit an id | report `agents/reports/2026-09-09-issue-157-r4.md` |
| Stage-1 QA r3 | acceptance for all three r2 findings, the type-scope edge case, and a reduced-breadth regression pass over every r1 acceptance | uncommitted over `ba1be9f`, tree untouched (167 porcelain rows, staged-content digests pinned independently at entry and exit) | 75 components and 3 folders created and deleted; closing sweep over six types finds 0 leftovers | all three r2 findings DISCHARGED, no r1 regression; 1 High new (QA-157-r3-01). The empty-update refusal for the two profile/map types was measured as PRE-WRITE, so no sibling of the partial-write shape is reachable. Recorded instrument note: the freeze guard's `dirty:` half hashes porcelain plus the UNSTAGED diff, so a fully staged slice reads the same stamp across rounds; QA pinned staged-content digests independently | report `agents/reports/2026-09-09-issue-157-r3.md` |
| Stage-1 QA r2 | fix-delta acceptance for all four r1 findings, the honesty and ambiguity controls, the legacy-route boundary, the update-binding control, the served texts, a late-binding proof and the shared-apply regressions | uncommitted over `ba1be9f`, freeze `ba1be9f34317/dirty:3ab624dc2a61`, PRE==POST==EXIT on all 15 runs, `worktree_moved=False` | 81 components and 10 folders created and deleted; closing sweep `net_zero: true` | all four r1 findings DISCHARGED; 2 High, 1 Low, 0 Critical new. Placement measured 8/8, 10/10 across two roots and 8/8 on the recipe intent; late binding proven twice, including one `plan_fingerprint` across two folder generations with different submitted-bytes digests; report `agents/reports/2026-09-09-issue-157-r2.md` |
| Stage-1 QA r1 | the plan §13 naming/folder governance scenario on both compiling intents, the multi-root control, and the negative probes for every new named diagnostic | uncommitted working tree over `ba1be9f`, freeze stamp `ba1be9f34317/dirty:73d99998d023/8f7c877c3c83`, PRE==POST==EXIT | 48 components created and 48 deleted, 8 folders created and deleted, prefix sweeps clean — net zero | 2 High, 1 Medium, 1 Low, 0 Critical; report `agents/reports/2026-09-09-issue-157-r1.md` (untracked by design: `agents/` is gitignored, so the report text is also quoted in this slice's final report) |

Archive root: `docs/architecture/evidence/issue-157/`. Every collected gate round is archived
there as it is collected (`scripts/archive_gate_round.py`), indexed in its `index.jsonl` and
hash-pinned in its `SHA256SUMS`, all tracked in git.

## Gate evidence

| Gate | Round | Run directory | Reviewed SHA | Outcome | Teardown |
| --- | --- | --- | --- | --- | --- |
| Stage-2 commit review | 1 | `cdx-review.oJAVqg` | `a80ab2b` | `completed`, 6 findings (1 P1, 5 P2), all validated and fixed | confirmed stopped |
| Stage-2 commit review | 2 (fix delta) | `cdx-review.tdJpns` | `a8abefe` | `completed`, 2 findings (P2), both validated and fixed | confirmed stopped |
| Stage-2 commit review | 3 (fix delta) | `cdx-review.wqssw8` | `ddb778e` | `completed`, 1 finding (P2), validated and fixed; checkpoint recorded | confirmed stopped |
| Stage-2 commit review | 4 (fix delta) | `cdx-review.j03yRY` | `6e38025` | `completed`, **CLEAN** — no regressions found | confirmed stopped |
| Stage-2 commit review | 5 (fix delta over the architect corrections) | `cdx-review.WoHUEv` | `557f1b7` | `completed`, 4 findings (1 P1, 3 P2), all validated and fixed | confirmed stopped |
| Stage-2 commit review | 6 (fix delta) | `cdx-review.vtNvH0` | `0bc76b3` | `completed`, 2 findings (P2), both validated and fixed; checkpoint recorded | confirmed stopped |
| Stage-2 commit review | 7 (fix delta) | `cdx-review.0176p1` | `78cf7e7` | `completed`, 2 findings (P2), both validated and fixed | confirmed stopped |
| Stage-2 commit review | 8 (fix delta) | `cdx-review.mfQy1C` | `993d9db` | `completed`, **CLEAN** — "No actionable regressions were found in the scoped diff" | confirmed stopped |
| architect implementation review | 2 | `/tmp/cdx-gate-review.VsHZ3l` (archived under `docs/architecture/evidence/issue-157/`) | `6e217b0` | `completed`, attested `ok:true` against the plan's own bytes; `VERDICT: ISSUES FOUND`. Three of the seven evaluation-1 findings CLOSED on this tree (e1-03, e1-05, e1-07), four partial, and five new conformance findings (ARCH-157-e2-01..05) | confirmed stopped |
| architect implementation review | 1 | `/tmp/cdx-gate-review.sP9bAv` (archived under `docs/architecture/evidence/issue-157/`) | `140f5e7` | `completed`, attested `ok:true` against the plan's own bytes; `VERDICT: ISSUES FOUND`, 7 conformance findings (ARCH-157-e1-01..07), all validated and fixed | confirmed stopped |
| architect plan | 1 (turn 1 + in-session retry, one collected round) | `/tmp/cdx-gate-architect.sxpAQQ` (removed after copy to `.codex/plans/issue-157.md` + `.attest.json`) | n/a (plan over `ba1be9f`) | `completed`, attested `ok:true`; turn 1 interrupted on an MCP tool-approval elicitation, turn 2 (retry) certified | confirmed stopped |

## Final-tree validation (filled at close; every roster gate current on the FINAL sha)

| Gate | Evidence (quoted output / run URL / archived round) | SHA |
| --- | --- | --- |

## Closing report — ESCALATE-OPEN, issue #157 stays OPEN

**Status: INCOMPLETE. The issue is not closed and nothing is pushed.**

**Last validated tree**: `9ae1222`, the Stage-1.5 commit of the QA-validated working tree on
`codex/issue-157` over baseline `ba1be9f34317d720d3b012484622ab7e2742143b`. That commit is a review
boundary, never completion. The full non-KB suite is green on it: 11,772 passed and 19 skipped in
the fast part, 153 passed in the reachability-freeze file, zero failures; the node manifest reports
11,944 required nodes and 76 active goldens.

**Gates run**: Stage-1 live QA, ten evaluations, all through the public MCP tool boundary against
the live `renera` account, each with its own freeze attestation and net-zero accounting. Forty-one
findings were raised across them and thirty-six fixed; five are deferred residue, all Standard tier,
all in one surface.

**Gates NOT run, and therefore owed before this slice can close**: the Stage-2 detached Codex commit
review, the §6 architect implementation review, and the composite wave gate. They are unrun because
the Stage-1 loop escalated, not because they were skipped or waived.

**Why it escalated**: one defect class recurred eight times, always in how a created component's
placement is resolved, submitted, attested and explained. Six corrections modelled or predicted what
a route submits; three more asked or read. Each closed the reported sites and the next round found
another. The checkpoint-3 fallback bound this run to escalate on a further instance, and the tenth
round found one and named the site.

**What the owner has to decide**, none of which this run should decide alone:

1. Whether governance or a caller's own raw-XML placement wins when both are present
   (QA-157-r10-01). The predicate's docstring commits to the caller; the sibling folderless-create
   lint exempts raw-XML creates on the same premise; the fan-out currently overrides both, silently.
2. Whether the served create templates should PROJECT each builder's declared key set rather than
   carry a second copy of it (QA-157-r10-02).
3. Whether the folderless-create lint's advice should name the spelling that actually places
   (QA-157-r10-03).
4. Whether a follow-up issue should be filed to carry the five deferred rows. Filing is
   owner-authorised in this repository, which is why the deferral has nowhere to land and why the
   outcome is escalation rather than `DEFER-STANDARD-AND-PROCEED`.

**What is complete and validated**, and is what the commit contains: every in-scope item of the
issue other than the placement residue — the typed per-root governance envelope with prefix-derived
names and the folder fan-out, the connection binding contract, the typed derived-only flows
projection with its persistent row-level baseline, the recorded-not-wired advisory channel, the
watermark and scheduling splits, the ten inert-metadata retirement proofs, the one canonical
required-target-leaf validator replacing six legacy sites and reached by both entry points, and the
served schemas, capabilities and terminology for all of it.
