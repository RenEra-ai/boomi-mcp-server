# Audit ledger — issue #184 (M12.24 ProcessIR native document-sequence composition and non-returning staged workflows, bridge A)

Audit record for the completion workflow (`CLAUDE.md`, amended 2026-08-12 / -08-14; standing
rules in `docs/architecture/COMPLETION_WORKFLOW_RULES.md`).

Plans of record, each attested by the dispatcher-owned gate collector:

| Plan | Attestation | Message sha256 | Thread | Result |
| --- | --- | --- | --- | --- |
| `.codex/plans/issue-184.md` — the architect design plan | `.codex/plans/issue-184.attest.json` | `cc60610bf32775899ab1c67df4e863407043e55ac81aaaeace081be48c445980` | `01a098f7-b151-7d51-a9c3-4458351d4efe` | turn token 1, gate `architect`, `ok:true`/`stopped:true`, teardown `confirmed` |
| `.codex/plans/issue-184-amendment-1.md` — the passthrough entry and form-correct child contracts | `.codex/plans/issue-184-amendment-1.attest.json` | `7f8eb9f8556a3c32f8d09a78422a375443bae8d5eb1cc755a878efbfb7e11c88` | `01a0993f-6b0c-7f13-be60-7d48320fef46` | turn token 1, gate `architect`, `ok:true`/`stopped:true`, teardown `confirmed` |
| `.codex/plans/issue-184-amendment-2.md` — evidence corrections and the safe survivor replacement | `.codex/plans/issue-184-amendment-2.attest.json` | `f45fe41883f1dc82774243ea2088aa394bb8ebacd2006eaa8d44a244138f2ef3` | `01a09949-d54a-73c2-be84-e663c02104cb` | turn token 1, gate `architect`, `ok:true`/`stopped:true`, teardown `confirmed` |
| `.codex/plans/issue-184-amendment-3.md` — runtime document emission, retrieve-property overlay, and the attested child hand-off (answers ledger rows E0-184-01..03 and E1-184-01..02) | `.codex/plans/issue-184-amendment-3.attest.json` | `21d93e0fc9605537264f591c5ba857eedf5b4db9db7956fcc74ecfa09030f5ff` | `01a099c6-2160-7d63-b090-e828169a9400` | turn token 1, gate `architect`, `ok:true`/`stopped:true`; the session ran against a detached worktree at `702b883`, so later edits to this tree were never visible to it |

Claude's implementation plan is `.codex/plans/issue-184.claude.md` (decisions D1–D12; its middle
file-by-file section was lost to a delivery failure and is covered by the design plan §3–§4). Later
plans override earlier ones where they say so. Where any plan and this ledger disagree, the
correction is recorded here with its evidence and the plan is NOT silently re-authored.

## Stage-1 step 0 — baseline

| Field | Value |
| --- | --- |
| Issue | #184 — M12.24 native document-sequence composition and non-returning staged workflows (bridge A) |
| Branch | `codex/issue-184` |
| Branch point | `dev` @ `cbab28ffc176ddb2378283cf7132eb9c37afc374` (closing commit of #158) |
| Pre-baseline commit | `73b9d3ddc8e547eba09b857275050fb336c06b59` — the frozen legacy both-sides oracle only (see below), required by A8 to precede the step-0 baseline; dark (no `src/` diff from the branch point, measured), full non-KB suite `12300 passed, 19 skipped, 19 warnings in 1073.10s (0:17:53)`, wave manifests `manifests ok (12319 required nodes, 79 active goldens)`; reviewed clean by loop L0 below |
| Step-0 baseline (`$BASELINE`) | `73b9d3ddc8e547eba09b857275050fb336c06b59` (printed by `git rev-parse HEAD` immediately after the oracle commit, before any behavioural edit) |
| Reference suite at the branch point | `12298 passed, 19 skipped, 19 warnings in 1001.00s (0:16:41)`, exit 0 — 12317 collected, equal to the recorded floor (`tests/fixtures/wave_gate/test_nodes.jsonl` header `minimum_collected` 12317); 78 active goldens — full non-KB, `python -m pytest tests --ignore=tests/kb -p no:cacheprovider`, local `.venv` 3.12, `PYTHONPATH=src`, detached worktree of the branch point, run to completion BEFORE any edit |
| Slice kind | behaviour-affecting (root and body grammar admissions, a derived per-path stream/profile proof, a lineage-carried cache-content profile fact, general document-property invalidation at stream-replacing reads, terminal-call prefix admission, a Data Passthrough entry form with form-correct child contracts, served-contract and revision changes) |

### Environment corrections recorded at step 0

1. **The architect gate sessions were told not to call Boomi MCP tools.** A Boomi MCP call parks a
   headless Codex turn on an elicitation it cannot answer; every brief grounded the plan in source
   reads and quoted documentation. All three plan turns completed on their primary prompts.
2. **The branch-point predicates were re-measured in the main thread, independently of the plans.**
   Every row of the issue's "Current predicates" table reproduced exactly — code, pointer and phase —
   through `parse_process_ir_v1` and `compile_process_ir_v1` against a pristine `git archive`
   extraction of the branch point. The probe and its output are archived under
   `docs/architecture/evidence/issue-184/predicates/`.
3. **No Boomi MCP server is connected to this session, and the local docs knowledge base exists only
   in the deployed container.** Platform facts were verified against the official help.boomi.com
   pages directly and quoted verbatim below.

## Loop roster (enumerated in advance, before the first correction)

| # | Logical loop | Authority | Scope |
| --- | --- | --- | --- |
| L0 | Pre-baseline oracle commit review (dark) | detached Codex review per `CLAUDE.md` §5b–5e | the oracle commit only, `--base cbab28ffc176ddb2378283cf7132eb9c37afc374`; run `cdx-review.w3jzMM` collected `STATUS: completed`, `SCOPE: branch diff against cbab28ffc176ddb2378283cf7132eb9c37afc374 (cbab28f) head=73b9d3ddc8e547eba09b857275050fb336c06b59 dirty=false`, no findings; archived |
| 1 | Stage-1 QA | `boomi-qa-tester`, live through the public MCP tool boundary | each admitted form separately, both child entry forms, the combined two-leg examples, the `EVAL-155-02` composite execution, and the named negative neighbours |
| 2 | Stage-2 repo commit review | detached Codex review per `CLAUDE.md` §5b–5e | initial: `--base $BASELINE`; then each fix delta |
| 3 | Architect implementation review | `/codex-issue` §6 gate (`--gate review`) | implementation vs the plans of record; **capped at 3 evaluations** by `docs/architecture/COMPLETION_WORKFLOW_RULES.md` |
| 4 | Composite wave gate | `scripts/wave_gate.py` | full suite, golden manifest, determinism, wave-delta review, one live scenario per changed capability class |
| 5 | Terminal correction loop | only via a recorded roster-addition checkpoint | that batch |

Two activities debit no loop because they validate nothing the slice changed: the pre-baseline
legacy-oracle capture (run from a pristine extraction of the branch point with no source change),
and the pre-implementation live evidence round `e0` (captures of platform behaviour from
causally independent fixtures, run from the branch-point worktree, feeding the admission rows).

## Pre-baseline legacy-oracle capture (A8)

A8 defines the mapped both-sides form by the legacy renderer's spine and requires the oracle to be
frozen before the step-0 baseline. None of the six existing dynamic-path goldens binds more than one
connector, so none is an oracle for it.

| Field | Value |
| --- | --- |
| Producer | `ProcessFlowBuilder.build(config, name="Dynamic Path Both Sides Golden", folder_name="Golden/Fixtures")` — the legacy renderer, never the canonical compiler |
| Config | the attested design's frozen config (`.codex/plans/issue-184.md` §3): REST GET source whose path composes from the run-supplied process property `seed_id`; map `MAP-UUID`; REST PATCH target whose path reads element 3 of `PROFILE-REQUEST` |
| Validator | `ProcessFlowBuilder.validate_config(config, depends_on=[], allow_rest_source=True)` returns None (measured) |
| Executed from | a pristine `git archive` extraction of the branch point, `.venv` symlinked, import origin asserted inside the extraction |
| Emitted spine (measured) | `start → documentproperties → connectoraction(GET, 1 dynamic Path property) → map → documentproperties → connectoraction(PATCH, 1 dynamic Path property) → stop` |
| Bytes / sha256 | 4471 / `633a197f3b67a0773a828acb378451d64183ca63a8c5ef60ddb095bbc053aede` |
| Determinism | two builds compared equal at capture |
| Archive | `docs/architecture/evidence/issue-184/oracle/` — capture script, input, output bytes, manifest |
| Registered as | `golden-000079` (`dynamic_path:both_sides`, owner `#184`, `transitional_oracle`) in the oracle commit |
| Provenance class | **legacy-oracle parity capture** from a tree causally independent of the implementation under test |

### Owner decision recorded at step 0 — both child entry forms

The implementation plan's D3 asked whether a child invoked by a terminal `process_call` receives
its caller's documents, because every canonical root emits a scheduled No Data Start. Verified
against official Boomi documentation (the Process Call step page): for a non-passthrough subprocess
"Documents are not passed into the subprocess" and "The parent process calls a new instance of the
subprocess for each document that reaches the Process Call step"; a passthrough subprocess receives
"any documents that reach the Process Call step as a group" in "one instance of the subprocess".
The #156 archive agrees: its No Data recovery child got its own execution record, one inbound
document per invocation.

The question was put to the owner. Decision, verbatim: "Well, we need to be able to support both
cases - most use of subprocesses use pass through but depending on logic it could be no data and in
this case we see the sub process as a separate in log, which could be useful for some cases, check
docs on this topic and apply what should we actually have for this case." #184 therefore supports a
Data Passthrough child and a No Data child, each with its documented semantics; the design is the
attested amendments 1 and 2.

### Verification of the amendments' load-bearing claims (main thread, before adoption)

Each claim was checked against its cited source, not accepted from the plan's prose.

| Claim | Cited source | Verdict | Evidence |
| --- | --- | --- | --- |
| a passthrough child must be called with wait=true | help.boomi.com Process Call step | **confirmed on re-fetch** (the first fetch wrongly reported it absent) | procedure step 4: "If the process being called is a passthrough process (Data Passthrough is selected in its Start step dialog), this check box cannot be turned off." The first fetch asked a summarizing model whether such a sentence existed and it answered "no"; amendment 2 disputed that, and a fetch reproducing the numbered procedure verbatim found it |
| passthrough reporting folds into the parent log; a No Data child gets its own execution record | same page | **confirmed** | "Process Reporting includes information about each subprocess as a step within the parent's process log. There are no separate execution records or logs for the subprocesses." / "Process Reporting contains a separate execution record and process log for each subprocess." |
| parent-set dynamic document properties are used by children receiving those documents | help.boomi.com Dynamic Document Property Examples | **confirmed on re-fetch** (the first fetch wrongly reported the page silent on parent/child) | the "Abstract profiles" example describes a shared child process that works provided each parent process sets the necessary dynamic properties before passing documents to it. Documented behaviour; the canonical bound-path use across the boundary is still unmeasured, so amendment 2 keeps it fail-closed until capture `cap184-passthrough-ddp-handoff` attests it |
| process properties are shared parent to child and child to parent after completion | help.boomi.com Process Property components | **confirmed** | "a process property value that is accessed or set in a parent process can be retrieved later in a child process. The opposite is also true…" |
| a document cache is shared between parent and child within one execution | help.boomi.com Document Cache components | **confirmed** | "A document cache can be shared among parent and child processes. You can add documents to the cache in the parent process, and those documents are available in any child processes." |
| the passthrough option bytes come from a UI-built pre-baseline capture | `docs/architecture/evidence/issue-175/stage1-qa-round-4.md` | **confirmed** | records the capture as "a UI-built live capture frozen 2026-07-03 — causally independent of the code under test" |
| the replacement survivor's bytes can come from `cap155-e1-source-dynamic-path` | `docs/architecture/evidence/issue-155/captures/cap155-e1-source-dynamic-path/` | **confirmed** | the directory holds `stored_process.xml`; `execute.json` supplies the process property `key` = `AC-0001`; the counterparty log reads `"GET /admin/cdscm/api/v1/clients/AC-0001 HTTP/1.1" 200 OK` and `"PATCH /admin/cdscm/api/v1/clients/AC-0001 HTTP/1.1" 200 OK` |

Code references in amendment 1 were checked for existence, not assumed: all fourteen test modules
its test table names exist; the execution-profile behaviour oracle it says must widen is
`authoring/contract.py` `_execution_profile_behaviour_oracle`; the asynchronous-ordering checks it
extends live in `semantic_validation/effects.py`; the reviewed placement prose table and the child
inspectability kind sets it cites exist in `authoring/process_ir_projection.py` and
`authoring/process_ir_effects.py`.

The two disputed citations and the survivor resolution were taken back to the architect as a dispute
round. `codex-drive start` refuses `--resume` together with a gate prompt hash, so the round ran in a
FRESH gated architect session with amendment 1 inlined verbatim by `cat` (never retyped) rather than
as an unattested resumed turn; its attested delta is amendment 2.

### Data Passthrough Start provenance source (pre-baseline, platform-authored)

`tests/fixtures/live_xml/m11/` holds four live captures committed at `e512c24` (2026-07-02, #119 census),
long before this slice's baseline, whose Start is `<passthroughaction/>`:
`process_setproperties_ddp_dpp_current_crossref.xml`, `process_cache_branch_load_remove.xml`,
`process_dpp_profile_decision_flow.xml`, `process_doccacheretrieve_loadalldoc_variant.xml`. Their
`<process>` open tag reads `allowSimultaneous="false" enableUserLog="false" processLogOnErrorOnly="false"
purgeDataImmediately="false" updateRunDates="false" workload="general"` (measured on two of the four),
with no `stopProcessingIfZeroDocuments` attribute. Provenance class: **live capture of a
platform-stored process**, causally independent of the implementation under test. Whether the
platform DELIVERS a caller's documents to such a child is not in these captures and is owed to
live evidence (`e0`) and Stage-1 QA.

### Pre-implementation sweeps (scratch worktrees of the branch point, never the working tree)

| Probe | Patch | Full non-KB suite | Non-vacuity witness |
| --- | --- | --- | --- |
| General document-property invalidation at stream-replacing reads (A5) | the general lattice also drops document-scoped keys where the property-survival table does not record `survives`, keeping keys the replacing node itself writes | `12298 passed, 19 skipped, 19 warnings in 1037.97s (0:17:17)` — no existing test changes | the issue's stale-read probe becomes refused (classified `PROCESS_IR_SEMANTIC_LINEAGE_DDP_SCOPE_INVALID` at `/body/steps/4`; the implementation must classify it read-before-write) |
| Absent-stream refusal for map and cache-put consumers (A4) | the per-path walk runs on binding-free documents too and refuses a map or cache put with no producer upstream | `12298 passed, 19 skipped, 19 warnings in 1046.30s (0:17:26)` — no survivor changes | both absent-stream probes become `PROCESS_IR_SEMANTIC_CARDINALITY_MISMATCH`; the staging skeleton still compiles |

So the A5 same-slice fixture sweep set is EMPTY at the branch point (measured, not assumed), and A4
breaks no survivor. The patches and witness outputs are recorded in
`docs/architecture/evidence/issue-184/predicates/SWEEPS.md`.

## Plan corrections (recorded before the first correction is applied)

| # | Plan said | Actually | Evidence |
| --- | --- | --- | --- |
| C1 | the design plan: the universal property-profile proof conflicts with active survivor `golden-000072`, which blocks completion until an initial-document authority or an acceptance-policy amendment exists | resolved by amendment 2 without a policy amendment: under the documented No Data semantics the survivor's first writer reads a profile element from the single empty start document, so its bound GET path gets an empty segment and addresses a different resource. It is not a VALID survivor, which is all A9 protects. A profile-valued source on the empty entry stream of a scheduled root is refused `PROCESS_IR_SEMANTIC_PROFILE_MISMATCH` at `/body/steps/0/source_values/1/profile_ref`; `golden-000072` is retired through the manifest's active-to-tombstone transition and replaced by a safe survivor whose path composes from a run-supplied process property, its expected shapes cut from `cap155-e1-source-dynamic-path` | `tests/fixtures/process_ir/issue155/source_dynamic_path_profile.json` (profile source at index 1); the precedent in `tests/fixtures/process_ir/issue155/PROVENANCE.md` (the canonical chain refuses the unsafe legacy DDP path shape and freezes a safe equivalent); the Start step page ("a single empty document is forwarded through the process flow") |
| C2 | the implementation plan D1: a profile writer on the untouched entry stream records an entry requirement, keeping `golden-000072` unchanged | superseded by amendment 1's entry model: a scheduled root's entry is the single EMPTY document (a profile read there is refused, C1); only a Data Passthrough root's entry carries caller documents, and only there does an entry-position profile consumer define a requirement a caller discharges | amendment 1 §2; the Process Call and Start step pages |
| C3 | the design plan: no new entry form; the implementation plan D3: escalate if a No Data child receives no documents | superseded by the owner decision: a third entry form (Data Passthrough) is added, with form-correct child contracts for both forms | the owner decision above |
| C4 | amendment 1: the catch-body non-notify prefix refusal keeps its `/steps/<offender>` pointer (implementation plan D9) | A6 requires the residual prefix refusal at `/…/terminal`; amendment 1 adopts `/…/terminal` and this ledger follows A6 | issue #184 A6 text |
| C5 | the implementation plan D2 consumer table: a call fed by a cache read is checked when it declares an input, at `/operation_ref` | not implemented. A cache's content may itself be a call's output, so comparing it with the next call's input is call-to-call profile equality, which D4 leaves unchecked because connector request and response profiles are documented as non-validating. A wrong map ahead of the write is already refused at the map, and a write that contradicts the cache's declared profile at `/cache_ref`. Only a map-origin stream is compared with the call it feeds, at the map's own `/map_ref` (#140's pointer) | the D4 rationale in `connector_resolution.py` (`validate_connector_call_semantics`); the connector-call branch of the stream step in `semantic_validation/lineage.py` |
| C6 | the implementation plan D2: the legacy flag means a legacy `source` is upstream on the path, and it is sticky | scoped to the dialect. A consumer is exempt only while the documents reaching it were last produced by a legacy `source` (a first-class call clears the flag, as the old walk re-checked a bracketed map after a call even under a source root), or when its subtree ends only at a legacy `target` with no connector call. Measured: goldens 000022/040/041/078 are the adapter spine `[listener, map_ref, target, stop]` with symbols that declare no profile at all, so their maps were unchecked since #158 and nothing about them is provable. The plan named this exact case as possible owner input "if adapter symbols lack map profiles"; they lack every profile and sit on a legacy `target`, which is the genuine legacy dialect the design plan exempts by name. A document-scoped flag was rejected on measurement: the model admits a connector-call leg beside a legacy-target leg under a control-only root, so it would have exempted the call leg's maps | scratch capture of the adapter IR and symbols for the four goldens; the mixing probe (`leg_target_under_root_call` and `leg_call_then_target_under_control_root` refused `PROCESS_IR_SCHEMA_INVALID_CARDINALITY`, sibling call and target legs admitted); `tests/test_issue_184_stream_profiles.py` legacy-scoping tests |
| C7 | the implementation plan D2: a Message or Data Process after the entry leaves an entry stream that is no longer pristine | kept as a separate "touched entry" state with no producer: a map or cache write there stays a cardinality refusal owned by connector resolution (A4), with no second profile finding beside it; a profile-valued source there is not checked, because a Message may build a document of that profile; a profile-valued source on the untouched empty entry is refused (amendment 2 §4). Document existence and stream profile therefore agree: exactly the no-producer points of the connector walk are the three no-producer stream states | `semantic_validation/lineage.py` stream states; `tests/test_issue_184_absent_stream.py` (`message_is_not_a_from_nothing_producer`) |
| C8 | the implementation plan D11: keep #141's pending-map obligation inside try and catch bodies as an error-context rule in the lineage controller | kept in connector resolution instead, verbatim, restricted by a per-path flag set when the walk passes a `try_catch`. The rule exists there already; restating it in the lineage controller would be a second copy of one rule. Everywhere else the bracketing half is removed and the stream-profile proof owns maps | `connector_resolution._PathState.error_context`; the existing error-handling and notify-recovery suites pass unchanged |
| C9 | none of the plans | the lineage controller now raises #140's profile code at maps, cache writes and property sources that connector resolution never visits, so the compile route had no delegated original to restore its static text from and would have served the generic fallback. The compile gate now rebuilds a code's static compiler text by code when no delegated original exists and the code has no validation-table text | `compiler/process_ir/pipeline.py` `_enforce_semantic_report` |
| C10 | the implementation plan D12's non-vacuity witness: run the old recursive walker from a `git archive` of the step-0 baseline against the new controller, over every committed IR fixture plus long generated cases | anchored at the stream-profile commit `2758e03` instead. The step-0 walker predates this slice's intended lineage changes (A5 invalidation, the stream-profile proof), so its findings differ by design; only the last recursive walker isolates the refactor. Scope: all 40 ProcessIR documents under `tests/fixtures` × 4 corpus symbol tables, plus two generated 300-step cases, = 160 walks. Result: 152 identical in findings, required reads and guaranteed writes; the 8 walks the old walker truncated (the generated cases) all keep every old finding in the new walk; 0 mismatches. A first run scanned only `tests/fixtures/process_ir` (27 documents) and was widened after the count was measured. Withdrawn along with the bound: the walk's `truncated` field, the `walk_truncated` inert reason, and its served clause. A non-tree graph now raises `PROCESS_IR_COMPILE_INTERNAL` | `docs/architecture/evidence/issue-184/sweeps/d12-controller/`; `tests/test_issue_184_lineage_controller.py` |
| C11 | C7 above: "exactly the no-producer points of the connector walk are the three no-producer stream states" | amendment 3 §3 separates two facts that C7's wording merged. The first is whether a document ARRIVES to trigger a step: a scheduled Start supplies one empty trigger document, and a cache load or an all-document remove exhausts its path. The second is whether the arriving payload is USABLE by a map or a cache write. A4's refusals on the empty entry are the compiler's usable-payload requirement; they are not evidence that a No Data Start emits zero documents. A cache read replaces the stream only when a document triggers it, and it cannot restart an exhausted one. A declared non-producing call followed by a cache read is refused at the call's `/operation_ref`, and `cap184-send-then-read` stays OPEN | rows E0-184-01, E0-184-03, E1-184-01; `agents/reports/2026-09-13-issue-184-evidence-e1.md`; amendment 3 §1, §3 |
| C12 | the design plan's A5 and this slice's A5 increment `703ba44`: a cache read loses every document-scoped property (the survival table's "lost" cell), extended to ordinary reads | contradicted by measurement. With exactly one current and one cached document, a triggered retrieve carries the cached document's DDPs overlaid on the current document's. On collision the cached value wins (R1), a current-only name survives (R2, R4), and the cached value alone is kept (R3). N current × M cached is OPEN. Amendment 3 §7 replaces invalidation with a bounded overlay over frozen cache cohorts with writer alternatives. The A5 probe's linear `cache_put → cache_get` graph never executed its read, so it is refused for its dead successor, and a staged equivalent (terminal write leg, separately triggered read leg) becomes the positive witness | row E0-184-02; `docs/architecture/evidence/issue-184/captures/cap184-retrieve-ddp-replacement/`; amendment 3 §7 |
| C13 | the branch point's cache adjacency rules: the model requires a cache read straight after a cache write (`_check_cache_put_followed_by_read`), tolerates a Stop or Exception after a trailing cache write (`_check_trailing_cache_put` and its terminal set), and the emitter registry gives cache load and remove a continuing successor. The design plan's matrix row `[call, cache_put, cache_get, stop]` is admitted | every one of these authorizes a dead edge: the platform skips the successor and reads COMPLETE. They are replaced by one terminal cache-action verdict derived from a document-emission authority. An authored successor of `cache_put` or `cache_remove` is refused `PROCESS_IR_SCHEMA_INVALID_CARDINALITY` at the cache node's `/cache_ref`. `cache_put` stays terminal in its Branch-leg and catch slots, and `cache_remove` gains a Branch-leg terminal only. The emitter registry gives load and remove exactly zero outgoing edges, and the renderer and graph verifier refuse an outbound wire. On the legacy route, the inline remove transform, flow-sequence cache successors and a DLQ cache with an authored Exception are refused, and the synthetic Stop after terminal DLQ staging is omitted | amendment 3 §2, §4, §5; the active-golden scan in C15 |
| C14 | amendment 2: the DDP hand-off into a passthrough child stays fail-closed until a capture attests it; amendment 1's prefix rows are keyed by context and direct predecessor only | `cap184-passthrough-ddp-handoff` discharged the gate for a waited passthrough child: the child, which has no Set Properties step, bound three inherited paths that matched their own payloads. Prefix admission is keyed by (context, direct predecessor, child entry form, wait), and only passthrough with wait=true is admitted. The per-document No Data capture attests no prefix, so No Data prefixes stay refused `PROCESS_IR_CAPABILITY_PROCESS_CALL_PLACEMENT_UNSUPPORTED` at `/…/terminal`. Passthrough wait=false stays refused under a new entry-context capability code at `/…/wait`. Child contracts are derived without requiring effect declarations and discharged per invocation. Direct execution of a passthrough process runs as No Data, so a called contract does not establish standalone validity | `agents/reports/2026-09-13-issue-184-evidence-e0.md`; amendment 3 §8 |
| C15 | amendment 2 §5's survivor inventory: golden-000072 is the only unsafe active survivor | measured over all 80 active goldens, seven more rows emit an outbound edge from a cache sink (ten edges): `golden-000005` (two), `golden-000012`, `golden-000018` (two), `golden-000019`, `golden-000059`, `golden-000060` (two), `golden-000066`. Each is retired through the active-to-tombstone transition, and its replacement is frozen before any producing change. The four canonical replacements are pristine branch-point renders, each cache and Exception shape checked against an executed capture or the platform-authored terminal-remove capture. The three notify replacements are recorded byte transforms of the goldens they retire. Seven additions and seven retirements keep the floor at 80. The #154 provenance claim that write-then-raise throws is withdrawn | `docs/architecture/evidence/issue-184/oracle/terminal_cache_canonical.MANIFEST.json`; `docs/architecture/evidence/issue-184/oracle/terminal_cache_dlq.MANIFEST.json`; `tests/fixtures/process_ir/issue184/PROVENANCE.md`; `tests/fixtures/process_ir/issue154/PROVENANCE.md` |
| C16 | amendment 3 §5–§6: omit the builder's synthetic Stop after terminal DLQ cache staging while RESERVING the removed slot, so every later shape keeps its id and coordinates; freeze the replacements for golden-000005 and golden-000060 as the old bytes minus the dead wire and the Stop | not adopted as worded. The canonical compiler was measured on the same graphs, with each catch-body cache write authored as the catch terminal. For golden-000059 the canonical render equals the reserved-slot bytes, because the reserved Stop was its last shape. For golden-000005 it differs exactly in the second catch leg: canonical numbers those shapes 10 and 11 in the next columns, while the reserved-slot form puts them at 11 and 12, one column right. Reserving would therefore end #156's canonical-to-legacy byte parity for the double guard, which is the parity #159 and #160 use to verify caller migration before the legacy chain is deleted. Instead the builder drops the Stop without reserving its index, as it already does after a terminal Process Call (#175). The freeze transform renumbers later shapes to their new columns. It asserts the column rule on the source golden first, and it asserts the golden-000005 and golden-000059 results byte-equal to the pristine branch-point canonical render of #156's documents. Golden-000060 has no canonical document and rests on the transform alone | `docs/architecture/evidence/issue-184/oracle/terminal_cache_dlq.MANIFEST.json` (the `canonical_cross_check` and `renumbered` fields); `docs/architecture/evidence/issue-184/oracle/freeze_terminal_cache_dlq.py` |
| C17 | none of the plans: amendment 3 §6 deletes each retired golden's expected XML | the #178 guard requires every path a ledger cites to exist in the git index, with a frozen exemption set that may not grow. The closed #156 ledger cites two of the retired files by full path: the notify and double-guard DLQ goldens. Those citations were true when written, and a closed slice's record is not edited. Instead a citation now also resolves when it names the expected file of a TOMBSTONED golden-manifest row. The manifest is the authority that the artifact existed and was deliberately retired, and the golden manifest test asserts the file is really gone. The rule is derived from the append-only manifest and does not grow the frozen set. A non-vacuity test measures the two #156 citations reported again when the tombstone set is emptied | `tests/test_issue_178_ledger_is_derived_from_its_archive.py` (`_tombstoned_golden_files`, `test_a_retired_golden_citation_resolves_only_through_its_tombstone`) |
| C18 | amendment 3 §5: keep `doccacheremove` in the flow_sequence vocabulary and refuse its successors, blaming the successor's `.kind` | not adopted as worded. With a removal allowed nowhere in `flow_sequence`, every position fails: a successor never runs; a trailing top-level or TRUE-arm step starves the implicit target; a FALSE-arm or leg end starves the fall-through Stop or leg target, and this surface authors no terminal removal. The #151 closed-vocabulary pin (every allowed flow_sequence kind is authored by a committed specimen that compiles) then measured `doccacheremove` as allowed but unauthorable. A kind no composition admits is not an authorable kind, so it is withdrawn from the builder's flow_sequence vocabulary and from the served `supported_sequence_kinds`. It is refused BY NAME with `PROCESS_FLOW_SEQUENCE_CONFIG_INVALID` at the step's own `.kind`, one identity in every position, with the remediation naming the ProcessIR branch-leg `cache_remove` terminal. `transform.mode="doccacheremove"` keeps its named `PROCESS_DOCCACHE_REMOVE_CONFIG_INVALID` refusal. The legacy-to-IR alias table keeps its `doccacheremove` entry | `tests/test_issue_151_reachability_freeze.py`; `tests/test_process_ir_flow_sequence_codec.py` (`test_a_removal_has_no_legal_flow_sequence_placement`); `tests/test_schema_template_process_flow.py` |
| C19 | amendment 1's diagnostic policy, kept by amendment 3 §8: a missing inherited dynamic-path writer proof keeps the specialized code at `/path_binding` | a process call has no `path_binding` field: the binding lives in the child. The caller's missing or invalid writer is reported with the existing dynamic-path codes at the call's `/process_ref`, the pointer amendment 1 already uses for the child's profile requirement. It goes through the same single construction site as a binding in the calling process. A called child's own validation treats the writer of each such binding as caller-composed. Which bindings qualify is measured on the child's own walk: a binding refused as not established qualifies exactly when seeding its property as caller-composed clears the refusal. The walk records that refusal per bound property, so a call whose child needs X and Y, in a process that writes X itself, qualifies Y alone (CDX-184-r2-03, Stage-2 correction batch 3). So a step that hands on other documents before the binding keeps the child's own refusal, and nothing is demanded of the caller. A No Data child imports neither parent document properties nor their writers | `tests/test_issue_184_child_entries.py` (`test_a_childs_bound_path_is_proved_against_each_callers_writer`, `test_a_step_that_hands_on_other_documents_keeps_the_childs_own_refusal`) |
| C20 | amendment 3 §8 and amendment 1 rule 7: child contracts carry possible and guaranteed execution-state effects, and a waited, successfully completed child may establish guarantees | derived contracts carry REQUIREMENTS and POSSIBLE mutations only, and never establish a downstream guarantee. The served #154 rule stays true: an undeclared or inert effect never establishes state, so a guarantee still needs a declared subprocess summary the server verifies. Every call adds unknown content and an unknown property cohort to the caches its child may write: those a contract with known cache writes names, whether or not the caller names them, or every cache the caller can observe when the child's cache writes are unknown or its contract is not derivable: its own cache nodes, the caches its callers filled, and every cache a called child's contract names. Corrected by Stage-2 correction batch 3 (CDX-184-r2-01): the first realization counted only the caller's own cache nodes, so a forwarding process that names no cache kept its caller's proof after a call appended to that cache. Pre-commit verification of that batch measured the opposite over-reach (SELF-184-03): a child that was opaque only because of a map was taken to write every cache. Only an Add to Cache or Remove from Cache step writes a cache, so the contract carries `cache_writes_known`, which is False only when the child calls a child whose own cache writes are unknown. A child whose state is otherwise unknown still lists every cache it may write. Neither a typed cache consumer nor the retrieve overlay then rests on what a child may have appended (`cap184-shared-cache`). A No Data child's document-property reads are dropped from its callers' obligations. The pre-existing seeding of a declared summary's required reads is filtered the same way. A No Data child that runs once per arriving document and may change state it requires is refused at `/…/terminal` (amendment 1 rule 8) | `tests/test_issue_184_child_entries.py` (`test_a_child_may_leave_unknown_content_in_a_shared_cache`, `test_a_no_data_child_run_per_document_may_not_change_the_state_it_requires`, `test_a_no_data_child_does_not_owe_its_document_property_reads_to_a_caller`) |
| C21 | amendment 1 rules 2, 3, 4 and 6: separate standalone analysis, root-to-leaf native-work markers, legacy empty-prefix placement, and typed cache requirements across the child boundary | three bounded realizations. Standalone validity is recorded per passthrough root with the typed build, under `standalone_entry` in the build record, read off the entry contract its plan compiled under. Deployment orchestration refuses a direct test run or schedule of a root that needs a caller with `PROCESS_IR_CAPABILITY_ENTRY_CONTEXT_UNSUPPORTED`, in both runs, before any SDK call. A test run's dynamic process properties may discharge property requirements; a schedule supplies none. Only the call's own body is a prefix. A call under a Branch or Decision whose own body is empty keeps the legacy empty-prefix placement (rule 4): the #141 capture attests `decision -> true -> processcall` after a leg step, and a first draft that treated the interposed control as the call's predecessor refused that shipped form (measured by `tests/test_process_ir_rich_control_bodies.py`). The interposition erases no obligation (rule 3): a known child's contract is discharged at that call like any other. A called child's cache read is established by its seeded required read. A first draft did not carry the profile of the cached content across the boundary, so a typed consumer after that read stayed refused. Stage-1 QA measured that as QA-184-s1-r1-02, and rule 6 requires the opposite, so correction batch 1 carries it. Every consumer of documents read from a cache no in-process write reached records what it needs, through the same recorder as a passthrough entry's consumers. The contract carries those requirements. A called child is validated with that content seeded only when every consumer names one profile. Every call proves the requirement against what its own writes stored there (`PROCESS_IR_SEMANTIC_PROFILE_MISMATCH` at `/process_ref`). The same child run alone stays refused, as amendment 3's matrix row for that root requires | `tests/test_issue_184_child_entries.py` (`test_a_direct_run_of_a_passthrough_root_that_needs_a_caller_is_refused_before_mutation`, `test_an_interposed_decision_keeps_the_prefix_obligation`, `test_a_childs_cache_consumer_is_proved_against_each_callers_writes`, `test_the_callers_cache_proof_is_load_bearing`) |
| C22 | amendment 1 §3 and amendment 3 §10: register `PROCESS_IR_CAPABILITY_ENTRY_CONTEXT_UNSUPPORTED` in `errors.py`, the model diagnostic machinery and the compiler diagnostic specifications | registered in `errors.py` and in the semantic-validation finding table, not the compiler table. Lineage is the only layer that raises it during compilation. The #177 served-code guard requires each layer's table to hold exactly the codes that layer emits, so a compiler row that nothing in the compiler layer emits fails it (measured by `test_the_served_code_set_is_exactly_what_the_authorities_account_for`). The parser never raises it, so the model tables carry no row. Deployment orchestration serves the same code string with its own message, before any mutation. The served legacy process template also stops listing `doccacheremove` under `supported_transform_modes`. It publishes the mode under a new `refused_transform_modes` key with its refusal code, because every configuration that uses it is refused (C18) | `tests/test_process_ir_served_text_enforcement.py`; `tests/test_schema_template_process_flow.py` (`test_template_supported_transform_modes`) |
| C23 | the #155 L4 pin `test_l4_legacy_cache_arm_still_requires_its_exemption`: the legacy flow_sequence cache arm fails `validation_policy=None` with the lineage cache-writer code, so a shared cache ref is what removes the dialect exemption | withdrawn by canonical cache identity (CDX-184-r1-01, correction batch 2). The legacy adapter spells the one handoff cache through per-occurrence refs (`$ref:legacy.adapter:/flow_sequence/1/legs/<n>/steps/0/document_cache_id`), and every one of them resolves to the same documentcache component. Keyed by the resolved component, the staging put establishes the consuming gets, so the arm now passes strictly. Measured: disabling only the canonical graph spelling restores exactly that refusal, and disabling only the capability canonicalization does not. The pin is replaced by `test_l4_legacy_cache_arm_passes_strictly_once_cache_identity_is_canonical`, which keeps both halves falsifiable: strict pass, the refusal restored under spelling-keyed identity, and the dialect policy still accepting it. The #145 evidence list in `M12_COMPATIBILITY_INVENTORY.md` §9 still cited the withdrawn name; correction batch 3 moves that citation to the new test (measured by `test_every_cited_regression_test_resolves`) | `tests/patterns/test_recipe_preset_parity.py` |
| C24 | amendment 1 (`recipes/materialization.py`): profile facts come only from configurations that will actually be materialized, and reused components stay UNKNOWN | realized from the builder's DECLARED bindings. A component contributes no profile facts when apply binds it to an existing component the request names: a `reference_only` spec, or a `create` that names an id under `conflict_policy="reuse"` (`_will_reuse_at_apply`, through `reused_keys_for_components`). The first realization read only `reference_only` (SELF-184-01). The same declared binding decides component identity for every same-component rule (`component_identity`, QA-184-s1-r3-01): an update or reuse by id, or a `reference_only` id. A binding only an account read answers is not known on the account-free plan surfaces, which read declared bindings only, as the typed plan's reuse set already does: a `create` whose NAME matches an existing component under `reuse`. Such a create keeps its config facts and its own identity at plan and compile; only the apply-time compile, over bound ids, unifies its identity. Recorded for the §6 architect implementation review, which judges this realization against the plan | `tests/test_issue_184_component_identity.py` |

## Defect-class ledger (a class is a (mechanism, runtime-authority) pair, assigned at reconciliation)

| Class | Mechanism | Runtime authority | Instances (derived from rows) | Resolution |
| --- | --- | --- | --- | --- |
| `cache-step-document-emission` | a hand-written rule for which steps hand documents to their successor: cache writes and removes are modelled as consuming the stream but restarted by an immediately following cache read, or as passing documents on (the emitter registry admits a successor after a cache load or remove; the model requires a cache read straight after a cache write) | platform execution: a step with no inbound documents is skipped ("No documents found. Skipping execution for …") while the execution still reads COMPLETE | E0-184-01, E0-184-03, E1-184-01 — **third instance** | structural fix mandatory in-slice: one measured document-emission authority replacing the enumerations, with a sibling sweep, a non-vacuity witness and a coverage claim. Planned by amendment 3 §1–§6 (C11, C13, C15); the confirmation captures are archived; the sibling inventory is recorded below |
| `exception-parameter-source-unrendered` | an accepted authoring value (`parameter_source` of `none`) that the renderer turns into an Exception with no parameter block | the platform's component schema, which requires one on create | E1-184-02 | first instance |
| `confounded-measured-cell` | a measured table cell whose capture could not exercise it (the r17 cache rows wired a cache load straight into the retrieve, so the retrieve never ran and "lost" was read off a skipped step) | platform execution | E0-184-02 | first instance; re-measure with discriminating captures, then re-derive the cell |
| `child-contract-omits-cache-content` | a hand-picked subset of a child's stream facts carried in its entry contract: its document-profile and writer requirements were carried, but not what its consumers need of a cache it reads first. So a caller's writes discharged the child's cache read without proving the profile its map consumes | the lineage stream-profile proof: content facts on the cache key, and the consumers that check them | QA-184-s1-r1-02 | first instance; one recorder (`_requires`) now feeds both requirement channels from every consumer site, so the contract carries every requirement the proof collects |
| `served-rule-absent-from-node-page` | a rule served on one node page (`node.passthrough`) and missing from the page a caller reads for the construct it constrains (`node.process_call`) | the served authoring-contract projection | QA-184-s1-r1-01 | first instance |
| `cache-identity-by-authored-ref` | cache facts keyed by the authored ref, with alias handling patched at individual sites (`_respelled` in the child derivation, `_cache_identity` in the child cache-write marking) | the symbol table's resolved documentcache component id | CDX-184-r1-01, QA-184-s1-r3-01, CDX-184-r2-02, SELF-184-04, CDX-184-r3-01 — **recurred after the structural fix** | structural on the first reported instance, because two per-site alias patches already existed. One canonicalization derived from the symbol table is applied at every validation entry, to the lowered graph copy and to the trusted context, and both per-site patches are deleted. Sibling sweep: every cache-keyed fact passes through it — establishment keys from reads and writes, cache content, property cohorts, whole-cache removal, external-writer rows, child-contract and seed cache refs, and effect read/write keys. Witness: `test_the_canonical_cache_spelling_is_load_bearing`. **Revision (correction batch 3; original retained above):** that fix derived identity from the symbol's `component_id`, which a plan-time symbol table sets to one placeholder per key, so on the public route it grouped nothing (QA-184-s1-r3-01). It also read a component's cache declaration off the canonical spelling's symbol alone (CDX-184-r2-02). The runtime authority is revised to the component each reference binds to at apply: the builder's declared binding (`declared_bindings_for_components`, from `_will_reuse_at_apply`), recorded on the symbol as `bound_component_id`, then the resolved id. One accessor, `component_identity`, answers every same-component question, and a cache's declarations are gathered across all its references (`canonical_cache_profiles`). Sibling sweep over every `component_id` comparison in validation and effect resolution. Moved to the accessor: the canonical cache spelling, profile identity in connector resolution and in the bound-path rule, and the effect resolver's alias binding, duplicate-binding claim and caller cache seeds. Unchanged because they compare EMITTED or account-observed ids: lowering, the emitter registry's requirement index, the invariant id set, the entry operation id, and replay-grant corroboration. Every production symbol-table construction passes the request's policy (`test_every_production_symbol_table_names_its_conflict_policy`, derived from the source). Witnesses: the QA round's own probes through `plan_authoring_request_v1` (`test_two_specs_binding_one_cache_are_one_cache_on_the_typed_plan_route`), the profile sibling (`test_a_profile_is_compared_by_the_component_its_references_bind`) and the revision oracle `component_identity`. Coverage: every binding the request declares by id — reuse, `reference_only` and update — under each policy; a name-only binding needs an account read (C24). A declared id spelled like one of the plan's own placeholder ids binds nothing, so the accessor never equates an account id with a placeholder (SELF-184-04, `test_a_declared_id_spelled_like_a_plan_placeholder_binds_nothing`). **Revision 2 (correction batch 4):** batch 3 unified identity but still let ONE reference's spec speak for the component in two places. Effect derivation took the first alias in sort order, a regression batch 3 introduced (CDX-184-r3-01). Each reference also kept its own profile facts, so a `reference_only` alias of a map the request updates carried none. The authority is now the config apply writes into the component. `_written_spec` selects it for effect derivation through the plan's own `_may_be_substituted` authority. `_bound_component_facts` projects its facts onto every bound reference, and projects none when nothing in the request writes the component or two writes disagree. Sibling sweep over every place a component fact is read through one reference: symbol profile facts (input, output and cache) are projected; the cache declaration merge stays for hand-built tables; the effect resolver's cache and subprocess paths select spellings and child roots, not configs, and a canonical process root carries no declared binding, so its alias set is its own ref; connector metadata for a reused component comes from the resolution snapshot's live reading (#155), a separate authority. Witnesses: `test_a_map_declaration_derives_from_the_config_apply_writes` (both sort orders, both policies), `test_two_writes_of_one_map_that_disagree_decide_nothing`, `test_every_reference_to_a_bound_component_carries_the_written_facts`, and the `component_identity` oracle's `map_through_the_reference` and `declared_map_effect_through_its_writer` roots |
| `child-derivation-without-trusted-context` | a child's entry contract walked under a context that omits the child's own resolved map, script and external-writer rows | the per-root trusted context the effect resolver builds | CDX-184-r1-02 | first instance |
| `child-requirement-not-inherited-at-call` | a child-contract field read off directly authored nodes (bound paths, cache consumers) instead of off the obligations the walk discharges at each of the process's own calls | the lineage walk's per-call discharge of a child's entry contract | CDX-184-r1-03, CDX-184-r1-04 | second instance within one round, so the structural fix is in the same batch: an obligation this process cannot meet at a call is recorded as its own. Cache requirements are recorded at the discharge. Writer requirements are derived from refusals at the call's `/process_ref` that clear under the seeded writer. Sibling sweep over every contract field: document requirements are recorded through `_requires` in the call's stream step; required reads through `_classify_unmet_read`; mutated state and state knowledge are read from the child's contract in the derivation; writers and cache requirements are fixed here. Witnesses: `test_a_forwarding_call_passes_its_childs_cache_requirement_to_its_caller`, `test_a_forwarding_passthrough_passes_its_childs_bound_path_writer_to_its_caller` |
| `cache-mutation-tracked-by-authored-refs` | the caches a call may write, enumerated from the calling process's own cache nodes | the execution's shared document caches as the calling process can observe them: its own nodes, the caches its callers filled, and every cache its children's contracts name | CDX-184-r2-01, SELF-184-03 — **second instance** | first instance; `_caches_a_call_may_write` marks every cache a known-state contract writes and every observable cache for an unknown child. **Revision (pre-commit verification of batch 3; original retained):** the same question was still answered by a proxy: a child with unknown STATE was taken to write every observable cache, though a map or script cannot write one (SELF-184-03). As the second instance, the fix is structural and lands in the same batch. The child contract carries `cache_writes_known`, derived from the only cache writers: Add to Cache and Remove from Cache steps, plus calls whose children's cache writes are themselves known. Every consumer of a call's cache writes reads that flag: `_caches_a_call_may_write` and the derivation's composition. Sibling sweep: rule 8 keeps `state_known`, because a map may change a process property the shared requirement reads; the retrieve overlay reads the cohorts this marking sets; no other site asks which caches a call writes. Witnesses: `test_a_call_whose_unknown_effects_are_a_map_writes_no_unlisted_cache` (with a hiding grandchild control) and the `child_forwarding` oracle's `a_mapping_call_before_a_reader`. Coverage: every node kind that writes a cache in this IR, `cache_put`, `cache_remove` and `process_call` |
| `obligation-expanded-from-diagnostic-pointer` | a caller obligation derived by expanding one deduplicated refusal pointer to every bound property behind it | the lineage walk's per-property binding check | CDX-184-r2-03 | first instance; the walk records one row per bound property it finds no writer for (`unestablished_bindings`) |
| `revision-oracle-omits-changed-behaviour` | compiler-revision behaviour oracles whose hand-picked scenarios exercise no alias, effect declaration or forwarding call | the validation behaviour those rules implement | CDX-184-r2-04 | first instance; two oracle rows project the changed behaviour (`component_identity`, `child_forwarding`), with one perturbation witness per rule |
| `reuse-decided-off-the-apply-predicate` | whether a component's config is materialized, read from `component_materialization_mode`, which knows `reference_only` but not the conflict policy | the builder's `_will_reuse_at_apply` predicate | SELF-184-01 | first instance in this slice |
| `declared-binding-id-unnormalized` | a declared component id compared as authored on the create and update binding paths, while the `reference_only` path strips it | the planner binding (`resolve_planner_binding`) | SELF-184-02 | first instance; `declared_bindings_for_components` compares the stripped id |

## Finding rows (one per raw finding; append-only; exactly one disposition each)

| ID | Source gate + run dir + attestation | Verbatim summary | Original label | Blocking class | Defect class | Derived tier (anchor inline) | SHA/delta | Disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| INH-EVAL-155-02 | Inherited — `docs/architecture/ISSUE_155_AUDIT_LEDGER.md` row `EVAL-155-02` (#155 Stage-1 step-0 self-measurement at `9860842`, no gate run); adopted by #184's "Adopted from #155" placement record | "The issue's "a dynamic-path binding on the source AND one on the target" in one document is only partly expressible: the mapped both-sides composition the legacy chain emits today is refused at parse time by the root map-adjacency rule, so it is unrepresentable on the canonical chain" | none (self-recorded scope finding) | capability reachability | none (scope finding) | Standard — anchor: no source label and no critical blocking class; the #155 row records Standard | inherited at `cbab28f` | *(open — the expressibility half is discharged `fixed` only when A8 holds on the final tree: the canonical spelling compiles through the public route, emits `<shapes>` byte-identical to the frozen oracle, and executes green once live; #159 keeps the caller transfer and #160 verifies both before deletion)* |
| INH-EVAL-155-02a | Inherited — `docs/architecture/ISSUE_155_AUDIT_LEDGER.md` row `EVAL-155-02a` (revision of `EVAL-155-02`, raised by a served-surface audit at `2c1c9c7`) | "Same finding; the DISPOSITION claimed the enumeration was recorded in BOTH issue descriptions, and it was recorded in neither" | none (unchanged) | capability reachability (unchanged) | none | Standard (unchanged) | inherited at `cbab28f` | *(open — discharged together with INH-EVAL-155-02, including the same-pass #159/#160 description edits the issue lists)* |
| E0-184-01 | Pre-implementation live evidence round e0 (debits no loop), `boomi-qa-tester`, report `agents/reports/2026-09-13-issue-184-evidence-e0.md` (gitignored), captures archived at `docs/architecture/evidence/issue-184/captures/cap184-prefix-predecessors/` | "A step after Remove from Cache never runs, while the process still reads COMPLETE" — five executions (Process Call successor with the cache empty and primed, in a Branch leg and a Decision TRUE arm; a REST connector successor) | High | runtime behavior | `cache-step-document-emission` | **Critical** — anchor: source label High | pre-existing at `cbab28f` (the legacy builder's cache-remove mode and the canonical `cache_remove` emitter both wire a successor; active goldens 000012 and 000018 emit one) | *(open — Critical, may not be deferred: fix and validate in-slice through the structural fix for its class, or escalate with the issue open)* |
| E0-184-02 | same round and report; captures `docs/architecture/evidence/issue-184/captures/cap184-passthrough-ddp-handoff/` | "The served [property-survival table] says a cache retrieve loses the DDP; here the retrieve kept it" (constant name elided for the ledger token scanner) — a DDP set before Add to Cache survived retrieve-all, in-process and in a passthrough child; the r17 capture that measured "lost" wired the load straight into the retrieve, which never ran | Medium | machine-served schemas/contracts | `confounded-measured-cell` | Standard — anchor: source label Medium, no critical class | pre-existing at `cbab28f`; this slice's A5 increment (`703ba44`) extends the contradicted cell to ordinary reads | *(open — the retrieve's property semantics are being re-measured; the cell errs toward refusal, so nothing unsafe is admitted in the meantime)* |
| E0-184-03 | same round and report; captures `docs/architecture/evidence/issue-184/captures/cap184-passthrough-ddp-handoff/` (`xr-ddp-cache-linear-1`) | "Add to Cache with a successor emits zero documents" — a cache load wired straight into a retrieve skipped the retrieve, and the execution read COMPLETE | Medium | runtime behavior | `cache-step-document-emission` | Standard — anchor: source label Medium; blocking class runtime behavior | pre-existing at `cbab28f` (the model's mandatory write-then-read adjacency, the registry's continuing cache load, active goldens 000018 and 000019) | *(open — second instance of its class; resolved only by the same structural fix as E0-184-01)* |
| E1-184-01 | Confirmation live evidence round e1 (debits no loop), `boomi-qa-tester`, report `agents/reports/2026-09-13-issue-184-evidence-e1.md` (gitignored), captures archived at `docs/architecture/evidence/issue-184/captures/cap184-cache-put-successor/` | "A catch-body `Add to Cache → Exception` never throws; the run reads COMPLETE and the caught error is swallowed" — the protected request reached the counterparty and the catch path ran, but the Exception step was skipped for lack of documents; the control with the Exception alone reads ERROR with the caught message | High | runtime behavior | `cache-step-document-emission` | **Critical** — anchor: source label High | pre-existing at `cbab28f` (the catch-body trailing cache-write rule tolerates an exception after the write; active golden 000066 emits it) | *(open — Critical, may not be deferred; third instance of its class, resolved only by the structural fix)* |
| E1-184-02 | same round and report; captures `docs/architecture/evidence/issue-184/captures/cap184-cache-put-successor/` | "Exception `parameter_source: \"none\"` emits no `<exParameters>`, and the platform refuses the component create" — HTTP 400 on create; the same graph with `caught_error` was accepted and ran | Medium | emitted XML or graph validity | `exception-parameter-source-unrendered` | Standard — anchor: source label Medium, no critical class | pre-existing at `cbab28f`; no committed golden uses the value | *(open)* |
| QA-184-s1-r1-01 | Stage-1 QA r1, `boomi-qa-tester`, report `agents/reports/2026-09-13-issue-184-stage1-r1.md` (gitignored); scenario "served contract spot checks" | "the process_call contract page omits the passthrough standalone-run refusal" | Low | machine-served schemas/contracts | `served-rule-absent-from-node-page` | Standard — anchor: served contract text is a blocking class; source label Low | QA at `0bba5e9`; correction batch 1 | fixed — the `node.process_call` ordering facts state it (`tests/test_issue_184_child_entries.py::test_the_process_call_page_states_the_passthrough_standalone_refusal`); verified by affected Stage-1 QA round r2 at `297c572` (report `agents/reports/2026-09-13-issue-184-stage1-r2.md`, gitignored; no findings) |
| QA-184-s1-r1-02 | same round and report; scenario D, child variant | "a No Data child `[cache_get, map_ref, stop]` primed by its caller is refused `PROCESS_IR_SEMANTIC_PROFILE_MISMATCH`" | Medium | capability reachability | `child-contract-omits-cache-content` | Standard — anchor: capability reachability is a blocking class; source label Medium | QA at `0bba5e9`; correction batch 1 | fixed — amendment 1 rule 6 requires typed cache requirements to cross the child boundary, which plan correction C21 had recorded as a limitation (corrected there); `tests/test_issue_184_child_entries.py` (`test_a_childs_cache_consumer_is_proved_against_each_callers_writes`, `test_a_caller_whose_writes_store_another_profile_is_refused_at_its_call`, `test_the_callers_cache_proof_is_load_bearing`); verified by affected Stage-1 QA round r2 at `297c572` (report `agents/reports/2026-09-13-issue-184-stage1-r2.md`, gitignored; no findings) |
| CDX-184-r1-01 | Stage-2 repo commit review round r1, detached Codex, run `/tmp/cdx-review.PJotK5`, archived at `docs/architecture/evidence/issue-184/commit-reviews/cdx-review.PJotK5/` (collector `STATUS: completed`, `SCOPE: branch diff against 73b9d3ddc8e547eba09b857275050fb336c06b59 (73b9d3d) head=29edc1b9fab793267d0260208a9e90bbb8622145 dirty=false`, teardown confirmed stopped) | "[P2] Key cache-content facts by resolved component identity — when two cache references resolve to the same component ID, these facts incorrectly treat them as separate caches … Normalize cache identities consistently for content, cohorts, reads, and removals so aliases cannot bypass validation" | P2 | runtime behavior | `cache-identity-by-authored-ref` | Standard — anchor: source label P2, which is not P0/P1/Critical/High; runtime behavior is a blocking class | `73b9d3d..29edc1b`; correction batch 2 | fixed — reproduced before the fix: a P2 write through the alias escaped the P1 map's content check, while the same-spelling control was refused `PROCESS_IR_SEMANTIC_PROFILE_MISMATCH`. Structural fix per the defect-class row; `tests/test_issue_184_child_entries.py` (`test_a_cache_alias_is_the_same_cache_for_every_cache_fact`, `test_the_canonical_cache_spelling_is_load_bearing`) |
| CDX-184-r1-02 | same round | "[P2] Derive child contracts using their resolved effect context — … a child reading a cache with external_writer=true and a matching declaration incorrectly acquires a caller cache requirement. Its otherwise-valid parent then fails with PROCESS_IR_SEMANTIC_LINEAGE_CACHE_WRITER_MISSING" | P2 | capability reachability | `child-derivation-without-trusted-context` | Standard — anchor: source label P2, which is not P0/P1/Critical/High; capability reachability is a blocking class | `73b9d3d..29edc1b`; correction batch 2 | fixed — reproduced before the fix (the parent was refused at `/body/steps/0/legs/0/terminal`). Entry contracts are now derived after the declared map, script and external-writer rows resolve, under that root's rows; `test_a_child_whose_declared_external_writer_fills_its_cache_owes_its_caller_nothing` |
| CDX-184-r1-03 | same round | "[P2] Propagate typed-cache requirements through forwarding calls — in a PARENT → MID → CHILD chain … MID's call checks this requirement but never records it in cache_requirement_refs" | P2 | capability reachability | `child-requirement-not-inherited-at-call` | Standard — anchor: source label P2, which is not P0/P1/Critical/High; capability reachability is a blocking class | `73b9d3d..29edc1b`; correction batch 2 | fixed — reproduced before the fix (MID refused `PROCESS_IR_SEMANTIC_PROFILE_MISMATCH` at its call). Structural fix per the defect-class row; `test_a_forwarding_call_passes_its_childs_cache_requirement_to_its_caller` |
| CDX-184-r1-04 | same round | "[P2] Include inherited bound-path writers in child requirements — for a passthrough MID that forwards directly to a child whose GET binds property X, the missing-writer finding appears at MID's /process_ref … MID advertises no writer requirement" | P2 | capability reachability | `child-requirement-not-inherited-at-call` | Standard — anchor: source label P2, which is not P0/P1/Critical/High; capability reachability is a blocking class | `73b9d3d..29edc1b`; correction batch 2 | fixed — reproduced before the fix (MIDP refused `PROCESS_IR_SEMANTIC_DYNAMIC_PATH_DDP_NOT_ESTABLISHED` at `/body/steps/1/process_ref`). This is the second instance of the class in one round; the structural fix is in the same batch, per the defect-class row; `test_a_forwarding_passthrough_passes_its_childs_bound_path_writer_to_its_caller` |
| QA-184-s1-r3-01 | Stage-2 support QA round r3 (billed to correction batch 2), `boomi-qa-tester`, report `agents/reports/2026-09-13-issue-184-stage2-r3.md` (gitignored); probes F1a, F1aro, F1b and F1bro with controls F1ad, F1bd and F1bs | "cache-ref unification never fires on the public route — two specs resolving to one cache component stay two caches" | Medium | runtime behavior; capability reachability | `cache-identity-by-authored-ref` | Standard — anchor: source label Medium, which is not P0/P1/Critical/High; runtime behavior is a blocking class | QA at `4a99c05`; correction batch 3 | fixed — reproduced offline through `plan_authoring_request_v1` from the round's own recorded requests before the fix: F1a and F1aro admitted, and F1b and F1bro refused exactly like the distinct-cache control. A recurrence of its class after the structural fix, so the fix is structural again, per the defect-class row's revision; `tests/test_issue_184_component_identity.py` |
| CDX-184-r2-01 | Stage-2 repo commit review round r2, detached Codex, run `/tmp/cdx-review.i7DdUD`, archived at `docs/architecture/evidence/issue-184/commit-reviews/cdx-review.i7DdUD/` (collector `STATUS: completed`, `SCOPE: branch diff against 29edc1b9fab793267d0260208a9e90bbb8622145 (29edc1b) head=6021e8d516920318735aa1f9beb33b089e6ea7df dirty=false`, teardown confirmed stopped) | "[P2] Track child mutations of inherited cache contents — When a forwarding MID has no cache nodes and its ordered Branch legs call WRITER then READER, this new requirement seeds MID with its caller's P2 cache contents. However, `_child_may_write_caches()` only tracks cache refs authored directly in MID, so WRITER appending P1 is ignored" | P2 | runtime behavior | `cache-mutation-tracked-by-authored-refs` | Standard — anchor: source label P2, which is not P0/P1/Critical/High; runtime behavior is a blocking class | `29edc1b..6021e8d`; correction batch 3 | fixed — reproduced: at `6021e8d` all four roots were admitted, while the review base refused MID `PROCESS_IR_SEMANTIC_PROFILE_MISMATCH` at `/body/steps/0/legs/1/terminal/process_ref`. Plan correction C20 is corrected in the same pass; `test_a_call_that_appends_to_a_forwarded_cache_leaves_it_unproved` (with an unknown child, a non-writing control and a non-vacuity perturbation) |
| CDX-184-r2-02 | same round | "[P2] Preserve profile facts when canonicalizing cache aliases — If cache aliases A and B resolve to one component but only B carries `cache_profile_ref=P2`, rewriting B to lexicographically earlier A discards that profile constraint … A P1 write through B now validates and compiles, whereas the base reports `PROCESS_IR_SEMANTIC_PROFILE_MISMATCH`" | P2 | runtime behavior | `cache-identity-by-authored-ref` | Standard — anchor: source label P2, which is not P0/P1/Critical/High; runtime behavior is a blocking class | `29edc1b..6021e8d`; correction batch 3 | fixed — reproduced: `6021e8d` admitted the P1 write, and the review base refused it at `/body/steps/0/legs/0/terminal/cache_ref`. Per the defect-class row's revision; `test_a_cache_declaration_on_either_reference_binds_the_component` |
| CDX-184-r2-03 | same round | "[P2] Inherit only the bound writers missing at each call — If MID establishes X locally and calls a child requiring both X and Y, the missing-Y diagnostic at `/process_ref` causes this code to collect both writers … A parent supplying only Y is then rejected for missing X, despite MID establishing X before the child runs" | P2 | capability reachability | `obligation-expanded-from-diagnostic-pointer` | Standard — anchor: source label P2, which is not P0/P1/Critical/High; capability reachability is a blocking class | `29edc1b..6021e8d`; correction batch 3 | fixed — reproduced: at `6021e8d` MID's row advertised X and Y and PARENT was refused `PROCESS_IR_SEMANTIC_DYNAMIC_PATH_DDP_NOT_ESTABLISHED` at `/body/steps/1/legs/0/terminal/process_ref`. Plan correction C19 records the per-property rule; `test_a_forwarder_owes_its_caller_only_the_bound_writers_it_does_not_establish` |
| CDX-184-r2-04 | same round | "[P2] Make the changed validation behavior rotate the compiler revision — Across this base-to-tip change, `get_authoring_revisions()` remains identical despite changed cache-alias acceptance and child-contract derivation" | P2 | machine-served schemas/contracts | `revision-oracle-omits-changed-behaviour` | Standard — anchor: source label P2, which is not P0/P1/Critical/High; machine-served schemas/contracts is a blocking class | `29edc1b..6021e8d`; correction batch 3 | fixed — measured: all three revisions were byte-identical at `29edc1b` and `6021e8d`. Two behaviour oracle rows now project the changed rules, and the rebaseline records the moved revisions in `M12_COMPATIBILITY_INVENTORY.md` §12.9; `test_the_revision_moves_with_component_identity_and_forwarding_behaviour` |
| SELF-184-01 | Main-thread measurement while reconciling QA-184-s1-r3-01 (no gate run; billed to correction batch 3) | "a `create` that names an existing component id under `conflict_policy=\"reuse\"` is reused at apply, yet its discarded config still contributes profile facts to validation" | none (self-recorded) | runtime behavior | `reuse-decided-off-the-apply-predicate` | Standard — anchor: no source label; runtime behavior is a blocking class and no critical class applies | pre-existing since this slice's symbol profile facts; correction batch 3 | fixed — `_profile_facts` now asks the reuse set the builder derives from `_will_reuse_at_apply` (plan correction C24); `test_a_reused_create_contributes_no_profile_facts` (with the clone and update controls) |
| SELF-184-02 | Pre-commit adversarial verification of correction batch 3 (a supporting read-only agent, no gate run; billed to correction batch 3); its probes were re-run by the main thread before the fix | "A padded `component_id` crashes the symbol builder on every route" | Standard (verifier) | runtime behavior | `declared-binding-id-unnormalized` | Standard — anchor: source label Standard, which is not P0/P1/Critical/High; runtime behavior is a blocking class | introduced by uncommitted correction batch 3; fixed in the same batch | fixed — reproduced: `plan_authoring_request_v1` raised a `ValidationError` naming `bound_component_id` for a cache spec whose top-level `component_id` carries surrounding spaces, where `ca1f97e` returned findings; `test_a_padded_declared_id_binds_like_its_stripped_spelling` |
| SELF-184-03 | same verification | "Forwarding flows are now refused after a child that only has an ordinary map" | Standard (verifier) | runtime behavior | `cache-mutation-tracked-by-authored-refs` | Standard — anchor: source label Standard, which is not P0/P1/Critical/High; runtime behavior is a blocking class | uncommitted correction batch 3 widened a rule `ca1f97e` already applied to caches the caller names; fixed in the same batch | fixed — reproduced: MID was refused `PROCESS_IR_SEMANTIC_PROFILE_MISMATCH` at `/body/steps/0/legs/1/terminal/process_ref` after a child `[GET, map, stop]`, where `ca1f97e` admitted it. Plan correction C20 is corrected in the same pass; `test_a_call_whose_unknown_effects_are_a_map_writes_no_unlisted_cache` |
| SELF-184-04 | same verification | "A declared id can collide with the placeholder id namespace" | Low | runtime behavior | `cache-identity-by-authored-ref` | Standard — anchor: source label Low, which is not P0/P1/Critical/High; runtime behavior is a blocking class | introduced by uncommitted correction batch 3; fixed in the same batch | fixed — reproduced: a `reference_only` spec naming `id-foo` merged with the new cache key `foo`, which erased the `PROCESS_IR_SEMANTIC_LINEAGE_CACHE_WRITER_MISSING` finding `ca1f97e` reported; `test_a_declared_id_spelled_like_a_plan_placeholder_binds_nothing` |
| CDX-184-r3-01 | Stage-2 repo commit review round r3, detached Codex, run `/tmp/cdx-review.0m4OoB`, archived at `docs/architecture/evidence/issue-184/commit-reviews/cdx-review.0m4OoB/` (collector `STATUS: completed`, `SCOPE: branch diff against 6021e8d516920318735aa1f9beb33b089e6ea7df (6021e8d) head=6b44f3da602445db379cf1666dd9a32412b1655c dirty=false`, teardown confirmed stopped) | "[P2] Preserve the authoritative map config when merging aliases — When an updated map `MAP` and an earlier-sorting `reference_only` spec [the alias] name the same component ID, this widened alias set makes `_by_identity()` select [the alias] for effect derivation. Its reused config makes the map declaration inert, so a valid map-write followed by a property read now fails with `PROCESS_IR_SEMANTIC_LINEAGE_PROPERTY_READ_BEFORE_WRITE`" (the alias key is elided for the ledger token scanner) | P2 | capability reachability | `cache-identity-by-authored-ref` | Standard — anchor: source label P2, which is not P0/P1/Critical/High; capability reachability is a blocking class | `6021e8d..6b44f3d`; correction batch 4 | fixed — reproduced: at `6021e8d` the declaration derived and the request was admitted; at `6b44f3d` it was inert and refused at `/body/steps/2`, while the control whose alias sorts after `MAP` was admitted. A regression of batch 3's identity widening, fixed structurally per the class row's second revision; `test_a_map_declaration_derives_from_the_config_apply_writes` |

**Supersession map** — `INH-EVAL-155-02a → INH-EVAL-155-02`.

Dispositions: `fixed` · `finding-refuted` · `severity-refuted` · `not-validated` · `deferred`
(issue, reason class, placement). An original label is never edited — a revision is a new dated line
with the original retained.

## Checkpoints (written in flight at every third evaluation of each loop)

| Loop | Evaluation (window / cumulative) | SHA (+dirty) | Outcome | Rationale |
| --- | --- | --- | --- | --- |
| Stage-2 repo commit review (loop 2) | 3 / 3 | `6b44f3d` (clean) | `CONTINUE` | The validation owed by correction batch 3 ran first. QA round r4 is clean; review round r3 raised one P2, CDX-184-r3-01, reproduced as a regression of batch 3. Trend from the rows' defect-class field, round r1 → round r2 (with QA round r3 and batch 3's self-verification) → round r3 (with QA round r4): highest unrefuted severity flat at Standard; unresolved blocking count 4 → 9 (CDX-184-r2-01..04, QA-184-s1-r3-01, SELF-184-01..04, all fixed in batch 3) → 1; affected-class breadth 3 → 6 → 1; no new class in round r3. Resolved since round r2: `cache-mutation-tracked-by-authored-refs`, `obligation-expanded-from-diagnostic-pointer`, `revision-oracle-omits-changed-behaviour`, `reuse-decided-off-the-apply-predicate` and `declared-binding-id-unnormalized`. Recurring: `cache-identity-by-authored-ref`. The recurring class is not being instance-patched. Correction batch 4 completes its structural fix: revision 2 of its row makes the config apply writes decide a component's facts and its effect derivation for every reference, with a sibling sweep, witnesses and a coverage claim, and no deviation is recorded against the class. No critical residue and no deferral. Named finite next correction: batch 4, then its affected QA round r5 and the fix-only review round r4 over `6b44f3d..HEAD`. The window resets; cumulative evaluations: 3 |

## Deferrals

Pointer-only — reason class, placement and lineage live on the finding row and in the filed issue.

## Evidence index

Collected run directories are archived under `docs/architecture/evidence/issue-184/` with
`index.jsonl` + `SHA256SUMS`, in the batch that collects them. The attestation scanner checks every
archive that has an `index.jsonl`: `SHA256SUMS` must cover exactly the on-disk archive (minus
itself) AND agree with the git index. So the skeleton created with this ledger covers the
pre-baseline `oracle/` capture and the `predicates/` measurements, and every later capture or
collected run is added to `SHA256SUMS` in the SAME commit that adds its files — an archive file
never sits uncommitted or unlisted across a validation boundary.

| Archived item | Path | Kind |
| --- | --- | --- |
| Legacy both-sides oracle capture | `docs/architecture/evidence/issue-184/oracle/` | pre-baseline evidence |
| Branch-point predicate probe and pre-implementation sweeps | `docs/architecture/evidence/issue-184/predicates/` | pre-implementation measurement |
| L0 pre-baseline oracle commit review | `docs/architecture/evidence/issue-184/commit-reviews/cdx-review.w3jzMM/` | collected commit review |
| `golden-000072` replacement survivor freeze (the recorded transform of the archived `cap155-e1-source-dynamic-path` capture) | `docs/architecture/evidence/issue-184/oracle/source_dynamic_path_dpp.MANIFEST.json` | recorded transform of a proven-operable capture |
| D12 controller refactor differential | `docs/architecture/evidence/issue-184/sweeps/d12-controller/` | measurement |
| A8 canonical golden freeze (the recorded slice of the pre-baseline legacy oracle, with no id substitution) | `docs/architecture/evidence/issue-184/oracle/both_sides_dynamic_path.MANIFEST.json` | recorded transform of the frozen oracle |
| Pre-implementation live evidence rounds e0 and e1 (stored and submitted components, execution records, counterparty windows) | `docs/architecture/evidence/issue-184/captures/` | platform evidence from causally independent fixtures |
| Terminal cache replacements for the seven cache-successor goldens (C15) | `docs/architecture/evidence/issue-184/oracle/terminal_cache_canonical.MANIFEST.json`, `docs/architecture/evidence/issue-184/oracle/terminal_cache_dlq.MANIFEST.json` | pristine branch-point renders checked against executed captures, and recorded byte transforms of immutable goldens |
| Stage-1 live QA (loop 1): round r1 at `0bba5e9` — 16 live executions, the A8 composite's stored `<shapes>` byte-equal to the frozen oracle, N1–N8 with controls, findings QA-184-s1-r1-01 and -02. Correction batch 1 at `297c572`, then round r2, the affected re-run at `297c572`, with no findings. Two evaluations, so no checkpoint was due | `agents/reports/2026-09-13-issue-184-stage1-r1.md`, `agents/reports/2026-09-13-issue-184-stage1-r2.md` (gitignored run reports; the component, execution and wire ids each quotes are recorded in the report) | live QA through the public MCP tool boundary |
| Stage-2 repo commit review (loop 2) round r1 over `73b9d3d..29edc1b`: four P2 findings, CDX-184-r1-01..04 | `docs/architecture/evidence/issue-184/commit-reviews/cdx-review.PJotK5/` | collected commit review |
| Stage-2 support QA (loop 2, billed to correction batch 2): round r3 at `4a99c05`, the affected re-run of batch 2. It covers the four review-finding probes with controls (plan and compile agree on every row) and live regressions B2, G, R2b, Fb and the forwarding-writer chain PARENT → MIDP → BOUND. One finding, QA-184-s1-r3-01 | `agents/reports/2026-09-13-issue-184-stage2-r3.md` (gitignored run report; the component, execution and wire ids each quotes are recorded in the report) | live QA through the public MCP tool boundary |
| Stage-2 repo commit review (loop 2) round r2, fix-only over `29edc1b..6021e8d` (correction batch 2 and its test re-pin): four P2 findings, CDX-184-r2-01..04 | `docs/architecture/evidence/issue-184/commit-reviews/cdx-review.i7DdUD/` | collected commit review |
| Stage-2 support QA (loop 2, billed to correction batch 3): round r4 at `6b44f3d`, the broad affected re-run of batch 3. 31 plan/compile probes with controls pass, plan and compile agreeing on every row. Live L-F1b (two `reference_only` specs binding one cache) and L-W pass, as do regressions B2, G, R2b, Fb, F4 and the A8 composite (stored `<shapes>` byte-equal to the frozen oracle, H green). No findings. One observation outside the slice's changed behaviour: a padded top-level `component_id` is echoed verbatim in `resolved_references` with `version_marker` null (`build_resolved_reference_summary`, unchanged by the slice) | `agents/reports/2026-09-13-issue-184-stage2-r4.md` (gitignored run report; the component, execution and wire ids each quotes are recorded in the report) | live QA through the public MCP tool boundary |
| Stage-2 repo commit review (loop 2) round r3, fix-only over `6021e8d..6b44f3d` (the round r2 archive and correction batch 3): one P2 finding, CDX-184-r3-01 | `docs/architecture/evidence/issue-184/commit-reviews/cdx-review.0m4OoB/` | collected commit review |
| Stage-2 support QA (loop 2, billed to correction batch 4): round r5 at `ae4f9ee`, the affected re-run of batch 4. 26 graded plan/compile rows with a parent A/B arm at `6b44f3d`, plan and compile agreeing on every row; live L-MAP (the updated map's process-property write reaches the read), L-F1b, Fb and the A8 composite pass. One finding, QA-184-s1-r5-01 | `agents/reports/2026-09-13-issue-184-stage2-r5.md` (gitignored run report; the component, execution and wire ids each quotes are recorded in the report) | live QA through the public MCP tool boundary |
| Stage-2 repo commit review (loop 2) round r4, fix-only over `6b44f3d..ae4f9ee` (the round r3 archive, the third checkpoint and correction batch 4): three P2 findings, CDX-184-r4-01..03 | `docs/architecture/evidence/issue-184/commit-reviews/cdx-review.JxcQ6s/` | collected commit review |

## Final-tree validation (filled at close; every roster gate current on the FINAL sha)

| Gate | Evidence (quoted output / run URL / archived round) | SHA |
| --- | --- | --- |
