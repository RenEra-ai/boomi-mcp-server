# Audit ledger — issue #153 (M12.15)

Instantiated at Stage-1 step 0 per `docs/architecture/templates/AUDIT_LEDGER_TEMPLATE.md`, and
committed — with its evidence-archive skeleton — in the Stage-1.5 baseline commit, before the
first Stage-2 round. Conventions inherited from #152's end state apply from row one.

Durable evidence archive: `docs/architecture/evidence/issue-153/` — every review round cited below
is present there as the collector's own artifacts (`commit-reviews/` for the repo's Stage-2
commit-review gate, `architect-reviews/` for the §6 architect gate), indexed by
`evidence/issue-153/index.jsonl` and hash-verified by `SHA256SUMS`. No round is cited here that is
not archived there, and no archived round is uncited.

Slice: **M12.15 — Canonical ProcessIR component materialization and direct apply (T1)**
Branch: `codex/issue-153`
Driver: `/codex-issue 153` (main-thread mode — this repo has no `.claude/workflows`, so the wrapping
pipeline runs `CLAUDE.md`'s own workflow rather than a composable Workflow engine).

## Baseline (Stage-1 step 0)

- Issue: #153 — M12.15 — Canonical ProcessIR component materialization and direct apply (T1)
- **Step-0 baseline SHA: `9f19aad5b280d58c02ef5cd840ff150d0193c1dd`** (== `origin/dev` at slice
  start; the tip #151 closed on)
- Slice kind: **behaviour-affecting** (it removes an apply refusal and adds a live mutation path)
- Artifact trust boundary:
  - **Created and owned by this slice** — the three new models (`ProcessComponentEnvelopeV1`,
    `ProcessAuthoringUnitV1`, `ProcessComponentMaterializationPlanV1`), the neutral
    `ProcessComponentMaterializer`, the relocatable plan fingerprint, the apply-time mutation
    attestation record, and the wave-gate plan-fingerprint provider registration.
  - **Consumed from outside** — the existing apply orchestration in
    `categories/integration_builder.py` (routing, preflight, dependency ordering, collision
    handling, create/update, preservation, verification), the legacy
    `process_flow_builder.py` arm (which stays live as the parity oracle until #160), the
    `compiler/process_ir` compile + emit chain, and the Boomi platform's own responses
    (server-assigned ids, folder attributes, readback bytes).
  - A finding that hardens a self-owned artifact gets a boundary verdict at reconciliation rather
    than a fix batch by default; a finding against a consumed artifact is in scope only where this
    slice changed its inputs.
- Expected defect classes (pre-enumerated from prior slices so a second instance triggers
  structurally ON ARRIVAL):
  - hand-enumeration shadowing a derivable authority (#149, #151, #165, #173);
  - hand-modelled platform behaviour in served text (#144, #151);
  - a guard whose expectation derives from the thing it guards, so it is vacuous (#149's derived
    gate, #151's `cls.__module__` witness, #165's collector-derived control);
  - an unpinned hand-copy of a fact whose runtime authority lives elsewhere (#140, #151);
  - a hand-typed count or ordinal where the value is derivable (#165, #173).

## Loop roster (fixed BEFORE the first correction; a gate not listed here cannot mint a loop
## mid-run — a roster addition is itself a recorded checkpoint decision)

| # | Logical loop | Gate purpose / authority | Scope | Checkpoint window |
|---|---|---|---|---|
| L1 | **Stage-1 QA** | `boomi-qa-tester` through the public MCP tool boundary, live on the renera account | this slice | fresh (3, 6, 9 …) |
| L2 | **Stage-2 repo Codex commit-review** | detached `codex-drive review --base` + `commit-review-collect.mjs`; every round COLLECTED, never read from `wait` | this slice | fresh |
| L3 | **§6 architect implementation review** (additive, declared by the wrapping `/codex-issue` pipeline) | detached `codex-drive --gate review` + `gate-attest.mjs collect`, judged against `.codex/plans/issue-153.md` | this slice | fresh |
| L4 | **Composite wave gate** | full non-KB suite + every active golden-manifest entry + determinism/byte-identity + `scripts/wave_gate.py wave … --require-plan-fingerprint` | Wave 0C slice set | fresh |
| L5 | Terminal correction loop | ONLY via a recorded roster-addition checkpoint | — | — |

Declared order of the additive gates: L1 → L2 → L3, with L4 run as one composite evaluation before
landing. Supporting QA/review runs bill the loop that caused the correction; a Stage-2 fix's QA
re-run never debits L1.

## Plan artifacts

- Codex architect design plan: `.codex/plans/issue-153.md` (gate-attested; attestation copied to
  `.codex/plans/issue-153.attest.json`). `.codex/` is gitignored, so these are session artifacts,
  not tracked evidence.
- Claude implementation plan: `.codex/plans/issue-153.claude.md`.

## Defect-class ledger (a class is a (mechanism, runtime-authority) pair, assigned at
## reconciliation, revisable with the original retained)

| Class | Mechanism | Runtime authority | Instances (derived from rows) | Resolution |
| --- | --- | --- | --- | --- |
| DC-1 | a documented COMPLETENESS claim that the code does not enforce — a docstring asserting "every field", "excluded by construction", "fingerprints behaviour", or "non-vacuity", restated in prose beside an enumeration that is free to fall behind | the authority the claim is about: the model's own `model_fields`, the dataclass's own `fields()`, the runtime dispatch table `PROCESS_FLOW_BUILDERS`, the emitter registration table | S5-01, S5-02, S5-03, S5-04, S5-05, S5-06, S5-07, S5-09, S5-10 (9 — derived from the rows above) | **STRUCTURAL FIX applied** — see below |
| DC-2 | a consumer that hand-models the PARTICIPANT UNIVERSE of a spec as `spec.components` alone, or reads a participant's backing component without first asking whether it has one | the one participant set `_integration_participants(spec)` — `components` **and** `processes` — which is also the authority `_topological_order` and the dependency graph read | S7-01, QA-153-r1-01, QA-153-r2-01, QA-153-r2-02, QA-153-r2-06, CX1-01, CX1-11, CX1-12 (8 — derived from the rows below) | **STRUCTURAL FIX applied in the r2 batch; its sibling sweep was INCOMPLETE** (it enumerated `integration_builder.py` only) and was extended over `authoring/workflow.py` in the Codex round-1 batch — see below |
| DC-4 | a field required on EVERY exit of a multi-exit function, added on the exit that was reported | the function's own exit set — `_apply_plan`'s failing returns, `_execute_canonical_process`'s early returns | QA-153-r2-05, CX5-04, CX6-01, CX6-02, CX7-01, CX7-02, CX8-01, QA-153-r10-01, CX10-01, CX11-01 (10 — derived from the rows below) | **STRUCTURAL FIX applied in the Codex round-6 batch** — see below |
| DC-3 | a serialized document searched as TEXT, with every matching string treated as structural | the SCHEMA's own reference-bearing fields — `ComponentRefV1`, identified by its validator in the field metadata | CX1-05, CX1-06, CX1-10 (3 — derived from the rows below) | **STRUCTURAL FIX applied in the Codex round-1 batch** — see below |
| DC-5 | a DISPOSITION recorded broader than the witness that proved it — a `fixed` row whose claim ("removes the partial write entirely", "drives that same public chain") describes more than its named test asserts | the named witness test's actual assertions | QA-153-r2-07, AR1-04a, AR1-07a, AR1-08a, AR1-10a (5 — every one measured by the §6 evaluation-2 verification round) | **STRUCTURAL FIX applied** — see below |
| DC-6 | a hand-assembled wire object, or a field's normalization, whose conformance to a SPECIFYING DOCUMENT is asserted in prose beside the assembly rather than pinned against it | the plan's own canonical object (§3 L252-255) and field contracts (L112/L120/L134) | AR2-05, AR2-06 (2 — derived from the rows below) | **STRUCTURAL FIX applied** — see below |

### DC-1 structural fix (mandatory — nine instances, far past the second-instance trigger)

**What the instances had in common.** Every one was a prose claim of completeness sitting next to a
hand-written enumeration: the canonical material "covers the model" while hand-building ten keys from
thirteen fields; the preservation policy "projects the runtime constant" while reading 2 of 8 fields;
the emitter revision "fingerprints behaviour" while covering only declared columns; the witness is
"THE non-vacuity witness" while touching no consumer; the builder list covers "every process builder"
while hand-copying the dispatch table. In each case the prose was true when written and had no
mechanism keeping it true.

**The invariant replacing the enumerations** — every completeness claim is now DERIVED from the
authority it is a claim about, so the enumeration cannot fall behind:

| Claim | Was | Now derived from |
| --- | --- | --- |
| fingerprint covers the plan | hand-built 10-key payload | `covered_plan_fields()` = `model_fields` − `EXCLUDED_PLAN_FIELDS` |
| projection covers the policy | 2 of 8 fields read by hand | `dataclasses.asdict(PROCESS_PRESERVATION_POLICY)` |
| guard covers every process builder | hand-listed 3 classes | `PROCESS_FLOW_BUILDERS.values()` |
| emitter revision covers the registry | declared columns only | declared columns **plus** `__qualname__`, with the residual limit stated |

**Sibling sweep.** Every completeness claim introduced by this slice was re-read and either derived
or narrowed: the four rows above are derived; `emitter_revision`'s claim was NARROWED to what it
measures (bytecode hashing rejected with a reason — `co_code` differs between the CI 3.11 and local
3.12 interpreters and would report drift between two correct deployments); `EXCLUDED_ENVELOPE_FIELDS`
is now read by `canonical_plan_material` rather than described by it. No remaining claim in the
slice's new modules asserts completeness over a hand-written list.

**Non-vacuity witness.** `test_all_consumers_observe_one_object_so_a_change_cannot_reach_only_some`,
re-measured against a PRISTINE `ec015d5` extract with the shared module added and the three
hand-copies left in place: **5 tests fail there**, including the witness itself and the regex sibling
sweep. The version it replaces PASSED on that same tree — which is what made it vacuous, and is the
concrete case the new invariant excludes.

**Coverage claim, derived from the authority's full case set.** `covered_plan_fields()` returns all
11 non-excluded fields of `ProcessComponentMaterializationPlanV1`; the preservation projection
carries all 8 `PreservationPolicy` fields and all 11 `OwnedPath` fields (asserted against
`dataclasses.fields`, not a literal count); `_PROCESS_BUILDERS` equals the 3 distinct values of
`PROCESS_FLOW_BUILDERS` and grows with it.

### DC-2 structural fix (mandatory — five instances; the first two were instance-patched, which is
### exactly what let the third and fourth ship)

**What the instances had in common.** Before #153 a spec had ONE participant family, so
`spec.components` and "everything this build owns" were the same set and every consumer could model
the universe by iterating it. #153 added `spec.processes`. Each consumer that kept the old model
became a defect, and — decisively — each was fixed AT ITS OWN CALL SITE:

| Instance | Where | How it failed |
| --- | --- | --- |
| S7-01 | `_build_plan` + `_apply_plan` component lookups | `KeyError` for any spec carrying a root — found by reading, fixed by guarding those two sites |
| QA-153-r1-01 | `_apply_plan`'s `existing_ids` comprehension | `KeyError: 'existing_component_id'` — fixed by adding the keys to the canonical step |
| QA-153-r2-02 | `_build_plan`'s issue-#86 advisory, ~1000 lines from the others | the r1 fix ADDED `planned_action`, which is what activated a third unguarded lookup. The guard had been False by accident, never by design |
| QA-153-r2-01 | `preflight_typed_apply_v1` | the bundle carried the SERVED projection, whose roots are withheld — the universe was not merely unmodelled but actively emptied |
| QA-153-r2-06 | `_verify_build` vs `_authoring_build_provenance` | two halves of one verify modelling two different universes, so a healthy build reported drift |

Three guarded call sites, and the fourth read shipped. That is the signature the structural-fix rule
names: patching instances of a hand-model does not converge, because the next consumer is written by
someone who never saw the patch.

**The invariants replacing the enumerations.**

| Claim | Was | Now derived from |
| --- | --- | --- |
| "this step has a component" | `components_by_key[step["key"]]`, unconditional, at three sites | `_step_component(step, …)` — discriminates on the step's own `materialization` marker, returns `None` for a canonical root, and raises a NAMED `IntegrationDependencyError` (not a bare `KeyError`) for a genuinely absent component |
| "these are the build's participants" | `for comp in spec.components` in `_verify_build` | `_integration_participants(spec)` — the same generator `_topological_order` and the dependency graph already read |
| "the bundle's spec can build what its compile describes" | trusted by convention, and violated | `CompiledBundle.__post_init__` compares the spec's root keys against `compile_result.process_cfg`, an authority INDEPENDENT of the projection under test |

The discriminator is deliberately the step's marker and NOT membership in `components_by_key`:
membership would fail OPEN and silently skip a component step whose key really was missing, turning
a crash into a wrong answer.

**Sibling sweep.** Every read of `components_by_key` in `integration_builder.py` was enumerated
(`grep -n 'components_by_key\['`) and converted: lines 5924 and 7820 now call `_component_for_key`,
line 6958's advisory calls `_step_component`, and the ONE remaining subscript is the accessor's own
lookup. The sole assignment (the wrapper construction site) is untouched by design. `_verify_build`
was converted to `_integration_participants`, and `_authoring_build_provenance`'s `results`-based
walk now agrees with it by construction.

**Non-vacuity witness — and it caught a defect in its own batch.** The invariant is asserted on the
SOURCE by `test_no_unguarded_component_lookup_survives_in_the_apply_pipeline`, because this
repository's own record is that prose structural fixes recur and executable ones hold. On first run
it immediately failed on a bare `verify_components_by_key[participant_key]` introduced by the
`_verify_build` conversion **in the same batch** — a fourth instance, written by the very change
meant to end the class, and caught before it left the working tree. Mutation-controlled in both
directions: reintroducing `comp = components_by_key[key]` at line 7820 reddens it; restoring the
accessor greens it.

**Coverage claim, derived from the authority's full case set.** `_build_plan` emits exactly two step
kinds, and the step-kind union is the authority: a component step (`materialization` absent) and a
canonical root step (`materialization == "process_ir_v1"`). `_step_component` has a case for each,
and both are exercised — the canonical case by
`test_a_plan_step_naming_no_component_is_a_named_refusal`, the component case by every apply test in
the file. `_integration_participants` yields both participant kinds and `_verify_build` now has a
`declared_type` branch for each.

Second-instance check: run against this table AT ROW-WRITE TIME, and also when a second finding
lands in the same file/subsystem within a loop (mechanism-family question: "what single authority
do these hand-model?"). On the second instance the structural fix is mandatory in that batch — or
in the immediately-next dedicated batch when it touches dispatch, compiler authority,
materialization, or shared apply code — with a sibling sweep, a non-vacuity witness, and an
authority-derived coverage claim.

## Finding rows (one per raw finding; append-only; exactly one disposition each)

| ID | Source gate + run dir + attestation | Verbatim summary | Original label | Blocking class | Defect class | Derived tier (anchor inline) | SHA/delta | Disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| INH-DR-1 | inherited seed — issue #149's trigger-map row 1, absorbed by this issue (#153 body, "#149 linkage"); #149 is CLOSED, so this slice is where the row is discharged | "**1 — direct roots validated late** (`recipes/composer.py:268-317`; `direct_process_roots` currently has ZERO callers, which is why this was non-blocking for #145)" — execution slot: "**In scope of #153.** Parse/validate direct roots BEFORE composition begins; a malformed root must fail typed, never as a raw `KeyError`. Small (S)." | non-blocking at #145 (recorded there: zero callers) | **not** a blocking class at this baseline — see the trigger correction below | (an unenforced type annotation trusted as a contract, the caller's actual input) — 1st instance | Standard-equivalent — no critical anchor, and no blocking class at this baseline since the parameter has no callers; hardened anyway because the issue puts it in scope | step 4 delta | `fixed` — `_validated_direct_roots` parses and validates every root, and both keys, before any classification/patch/composition phase; 19 tests, mutation-controlled (preflight removed → 17/19 fail) |
| S2-01 | self/measure — step 2 (typed contracts), working tree | Adding `IntegrationSpecV1.processes` inlines the entire ProcessIR schema into the SERVED `IntegrationSpecV1` schema template: canonical length `11390 -> 61651` bytes, `$defs` `4 -> 49`. Six served artifacts move: `SS-CAPABILITY-CATALOG:authoring_contract`, the `AuthoringCompileResultV1` / `AuthoringPlanResultV1` / `AuthoringRequestV1` / `IntegrationSpecV1` schema templates, and `SS-SCHEMA-TEMPLATES:walked_surface_digest`. | n/a (self-found measurement) | machine-served schemas/contracts | n/a — mandated rebaseline, not a defect: issue #153 in-scope item 4 requires the field | n/a (no gate label; recorded so a reviewer can weigh the payload growth) | working tree | `fixed` (rebaselined). **Verified benign, non-vacuously** — see the measurement note below |
| S3-01 | self/measure — step 3 (wire reshape), caught by `test_the_diagnostic_never_echoes_an_authored_value` | Moving process roots INTO `IntegrationSpecV1` put the caller's authored ProcessIR inside a SERVED field for the first time. The clean-room fixture's sentinel value, planted in the authored root, appeared in the served plan result at exactly two paths: `.authoring_result.integration_spec_preview` and `.integration_spec`. Zero diagnostics leaked. | n/a (self-found; the existing clean-room guard caught it) | **secrets/security** | (an internal structure promoted into a served field without re-applying the serving redaction rule, ADR-001 §11 value-free results) — 1st instance | **Critical** — anchor: CLAUDE.md makes any validated secrets/security finding critical, regardless of source label | working tree, step 3 | `fixed` — `build_integration_spec_preview` now withholds the roots; guard added with a positive control |
| S3-02 | self/measure — step 3, caught by re-running `test_an_in_plan_reference_is_resolved_not_dangling` after fixing S3-01 | The S3-01 fix regressed reference resolution: `build_resolved_reference_summary` and `_materialization_gaps` were both fed `spec_preview`, so redacting the preview dropped every process participant and a `$ref` to a DECLARED root was reported as dangling — the precise distinction that summary exists to draw. | n/a (self-found) | runtime behavior | (one projection serving both the served echo and an internal computation) — 1st instance; the same root cause as S3-01 viewed from the other side | Standard — no critical anchor; caught before any commit, fixed in the same batch | working tree, step 3 | `fixed` — both call sites now read `normalized.integration_spec`; redaction lives only at the serving boundary |
| S10-01 | composite wave gate (L4), `scripts/wave_gate.py wave --base 9f19aad --require-plan-fingerprint` | `PYTEST_NODE_MISSING 6 required node id(s) are not in the collection; removing a required test needs an explicit manifest tombstone in the same change.` | n/a (gate diagnostic) | — (verification bookkeeping; non-blocking class) | n/a — a deliberate rename, not a defect | Standard-equivalent | step 10 | `fixed` — the six retired refusal tests were RENAMED when inverted, because names asserting `cannot_be_applied` on tests that now assert the opposite would be actively misleading. Tombstoned the six retired node ids and registered the six replacements; the rename is 1:1, so the active count is unchanged (asserted in the same edit, not assumed). |
| S10-02 | composite wave gate (L4), earlier run | `WORKTREE_DIRTY the gate changed the worktree; it must be read-only.` — `M docs/architecture/ISSUE_153_AUDIT_LEDGER.md` | n/a (gate diagnostic) | — (non-blocking) | n/a — operator error, not a code defect | n/a | steps 5 and 10 | `fixed` — the ledger was edited WHILE the gate was in flight, which this repository's memory explicitly warns against. **This happened TWICE** (step 5 and again at step 10), which makes it a recurring operator error rather than a slip, so it gets a MECHANISM and not a resolution to be careful: the gate is now run only from a tree with `git status --porcelain` empty, verified in the same command that launches it, and nothing is edited until it reports. Every substantive check in that run had passed (suite green, 60 goldens byte-exact, `plan fingerprint checked:2 case(s)`), but a `WORKTREE_DIRTY` result is not a pass and was not treated as one; the gate was re-run on a clean tree. |
| S6-01 | self/measure — step 6, caught by the plan's own two-oracle control | The byte-parity differential lifted `process_options` OUT of each committed golden and passed it straight back, so it never exercised `DEFAULT_PROCESS_OPTIONS`. A one-byte mutation of that constant reddened the golden corpus (46 rows) but left the differential GREEN — the plan's stated "if only one goes red, one of the two oracles is not wired" case. | n/a (self-found) | — (test-oracle integrity; non-blocking class) | (a differential whose expected value is derived from the same source as its input, the golden corpus) — instance of DC-1's family, caught by a control rather than by review | Standard-equivalent | step 6 | `fixed` — the differential now also reassembles with `process_options=None` whenever the golden carries the scheduled default, and asserts at least one golden exercised it; re-running the same mutant now reddens BOTH oracles |
| S7-01 | self/measure — step 7, found by READING the consumers rather than by a test | Extending `_topological_order` over both tuples made `execution_order` contain process keys, while two loops (`_build_plan`, `_apply_plan`) still did `components_by_key[key]` unconditionally — a `KeyError` for every spec carrying a `processes` entry. No test caught it because none exercised that path yet. | n/a (self-found) | runtime behavior | (a producer widened without its consumers, the one shared execution order) — 1st instance | Standard — no critical anchor; would have failed loudly, not silently | step 7 → fixed in step 9 | `fixed` — `_build_plan` gained the canonical planning branch (emitting a `materialization: process_ir_v1` step) before the component lookup; surfaced first as `AUTHORING_COMPILE_BLOCKED` from the component-plan lint, which is how it was confirmed |
| S9-01 | self/measure — step 9, surfaced by `test_compile_routes_through_the_dispatcher` | The new `$ref` -> `depends_on` requirement fired on the M12.11 fixture, whose process unit references four components and declared none. | n/a (self-found) | runtime behavior | n/a — the requirement is correct; the FIXTURE predates it | Standard-equivalent | step 9 | `fixed` — fixture declares its four dependencies; the clean-room fixtures got theirs DERIVED from the refs each IR actually uses (intersected with the components that fixture declares), never hand-typed |
| S9-02 | self/measure — step 9, surfaced by the clean-room discoverability test | The design brief published `authoring.typed_apply.process_materialization` in `process_ir_capability_gaps` UNCONDITIONALLY, and stripped `apply` from `typed_next_steps` — so after the capability flipped, the brief advertised a supported capability inside a list of gaps and told the caller apply was refused. | n/a (self-found) | machine-served schemas/contracts | (a served projection hand-modelling a capability state the registry owns, `AUTHORING_CAPABILITY_REGISTRY`) — recurrence of DC-1's family in served text | Standard — served contract text is blocking class | step 9 | `fixed` — the gap row is now conditional on the registry state, and the `apply`-stripping filter is DELETED rather than inverted (a conditional re-adder would re-model the same fact) |
| S5-01 | self-run adversarial verification (workflow `wf_71a55dc6-cd0`, agent `verify:relocatability`), step 5 | "The canonical material carries an account-bound literal Boomi component id through `envelope.process_extensions.connections[].connection_id`, so the same logical process fingerprints differently in two accounts." | **refuted the design** (verifier verdict `design_holds: false`) | machine-served schemas/contracts + mutation accounting | (a documented completeness claim the code does not enforce, the model's own field set) — **1st instance** | **Critical** — anchor: mutation accounting; a plan whose central promise is false would attest a build that differs per account | step 5 working tree | `fixed` — reproduced independently (bytes differed across two accounts, account id present in material), then refused at plan validation with `PROCESS_MATERIALIZATION_REFERENCE_NOT_RELOCATABLE` |
| S5-02 | same verification run | "The emission plan's account-independence is an unenforced caller convention, not an invariant of the plan. Real account ids flow into the covered material whenever a non-default resolver is used." | refuted | mutation accounting | same pair as S5-01 — **2nd instance** → structural fix (below) | **Critical** — same anchor | step 5 | `fixed` — `build_materialization_plan` now OWNS compilation and forces `placeholder_backed_symbols`, so a real-id emission plan is unrepresentable; measured: a real-id resolver now yields material byte-identical to the placeholder run |
| S5-03 | same verification run | "`preservation_policy_v1()` is a lossy hand-enumeration wearing a projection's docstring: it reads 2 of `PreservationPolicy`'s 8 fields and 1 of `OwnedPath`'s 11, so the field that decides whether live state is discarded (`OwnedPath.mode`) is not covered by the fingerprint at all." | refuted | apply/update preservation | same pair — **3rd instance** | **Critical** — anchor: apply/update preservation; two materially different policies fingerprinted identically | step 5 | `fixed` — projection is now `dataclasses.asdict` of the runtime constant, complete by construction; test asserts every runtime field reaches the plan |
| S5-04 | same verification run | "`build_plan_fingerprint_fields` computes the digest via `model_construct` (no validation), so the plan-level slot-canonicalizing sort never runs on the material it hashes — making a plan with unsorted slots impossible to construct." | refuted | runtime behavior | same pair — 4th instance | Standard — no critical anchor; fails closed rather than silently | step 5 | `fixed` — slots are sorted before the digest is taken, inside the owning builder |
| S5-05 | same verification run | "`frozen=True` is shallow: `emission_plan` is stored as a mutable dict, so the stored `plan_fingerprint` can be desynchronized from the plan's own material after validation." | refuted | mutation accounting | same pair — 5th instance | Standard — defeats tamper-evidence but only under in-process mutation | step 5 | `fixed` — the emission plan is stored as canonical JSON TEXT, which is immutable |
| S5-06 | same verification run | "`EXCLUDED_PLAN_FIELDS` is dead documentation and the canonical material is a hand-written enumeration of the model's fields, with no coverage test anywhere — a new plan field is silently uncovered by the fingerprint." | refuted | machine-served schemas/contracts | same pair — 6th instance | Standard | step 5 | `fixed` — `canonical_plan_material` now walks `covered_plan_fields()`, derived from `model_fields` minus the exclusion set, so the set is load-bearing and a new field is covered by default |
| S5-07 | same verification run | "`emitter_revision()` does not cover what an emitter emits. Replacing an emitter's `emit` callable outright leaves the revision byte-identical, defeating the stated purpose of recording it on the plan." | refuted | machine-served schemas/contracts | same pair — 7th instance | Standard | step 5 | `fixed` by **narrowing the claim to what is measurable**, not by overreaching: `__qualname__` is now covered (a REPOINTED emitter is caught); bytecode hashing was considered and rejected because `co_code` differs between CPython 3.11 (CI) and 3.12 (local) and would report drift between two correct deployments. The docstring now states this limit explicitly. |
| S5-08 | same verification run | "`compiler_revision` is environment-dependent: every authority import is wrapped in a bare `except Exception` that substitutes the literal string `unavailable`, so a deployment where one optional import fails produces a different plan fingerprint for identical logical input." | refuted | machine-served schemas/contracts | (pre-existing degradation-to-`unavailable` in `_compiler_revision`, #146's surface) — distinct pair, not this slice's | Standard — real, but the mechanism predates this slice and is owned by `authoring/contract.py` | step 5 | `not-validated` **as a #153 defect** — the behaviour is #146's and unchanged here; #153 only consumes the value. Recorded rather than dismissed: the wave gate cannot catch it (both identities run in ONE process), so it is a genuine cross-deployment relocatability hazard. Disposition is to raise it with the review gates rather than silently widen this slice's scope. |
| S5-09 | self-run adversarial verification (agent `verify:preservation-parity`), step 5 | "The test advertised as 'THE non-vacuity witness' is vacuous: it touches no consumer and passes when literally nothing references the shared constant. Its docstring claim 'Under the old three-hand-copy arrangement this test could not pass' is false." — MEASURED against a pristine `ec015d5` extract | **refuted** | — (audit-record integrity; non-blocking class) | same (documented claim the code does not keep) pair — 8th instance | Standard-equivalent; audit integrity is non-blocking from row one per the template | step 5 | `fixed` — rewritten to assert every consumer resolves to ONE object; **re-measured against a pristine pre-extraction tree with the shared module added: 5 tests fail there, including the witness itself**, which the old version did not |
| S5-10 | same verification run | "The sibling sweep is a whitespace-exact substring match, and `_PROCESS_BUILDERS` is a hand-enumeration disconnected from the runtime authority `PROCESS_FLOW_BUILDERS`. A fourth registered process builder carrying its own hand-copied PreservationPolicy passes all 7 tests." | refuted | capability reachability | same pair — 9th instance | Standard | step 5 | `fixed` — the builder set is derived from `PROCESS_FLOW_BUILDERS`, and the sweep is a whitespace-insensitive regex with three positive controls covering the spellings the literal match missed |
| S5-11 | same verification run | "The frozen reachability inventory gains a scan_contract row that did NOT exist at the ec015d5 baseline: `scan_contract.python_source_count: 209 -> 212`." | refuted | capability reachability | n/a — mandated rebaseline for three new modules | Standard | step 5 | `fixed` — inventory and the paired §11 markdown regenerated together; served drift verified confined (5 aggregate-driven artifacts, ZERO structural schema change: intent properties added/removed both empty) |
| S5-12 | same verification run | "Dead import left behind: `from ._preservation_policy import OwnedPath, PreservationPolicy` is now unused in that module." | refuted (cosmetic) | — (non-blocking) | n/a | non-blocking | step 5 | `fixed` — removed after confirming nothing re-imports those names from `process_flow_builder` |
| S3-04 | #149 reachability census (`tests/_m12_12_legacy_inventory.py --check`), step 3 | New census row: `process_kind_producer \| src/boomi_mcp/authoring/workflow.py \| build_resolved_reference_summary \| IntegrationComponentSpec(type='process')`. The first version of the process-participant projection built a real legacy component spec as an internal shim. | n/a (self-found; the #149 census caught it) | capability reachability | (a synthetic internal shim built from the legacy model the census enumerates, `_m12_12_legacy_inventory` process-producer scan) — 1st instance | Standard — no critical anchor; latent, no behaviour change, but it GROWS the legacy surface #160 must retire | working tree, step 3 | `fixed` — replaced with a purpose-built `_ParticipantView` exposing only the four read-only attributes the lookup consults; census row confirmed gone by re-running `--check` |
| S3-03 | self/measure — step 3, caught by `test_an_absent_process_ir_still_reports_missing_not_a_shape_error` | `units: Tuple[...] = Field(min_length=1)` made a request carrying exactly one unit with a malformed root report BOTH `missing` (correct) and `too_short` on `units` (wrong): `min_length` counts elements that VALIDATED, so a single failing element reads as an empty list. A caller who forgot one key was told to send a second unit. | n/a (self-found) | machine-served schemas/contracts | (a field-level constraint evaluated against post-validation state, pydantic's `min_length` semantics) — 1st instance | Standard — served diagnostic text is blocking class; no critical anchor | working tree, step 3 | `fixed` — non-emptiness moved into the field validator, which does not run when an element fails, so only the diagnostic naming the real mistake is served |
| S2-02 | self/measure — step 2, working tree | The eleven per-schema entries inside `SS-SCHEMA-TEMPLATES:walked_surface_digest` ALL moved, including `ProcessIRV1`, `SystemTopologySpecV1` and `validation_report`, which this slice does not touch. | n/a (self-found) | machine-served schemas/contracts | (aggregate revision embedded in every served response, `revision_binding.schema_revision`) — explains the movement; not a hand-model, so no structural-fix trigger | n/a — expected consequence of a derived aggregate | working tree | `finding-refuted` as a defect: the movement is the derived aggregate, not a schema change. Each untouched schema's OWN `schema_hash` is unchanged; only the shared `revision_binding.schema_revision` moved, and it is derived over all owned schemas by design (`authoring/contract.py`). |
| QA-153-r1-01 | L1 Stage-1 QA round 1, `boomi-qa-tester`, report `agents/reports/2026-08-17-issue-153-m12-15-stage1-r1.md`, live against `traininghlibbochkarov-JKIY2X` | Typed apply raised `KeyError: 'existing_component_id'` for every spec carrying process units; the direct-apply capability was unreachable at the public boundary. | **Critical** (source label) | runtime behavior · capability reachability | DC-2 — **2nd instance** | **Critical** — anchor: source label Critical (immutable) | `acdb793` → fixed in `d94273e` | `fixed` — the canonical step carries `existing_component_id`/`planned_action`, and `_apply_plan` runs `_execute_canonical_process` BEFORE the component lookup. Re-verified live in r2: the apply loop now reaches the canonical arm. **Note the instance-patch cost: adding `planned_action` is what activated QA-153-r2-02**, which is the evidence that made DC-2's structural fix mandatory. |
| QA-153-r1-02 | same run | The S3-01 withholding never took effect on the served response: the legacy component-plan echo is rebuilt from the normalized spec and overwrote the withheld preview ~50 lines later, so planted canaries appeared in every served plan and compile response. | **Critical** (source label) | **secrets/security** | (a redaction applied before a later overwrite reinstates the redacted data, the last write to `spec_preview`) — 1st instance | **Critical** — anchor: secrets/security is critical regardless of label | `acdb793` → fixed in `d94273e` | `fixed` — `_withhold_process_roots` re-applied AFTER the echo, idempotently. Re-verified in r2 by a decisive A/B on one run: as shipped **0** canary hits at plan and compile; with the withholding neutralised to identity, **6** hits at plan and **3** at compile, at r1's exact six paths. |
| QA-153-r1-03 | same run | `canonical_process_apply.py` was imported by NOTHING in `src/`; the relocatability validator and the published `PROCESS_MATERIALIZATION_REFERENCE_NOT_RELOCATABLE` code were unreachable from any tool call. | **High** (source label) | capability reachability · machine-served schemas/contracts | (a validator whose only trigger is a code path no served route constructs, the served route set) — 1st instance | **Critical** — anchor: source label High | `acdb793` → partially fixed in `d94273e`, completed in the r2 batch | `fixed` — the module is on the apply path (r1) **and** the rule is now decided at plan/compile before any write, via the single shared authority `envelope_relocatability_offenders` (r2). r2 re-verification found the r1 fix PARTIAL and the residue is carried as QA-153-r2-07 rather than closed here. |
| QA-153-r1-04 | same run | A duplicate component key was served as a raw pydantic `ValidationError` with no `error_code`, while its two sibling graph rules were served properly — one rule family, two serving behaviours. | **High** (source label) | machine-served schemas/contracts | (a named rule served without its name because it raises through a different arm, `errors.py`'s taxonomy) — 1st instance | **Critical** — anchor: source label High | `acdb793` → fixed in `d94273e` | `fixed` — `_named_error_code_from_validation` maps the pydantic `type` to the served code. Re-verified live in r2: plan and compile both serve `INTEGRATION_COMPONENT_KEY_DUPLICATE`, with a control (renaming the collision compiles clean). |
| QA-153-r2-01 | L1 Stage-1 QA round 2, `boomi-qa-tester`, report `agents/reports/2026-08-17-issue-153-m12-15-stage1-r2.md`, live against `traininghlibbochkarov-JKIY2X` | "the typed apply silently DROPS every ProcessIR root and reports a successful mutation" — `_success: true`, `mutation_status: "performed"`, "Applied … with 2 steps", zero process components, no warning naming the dropped root. | **Critical** (source label) | runtime behavior · capability reachability · **mutation accounting** | DC-2 — **4th instance** (projection variant) | **Critical** — anchor: mutation accounting; an envelope attesting a mutation that did not occur | `d94273e` | `fixed` — `CompiledBundle` now carries `internals.normalized.integration_spec`, and `__post_init__` refuses any bundle whose spec lacks a root its own `process_cfg` describes. Introduced BY the r1-02 fix, which is recorded rather than smoothed over: the withholding was correct and the plumbing was not, and a silent no-op is strictly worse than the loud crash it replaced. |
| QA-153-r2-02 | same run | "a second unguarded `components_by_key[...]` raises `KeyError: '<process key>'` for every spec whose roots reach `_build_plan`" — the issue-#86 design-doctrine advisory at `integration_builder.py:6958`; three such reads exist, the slice guarded two. | **Critical** (source label) | runtime behavior · capability reachability | DC-2 — **3rd instance** → structural fix | **Critical** — anchor: source label Critical | `d94273e` | `fixed` **structurally, not at the site** — see the DC-2 structural fix. Also fixes a second, quieter failure QA's finding implies: `_legacy_plan_echo` swallows any `_build_plan` exception, so the KeyError silently DISABLED the duplicate-connection/base-URL/folder/name lints on the typed route while reporting success. Witnessed by `test_the_component_plan_lint_still_runs_for_a_canonical_root`. |
| QA-153-r2-03 | same run | "`_execute_canonical_process` calls an undefined name — every canonical apply is a `NameError`": `_connector_metadata_from_components` is defined in `authoring/workflow.py` and never imported, and the error is re-served as `PROCESS_MATERIALIZATION_PLAN_INVALID` with an internal symbol name on the wire, after connectors are already written. | **Critical** (source label) | runtime behavior · capability reachability · machine-served schemas/contracts | (a server fault served under a caller-blaming contract code, `errors.py`'s subject-per-prefix split) — 1st instance | **Critical** — anchor: source label Critical | `d94273e` | `fixed` — the function is IMPORTED, not reimplemented (copying its four lines would have removed the crash and reintroduced the hand-model this slice is closing). The generic arm now serves `PROCESS_MATERIALIZATION_INTERNAL_ERROR` for a server fault and consults `_NAMED_VALIDATION_CODES` for a real pydantic refusal, so `PLAN_INVALID` again means what it says. |
| QA-153-r2-04 | same run | "`bind_symbols_to_applied_ids` demands an applied id for the root being created — single-root apply fails 100% of the time", and with that repaired a multi-root spec fails on the first root because the sibling root's symbol is equally unapplied. | **Critical** (source label) | runtime behavior · capability reachability | (an enumeration — "every symbol in the table" — standing in for a derived requirement set, the root's declared `depends_on`) — 1st instance | **Critical** — anchor: source label Critical | `d94273e` | `fixed` — the binder resolves the root's DECLARED dependencies (already enforced as a superset of what it can reference, since an undeclared `$ref` is refused at compile) and leaves other symbols on their placeholder. Made fail-closed rather than optimistic: `materialize_canonical_process_xml` refuses any artifact in which a placeholder or `$ref` token actually survived, so a reference the rule wrongly judged unreachable stops the apply instead of shipping `id-db_conn` into a component that looks applied. |
| QA-153-r2-05 | same run | "both attestations are computed and then DISCARDED on the success path" — served only on a partial-failure envelope; absent from `apply_result` and from the build record, so verify has nothing to compare against. | **Critical** (source label) | **mutation accounting** | (an accounting record produced on the failure path only, the apply loop's own accumulator) — 1st instance | **Critical** — anchor: mutation accounting | `d94273e` | `fixed` — serialized once and attached to both the apply envelope and the build record. Additive: the keys are absent (not null) when no canonical root took part, so a legacy build keeps exactly its five original record keys — asserted by `test_a_build_with_no_canonical_root_keeps_its_original_record_shape`. |
| QA-153-r2-06 | same run | "`verify` never covers a process root — and on the typed path reports FALSE DRIFT for a perfectly healthy build": `_verify_build` walks `spec.components`, `_authoring_build_provenance` walks `results`, so the root is recorded and never observed → `missing` → `AUTHORING_LIVE_DEPLOYMENT_DRIFT`. | **High** (source label) | runtime behavior · machine-served schemas/contracts · capability reachability | DC-2 — **5th instance** | **Critical** — anchor: source label High | `d94273e` (latent, masked by r2-01) | `fixed` — `_verify_build` iterates `_integration_participants(spec)`, so the root is existence-checked AND graph-verified like any other process. Witness round-trips the REAL emitted artifact as the live readback rather than a hand-written stub (a stub would be a second hand-model of the emitter's output, silently deciding what verify sees) and asserts `_success is True` on a healthy build. |
| QA-153-r2-07 | same run | "the relocatability refusal is still absent at plan/compile, fires only after components are written, and is served under the wrong error code" — the r1-03 residue, in three parts. | **High** (source label) | machine-served schemas/contracts · runtime behavior | shares r1-03's pair — **2nd instance** → resolved by derivation rather than a second guard | **Critical** — anchor: source label High | `d94273e` | `fixed` — (a) `_validate_processes` now decides it, so `plan` REPORTS it (`is_valid: false`, error row at the offending path) and `compile` REFUSES it, before any write; (b) that removes the partial write entirely; (c) the apply arm consults `_NAMED_VALIDATION_CODES`, so the served code is `PROCESS_MATERIALIZATION_REFERENCE_NOT_RELOCATABLE`. The rule itself is NOT restated — both consumers call the one `envelope_relocatability_offenders`, so a second opinion cannot drift from the first. |
| QA-153-r2-08 | same run | "refusal envelopes echo caller-authored values and internal plan material, and omit the typed-apply envelope fields" — a 16-character prefix of an authored value measured on the wire via pydantic's `input_value=`, internal plan state in a caller-facing error, and no `action`/`mutation_performed`/`mutation_status` on the blanket-handler arms. | **Medium** (source label) | machine-served schemas/contracts | (a framework's default error rendering serving caller content, ADR-001 §11 value-free results) — 1st instance | **Standard** — no critical anchor; source label Medium. Recorded deliberately: the *class* (secrets) is critical, but the echo is of the caller's own payload back to that same caller, which is a served-contract defect rather than a disclosure to a third party — and it is fixed in this batch either way, so the tier changes nothing operationally | `d94273e` | `fixed` — `_validation_error_message` renders `loc`/`msg`/`type` and drops `input`/`ctx`; `_decorate_refusal_route` gives apply-route refusals the fields the contract tells callers to read. **The first version of the regression test passed on the broken tree**: it asserted the canary's first 16 characters were absent while pydantic had clipped the leak at 15, so the probe sat just past the window. Corrected to 12 and re-controlled. |
| SELF-r2-01 | self/measure — found while correcting the stale prose QA-153-r2-* exposed, by reading `plan_authoring_request_v1`'s comment against the behaviour this slice ships | The component-plan lint's unexecutable-step finding was downgraded from error to warning for `intent_kind == "process_ir"`, justified by "a direct ProcessIR intent is plan/compile-only by design, so nothing would be built from it either way". This slice makes that premise false: the intent IS applied, its component plan IS built, and `_apply_plan` refuses on the same `error_` prefix — so compile issued a binding whose apply could not succeed, for precisely the intent kind that had just become appliable. | n/a (self-found) | runtime behavior | (a conditional whose justifying premise the same slice invalidated, the set of appliable intent kinds) — 1st instance | Standard — no critical anchor; fails at apply rather than silently, and no mutation is mis-accounted | r2 batch | `fixed` — the exemption is DELETED, not re-conditioned (the replacement condition would be "will this plan be built?", now unconditionally yes). **Nothing failed when it was removed except the two tests that asserted the stale premise**, which is the finding: `test_a_process_ir_compile_is_not_blocked_by_a_lint_it_can_never_apply` was inverted and renamed (node tombstoned, replacement registered) and `test_compile_routes_through_the_dispatcher` re-pointed at an appliable fixture. A control — `test_an_appliable_process_ir_compile_is_NOT_blocked` — was added so the inversion cannot be satisfied by a tree that simply blocks every ProcessIR compile. |
| SELF-r2-02 | self/measure — found by re-reading my own QA-153-r2-08(c) fix rather than by a test | Giving apply-route refusals the typed envelope fields made the blanket handlers assert `mutation_status: "none"` — computed from an envelope that carries no `results` at all. For an exception that escapes the apply LOOP that is a guess, and it is the one guess that must never be made: `none` reads as retry-safe, and a retry under `conflict_policy="clone"` duplicates whatever was already written. | n/a (self-found, in this batch) | **mutation accounting** | (an accounting value computed from an envelope that cannot contain the evidence, `_apply_plan`'s own progress) — 1st instance | **Critical** — anchor: mutation accounting | r2 batch | `fixed` — the apply loop raises `_ApplyExecutionError` on any escape and the handler serves `mutation_status: "possible"` with a reconcile-before-retry hint. Scoped to the LOOP, not the whole function: everything above it is preflight, and a spec that fails to parse has provably written nothing, so reporting `possible` there would be alarmist and equally wrong. The first attempt DID wrap the whole call and was caught by `test_an_apply_route_refusal_carries_the_typed_apply_fields` flipping to `mutation_performed: true` on a pure validation refusal. Witness: `test_an_apply_that_throws_mid_flight_does_not_claim_it_was_retry_safe`, controlled against the pre-fix tree. |
| QA-153-r3-01 | L1 Stage-1 QA round 3, `boomi-qa-tester`, report `agents/reports/2026-08-17-issue-153-m12-15-stage1-r3.md`, live against `traininghlibbochkarov-JKIY2X` | The r2-08 sibling sweep missed a third arm: the apply-escape handler renders `f"Integration builder failed: {exc.cause}"` — raw `str()` — while the same handler calls `_named_error_code_from_validation(exc.cause)`, which anticipates precisely the cause type whose `str()` carries caller payload. Its two siblings both use `_validation_error_message`. Proven live: a `RuntimeError` cause serves clean, a `ValidationError` cause puts `input_value=` on the wire. | **Medium** (source label) | **secrets/security** · machine-served schemas/contracts | shares r2-08's pair (a framework's default error rendering serving caller content, ADR-001 §11 value-free results) — **2nd instance** → structural fix | **Critical** — anchor: any validated secrets/security finding is critical regardless of source label | `8d54f1a` | `fixed` structurally — see below. QA is explicit that reachability with caller-authored content is **unproven** (no natural pydantic error escaped the loop across 31 envelopes), so this is a latent path with proven serving behaviour; it is fixed as critical anyway because the anchor governs, not the reachability. |
| QA-153-r3-02 | same run | The canonical partial-failure envelope hard-sets `error_code: None` while the real code sits one level down at `partial_results[key].result.error_code`; records `status: "updated"` for a step that provably did not mutate (the target id returns HTTP 400 "ComponentId … is invalid"); and drops the `step_result` the component arm lifts. | **Medium** (source label) | **mutation accounting** | (a per-step outcome recorded from the ACTION ATTEMPTED rather than from the platform's answer, the step result's own `_success`) — 1st instance | **Critical** — anchor: mutation accounting | `8d54f1a` | `fixed` — the canonical arm now lifts the step's own `error_code` and `step_result`, and derives `status` from whether the platform confirmed the write rather than from the action that was attempted. QA notes `mutation_status: performed` was already correct, so no caller was misled about the ACCOUNT — the defect is in the per-step record, which is what verify and a human reconciler read. |
| QA-153-r3-03 | same run | Independent measurement of the leak window: pydantic renders a HEAD-position value's first **15** characters, so the 12-character assertion is non-vacuous and a 16-character probe would indeed sit past it — but pydantic elides the MIDDLE and keeps BOTH ends, so a TAIL-position value leaks its last ~24 characters with no prefix at all. A prefix sweep is therefore position-dependent and scored 0 on the QA-153-r3-01 envelope, which does render `input_value=`. | **Low** (source label) | machine-served schemas/contracts | n/a — a defect in the TEST's predicate, not in served behaviour | Standard — no critical anchor; source label Low | `8d54f1a` | `fixed` — the guard is now the position-independent invariant `"input_value=" not in served`, which is also what makes the structural fix for r3-01 checkable. This is the second time this slice that a probe measured the wrong thing and reported clean; recorded as such rather than as a passing detail. |
| QA-153-r4-01 | L1 Stage-1 QA round 4, `boomi-qa-tester`, report `agents/reports/2026-08-17-issue-153-m12-15-stage1-r4.md` | The source guard added for QA-153-r3-01 enforces ONE SPELLING, not the invariant it names: its predicate matches `{exc}` f-string interpolation and misses `str(exc)`, `%` and `.format` — and `str(exc)` is the spelling already used at ~20 sites in the same file. Mutation-controlled against the committed blob, so the measurement is immune to the dirty worktree. | **Low** (source label) | machine-served schemas/contracts | shares r2-08/r3-01's pair — **3rd instance**, and specifically a defect in the STRUCTURAL FIX itself | Standard — no critical anchor; source label Low | `98f58e5` | `fixed` — the guard is now an AST check. Scope is derived rather than enumerated: an `except` arm is IN SCOPE when it can receive an exception this module did not raise (bare, or `Exception`/`BaseException`/`ValueError` — QA confirmed at runtime that pydantic's `ValidationError` subclasses `ValueError`), and a handler catching a type this repo defines is out of scope by construction, not by allowlist. Mutation-controlled against all four spellings QA named; each reddens it, the clean tree passes. Two open arms it found were converted (`_build_plan`'s `except ValueError`, `_verify_build`'s per-participant catch). QA's own note recorded: the `re.error` conversion buys the invariant, not a behaviour change, because `re.error.__str__` omits the pattern. |
| QA-153-r4-02 | same run | The `_apply_structured_update` PUSH arm serves no `error_code`, so QA-153-r3-02's lift has nothing to lift. "The asymmetry runs backwards to the stakes: the fetch arm (nothing written) is machine-classifiable, the push arm (a write may have landed) isn't." | **Low** (source label) | machine-served schemas/contracts · mutation accounting | (a named diagnostic on the low-stakes arm of a pair and none on the high-stakes one, the pair's own failure modes) — 1st instance | Standard — no critical anchor; source label Low. Recorded deliberately: the mutation-accounting CLASS is a critical anchor, but this row is the absence of a machine-readable label on an already-served failure, not a mis-stated mutation | `98f58e5` | `fixed` — the push arm serves `UPDATE_PRESERVATION_PUSH_FAILED`, so the canonical arm's lift has something to lift on the arm that matters most. |
| CX1-01 | L2 Stage-2 Codex commit-review round 1, run dir `cdx-review.xSupcS`, `STATUS: completed`, `SCOPE: branch diff against 9f19aad5b280d58c02ef5cd840ff150d0193c1dd (9f19aad) head=98f58e56fcde5846789ae7e6b31d2d43cae574e9 dirty=false` | "Resolve canonical process targets during planning … an update-by-name passes fail-fast and can fail only after dependencies have mutated, while a create does not honor `reuse`, `fail`, or `clone` for a same-name live process and attempts another create." | **P1** | runtime behavior · mutation accounting | DC-2 — **6th instance** (a canonical root denied the collision planning every component step gets) | **Critical** — anchor: source label P1 | `98f58e5` | `fixed` — the canonical branch resolves by name through the SAME `_resolve_existing_components` the component arm uses, and derives `planned_action` under the same policy rules (`reuse`/`clone`/`error_if_exists`, plus `error_missing_target` / `error_ambiguous_match` for an update). |
| CX1-02 | same round | "Keep clone policy on the create path … this line selects `update` merely because `target_id` is truthy. The canonical path therefore overwrites the existing process instead of suffixing the name and creating a clone." | **P1** | runtime behavior · **data loss** | (a policy branch selected by a value's truthiness rather than by the policy, `conflict_policy`) — 1st instance | **Critical** — anchor: data loss (it overwrites a live component the policy exists to leave alone), and source label P1 | `98f58e5` | `fixed` — a clone clears `target_id` and creates under a suffixed name, through `_CLONE_SUFFIX_FORMAT`, which is now the ONE clone-naming rule both participant kinds format through. The decision moved AHEAD of materialization so the plan describes what will actually be built, rather than renaming an artifact already fingerprinted under the original name. |
| CX1-03 | same round | "Bind the materialization fingerprint into compile … the caller's compile hash does not cover this plan's materializer revision, emitter revision, or preservation policy; after such behavior changes, preflight can accept the old compile binding." | **P1** | machine-served schemas/contracts · mutation accounting | (a binding that cannot observe the artifact it certifies, the artifact set the compile hash covers) — 1st instance | **Critical** — anchor: source label P1; a staleness check that cannot fire is a false assurance | `98f58e5` | `fixed` — `build_artifact_descriptors` now emits a `process_component_materialization_plan` fingerprint per root, through `build_materialization_plan` itself rather than a second implementation of it. The model already declared that `artifact_kind`; the slot existed and was unpopulated. Witness moves ONLY the materializer revision and asserts the compile hash moves. Cost recorded: each root now compiles twice, and the second compile is load-bearing — `build_materialization_plan` owns its compilation and forces placeholder-backed symbols, which is the S5-02 structural guarantee; reusing the outer compile would hand it a table it did not build. |
| CX1-04 | same round | "Mark pre-write refusals as non-mutating … a process-only request rejected here therefore reports `mutation_status="possible"` and tells the caller to reconcile despite having issued no mutation." | **P1** | mutation accounting | (an outcome inferred from a status vocabulary that does not distinguish the case, the step's own write attempt) — 1st instance | **Critical** — anchor: mutation accounting | `98f58e5` | `fixed` — pre-write refusals record `status: "refused"`, added to `_NON_WRITING_STEP_STATUSES`. `failed` keeps meaning "a write was attempted and did not succeed", which is genuinely unknown and correctly reads as `possible`. Witness asserts both directions. Note this is the exact complement of QA-153-r3-02, which pushed `status` toward `failed`; the two together give three distinct values for three distinct facts. |
| CX1-05 | same round | "Inspect structural references instead of arbitrary XML text … a process with component key `db` and message text containing `id-db` is rejected after its dependencies may already have been created even though every structural reference was correctly rebound." | **P1** | runtime behavior | **DC-3** (a serialized document scanned as TEXT, with any matching string treated as structural; authority: the schema's own reference-bearing fields) — **1st instance** | **Critical** — anchor: source label P1 | `98f58e5` | `fixed` — the guard parses the artifact and matches a placeholder only as a COMPLETE attribute value. A structural reference IS the whole value of an attribute; authored prose containing the same token is not. Exact attribute-value equality separates them without enumerating which attributes carry references, which would be a hand-model of the emitter. |
| CX1-06 | same round | "Reject unmaterializable process self-references early … The request therefore passes planning, retains its own placeholder, and fails during materialization after earlier dependencies may have been written." | **P1** | runtime behavior | shares CX1-05's family (a reference rule decided late, on the artifact, rather than at preflight on the document) | **Critical** — anchor: source label P1 | `98f58e5` | `fixed` — `_check_process_root_dependencies` refuses a root that references itself, with its own message, at plan time. Witness asserts the guard directly AND carries a control (the same envelope naming a real dependency is accepted), because the legacy component-plan lint also refuses this shape end-to-end and would otherwise mask the new guard. |
| CX1-07 | same round | "Attest the placement the update actually preserved … This attestation nevertheless records the requested envelope name as the resolved placement, producing a false mutation record." | **P1** | **mutation accounting** | shares QA-153-r3-02's pair (an outcome recorded from the INTENT rather than from what the platform received) — **2nd instance** → structural fix | **Critical** — anchor: mutation accounting | `98f58e5` | `fixed` — `resolved_placement` is read from the SUBMITTED bytes for an update (`applied_folder_name`), which for a read-merge-write are the merged result the platform actually received. The pair's structural rule is now explicit and applied at both instances: **an attestation field is derived from the submitted/observed artifact, never from the request** — the same rule `submitted_xml_digest` already followed, extended to placement and to per-step `status`. |
| CX1-08 | same round | "Preserve process attestations inside authoring provenance … A successful apply consequently returns non-empty top-level attestations alongside contradictory empty arrays in `authoring_provenance`, and `verify` later returns only the empty nested values." | **P1** | mutation accounting | (a record written to one of two places that both claim to hold it, `AuthoringBuildProvenanceV1`'s declared fields) — 1st instance | **Critical** — anchor: mutation accounting | `98f58e5` | `fixed` — `_authoring_build_provenance` takes the attestations and populates the model fields that were declared for exactly this and left at their defaults. This is the completion of QA-153-r2-05, whose fix attached them at the record root only. |
| CX1-09 | same round | "Reject malformed recipe extension bindings … The strict typed models therefore never see the malformed data, and canonical apply emits a process without the requested environment overrides where the legacy renderer would have refused it." | **P1** | runtime behavior · machine-served schemas/contracts | (a tolerant normalizer silently discarding what it cannot interpret, the legacy reader's own refusals) — 1st instance | **Critical** — anchor: source label P1 | `98f58e5` | `fixed` — a non-Mapping block and a non-Mapping connection/field entry now raise `AUTHORING_COMPILE_BLOCKED` with the offending path. `None` still normalizes to empty bindings, because "absent" is a legitimate input and only "present but uninterpretable" is the defect. |
| CX1-10 | same round | "Restrict dependency discovery to typed reference fields … This recursive walk classifies every string beginning with `$ref:` as a component dependency, including literal message text, script bodies, static values, and templates." | **P2** | runtime behavior | **DC-3** — **2nd instance** → structural fix | Standard — in a blocking class, no critical anchor, source label P2 | `98f58e5` | `fixed` structurally — see the DC-3 block below. |
| CX1-11 | same round | "Include process edges in the dependency summary … A process depending on four supporting components therefore returns an empty dependency summary even though the unified execution graph enforces all four edges." | **P2** | machine-served schemas/contracts | DC-2 — **7th instance** | Standard — source label P2 | `98f58e5` | `fixed` — `build_component_dependencies` walks both participant families, and is fed the INTERNAL spec. The edge summary is value-free (keys only), so the root withholding that protects authored bodies does not apply to it. |
| CX1-12 | same round | "Keep lifted recipe roots in topology validation … A recipe request whose topology relation references its composed process root therefore regresses from a valid process symbol to `TOPOLOGY_REFERENCE_NOT_FOUND`." | **P2** | runtime behavior · capability reachability | DC-2 — **8th instance** | Standard — source label P2 | `98f58e5` | `fixed` — `_validate_topology` projects symbols from both families. **This and CX1-11 are the evidence that the DC-2 sibling sweep was INCOMPLETE**: it enumerated `integration_builder.py` and stopped there, while the class spans every consumer of a spec's participant set, including three in `authoring/workflow.py`. Recorded rather than quietly folded in — an incomplete sweep is how a structural fix becomes another instance patch. |
| CX2-01 | L2 Stage-2 Codex commit-review round 2, run dir `cdx-review.RcEkZs`, `STATUS: completed`, `SCOPE: branch diff against 98f58e56fcde5846789ae7e6b31d2d43cae574e9 (98f58e5) head=517f31d32acadb2f13c117224aa5e22c10b487ed dirty=false` | "Use the bound policy for plan fingerprints … compile fingerprints a plan built as `\"reuse\"`, while `_execute_canonical_process` builds and attests one using the requested policy … so the newly served provenance certifies a different plan than the mutation executed." | **P1** | **mutation accounting** · machine-served schemas/contracts | (a fingerprint computed under a value the caller did not request, the request's own `conflict_policy`) — 1st instance; introduced BY the CX1-03 fix | **Critical** — anchor: mutation accounting | `517f31d` | `fixed` — the compile-time fingerprint uses `request.intent.conflict_policy`. One divergence is retained DELIBERATELY and documented: when a create collides under `clone`, apply materializes a renamed envelope, so its plan genuinely differs from the compiled one. Making them equal would require compile to resolve live collisions, which is precisely what would make the plan account-bound and destroy the relocatability the fingerprint exists to certify — so the difference is the correct signal that a clone occurred, not a defect. |
| CX2-02 | same round | "Handle multiple process matches on create … Apply consequently performs an ordinary unsuffixed create under `reuse`, `fail`, and `clone`, violating all three collision policies and allowing an unintended duplicate instead of refusing or cloning safely." | **P1** | runtime behavior · data loss | DC-2's family (a collision case the component arm handles and the canonical arm did not) — introduced BY the CX1-01 fix, which added resolution but only branched ambiguity for updates | **Critical** — anchor: source label P1 | `517f31d` | `fixed` — ambiguity is treated as a collision whatever the action: `clone` proceeds as `create_clone` (it creates something new either way), `reuse` and `fail` refuse with `error_ambiguous_match`. Mirrors the component arm's rule rather than inventing a second one. |
| CX2-03 | same round | "Reject malformed connections containers … For a present block such as `{\"connection\": [...]}`, `{\"connections\": null}`, or `{\"connections\": {}}`, this coalesces the malformed value to an empty iterable and returns empty bindings … recipe normalization still silently drops requested environment overrides." | **P1** | runtime behavior · machine-served schemas/contracts | **DC-1's family** — a hand-copied SUBSET of an authority's refusals, authority `extension_bindings_from_legacy_config`; introduced BY the CX1-09 fix | **Critical** — anchor: source label P1 | `517f31d` | `fixed` structurally — the normalizer no longer answers the tolerance question, it DELEGATES it. See below. |
| CX2-04 | same round | "Preserve the legacy empty-list no-op … The legacy `extension_bindings_from_legacy_config` explicitly accepts an empty list as a no-op, and this boundary promises to preserve every legacy-accepted input, so existing equivalent recipe configuration now fails instead of emitting empty overrides." | **P2** | runtime behavior | same instance as CX2-03 — the OTHER direction of the same hand-copy | Standard — in a blocking class, no critical anchor, source label P2 | `517f31d` | `fixed` by the same delegation. Recorded as its own row because it is the evidence that a hand-copy of an authority is wrong in BOTH directions at once: the same 38 lines rejected what the authority accepts AND accepted what it rejects. |
| CX2-05 | same round | "Register the new preservation error code … callers now receive `UPDATE_PRESERVATION_PUSH_FAILED`, but this patch defines it only as a local string and never adds it to the shared `boomi_mcp.errors.ERROR_TAXONOMY`." | **P2** | machine-served schemas/contracts | (a served stable code absent from the taxonomy consumers classify by, `ERROR_TAXONOMY`) — 1st instance | Standard — source label P2 | `517f31d` | `fixed`, but NOT as prescribed — recorded because the remedy differs from the request. Registering only the new member would have put an #153-owned code outside #153's three declared prefixes and broken the one-introducer-per-family biconditional `test_issue_153_owns_its_three_families_as_a_biconditional` enforces. The whole `UPDATE_PRESERVATION_*` family (4 codes) is registered instead, under **#45**, the issue `git log -S` identifies as introducing it — which closes the real gap (the family was never discoverable) rather than half of it. `FETCH_FAILED` is declared `retryable=True` in the named retryable set with its reason; `PUSH_FAILED` deliberately is not, because the merged document was submitted and a write may already have landed. |
| QA-153-r6-01 | L1 Stage-1 QA round 6, `boomi-qa-tester`, report `agents/reports/2026-08-18-issue-153-m12-15-stage1-r6.md`, freeze stamp `6fa44c4102cb/clean/9cd84129b28b` identical at entry and exit of all 20 scenarios | "`conflict_policy` silently degrades to CREATE for any name that was previously soft-deleted." A live process resolves to ZERO candidates after its name has been deleted and recreated, so the policy block never runs: `fail` does not fail and `reuse` does not reuse. | **High** (source label) | **mutation accounting** · runtime behavior | (a guarantee about an existing component decided by a lookup that cannot prove absence, the metadata query) — 1st instance | **Critical** — anchor: source label High | `6fa44c4` | `fixed` **as far as this slice can soundly fix it, and the remainder is stated rather than implied.** QA disproved timing as the cause (0/2/5/10 s; a fresh name resolves in ~3.9 s and `fail` refuses every time) — the discriminator is deletion history. The mechanism lives in the metadata query and is SHARED with the component arm; what this slice changed is that a canonical root's policy now rests on it. Refusing every create-by-name would break the ordinary case, so a create-by-name under `fail`/`reuse` that resolves to zero now carries a served warning naming the limit and the guaranteed alternative (`envelope.component_id`, which skips resolution entirely — QA confirmed GET-by-id is unaffected). Carried to QA r7 as a live question: is the recreated component returned by the query at all, and with what `current_version`/`deleted`? That determines whether a `show_all=True` + local-dedupe fix in `paginate_metadata` is correct, which would be the real repair and belongs where the mechanism is. |
| CX3-01 | L2 Stage-2 Codex commit-review round 3, run dir `cdx-review.tqN8HE`, `STATUS: completed`, `SCOPE: branch diff against 517f31d32acadb2f13c117224aa5e22c10b487ed (517f31d) head=6fa44c4102cb17c9fcc852f9bf5d7e4c5a5e9d66 dirty=false` | "Keep ambiguous updates from selecting an arbitrary target … `_execute_canonical_process` ignores `planned_action`, sees the update action plus that ID, and updates the first match, silently rewriting an arbitrary process." | **P1** | runtime behavior · **data loss** | shares CX1-02's pair (a policy branch applied without checking the action it is valid for, `conflict_policy`'s create-only semantics) — **2nd instance** | **Critical** — anchor: data loss, and source label P1 | `6fa44c4` | `fixed` — `create_clone` is restricted to `action == "create"`; an ambiguous update stays `error_ambiguous_match` with no existing id. Introduced BY the CX2-02 fix: mirroring the component arm's rule, I mirrored the OUTCOME (`clone` proceeds) without its GUARD (`clone` proceeds *on the create path*). Recorded as the second instance of the same pair because it is: both are a clone decision made from a truthy value rather than from the action. |
| CX3-02 | same round | "Include matched candidates in canonical ambiguity steps … the plan omits the IDs needed to disambiguate, and apply's fail-fast message reports that zero components matched because it defaults the missing list to `[]`." | **P2** | machine-served schemas/contracts | DC-2's family (a canonical step omitting a field its component sibling carries) | Standard — source label P2 | `6fa44c4` | `fixed` — the canonical step carries the same sanitized `candidates` shape (`component_id`/`name`/`folder_name`) component steps carry. No extra live metadata is fetched: the list is already in hand from the resolution the previous round added. |
| CX3-03 | same round | "Convert indexed extension fields into JSON pointers … replacing only dots emits `/process_extensions/connections[0]/fields[0]` … Consumers following the diagnostic path therefore look for nonexistent keys such as `connections[0]`." | **P2** | machine-served schemas/contracts | (a format conversion that handled one separator of two, the builder's bracket notation) — 1st instance; introduced BY the CX2-03 delegation | Standard — source label P2 | `6fa44c4` | `fixed` — `_json_pointer` splits on dots AND brackets, so `connections[0].fields[0]` becomes `/connections/0/fields/0`. Witness asserts no bracket survives in any served diagnostic path. |
| CX3-04 | same round | "Register the entire preservation error family … The merge engine also serves `UPDATE_PRESERVATION_XML_PARSE_FAILED`, `UPDATE_PRESERVATION_OBJECT_MISSING`, and `UPDATE_PRESERVATION_MERGE_FAILED` … while the new exact-set test codifies the incomplete catalog." | **P2** | machine-served schemas/contracts | **DC-1** — a hand-listed set asserted as complete, authority: what the emitting modules actually serve; introduced BY the CX2-05 fix | Standard — source label P2 | `6fa44c4` | `fixed` — all **seven** emitted members registered under #45. The sharper half of this finding is the second sentence: my own witness hand-listed four codes and asserted the set was exact, so it PINNED the incomplete catalog as correct. The test now DERIVES the expected set by scanning what `integration_builder` and `component_update_preservation` emit, and asserts a floor of 7 so an empty scan cannot pass vacuously. |
| QA-153-r7-01 | L1 Stage-1 QA round 7, `boomi-qa-tester`, report `agents/reports/2026-08-18-issue-153-m12-15-stage1-r7.md`, freeze stamp `c46d031fce55/clean/c26d22adff79` identical at entry and exit of all 17 scenarios | "The apply creates a component under a **different name than authored** and reports success with `results.<key>.name` set to the *requested* name." Boomi treats a soft-deleted predecessor's name as taken and appends a counter, so authoring `X` against a deleted `X` creates `"X 2"`. Nothing is then named `X`, so `conflict_policy` never engages — three `reuse` re-applies created `x 3`, `x 4`, `x 5`. Unbounded duplication. | **High** (source label) | **mutation accounting** · runtime behavior | (a result recorded from the REQUEST rather than from the platform's answer, the live readback) — recurrence of QA-153-r3-02's pair, now on `name` rather than `status` | **Critical** — anchor: mutation accounting, and source label High | `c46d031` | `fixed` — the readback is already fetched, so the comparison is free, and it is the ONLY place the truth exists: the create response does not carry the assigned name. `results.<key>.name` now records the ACTUAL name; a mismatch adds `requested_name` + `name_reassigned_by_platform` and an execution-time warning naming both and the remedy. Live XML is now fetched ONCE and read for both the digest and the name, so the two cannot describe different reads. |
| QA-153-r6-01 (revision) | same run | Supersedes the r6-01 disposition on evidence. The original row read "a live process resolves to ZERO candidates … the mechanism lives in the metadata query"; QA's raw-row capture disproves it. | — (revision of an existing row; the original label and evidence are retained above) | — | — | — | `c46d031` | `finding-refuted` **as diagnosed**, superseded by QA-153-r7-01. The recreated component IS returned by the query and is healthy (`current_version=True`, `deleted=False`) — it is simply named `"<name> 2"`. `paginate_metadata`'s client-side filter is CORRECT and was not patched; the resolver is CORRECT and finds nothing named `X` because nothing is. QA identified its own r6 probe as the error (it filtered raw rows by the requested NAME rather than by component id). Recorded rather than quietly rewritten: I had already shipped a warning built on the wrong premise, and Codex round 4 was right that it repaired nothing. |
| CX4-01 | L2 Stage-2 Codex commit-review round 4, run dir `cdx-review.iRbfUx`, `STATUS: completed`, `SCOPE: branch diff against 6fa44c4102cb17c9fcc852f9bf5d7e4c5a5e9d66 (6fa44c4) head=c46d031fce55204d18af6ffdcd63803f1d0b7213 dirty=false` | "Preserve conflict policy when zero-match lookup under-reports … this branch only appends a warning and leaves `canonical_action` as `create`. Apply therefore still creates a component under both `fail` and `reuse`, violating the requested mutation policy; fix the shared lookup or otherwise block the mutation rather than treating a warning as sufficient." | **P1** | **mutation accounting** · runtime behavior | same pair as QA-153-r7-01 | **Critical** — anchor: mutation accounting, and source label P1 | `c46d031` | `fixed`, and the CONCLUSION is upheld while the diagnosis is corrected. Codex was right that a warning is not a repair — that is the half I acted on. Its prescribed remedies were both unavailable: the lookup is not broken (QA's raw rows), and blocking every create-by-name would make `fail` refuse ordinary first-time creates, which is the policy's normal case. The actual repair is the post-write name comparison in QA-153-r7-01, which restores the policy by making the authored name observable. The warning Codex objected to is DELETED, not narrowed — QA measured it firing on every ordinary create, where the caller can do nothing. |
| CX4-02 | same round | "Keep preservation summaries neutral about the failing side … `merge_for_update` raises this same code with `field=\"desired_xml\"`, but the new taxonomy summary incorrectly blames the live component." | **P2** | machine-served schemas/contracts | (a summary asserting a fact narrower than the code it describes, what the emitting call sites actually pass) — 1st instance; introduced BY the CX3-04 registration | Standard — source label P2 | `c46d031` | `fixed` — `UPDATE_PRESERVATION_XML_PARSE_FAILED` and `_OBJECT_MISSING` are side-neutral and point at `field`, which names the side. Both summaries were mine, written one round earlier while registering the family — a code I had just catalogued, described from only one of its two call sites. |
| CX5-01 | L2 Stage-2 Codex commit-review round 5, run dir `cdx-review.HmYvDS`, `STATUS: completed`, `SCOPE: branch diff against c46d031fce55204d18af6ffdcd63803f1d0b7213 (c46d031) head=73e2da8917c76fd026478478b9172ce526a7d684 dirty=false` | "Mark the applied name unknown when readback fails … This branch then leaves `result[\"name\"]` initialized to `envelope.name` and emits no reassignment marker or warning, even though the platform may have assigned `\"X 2\"` … This recreates the mutation-accounting bug on the explicitly supported best-effort readback-failure path." | **P1** | **mutation accounting** | same pair as QA-153-r7-01 (a result recorded from the REQUEST rather than from the platform's answer) — 2nd instance, on the FAILURE path of the fix for the first | **Critical** — anchor: mutation accounting, and source label P1 | `73e2da8` | `fixed` — `applied_name_verified` is now on every canonical result, so a caller branches on a field rather than on absence; when the readback cannot confirm the name, `requested_name` is recorded and a warning says plainly that the reported name is the one REQUESTED, not one this server observed. Worth stating: I fixed the happy path and left the documented best-effort failure path asserting the same falsehood the fix was written to remove. |
| CX5-02 | same round | "Make reassignment warnings action-aware … For an `action=\"update\"` request whose readback name differs, this same block says the component was created and that later applies will create more components … both the reported mutation type and retry diagnosis are false." | **P2** | machine-served schemas/contracts · mutation accounting | (one explanation written for the case that motivated it, applied to every case, the step's own action) — 1st instance | Standard — source label P2 | `73e2da8` | `fixed` — the create explanation is gated on `status == "created"`; an update gets its own text saying the update targeted the correct component and the name was not changed. |
| CX5-03 | same round | "Point OBJECT_MISSING consumers to `details.side` … both emitting branches set `field=\"owned_path\"`; the current-versus-desired discriminator is stored in `details.side`. The taxonomy summary therefore directs consumers to a field that cannot identify the failing side, defeating this contract correction." | **P2** | machine-served schemas/contracts | DC-1 — a summary asserting where to look, written without checking what the call sites pass; 2nd instance of CX4-02's pair | Standard — source label P2 | `73e2da8` | `fixed` — the summary names `details.side` (`current`/`desired`) and says explicitly that `field` cannot identify it. Both branches verified at the source. This is the second consecutive round in which a summary I wrote for a code I had just registered was wrong about that code's own call sites; the correction each time came from reading them. |
| CX5-04 | same round | "Carry reassignment warnings into partial failures … this warning remains only in `apply_warnings`; the subsequent partial-failure returns bypass the final merge into `apply_result`. The caller therefore loses the actionable remediation for a mutation that already occurred." | **P2** | mutation accounting | (an accumulator merged on one exit path of several, the envelope shape) — recurrence of QA-153-r2-05's pair, which was the same defect for the attestations | Standard — source label P2 | `73e2da8` | `fixed` — the canonical partial-failure envelope carries the accumulated execution warnings. Recorded as a recurrence rather than a new class: r2-05 was "attestations returned only on the failure envelope", this is "warnings returned only on the success envelope" — the same failure to treat every exit path as a serving boundary, mirrored. |
| QA-153-r8-01 | L1 Stage-1 QA round 8, `boomi-qa-tester`, report `agents/reports/2026-08-18-issue-153-m12-15-stage1-r8.md` — adversarial question, answered by INVERTING it | "Exact inequality cannot *miss* a rename … The real exposure is the opposite — if the platform normalizes a name it accepted, the flag fires on a component nobody renamed, and your warning would tell the caller to delete a stale component that doesn't exist." | **advisory** (posed as an answer to my own adversarial question, not filed as a defect) | machine-served schemas/contracts | (a remediation asserting an unverified CAUSE rather than stating the observed FACT) — 1st instance | Standard — no critical anchor; the served text is blocking class, and the exposure is a false instruction rather than a wrong mutation | `73e2da8` + this batch | `fixed` — the warning now states the fact (live name ≠ authored name, and conflict_policy will not match it) and offers the counter-append as the *usual* cause to check, rather than asserting it and prescribing a deletion. Recorded because I asked the wrong question: I worried the check would MISS a rename, and the measurable risk was that it fires correctly on a difference my explanation then mis-attributes. |
| CX6-01 | L2 Stage-2 Codex commit-review round 6, run dir `cdx-review.44SGLy`, `STATUS: completed`, `SCOPE: branch diff against 73e2da8917c76fd026478478b9172ce526a7d684 (73e2da8) head=9fe3e35efb7a8d92556ea9cc98a7c0b441faa605 dirty=false` | "Mark the no-ID mutation's name as unverified … `build_mutation_attestation` raises and `_execute_canonical_process` returns before this assignment. That documented path may already have created the component, yet `partial_results[key]` still exposes the requested `name` without an `applied_name_verified` discriminator or warning." | **P1** | **mutation accounting** | **DC-4** — 3rd instance | **Critical** — anchor: mutation accounting, and source label P1 | `9fe3e35` | `fixed` — `requested_name` and `applied_name_verified: False` are initialized where the result dict is BUILT, and the readback UPGRADES the flag rather than introducing it. Every early return therefore carries the discriminator by construction. |
| CX6-02 | same round | "Preserve warnings on every partial-failure exit … execution takes the component failure return rather than this canonical return, bypassing the added warning merge … the failure envelope drops remediation for an already-completed process mutation." | **P2** | mutation accounting | **DC-4** — 4th instance | Standard — source label P2 | `9fe3e35` | `fixed` structurally — see below. |
| CX7-01 | L2 Stage-2 Codex commit-review round 7, run dir `cdx-review.nPrkQ5`, `STATUS: completed`, `SCOPE: branch diff against 9fe3e35efb7a8d92556ea9cc98a7c0b441faa605 (9fe3e35) head=512ec82ee5ac6b490d86c00f3d59a6b54897f057 dirty=false` | "Gate readback warnings on successful write attempts … When a canonical create/update returns `_success: False` (for example, a platform 400), this new default is already false, so `_apply_plan` adds the \"live read-back did not confirm\" warning … No readback is attempted on this branch." | **P2** | machine-served schemas/contracts | **DC-4** — 5th instance, inverted: a default that is correct at construction became a false claim on the paths that never reach the step it describes | Standard — source label P2 | `512ec82` | `fixed` — the warning is gated on `_success is True` as well as the flag, so it fires only where a write succeeded and its name could not be confirmed. |
| CX7-02 | same round | "Cover pre-execution canonical exits with the discriminator … results returned for `reuse`, `conflict_policy=fail`, materialization errors, or a missing update ID still omit them. This makes the new \"every canonical result\" contract path-dependent." | **P2** | machine-served schemas/contracts | **DC-4** — 6th instance | Standard — source label P2 | `512ec82` | `fixed` — `_canonical_result()` is now the one constructor for every exit of `_execute_canonical_process`, exactly as `_partial_failure()` is for `_apply_plan`. The round-6 fix moved the initialization to "construction" but there were SIX construction sites; naming one of them the constructor is what makes the claim true. |
| CX7-03 | same round | "Make the structural test reject helper bypasses … This assertion only rejects dict returns that already contain `partial_results`. A new failure return inside the mutation loop that omits `partial_results`, warnings, and attestations — the exact regression this guard is meant to catch — has no such key, so `hand_built` remains empty." | **P2** | — (guard integrity; non-blocking class) | (a guard whose predicate tests what a violation HAPPENS to contain rather than what it does) — recurrence of the vacuity pattern this slice has hit at S5-09, QA-153-r3-03 and QA-153-r4-01 | Standard-equivalent | `512ec82` | `fixed` — the guard now walks the mutation loop and requires EVERY `return` in it to be a `_partial_failure(...)` call, rather than inspecting dict contents. Mutation-controlled: planting a bare `return {"_success": False, ...}` inside the loop reddens it; removing the plant greens it. The old predicate passed with that plant in place, which is the finding. Fourth time in this slice a guard I wrote tested the wrong thing — recorded as a pattern, not a slip. |
| CX8-01 | L2 Stage-2 Codex commit-review round 8, run dir `cdx-review.d8ucth`, `STATUS: completed`, `SCOPE: branch diff against 512ec82ee5ac6b490d86c00f3d59a6b54897f057 (512ec82) head=832305fe9244ef38ed389e01042b861552e12bf4 dirty=false` | "Exclude reused processes from readback warnings … `_execute_canonical_process` returns `_success=True` and the new constructor sets `applied_name_verified=False`. This condition therefore emits the failed-readback warning even though no write or readback occurs — and an exact-name metadata lookup already observed the name." | **P2** | machine-served schemas/contracts | **DC-4** — 7th instance, and the third narrowing of ONE condition: the flag is False by construction on every result, so every path that does not reach the readback satisfies it | Standard — source label P2 | `832305f` | `fixed` — the condition now requires the step's status to be a WRITING one, derived from `_NON_WRITING_STEP_STATUSES` (which `_mutation_status` already reads) rather than from a fresh list of `created`/`updated`. Recorded as the third narrowing because that is the shape: a constructor-set default is correct at construction and becomes a false claim on every exit that does not reach the step it describes, so the guard around it has to be positive ("a write succeeded and its name is unconfirmed") rather than negative. |
| QA-153-r10-01 | L1 Stage-1 QA round 10, `boomi-qa-tester`, report `agents/reports/2026-08-18-issue-153-m12-15-stage1-r10.md`, freeze stamp `dbeae489b50d/clean/…` on every scenario, `import_race=NO`, exit 0 | Answer to my own adversarial question: the readback warning, narrowed twice off false positives, had become too narrow in one direction. Its `_success is True` clause excludes **commit-then-fail** — proven live on both paths, a create whose component provably exists (`9ab7b41f…`) and an update whose push committed — leaving `applied_name_verified=False` and the requested name with no warning. | **Low** (source label) | machine-served schemas/contracts | **DC-4** — 8th instance, and the mirror of the previous three: those were the condition being too WIDE, this is the same condition too NARROW | Standard — no critical anchor; QA reasoned the tier explicitly and I agree: `applied_name_verified: False` IS served on those results and `mutation_status` is correct (`performed`/`possible`), so the machine-readable contract held and only the prose was missing | `dbeae48` | `fixed` — the `_success` clause is dropped. `failed` is already a WRITING status under `_NON_WRITING_STEP_STATUSES`, so the two remaining clauses select exactly "a write may have landed and its name is unconfirmed", covering commit-then-fail without re-widening onto `refused` or `reused` — the two false-positive classes the earlier narrowings were for. The witness asserts all five statuses in both verification states so a future widening cannot silently undo either narrowing. |
| CX10-01 | L2 Stage-2 Codex commit-review round 10, run dir `cdx-review.9VfLn9`, `STATUS: completed`, `SCOPE: branch diff against dbeae489b50d2c9f433edeccdd863bbda1e3555f (dbeae48) head=98a9c7aa2061fd04d20cf020f81f68583f89f40d dirty=false` | "Exclude pre-write failures from the name warning … When a canonical update has no resolved component ID, or `_apply_structured_update` fails while fetching or merging the current XML, the step is marked `failed` even though no write was attempted … falsely implying that a write may have landed." | **P2** | machine-served schemas/contracts · mutation accounting | **DC-4** — 9th instance | Standard — source label P2 | `98a9c7a` | `fixed`, and this row is where the condition finally stopped moving. QA-153-r10-01 widened it onto `failed` because a commit-then-fail may have landed; Codex found `failed` also covers provably pre-write failures. **Both are right, which is the finding**: `status` cannot answer "was anything written". The discriminator is now the ERROR TAXONOMY's own `retryable` flag, which this slice set to `True` on `UPDATE_PRESERVATION_FETCH_FAILED` *precisely because nothing was written* and `False` on `..._PUSH_FAILED` *because a write may have landed* — so the question is answered by the catalogue that was written to answer it, not by a third list here. A failure whose code is retryable now reports `refused` (a non-writing status); the missing-update-target exit, decided before any call, reports `refused` too. |
| CX10-02 | same round | "Drive the regression test through the production path … The local `_warns` helper merely reimplements the desired predicate and never invokes `_apply_plan` or `build_integration_action`. Restoring the removed `_success` condition — or deleting the production warning entirely — would therefore leave this test green." | **P2** | — (guard integrity; non-blocking class) | recurrence of the vacuity pattern (S5-09, QA-153-r3-03, QA-153-r4-01, CX7-03) — **5th instance in this slice** | Standard-equivalent | `98a9c7a` | `fixed` — the witness drives `build_integration_action` with a commit-then-fail injected at the network boundary and asserts the warning on the returned envelope; a sibling asserts the pre-write case does NOT warn. Mutation-controlled: restoring the `_success` clause reddens it. Recorded as the fifth instance because that is what it is — writing a test that re-states the rule instead of exercising it is the single most repeated mistake I made in this slice, across guards for four different subjects. |
| CX11-01 | L2 Stage-2 Codex commit-review round 11, run dir `cdx-review.L3nYfp`, `STATUS: completed`, `SCOPE: branch diff against 98a9c7aa2061fd04d20cf020f81f68583f89f40d (98a9c7a) head=b115481c00f47098eb187758d78693ad9434c2ba dirty=false` | "Derive write evidence from the update phase, not retryability … `merge_for_update` raising XML_PARSE_FAILED, OBJECT_MISSING, MERGE_FAILED, or TYPE_MISMATCH returns before `update_component_raw`, so nothing was written. Those taxonomy entries intentionally have `retryable=False`, however … `COMPONENT_GET_DEADLINE_EXCEEDED` is another pre-push failure absent from the taxonomy … Retryability describes whether the same request can be retried, not whether a write occurred." | **P1** | **mutation accounting** | **DC-4** — 10th instance, and the one that ends it | **Critical** — anchor: mutation accounting, and source label P1 | `b115481` | `fixed` — **I picked the wrong authority and the correction is the point of this row.** CX10-01 was answered by reading `ERROR_TAXONOMY[...].retryable`, on the reasoning that this slice had set it `True` exactly where nothing was written. That reasoning does not survive the full family: four merge failures write nothing and are correctly `retryable=False`, and the deadline code is not in the taxonomy at all. Write evidence now comes from `_apply_structured_update` itself, which reports `write_attempted` on **every** exit, stamped by position relative to its own push — the function that issues the write is the only thing that knows whether it did. The condition has stopped moving because it is no longer inferred. |
| CX11-02 | same round | "Exercise the actual canonical update path … this test mocks `create_component` to return an update-preservation fetch code that production creates cannot emit; only canonical updates invoke `_apply_structured_update`. The test therefore never exercises the fetch/merge path it claims to protect." | **P2** | — (guard integrity; non-blocking class) | recurrence of the vacuity pattern — **6th instance in this slice** | Standard-equivalent | `b115481` | `fixed` — the witness now drives a real canonical UPDATE with an existing component id and fails the actual GET and merge seams, and a sibling exercises all four `_apply_structured_update` exits directly, asserting the flag VARIES so a constant could not satisfy it. Confirmed non-vacuous the hard way: the first rewrite still passed under the discarded retryability discriminator, because a fetch failure is `retryable=True` and the two authorities AGREE there — the discriminating case is a merge failure, and only adding it made the test able to tell the two apart. Sixth vacuity instance, and the second in this slice where a corrected guard was still not discriminating until the disagreeing case was constructed. |
| QA-153-r11-01 | L1 Stage-1 QA round 11, `boomi-qa-tester`, report `agents/reports/2026-08-18-issue-153-m12-15-stage1-r11.md`, freeze stamp `2e96f1c58408/clean/…` on every scenario — **round CLEAN, zero findings**; this row records the one caveat QA volunteered | "I verified purity *structurally*, not by injecting into `merge_for_update`'s callees. If a preservation helper ever gains a network call, the positional stamp goes silently wrong. A guard asserting the pre-push callees take no client would pin the assumption cheaply." | **advisory** (offered as a caveat on a clean result, not filed as a defect) | mutation accounting | (an invariant depended on but not pinned — the correctness of a positional stamp resting on a purity property nothing enforces) — 1st instance | Standard — nothing is currently wrong; the exposure is a future silent regression | `2e96f1c` + this batch | `fixed` — `test_nothing_before_the_push_can_write` walks `component_update_preservation`'s AST and fails if the pre-push merge path names a client, a raw update/create, or an HTTP library, with a positive control proving the probe can see those names. Adopted rather than noted because QA is right about the failure mode: the stamp is sound today by a property no test asserted, and a lost write reported as "nothing happened" is the worst direction this contract can fail in. |
| AR1-01 | L3 §6 architect implementation review, run dir `cdx-gate-review.tD0IwO`, `gate-attest collect` `ok:true`, `status: completed`, `parsedVerdict: ISSUES FOUND`, `stopped: true`, judged against `.codex/plans/issue-153.md` at HEAD `1df67f16ee4b809802af7c4bdec98a18947a82f6` | "apply does not execute the compiled materialization plan … Compile explicitly fingerprints the plan but does 'NOT' store it … Apply rebuilds a fresh plan; its later check proves only that this new plan matches itself." | **Critical** (source label) | mutation accounting · machine-served schemas/contracts | (a binding that certifies an artifact the executing path rebuilds rather than consumes, the compiled plan itself) — 1st instance | **Critical** — anchor: source label Critical, and mutation accounting | `1df67f1` | **OPEN** — not fixed. The compile binding does now COVER the plan digest (CX1-03), so a materializer/emitter/preservation change invalidates a stale binding; what is missing is an equality assertion between the compile-time artifact digest and the plan apply rebuilds, with the recorded clone divergence as the sole exception. Concrete and small, but unimplemented at this SHA. |
| AR1-02 | same round | "the plan model is not genuinely account-independent for all valid ProcessIR. Its relocatability validator checks only envelope extension references, while canonical material includes `process_ir` wholesale … I constructed a validated, self-consistent plan whose canonical bytes contained `ACCOUNT-LITERAL-ID`." | **Critical** (source label) | machine-served schemas/contracts · mutation accounting | shares S5-01's pair (a literal account id reaching covered material) — **2nd instance**, on the IR rather than the envelope | **Critical** — anchor: source label Critical | `1df67f1` | **OPEN** — not fixed, and it is the most clearly actionable of the ten: `iter_component_refs` (added for DC-3) already enumerates every `ComponentRefV1` in the IR, so extending `envelope_relocatability_offenders` to walk the IR as well as the envelope is a small, derived change. The reviewer built the violating case, so a witness exists to write against. |
| AR1-03 | same round | "the required unresolved-symbol-slot layer is absent in production … both production callers omit them … Apply binds against the complete `envelope.depends_on`, not recorded slots." | **Standard** (source label) | machine-served schemas/contracts | (a plan field the design requires, modelled but never populated) — 1st instance | Standard — in a blocking class, no critical anchor | `1df67f1` | **OPEN** — accurate. The binding rule this slice landed derives from `depends_on` (QA-153-r2-04), which works, but the plan specified a recorded slot inventory and production never populates one. |
| AR1-04 | same round | "partial mutation evidence is not durable … `_partial_failure` returns ephemeral attestations without a `build_id` or registry write … There is no pre-mutation `in_progress` record or `failed_partial` transition as the plan required." | **Critical** (source label) | **mutation accounting** | (evidence of a mutation that exists only in a response, the build registry) — 1st instance | **Critical** — anchor: mutation accounting | `1df67f1` | **OPEN** — accurate and material. This slice made the attestations reach every failing exit (DC-4) but they are still only in the envelope; a caller that loses the response has no record of a write that happened. |
| AR1-05 | same round | "mutation-attestation values do not follow the specified accounting contract" — (a) execution passes `config.get("account_id")` rather than the account typed preflight derived, so callers can influence it; (b) `submitted_xml_digest` hashes canonical JSON `{"component_xml": …}` rather than the raw bytes; (c) the digest is created after the API call rather than immediately before it. | **Critical** (source label) | **mutation accounting** · secrets/security-adjacent (caller-influenced scope hash) | (attestation inputs taken from the request rather than from the connected account and the submitted bytes) — recurrence of QA-153-r7-01's pair | **Critical** — anchor: mutation accounting | `1df67f1` | **OPEN** — all three verified in the code by the reviewer. (a) is the sharpest: a caller-supplied `account_id` influencing the attested scope hash defeats what the hash is for. |
| AR1-06 | same round | "folder placement resolution is not implemented. The required placement errors exist only in the taxonomy; there are no production consumers … No exact, non-deleted, unique folder lookup occurs." | **Critical** (source label) | capability reachability · machine-served schemas/contracts | (published error codes with no reachable producer — the inverse of QA-153-r1-03, where the producer existed and nothing called it) | **Critical** — anchor: source label Critical | `1df67f1` | **OPEN** — a whole plan item unimplemented. `PROCESS_MATERIALIZATION_PLACEMENT_NOT_FOUND` / `_AMBIGUOUS` are registered and unreachable, which is exactly the condition #153's own r1-03 row treats as a defect. |
| AR1-07 | same round | "the internal plan and canonical material do not have the exact structure specified" — opaque JSON strings for the emission plan and preservation policy, `wire_version="2"`, flattened revisions, double-serialized emission plan instead of the specified nested version-1 object. | **Standard** (source label) | machine-served schemas/contracts | n/a — partly the RECORDED deviations S5-03 and S5-05 | Standard — source label Standard | `1df67f1` | **OPEN, partly justified** — the JSON-string choices are recorded deviations with reasons (a `Mapping` field is mutable on a frozen model; the policy projection must be complete by construction). The reviewer acknowledges both. What is not yet reconciled is the material's shape versus the plan's nested version-1 object. |
| AR1-08 | same round | "several explicit input-model constraints were deliberately relaxed or omitted" — recipe lifting accepts a config-derived name and a defaulted `action`; `IntegrationSpecV1.processes` is a mutable list not a tuple; already-instantiated `ProcessIRV1` direct roots are trusted without reparsing (reviewer reproduced a raw `KeyError("steps")`); unknown strict-model fields serve `INVALID_INPUT` rather than `PROCESS_COMPONENT_SCHEMA_UNKNOWN_FIELD`. | **Standard** (source label) | machine-served schemas/contracts · runtime behavior | mixed; the reparse item shares INH-DR-1's pair (an unenforced type annotation trusted as a contract) | Standard — source label Standard | `1df67f1` | **OPEN** — four sub-items. The reproduced `KeyError` is the most concrete: INH-DR-1 hardened `_validated_direct_roots` for RAW roots, and an already-parsed root bypasses it. |
| AR1-09 | same round | "atomic publication was incomplete. Six authorities the plan required bumped remain at version 1 … Machine-served guidance still says direct ProcessIR is never applied and ends at compile … This directly contradicts the now-supported apply capability." | **Standard** (source label) | **machine-served schemas/contracts** | shares S9-02's pair (served text describing a capability state the registry no longer has) — 2nd instance | Standard — source label Standard; served contract text is blocking class | `1df67f1` | **OPEN, and the highest-value Standard of the ten.** The capability flipped to `supported` and `meta_tools.py` / `server.py` still tell callers direct ProcessIR is never applied and ends at compile. That is served guidance contradicting shipped behaviour — precisely what S9-02 was raised and fixed for, recurring in the surfaces that row did not cover. |
| AR1-10 | same round | "required test/golden scope is missing. The diff adds no canonical full-component-envelope entry to `tests/fixtures/wave_gate/goldens.jsonl` or new golden XML. The legacy-independence test … invokes the neutral materializer leaf directly rather than exercising the required public plan→compile→materialize path." | **Standard** (source label) | — (verification coverage) | recurrence of the vacuity pattern — a test that exercises a leaf while claiming the path | Standard-equivalent | `1df67f1` | **OPEN** — accurate. `test_issue_153_canonical_apply_e2e.py` does drive the public path end to end, which the reviewer did not credit against this item, but the golden-corpus entry the plan required is genuinely absent. |
| AR1-01a | revision of AR1-01 — the §6 round-1 correction batch, applied after per-finding adversarial verification (10 verifiers + completeness critic; every CONFIRMED verdict quotes the plan lines that mandate it) | (supersedes AR1-01; original text and label retained above) | — (revision) | — | — | — (Critical tier inherited from the original row) | correction batch on top of `1df67f1` | `fixed` — Verified first (verification workflow: CONFIRMED — plan L490-513 mandate retained keyed plans and verification of the STORED fingerprint). `_PlanInternals`/`CompiledBundle` carry the compiled plan per key; `__post_init__` refuses a bundle whose described root lacks one; apply CONSUMES the stored plan and asserts its fingerprint against the served compile artifact digest before mutating; an unconstructible plan is a loud compile refusal, never a silent artifact omission. The clone name became an execution-time OVERLAY (plan L282's exclusion): plan and fingerprint stay what compile certified, only the emitted XML and mutation attestation carry the suffix — closing the CX2-01 divergence, whose recorded justification the verifier showed the plan's own design contradicts. Witnesses: a builder spy proving exactly ONE build (the preflight's, never the arm's), and a clone whose `process_mutations[].plan_fingerprint` EQUALS the compiled digest while the submitted XML carries `-clone`. |
| AR1-02a | revision of AR1-02 — the §6 round-1 correction batch, applied after per-finding adversarial verification (10 verifiers + completeness critic; every CONFIRMED verdict quotes the plan lines that mandate it) | (supersedes AR1-02; original text and label retained above) | — (revision) | — | — | — (Critical tier inherited from the original row) | correction batch on top of `1df67f1` | `fixed` — CONFIRMED (plan L201-203: "materializable ProcessIR or extension references" alike). One enumeration authority — `iter_plan_component_refs` — now feeds BOTH relocatability and the slot inventory (the critic's DC-3 interaction), chaining envelope refs with `iter_component_refs` over every `ComponentRefV1` in the IR. Model validator and plan/compile check both walk the whole plan. Witness authors the reviewer's violating case (literal Boomi id in `source.connection_ref`) and asserts the named refusal, with the `$ref` control compiling clean. |
| AR1-03a | revision of AR1-03 — the §6 round-1 correction batch, applied after per-finding adversarial verification (10 verifiers + completeness critic; every CONFIRMED verdict quotes the plan lines that mandate it) | (supersedes AR1-03; original text and label retained above) | — (revision) | — | — | — (Standard tier inherited from the original row) | correction batch on top of `1df67f1` | `fixed` — CONFIRMED (plan L172/191/270/510-512). `derive_symbol_slots` derives the inventory inside `build_materialization_plan` from the SAME enumeration relocatability reads: one slot per occurrence, `slot_id` = stable source pointer, `expected_component_types` from the compile symbol table, required NONEMPTY per the plan. The caller-supplied slot parameter is REMOVED — an authored inventory could disagree with what the plan binds, the hole S5-02 closed for symbols. Late binding's required set now derives from the recorded slots. |
| AR1-04a | revision of AR1-04 — the §6 round-1 correction batch, applied after per-finding adversarial verification (10 verifiers + completeness critic; every CONFIRMED verdict quotes the plan lines that mandate it) | (supersedes AR1-04; original text and label retained above) | — (revision) | — | — | — (Critical tier inherited from the original row) | correction batch on top of `1df67f1` | `fixed` — CONFIRMED (plan L366-371 verbatim). A typed canonical apply allocates its build record BEFORE the first mutation (`in_progress`), appends each root's attestations durably as they land, transitions to `failed_partial` on failure with the `build_id` served on the failure envelope, and success COMPLETES the same id. Scoped exactly as the plan scopes it; legacy records keep their shape. Witness discards the response and recovers the applied root's attestation from the registry. |
| AR1-05a | revision of AR1-05 — the §6 round-1 correction batch, applied after per-finding adversarial verification (10 verifiers + completeness critic; every CONFIRMED verdict quotes the plan lines that mandate it) | (supersedes AR1-05; original text and label retained above) | — (revision) | — | — | — (Critical tier inherited from the original row) | correction batch on top of `1df67f1` | `fixed` — Verdict UPGRADED to CONFIRMED by the verification critic: the PARTIAL under-read plan L360-362, which mandates the bytes AND the timing; only (a)'s derivation source is unspecified, and the caller-influence defect stands regardless. (a) apply derives `account_id` from the CONNECTED client exactly as preflight/verify — witness proves a caller-supplied `account_id` no longer moves the attested scope hash; (b) `submitted_xml_digest` is raw SHA-256 over the exact UTF-8 submitted bytes, with the old object-wrapped convention asserted UNEQUAL as the control; (c) computed immediately before the wire call on each path and passed into the attestation, never recomputed after the platform answered. |
| AR1-06a | revision of AR1-06 — the §6 round-1 correction batch, applied after per-finding adversarial verification (10 verifiers + completeness critic; every CONFIRMED verdict quotes the plan lines that mandate it) | (supersedes AR1-06; original text and label retained above) | — (revision) | — | — | — (Critical tier inherited from the original row) | correction batch on top of `1df67f1` | `fixed` — CONFIRMED (plan L382-388 verbatim). `_resolve_canonical_placement` resolves `folder_name` to an exact, non-deleted, UNIQUE folder before anything is compiled or written; zero and multiple matches raise the two previously-unreachable placement codes; create attests the requested name AND resolved id; an update's effective placement stays the submitted merged bytes. The legacy first-match helper is deliberately not reused. Witness covers unique/zero/many/account-root with pre-write `refused` outcomes. |
| AR1-07a | revision of AR1-07 — the §6 round-1 correction batch, applied after per-finding adversarial verification (10 verifiers + completeness critic; every CONFIRMED verdict quotes the plan lines that mandate it) | (supersedes AR1-07; original text and label retained above) | — (revision) | — | — | — (Standard tier inherited from the original row) | correction batch on top of `1df67f1` | `fixed` — As scoped by the upheld PARTIAL: storage stays the recorded S5-03/S5-05 deviations (both justifications judged sound — the plan's own four-tuple policy spec is precisely the lossy enumeration S5-03 proved dangerous); the hashed MATERIAL takes the plan's exact canonical object — `version` "1" from the model field, the emission plan as a NESTED object, `policies`/`revisions` as the plan's nested groups. Coverage stays derived from `covered_plan_fields()`. |
| AR1-08a | revision of AR1-08 — the §6 round-1 correction batch, applied after per-finding adversarial verification (10 verifiers + completeness critic; every CONFIRMED verdict quotes the plan lines that mandate it) | (supersedes AR1-08; original text and label retained above) | — (revision) | — | — | — (Standard tier inherited from the original row) | correction batch on top of `1df67f1` | `fixed` — As scoped by the PARTIAL with the critic's corrections adopted. Fixed: (1) recipe lifting requires caller-authored `name` AND `action` via `model_fields_set`, the non-allowlisted `config.component_name` fallback removed; (2a) `IntegrationSpecV1.processes` is a `Tuple` per the plan's verbatim type — no pre-#153 caller carries the field; (3) `compose()` REPARSES every direct root, instantiated models included, from their CURRENT dump — the reviewer's mutated-model `KeyError("steps")` is now a typed refusal; (4) an unknown field on a process envelope/unit serves `PROCESS_COMPONENT_SCHEMA_UNKNOWN_FIELD`, scoped by location. NOT changed, refutation upheld by the critic: the components-only duplicate-validation half — the plan self-contradicts (L158 vs L159) and the code honours L159's no-strictification clause. |
| AR1-09a | revision of AR1-09 — the §6 round-1 correction batch, applied after per-finding adversarial verification (10 verifiers + completeness critic; every CONFIRMED verdict quotes the plan lines that mandate it) | (supersedes AR1-09; original text and label retained above) | — (revision) | — | — | — (Standard tier inherited from the original row) | correction batch on top of `1df67f1` | `fixed` — CONFIRMED (plan L569/590-598 name the six authorities). All six bump to "2" atomically in `AUTHORING_CAPABILITY_REGISTRY` (topology/recipe stay at "1"); the `meta_tools` brief and both `server.py` docstrings now describe the apply-capable workflow, and version prose defers to `list_capabilities` instead of asserting "all at version '1'". The conditional unsupported-state gap note is untouched — it serves only in the state it correctly describes. |
| AR1-10a | revision of AR1-10 — the §6 round-1 correction batch, applied after per-finding adversarial verification (10 verifiers + completeness critic; every CONFIRMED verdict quotes the plan lines that mandate it) | (supersedes AR1-10; original text and label retained above) | — (revision) | — | — | — (Standard tier inherited from the original row) | correction batch on top of `1df67f1` | `fixed` — CONFIRMED (plan L730's imperative Update list; L681-687). Golden `canonical_envelope:appliable_rest` (`golden-000061`, renderer `process-component-v1`, so the frozen envelope is GRAPH-VERIFIED) renders through the PUBLIC normalize -> build plan -> late-bind -> emit -> materialize chain with deterministic ids, byte-frozen at `tests/fixtures/golden_xml/canonical_process_envelope.xml`; the corpus case inlines its fixture because goldens must render with every test module unimportable. The legacy-independence proof now drives that same public chain under the bombed legacy entry points instead of the materializer leaf, and asserts the bound ids reached the artifact. Rows 25-27 stay semantic-only as the plan directs. |
| CX13-01 | L2 Stage-2 Codex commit-review round 13, run dir `cdx-review.Y3Vlgt`, `STATUS: completed`, `SCOPE: branch diff against 7c997e1d31b999fd5263c427f1c04ced823894a4 (7c997e1) head=c48caa9af40dce56bbe2172e7b37d89c1d9ce26b dirty=false` | "Suppress serializer warnings before reparsing mutable roots … mutating `body` to `{\"password\": ...}` writes that secret to stderr/logs even though the eventual `RecipeError` is sanitized." | **P1** | **secrets/security** | (a framework's default rendering leaking caller content on a SIDE channel — stderr — rather than the response; sibling of r2-08's pair) — introduced BY the AR1-08 reparse fix | **Critical** — anchor: secrets/security | `c48caa9` | `fixed` — `model_dump(mode="json", warnings=False)`; witness mutates a parsed root's body to carry a canary, asserts the refusal AND that no captured warning renders it. |
| CX13-02 | same round | "Resolve folders before mutating process dependencies … A missing or ambiguous `folder_name` therefore produces a fully pre-decidable placement refusal only after earlier mutations have landed." | **P1** | runtime behavior · mutation accounting | (a pre-decidable refusal decided inside the mutation loop; same family as QA-153-r2-07's partial-write) — introduced BY the AR1-06 fix, which placed resolution in the arm | **Critical** — anchor: source label P1 | `c48caa9` | `fixed` — placement resolves for EVERY canonical root before the loop starts; a refusal now returns with `partial_results == {}`, and the witness asserts exactly that. Only the repo's own `CanonicalProcessApplyError` is caught pre-loop (my AST guard caught the first draft's open `except` rendering raw) — a transport failure propagates to the blanket handlers, which honestly report `mutation_status: "none"` because nothing has been written. |
| CX13-03 | same round | "Keep the clone overlay in the outer requested name … `_apply_plan` compares that value with the live suffixed name, so every healthy clone receives the platform-rename/duplicate warning." | **P2** | machine-served schemas/contracts | **DC-4** — 11th instance: a field made overlay-aware at the STEP level and left raw on the outer return | Standard — source label P2 | `c48caa9` | `fixed` — the outer return reads `_requested_name()` too; witness runs a healthy clone end to end and asserts no rename and no unconfirmed-name warning. |
| CX13-04 | same round | "Recognize units nested under integration-spec processes … An extra field directly on that `ProcessAuthoringUnitV1` matches neither `.units.` nor `.envelope`, so this branch serves generic `INVALID_INPUT`." | **P2** | machine-served schemas/contracts | (a location predicate enumerating one intent shape of two) — introduced BY the AR1-08 unknown-field fix | Standard — source label P2 | `c48caa9` | `fixed` — `.processes.` paths serve the registered code; witness drives the integration_spec-intent shape. |
| CX14-01 | L2 Stage-2 Codex commit-review round 14, run dir `cdx-review.j3NUTW`, `STATUS: completed`, `SCOPE: branch diff against c48caa9af40dce56bbe2172e7b37d89c1d9ce26b (c48caa9) head=c0e3ba78852aac7f07ebd1835bc9ccec36d471dc dirty=false` | "Skip placement checks for reused process roots … a moved/deleted folder or missing folder-list permission now rejects an otherwise valid idempotent reuse." | **P2** | runtime behavior | (a pre-pass validating an input the selected action never consumes) — introduced BY the CX13-02 hoist | Standard — source label P2 | `c0e3ba7` | `fixed` — the pre-pass reads each root's `planned_action` and skips `reuse`, which writes nothing and uses no folder id. Witness: a vanished folder no longer blocks a reuse, with the control showing the same vanished folder still refusing a root that would CREATE. |
| CX14-02 | same round | "Limit `.processes.` matching to component-schema paths … `processes.0.process_ir.body.steps.0...` … incorrectly serves `PROCESS_COMPONENT_SCHEMA_UNKNOWN_FIELD` … registered for envelope, unit, and extension-binding fields, not ProcessIR schema failures." | **P2** | machine-served schemas/contracts | (a location predicate matching by substring across two schemas' paths) — 2nd refinement of the CX13-04 predicate | Standard — source label P2 | `c0e3ba7` | `fixed` — the exclusion anchors on the UNIT's own field: `.units.<n>.process_ir.` / `.processes.<n>.process_ir.`. Two probe corrections recorded on the way: the first exclusion draft used a bare substring and immediately broke genuine envelope failures, because the typed intent's discriminator TAG is itself literally `process_ir` (caught by the existing witness); and the first witness used the typed intent shape, where IR failures never reach this predicate at all — the pre-fix control stayed green until the probe was re-aimed at the integration_spec shape the reviewer actually named. |
| QA-153-r12-01 | L1 Stage-1 QA round 12, `boomi-qa-tester`, report `agents/reports/2026-08-18-issue-153-m12-15-stage1-r12.md`, freeze stamp `a614519cdbbd/clean/…` on every scenario | "The attested placement never happens." A create validates the folder, resolves it, attests `resolved_placement.folder_id` — and the component is created at the ACCOUNT ROOT. Measured: the submitted bytes provably carried `folderName=<leaf>` and the platform ignored it; legacy builders land at root for every spelling (leaf, full path, folder id alike), so no builder on this surface has ever placed a component on this account. What the AR1-06 fix added was a validated, ATTESTED promise on top of an inert platform mechanism. | **High** (source label; QA marked the anchor explicitly) | **mutation accounting** | shares QA-153-r3-02/r7-01's pair (a result recorded from the REQUEST rather than from the platform's answer) — 3rd instance of the pair, on placement | **Critical** — anchor: mutation accounting | `a614519` | `fixed` — the r10 pattern, exactly as QA prescribed: placement is verified from the READBACK (`observed_folder_leaf` over `folderFullPath`), the resolved id is attested only when the platform honoured the request, an ignored placement attests the OBSERVED location with `folder_id: None`, the step carries `requested_folder_name`/`placement_verified`/`observed_folder`, and an actionable execution warning names where the component actually is and the remedy. The pre-write refusals (NOT_FOUND/AMBIGUOUS, reuse-skips) are unchanged — QA confirmed those correct; only the success path's over-claim is closed. The platform fact itself (folderName ignored on create) is recorded here as a live-measured constraint, not modelled around: a post-create folder move was considered and rejected in-slice because it is a SECOND mutation with its own failure modes, and #153's contract is honest attestation of the first. Witness models BOTH platforms — honouring (id attested, no warning) and ignoring (observed attested, id withheld, warning names the requested folder). |
| CX16-01 | L2 Stage-2 Codex commit-review round 16, run dir `cdx-review.MF4qC5`, `STATUS: completed`, `SCOPE: branch diff against a614519cdbbd14881b3dbda73a52f1525e3e4a72 (a614519) head=511871078fd88d2dd3471b316ec9fe2a5560203c dirty=false` | "Treat an account-root readback as unplaced … Boomi's account-root readback has folderFullPath equal to the account name and no folderId, so this reduction attests `folder_name=<account name>` for a create that landed in no folder — and a child folder named like the account would falsely set `placement_honoured`." | **P1** | **mutation accounting** | (a lossy leaf projection of the platform's readback standing in for the location's identity, the readback's own folderFullPath/folderId) — 1st instance; arose refining the QA-153-r12-01 fix, whose readback consultation was right but whose reading was lossy | **Critical** — anchor: mutation accounting, and source label P1 | `5118710` | `fixed` — the leaf reducer is replaced by `observed_folder_identity`, which preserves what the leaf destroyed: a single-segment full path IS the account (root, not a folder → attests no folder name), and the readback's own folderId, when present, outranks leaf-name equality as the comparison basis. Witness models the trap the reviewer named — a requested folder named like the account no longer confirms on a root readback — plus both identity directions (a matching folderId confirms, a different folderId with the same leaf refuses). Mutant control: reverting to leaf comparison reddens the trap witness. Sibling justification: the name verification's requested-vs-observed comparison stays a name comparison because the NAME is itself the question there; no projection loss. |
| CX16-02 | same round | "Only warn 'ignored placement' after a successful readback … when the early read-back fails the step records `placement_verified=False` without `observed_folder`, and the warning falls back to claiming the component 'shows in the account root' — asserting an account-root placement that was never observed." | **P2** | runtime behavior | same pair as QA-153-r3-02/r7-01/CX5-01/QA-153-r12-01 (a served claim recorded from a default rather than from the platform's answer) — recurrence, on the failure path of the r12-01 fix, exactly as CX5-01 was on the failure path of the r7-01 fix | Standard — source label P2. The verbatim summary touches placement, but the ATTESTATION on this path already records no folder name — the defect is confined to served warning prose, which is why this row is runtime behavior rather than mutation accounting | `5118710` | `fixed` — the pair's structural rule ("an attestation field is derived from the submitted/observed artifact, never from the request") is extended to warning text: a location claim is licensed only by a parsed readback (`observed_folder` present on the step — absent means unknown), and the failure path now says the placement is unverified instead of naming a place. Witness drives a failing readback and asserts the unverified wording, no location claim on the step or in the attestation; mutant control (forcing the parsed-mismatch branch) reddens it. |
| CX17-01 | L2 Stage-2 Codex commit-review round 17, run dir `cdx-review.QwDEyQ`, `STATUS: completed`, `SCOPE: branch diff against 511871078fd88d2dd3471b316ec9fe2a5560203c (5118710) head=9968db2cce27f42fe043eb42bb137e4bbf4de2f9 dirty=false` | "Carry the observed folder identity into the attestation … when a non-root readback has a folderId different from the resolved id, the code detects the mismatch but reduces the observed location to its leaf; the attestation reports the leaf with no folder id despite knowing the actual one — the actual placement is indistinguishable from the requested same-named folder, and the warning says requested and observed match while claiming the component was not placed there." | **P1** | **mutation accounting** | same pair as CX16-01 (a lossy projection of the platform's readback standing in for the location's identity) — **2nd instance** → structural fix | **Critical** — anchor: mutation accounting, and source label P1 | `9968db2` | `fixed` structurally — the identity object is the single carrier and EVERY fact it reports reaches a served record: leaf → attestation folder name, the readback's own folderId → attestation folder id (a create's unhonoured requested resolution is never attested) plus the step, full path → the step's observed folder (so colliding leaves stay distinguishable in the warning), root-ness → gates both. Sibling sweep over the mechanism's instances in this arm: the leaf reduction (fixed at CX16-01), the folderId discard (this row), the name readback (no projection — the name is read directly and IS the question), the readback digest (lossless by construction over the full bytes). Non-vacuity: the imposter witness — pre-fix tree reddens on the attested observed id, the full-path step field, and the full-path warning; the identified/honoured controls stay green. |
| CX17-02 | same round | "Distinguish unavailable readbacks from parse failures … when the fetch times out or raises, nothing was parsed, but the warning says the readback 'could not be parsed' — callers receive a false diagnosis." | **P2** | runtime behavior | same pair as CX16-02 (a served claim recorded from a default rather than from the platform's answer) — a defect in the CX16-02 correction's own wording, exactly as QA-153-r4-01 was a defect in its structural fix | Standard — source label P2 | `9968db2` | `fixed` — the wording claims only what is known: the readback could not be read (unavailable or unparseable). Witness asserts the parse-failure diagnosis is absent on a transport-failure path; pre-fix tree reddens on it. |
| CX18-01 | L2 Stage-2 Codex commit-review round 18, run dir `cdx-review.XAct4H`, `STATUS: completed`, `SCOPE: branch diff against 9968db2cce27f42fe043eb42bb137e4bbf4de2f9 (9968db2) head=a7752ad964b44dc13fe0ee482c6392e4de2a3141 dirty=false` | "Avoid pairing a root placement with a folder ID … when a Component GET response includes folderId but omits the path attributes (optional response metadata), the identity retains the id but classifies the location as root; the id still reaches the attestation while the step records no observed folder and the warning reports the account root — one create attested as both folder-placed and root." | **P1** | **mutation accounting** | same pair as CX16-01/CX17-01 (a lossy projection of the platform's readback standing in for the location's identity) — **3rd instance**, a case the CX17-01 sibling sweep's case set missed: the sweep enumerated the identity's FIELDS, not the readback's attribute-presence combinations | **Critical** — anchor: mutation accounting, and source label P1 | `a7752ad` | `fixed` — the invariant is now attribute-presence-complete: `is_root` requires NO folder evidence at all. An id-bearing pathless readback is a folder with an unreported name (compared by id, named by id in the warning); a single-segment path that contradicts a present id yields to the id, which is the stronger claim. Coverage claim derived from the authority's case set: the readback's folder evidence is exactly {full path, name, id}, and all eight presence combinations now map to root (none present, or a bare single-segment path), a named folder (any path/name evidence), or an id-identified folder (id without usable path) — no combination can pair `is_root` with a retained id, so the contradiction is unrepresentable. Witness: an id-only readback confirms by identity when the id matches and refuses with the id named in the warning when it differs; pre-fix tree reddens on both halves. |
| CX19-01 | L2 Stage-2 Codex commit-review round 19, run dir `cdx-review.uibTeK`, `STATUS: completed`, `SCOPE: branch diff against a7752ad964b44dc13fe0ee482c6392e4de2a3141 (a7752ad) head=2b6c53705a8896a13e00527e8009710186f6e1ec dirty=false` | "Do not override an explicit account-root path with folderId … the repository contains live evidence that account-root components carry folder ids: `tests/fixtures/live_xml/m11/processproperty_minimal.xml` has a single-segment folderFullPath plus folderId. The round-18 tie-break flips that established root classification to a non-root folder, so placement comparison can treat the root as a requested folder and the warning reports it as an unknown folder id." | **P1** | **mutation accounting** | same pair as CX16-01/CX17-01/CX18-01 — **4th instance**: the CX18-01 coverage claim modelled the authority's case set from the reviewer's premise (path attributes optional, id implies folder) instead of from live capture, and the live root shape refutes the tie-break | **Critical** — anchor: mutation accounting, and source label P1 | `2b6c537` | `fixed` — the invariant is corrected AGAINST LIVE EVIDENCE, not re-patched: a single-segment full path is authoritative for root even with an id present, because Boomi's root IS a folder with an id of its own; the id is retained as knowledge in the identity but a root identity's id never reaches the attestation, the step, or the warning (that pairing is the round-18 contradiction from the other direction). The pathless id-only branch keeps round 18's semantics — such an id may be any folder's, comparison by id stays valid, and a root id can never equal a resolved non-root folder id, so no false confirm exists. Witness mirrors the live fixture's exact attribute triple and reddens on the pre-fix tree at the step id, the attested id, and the warning wording; the id-only and named-folder witnesses stay green. Coverage claim, re-derived: root ⇔ the path evidence says account (single segment) or no folder evidence exists; every id propagates only from a non-root identity — and the classifier is now GRADED AGAINST THE WHOLE LIVE-CAPTURE CORPUS (every `tests/fixtures/live_xml/` capture carrying folder attributes must classify to what the platform said, ≥10 graded, both classifications exercised), so the next platform shape lands as a fixture, not as a review round. |
| CX20-01 | L2 Stage-2 Codex commit-review round 20, run dir `cdx-review.2QKxup`, `STATUS: completed`, `SCOPE: branch diff against 2b6c53705a8896a13e00527e8009710186f6e1ec (2b6c537) head=b59d414a82a93034dd1201b63a0e2a199d55fe25 dirty=false` | "Handle explicitly requested account-root placement … the account root is a folder row, so resolution accepts its id when the caller names it; the readback is the root with that same id, but the comparison forces unhonoured and the round-19 suppression discards the matching evidence — a false refusal and a misleading warning for an honoured placement." | **P2** | runtime behavior | same pair as CX16-01/CX17-01/CX18-01/CX19-01 — **5th instance**: the round-19 suppression, correct for the unrequested root, was applied unconditionally and discarded the one case where the root id IS the requested placement | Standard — source label P2; the served attestation on this path recorded no false placement (it under-claimed), which is why this row is runtime behavior rather than mutation accounting | `b59d414` | `fixed` — the retained root id is compared BEFORE suppression, per the reviewer's own third option: a match means the platform honoured an explicitly requested root placement, which is attested name+id like any folder (the round-18 pairing ban is about an id beside an EMPTY folder name; both fields are present and consistent here); suppression now applies only to a root the caller did not ask for. Witness resolves the root row by name, reads back the root with the same id, and asserts verification, the attested name+id pair, and no warning; pre-fix tree reddens on all three. The unrequested-root, trap, and id-only witnesses stay green — the suppression they pin is untouched. |
| AR2-01 | L3 §6 architect implementation review, evaluation 2, run dir `cdx-gate-review.mcjUxW`, gate-attest `turn.status: completed`, `parsedVerdict: ISSUES FOUND`, reviewed tree `3bbc5f9`, prompt sha256 `f65d0d7d…`, plan sha256 `dabc2011…` | "AR1-08 — mutable nested ProcessIR still leaks authored values during typed compilation … Direct intent normalization reuses the caller's `ProcessAuthoringUnitV1` objects without reparsing them (workflow.py:390); semantic validation then receives that mutable IR directly (workflow.py:1196) and snapshots it with warning-enabled `model_dump()` (context.py:143). Mutating `body` to a canary produced a Pydantic warning containing the canary, followed by a raw ValidationError. This repeats the secrets-side-channel class already fixed in composer with `warnings=False`." | **Critical** (source label) | **secrets/security** · runtime behavior | shares CX13-01's pair (a warning-enabled dump of a caller-mutable model rendering caller values into a diagnostic channel; authority: ADR-001 §11 value-free serving) — **2nd instance** → structural fix | **Critical** — anchor: any validated secrets/security finding is critical regardless of label; source label Critical | PENDING-SHA | `fixed` — the direct arm now REPARSES each unit's nested IR at intake (`_reparsed_unit`), so every downstream dump operates on a server-owned object, and the parser's refusal is translated into this module's own `AuthoringWorkflowError` channel rather than escaping raw. Structural sibling sweep over the mechanism (a warning-enabled dump of a caller-reachable model): the semantic validator's snapshot and the legacy-adapter's authored-pipeline dump both take `warnings=False`; the compile-time reparse already had it; the composer arm was the first instance. The context docstring's worked example was corrected to the same spelling — it is the prose a reader copies. Non-vacuity: the canary witness drives `plan_authoring_request_v1` with a unit mutated after construction and asserts no warning and no served text renders it; measured RED on the pre-fix tree (a serializer warning plus a raw ValidationError, both carrying the canary) and GREEN after. |
| AR2-02 | same round | "AR1-02 — raw `IntegrationSpecV1.processes` apply can mutate dependencies before rejecting literal references … Plan-time dependency checking ignores non-`$ref:` component references (integration_builder.py:953). The raw integration-spec route does not build its materialization plan until the process's execution turn (integration_builder.py:7642) … A mocked execution probe recorded two supporting-component writes before refusal. A literal IR reference surfaced as `PROCESS_MATERIALIZATION_INTERNAL_ERROR`; a literal extension reference got the named code, but still after the writes." | **Critical** (source label) | runtime behavior · machine-served schemas/contracts | shares QA-153-r2-07/CX13-02's pair (a fully request-decidable refusal decided inside the mutation loop; authority: the one relocatability predicate `envelope_relocatability_offenders`) — **2nd instance** → structural fix | **Critical** — anchor: source label Critical (immutable). Recorded justification for the class assignment: the verbatim summary names writes, but the partial writes ARE reported (`partial_results` carries them), so mutation accounting is intact — the defect is an unnecessary mutation plus a wrong served code | PENDING-SHA | `fixed` — relocatability is decided BEFORE anything is written, at three call sites of the one predicate: `build_materialization_plan` checks it before compilation (so a literal reference inside the IR serves the named code instead of dying inside compile as an internal error), `_apply_plan`'s pre-loop pass refuses it for every root before the first mutation, and `_build_plan` reports it as a plan WARNING (plan reports, it does not refuse — QA-153-r2-07(a)). Both raise shapes share one message constructor. Sibling sweep over pre-decidable canonical refusals: the only other candidate — an update naming no target — is ALREADY decided before the loop (raw route: plan resolution reports `error_missing_target` and executes nothing; typed route: compile blocks it), so a third copy would be an unreachable guard; that measurement is pinned by its own witness rather than asserted. Non-vacuity: witnesses assert zero dependency writes before the refusal and the named code on both reference locations; each half measured RED with its own fix reverted. |
| AR2-03 | same round | "AR1-04 — unexpected exceptions leave durable mutation records permanently `in_progress` … unexpected loop exceptions are rethrown instead (integration_builder.py:8650). The outer handler returns `mutation_status="possible"` without the allocated `build_id`, partial results, or attestations (integration_builder.py:9580). It also never changes the registry row from `in_progress`. Post-loop serialization/provenance finalization is outside the guarded block (integration_builder.py:8655), creating the same exposure there." | **Critical** (source label) | **mutation accounting** | **DC-4** — recurrence AFTER the DC-4 structural fix, on a materially different EXIT KIND: the constructors made every RETURN exit carry the field, and the raise exits were never in that case set | **Critical** — anchor: mutation accounting, and source label Critical | PENDING-SHA | `fixed` — the exception exit now carries what the returned exit always carried: `_apply_escape_evidence` finalizes the durable record to `failed_partial` and packages the build id, partial results and attestations onto `_ApplyExecutionError`, which the outer handler serves. `mutation_status` stays `possible` — that is the honest reading of an escape and is plan-sanctioned; what changes is that the caller can name the record to reconcile. The post-loop finalization was ALSO moved inside a guard (`_finalize_apply_success`): every statement there runs after the last write, so a raw escape was reported as `none` — mutations performed, caller told none, inviting a duplicating retry. The pre-loop preflight is deliberately left OUTSIDE the guard: it decides before anything can be written and must keep saying `none`. Non-vacuity: two witnesses (mid-flight throw, post-write throw), both measured RED with the evidence carry removed. |
| AR2-04 | same round | "AR1-06 — successful updates omit an available effective folder ID from mutation attestations … Execution extracts only `folderName` (canonical_process_apply.py:374) and deliberately passes `resolved_folder_id=None` for updates (integration_builder.py:7863). Although the observed folder ID is supplied, the attestation helper ignores it on the update branch (canonical_process_apply.py:351). A direct probe with submitted `folderName="F" folderId="fid"` produced `{"folder_name":"F","folder_id":null}`." | **Critical** (source label) | **mutation accounting** | same pair as CX16-01/CX17-01/CX18-01/CX19-01/CX20-01 (a lossy projection of the placement authority) — **6th instance**, now on the UPDATE branch whose authority is the merged submitted bytes rather than the readback; the create-side sibling sweep never crossed to it | **Critical** — anchor: mutation accounting, and source label Critical | PENDING-SHA | `fixed` — `applied_placement` reads BOTH placement facts from ONE parse of the merged submitted bytes, and the update branch attests the id from that same authority instead of falling back to the plan's requested resolution (which for an update describes intent, not what was applied — so it is replaced, not augmented). `applied_folder_name` is now a thin half of the shared reader, so the attested name and id cannot describe different reads. Non-vacuity: witness asserts the id travels for merged bytes carrying one and that bare bytes attest neither; measured RED against the previous expression. |
| AR2-05 | same round | "AR1-07 — the hashed preservation-policy shape is still not the specified shape … canonical material emits `preservation_policy` as `{"policy_id": ..., "projection": {...}}` (process_materialization.py:378), rather than placing the full normalized projection directly at `policies.preservation_policy`. The added `projection` wire level is not justified by either recorded deviation." | **Medium** (source label) | machine-served schemas/contracts | **DC-6** (new) — a hand-assembled wire object whose conformance to a SPECIFYING DOCUMENT is asserted in prose beside the assembly; authority: the plan's canonical object §3 L252-255 — 1st instance | Standard — in a blocking class, no critical anchor; source label Medium | PENDING-SHA | `fixed` — `canonical_plan_material` places the full normalized projection directly at `policies.preservation_policy` with `policy_id` as a sibling, exactly the plan's canonical object; storage keeps the recorded S5-03/S5-05 JSON-text deviations untouched. Every plan fingerprint VALUE moves, which is why this landed before any identity pin or golden freeze; nothing pins a literal plan digest (verified across tests, goldens manifest, wave-gate provider and the census). Non-vacuity: the witness derives the required key set from `PROCESS_PRESERVATION_POLICY`'s own dataclass fields, so a partial projection fails it too; measured RED against the wrapper shape. |
| AR2-06 | same round | "AR1-08 — explicit trim-once normalization is not implemented. `_require_unpadded` rejects padded values instead of trimming them (process_component.py:74), affecting override IDs and connector types. Envelope names merely reject blank strings and preserve surrounding whitespace (process_component.py:209). A probe confirmed `name="  N  "` remains padded." | **Medium** (source label) | machine-served schemas/contracts | **DC-6** — the same class as AR2-05 (a field's normalization asserted by the plan and neither implemented nor deviated from in the record) — 2nd instance → structural fix | Standard — in a blocking class, no critical anchor; source label Medium | PENDING-SHA | `fixed` (name half) **plus a recorded deviation** (id/connector_type half). `name` now routes through `_require_unpadded`, so padding is refused on the same footing as every other structural string in the module — it previously accepted `"  N  "` verbatim, which is worse than either alternative, because the name is fingerprint-covered AND emitted into the XML, so two spellings the plan defines as ONE canonical envelope minted different fingerprints and different bytes. **Deviation from the plan's literal "trim once", covering `id`, `connector_type` and `name`:** this module rejects rather than silently rewrites. Grounds — the hand-model IS the authority is not claimed; the recorded ground is that rejection is a fail-closed strengthening under which every accepted value already equals its trimmed form, it is the module's established idiom at every other structural field, and trim-once still happens at the legacy adapter boundary where the legacy contract needs it. No follow-up issue is minted: this is an accepted in-slice limitation with no residual defect, not deferred work. Non-vacuity: the witness enumerates the structural fields and asserts each refuses padding, with an unpadded control. |
| AR2-07 | same round | "AR1-08 — most issue-owned model error codes are unreachable through the served wrapper. The shared Pydantic mapping includes only four codes (integration_builder.py:7306); typed-request rejection special-cases only unknown fields and maps everything else to `INVALID_INPUT` (integration_builder.py:9131). Blank names, empty units, and malformed extension references therefore do not serve the registered `PROCESS_COMPONENT_SCHEMA_INVALID`, `_INVALID_CARDINALITY`, and `_REFERENCE_INVALID_FORMAT` contracts." | **Medium** (source label) | machine-served schemas/contracts · capability reachability | shares QA-153-r1-03/AR1-06's pair (a registered named error with no reachable producer; authority: the registry's own issue-owned code set) — **2nd instance** → structural fix | Standard — in a blocking class, no critical anchor; source label Medium | PENDING-SHA | `fixed` — the shared pydantic map gained the process-component models' own custom types, and `_reject_invalid_typed_request` now CONSULTS that map for every error rather than special-casing unknown fields alone, so blank/padded values, bad cardinality and malformed references serve the codes registered for them. Structural half (2nd instance of registered-with-no-producer): an invariant test derives the process-component code family from the taxonomy itself and fails if any member has no reachable producer at the served boundary — registering a sixth code without a producer now fails rather than ships. Non-vacuity: removing one mapping reddens the invariant. |
| AR2-08 | same round | "Direct-root preflight drops ProcessIR diagnostic paths. Composer preserves cause codes but constructs one `RecipeDiagnosticV1` without a path (composer.py:286); the path consequently remains its empty default (errors.py:167). This does not meet the plan's requirement to preserve both ProcessIR cause codes and paths." | **Medium** (source label) | machine-served schemas/contracts | (a translation collapsing a structured diagnostic SET into one aggregate, losing per-item location; authority: the source diagnostics' own list) — 1st instance | Standard — in a blocking class, no critical anchor; source label Medium | PENDING-SHA | `fixed` — the direct-root preflight emits ONE diagnostic PER cause, each carrying the authority's own `path` and its single cause code, instead of one aggregate that kept the codes and dropped every location; a refusal with no itemized cause still reports the refusal. Non-vacuity: the witness asserts per-cause diagnostics with paths and at most one cause code each; measured RED with the path dropped. |
| AR2-09 | same round | "AR1-09 adjacent — machine-served schema prose contradicts the model. `IntegrationSpecV1.processes` is correctly a tuple (integration_models.py:74), but its emitted JSON-schema description says "A LIST, not a tuple" and claims it is edited in place (integration_models.py:92)." | **Medium** (source label) | machine-served schemas/contracts | shares S9-02/AR1-09's pair (served prose asserting a fact the code contradicts; authority: the model's own field type) — **3rd instance**, and introduced BY the AR1-08a correction batch that flipped the type | Standard — in a blocking class, no critical anchor; source label Medium | PENDING-SHA | `fixed` — the served description states the tuple type positively and drops the contradicting claims. Because this is the THIRD instance of served-prose-contradicting-code in this slice, and the second introduced by a correction batch, the fix is EXECUTABLE rather than another prose patch: a guard derives each field's container from its own annotation and fails when a tuple-typed field's served description calls itself a list or claims in-place editing. The guard proved itself immediately — it rejected the first replacement wording I wrote, which still contained "not edited in place". |
| AR2-10 | same round | "AR1-10 — the legacy-independence witness still does not exercise the public chain. The golden now exists, but the bomb test calls private `_normalize_intent`, internal `build_materialization_plan`, and internal `materialize_canonical_process_xml` directly (test_process_component_materializer.py:269) … A regression making the public compile/apply orchestration resolve a legacy builder could leave this proof green." | **Medium** (source label) | capability reachability | shares the slice's recurring guard pair (a guard that re-assembles the thing it guards instead of exercising the production entry point; authority: the public entry point `compile_authoring_request_v1` → `build_integration_action`) — recurrence → the fix drives the public chain, it does not add a second hand-assembly | Standard — in a blocking class, no critical anchor; source label Medium | PENDING-SHA | `fixed` — the legacy-independence witness now drives the PUBLIC orchestration (`compile_authoring_request_v1` → `build_integration_action`) under the bombs, instead of hand-assembling normalize/plan/materialize itself; a regression making the orchestration resolve a legacy builder now fails it. The golden renderer keeps its deterministic manual form, justified rather than changed: the corpus requires goldens to render with test modules unimportable and `dry_run` stops before materialization, so the public route cannot render there — and no plan line requires it to. Non-vacuity: injecting a legacy call into the public chain reddens the test. Harness note: the first draft compiled OUTSIDE the network-patch context, where `paginate_metadata` against a MagicMock never terminates and memory grows until the process dies — the same runaway that left multi-GB probes behind during the review that raised this finding. |
| QA-153-r2-07a | **DC-5 revision**, §6 evaluation-2 ledger pass | Revision of QA-153-r2-07's disposition, not of its finding: clause (b) "that removes the partial write entirely" was true of the TYPED route only. The raw `integration_spec` route has no compile step, so it kept reaching the refusal inside the mutation loop — measured by AR2-02, which fixes it. The original row and its evidence stand unchanged. | n/a (disposition revision) | n/a | **DC-5** — 1st instance | n/a — the original tier governs | PENDING-SHA | `fixed` — the overstated clause is corrected here and the residue it hid is fixed under AR2-02; the claim is now scoped to what its witnesses assert (the plan-report and compile-refusal witnesses, both typed-route). |
| QA-153-r6-01a | **DC-5 revision**, §6 evaluation-2 ledger pass | Revision of QA-153-r6-01's disposition: it uses "entirely" while describing a limit this slice deliberately did NOT close. The claim is narrowed to what was measured — timing was disproved as the cause (0/2/5/10 s), the discriminator is deletion history, and the shared metadata-query mechanism is unchanged by this slice. | n/a (disposition revision) | n/a | **DC-5** — 2nd instance → structural fix | n/a — the original tier governs | PENDING-SHA | `fixed` — wording narrowed; the underlying disposition (an accepted, stated limit) is unchanged and its measurements are quoted in the original row. |
| CX5-04a | **DC-5 revision**, §6 evaluation-2 ledger pass | Revision of CX5-04's disposition: "every exit path" is asserted where the row names no test. The evidence is the AST guard over partial-failure envelope construction, which derives the exit set from the function itself rather than from an enumeration in prose. | n/a (disposition revision) | n/a | **DC-5** — 3rd instance | n/a — the original tier governs | PENDING-SHA | `fixed` — the claim now cites the guard that makes it checkable; no behaviour change. |
| CX6-01a | **DC-5 revision**, §6 evaluation-2 ledger pass | Revision of CX6-01's disposition: "by construction" was asserted before a constructor existed — there were six construction sites, which is precisely what CX7-02 then found. The claim is corrected to name the constructor and its guard. | n/a (disposition revision) | n/a | **DC-5** — 4th instance | n/a — the original tier governs | PENDING-SHA | `fixed` — evidence named: `_canonical_result()` is the single constructor and the AST guard over envelope construction derives the exit set from the function itself, so the claim is measured rather than asserted. |
| CX7-02a | **DC-5 revision**, §6 evaluation-2 ledger pass | Revision of CX7-02's disposition: it says "every exit" without naming the evidence. Cited here: the AST guard enumerates the function's own exits, so a new exit that skips the constructor fails the guard. | n/a (disposition revision) | n/a | **DC-5** — 5th instance | n/a — the original tier governs | PENDING-SHA | `fixed` — claim scoped to its guard; no behaviour change. Note the AR2-03 residue this pair did not cover: the guard enumerates RETURN exits, and the raise exits were never in its case set. |
| CX8-01a | **DC-5 revision**, §6 evaluation-2 ledger pass | Revision of CX8-01's disposition: "every exit" again asserted without named evidence. The derivation from `_NON_WRITING_STEP_STATUSES` is the substance and it is witnessed by the warning tests around the writing/non-writing split. | n/a (disposition revision) | n/a | **DC-5** — 6th instance | n/a — the original tier governs | PENDING-SHA | `fixed` — claim cites the derived authority and the tests that exercise both sides of the split (the pre-write-refusal witness and the ignored-placement warning witnesses). |
| CX22-01 | L2 Stage-2 Codex commit-review round 22, run dir `cdx-review.zbn2q3`, `STATUS: completed`, `SCOPE: branch diff against 65e689f4bb44e126fe24ea11e50a36a51984b8c8 (65e689f) head=08d92413b6f4642f379fd0b1e7cc598a8941ea74 dirty=false` | "Preserve no-write status on finalizer failures … When an apply consists only of `reuse` steps and `_finalize_apply_success` raises, this new catch wraps it as `_ApplyExecutionError`; the outer handler then reports `mutation_status="possible"` and `_apply_escape_evidence` marks the durable row `failed_partial`, although `results` proves no write was attempted." | **P1** | **mutation accounting** | (a served outcome asserted from a default rather than derived from the record that can answer it) — introduced BY the AR2-03 fix, which widened the guard without deriving the state the wider guard now had to report | **Critical** — anchor: mutation accounting, and source label P1 | PENDING-SHA | `fixed` — the escape derives write state from `_mutation_status` (the one reader the success path already uses), but ONLY where the record can answer: the post-loop escape, where every step has recorded its own status. The mid-loop escape keeps `possible` unconditionally, because `results[key]` is assigned after a write RETURNS — an empty `results` mid-loop means unknown, never nothing. The first draft derived it at both sites and was caught by the existing mid-flight witness, which is the control that shaped the fix. The no-write escape also drops the reconcile hint, which would be false. Witness measured RED under the pre-fix assumption; its own first draft was VACUOUS twice (the config was refused up front, then the apply was not actually all-reuse because the supporting components still created) — both corrected before the control was believed. |
| CX22-02 | same round | "Skip relocatability refusal for reused roots … the planner selects `planned_action="reuse"` and execution deliberately returns without compiling or materializing the authored body. This check runs before the reuse branch, so a literal reference in that unused body now rejects an otherwise valid idempotent no-write reuse." | **P2** | runtime behavior | shares CX14-01's pair (a pre-pass validating an input the selected action never consumes) — **2nd instance** → structural fix | Standard — in a blocking class, no critical anchor; source label P2 | PENDING-SHA | `fixed` structurally — the non-writing skip is made ONCE, at the top of the pre-decidable pass, for every check in it. Round 14 fixed exactly this for the placement check alone; leaving the skip per-check is what let the next check repeat it, so the decision moved to the pass. Witness: a reuse with a literal reference in its unused body now succeeds, with the control showing the same literal still refused pre-write on a root that would CREATE; the round-14 placement witness reddens under the same mutant, which is what confirms both checks now share the one skip. |
| CX22-03 | same round | "Keep nested ProcessIR errors in the IR taxonomy … Pydantic emits `process_ir_reference_invalid_format`, but this global mapping now serves the process-component reference code. The ProcessIR authority maps that same type to its own reference code, so callers receive the wrong machine code; use the error location to reserve the component code for envelope/extension references." | **P2** | machine-served schemas/contracts | (a mapping keyed on a type two schemas share, decided without the location that distinguishes them) — introduced BY the AR2-07 fix; same shape as CX13-04/CX14-02's location predicate, now on the code side rather than the unknown-field side | Standard — in a blocking class, no critical anchor; source label P2 | PENDING-SHA | `fixed` — the component code is RESERVED for the component's own paths and an IR-located reference keeps the IR authority's code, decided by `_is_process_component_path`, which is now the ONE location classifier: the unknown-field rule consults the same function instead of carrying its own copy of the path test. That extraction is the point — two copies of a location rule are how two overlapping schemas get judged two ways, which is the defect this row records. Witness asserts both directions of the classifier and both served codes. |
| CX23-01 | L2 Stage-2 Codex commit-review round 23, run dir `cdx-review.7i57sC`, `STATUS: completed`, `SCOPE: branch diff against 08d92413b6f4642f379fd0b1e7cc598a8941ea74 (08d9241) head=915cae64e0266fa5ba03ff9232d658d34f6f729f dirty=false` | "Suppress stale relocatability warnings for reuse … `_build_plan` still warns that a literal reference means "Apply refuses this before writing anything." This new early return instead skips validation and allows the apply to succeed, so plan and dry-run now publish a false refusal warning. Generate or filter that warning using the same planned-action decision." | **P2** | machine-served schemas/contracts | (two surfaces deciding the same question from different inputs — the preview from the request alone, the apply from the planned action) — introduced BY the CX22-02 fix, which changed one side of a rule the other side predicts | Standard — in a blocking class, no critical anchor; source label P2 | `915cae6` | `fixed` — the warning is emitted from inside the step-building loop, after `canonical_action` is decided, and suppressed for a root the apply will reuse. Plan and apply now read the SAME decision rather than two computations of it, which is what makes them unable to disagree. Witness asserts silence for a reusable root and the warning for the identical root with nothing to reuse; measured RED with the gate removed. |
| CX23-02 | same round | "Emit an error code for no-write finalizer failures … the response now correctly reports `mutation_status="none"`, but this arm only replaces the hint. `_decorate_refusal_route` does not supply the code that `_decorate_typed_apply` adds to every other no-write failure, so the resulting retry-safe typed failure has no `error_code` and code-based clients cannot classify it." | **P2** | machine-served schemas/contracts | **DC-4** — a field required on every exit, absent on the exit that was newly reached; the CX22-01 fix created a no-write ESCAPE, a shape `_decorate_typed_apply`'s rule had never had to cover | Standard — in a blocking class, no critical anchor; source label P2 | `915cae6` | `fixed` — the escape sets the same validation-required constant `_decorate_typed_apply` sets for every other no-write typed failure, so the one failure a caller may safely retry is classifiable. The constant is shared, not a second policy for the same fact. Witness asserts the code on an all-reuse apply whose finalizer raised; measured RED with the line removed. |
| CX24-01 | L2 Stage-2 Codex commit-review round 24, run dir `cdx-review.uymbJ0`, `STATUS: completed`, `SCOPE: branch diff against 915cae64e0266fa5ba03ff9232d658d34f6f729f (915cae6) head=bade0e718b0ef857ca9a5a08016b7f6338478602 dirty=false` | "Use a finalizer-failure code for finalizer crashes … The registered taxonomy defines that code as a missing or unreproducible compile binding and marks it non-retryable, while this response explicitly says retrying is safe; code-based clients will therefore apply the wrong remediation to a server-side finalizer fault. Preserve named cause codes and use an internal/finalization failure code for unnamed exceptions instead." | **P2** | machine-served schemas/contracts | (a served code chosen by convenience rather than from its registered spec, so the machine-readable half of a response contradicts its prose; authority: the code's own `ERROR_TAXONOMY` entry) — 1st instance; introduced BY the CX23-02 fix, which borrowed the nearest existing constant | Standard — in a blocking class, no critical anchor; source label P2 | `bade0e7` | `fixed` — a code is REGISTERED for what actually happened: `PROCESS_MATERIALIZATION_FINALIZATION_FAILED`, category `process_materialization`, owner #153, and `retryable: True` — the first #153 code that advertises a retry, which is legitimate here on the same evidence the envelope serves (the steps' own statuses prove no write was attempted, and the code is reachable only on that no-write escape). The mid-write escape still carries no code, because there the outcome is genuinely unknown and advertising a retry would be the very defect this row records. Named cause codes still win, applied after. The family test is tightened rather than relaxed: #153's codes remain non-retryable except for this one named member, asserted as an exact set so a second cannot slip in. Witness pins the agreement itself — the served code's REGISTERED `retryable` must match the envelope's retry-safe prose — which is the check whose absence let a contradictory pair ship. |
| QA-153-r14-01 | L1 Stage-1 QA round 14, `boomi-qa-tester`, report `agents/reports/2026-08-18-issue-153-m12-15-stage1-r14.md`, freeze stamp `48dd8ef83baa/clean/3a680853b8de` on every scenario, live against `traininghlibbochkarov-JKIY2X` | AR2-07 added the ProcessIR reference type to the shared code map; the round-22 fix added the location classifier to only ONE of that map's two readers. `_named_error_code_from_validation` — used by the raw `integration_spec` route and by the apply-escape handler — is still type-only, so BOTH an IR-located and an envelope-located reference collapse to the component code while the served prose still names the ProcessIR type. Measured A/B against a baseline sandbox: baseline served no code for either; HEAD serves the component code for both. | **Medium** (source label) | machine-served schemas/contracts | (one authority with two readers, a rule added to one of them) — a defect IN the CX22-03 correction, which extracted the location classifier and then updated a single call site; the same incomplete-sweep shape as the DC-2 and r2-08 sweeps | Standard — in a blocking class, no critical anchor; source label Medium | PENDING-SHA | `fixed` structurally — the exception reader now BUILDS the same `{path, type}` rows and delegates to `_named_code_from_locations`, so the map has one resolver and a rule added to its semantics reaches every reader by construction rather than by a sweep somebody has to remember. Witness asserts BOTH readers agree on the same two inputs — the property whose absence was the defect — and reddens when the reader is returned to its type-only form. |
| QA-153-r14-02 | same round | The placement-honesty markers are gated on create, so an UPDATE naming a real folder is accepted, is not relocated, and serves no `requested_folder_name`, no `placement_verified` and no warning — while the same field with an unresolvable value refuses the update outright. Validated, then silently discarded: the r12-01 shape on the update path. (Pre-existing at `3bbc5f9`; surfaced by the AR2-04 scenario.) | **Medium** (source label) | runtime behavior | same pair as QA-153-r12-01 (a request the server validates and then drops without telling the caller) — recurrence on the UPDATE path, which the create-side fix never covered | Standard — in a blocking class, no critical anchor; source label Medium. The ATTESTATION on this path was already honest after AR2-04, so no mutation record was wrong — the defect is the envelope's silence | PENDING-SHA | `fixed` — the markers are served for an update as well, and the warning names the UPDATE mechanism: update preservation keeps the component where it already lives, so a requested folder is not applied. The create wording would have been false here — QA measured that a raw component update DOES honour a folder id on this account, so it is preservation, not the platform, that declines the move. The warning's mechanism half is also split from its outcome half, so the unverified-readback branch names why without asserting where (the round-16 rule). Witness drives an update whose live folder differs from the requested one and asserts the markers, the observed path and the update-specific wording; measured RED with the gate returned to create-only. |
| CX26-01 | L2 Stage-2 Codex commit-review round 26 (the RETRY of the stuck round; run dir `cdx-review.qayr4Q`), `STATUS: completed`, `SCOPE: branch diff against 61beae0b3445bc1ff7231706a9ca86e015a5bca8 (61beae0) head=2fd2b8ccfa31ee15bc3666188e288bf823f3435d dirty=false` | "Restore the actual placeholder in the batch note … this sentence incorrectly says the AR2/DC-5 rows contain `ac3c195`; those rows still contain the placeholder, and the following sentence correctly resolves them to `6ef9cdd`. Restore it here so the audit note is not self-contradictory." | **P3** | none — audit-record prose, not served to callers | (a blanket find-and-replace applied to a document where the search term is also its SUBJECT) — the same edit that twice violated the append-only rule, here corrupting the note that explains the placeholder by replacing the placeholder inside it | Standard — outside the blocking classes (the ledger is not served to callers); source label P3. Corrected in this batch rather than batched to the end, because a self-contradicting audit note is exactly the failure the record exists to prevent | `2fd2b8c` | `fixed` — the note again names the literal placeholder it is explaining. The underlying lesson is recorded with it: the repair for a placeholder committed into a frozen row is NEVER a global replace — it edits rows in other batches as a side effect, which the append-only scanner caught twice, and it corrupts the prose that documents the placeholder, which no scanner catches and a reviewer did. |
| CX27-01 | L2 Stage-2 Codex commit-review round 27, run dir `cdx-review.GtJsq7`, `STATUS: completed`, `SCOPE: branch diff against 2fd2b8ccfa31ee15bc3666188e288bf823f3435d (2fd2b8c) head=41dd2155d9e4baeb017493b4f4453fee163ec17c dirty=false` | "Correct the retry's claimed identical delta … the failed run started at `02bc482`, while the retry started at `2fd2b8c`; the intervening commit archives the failed run and changes `tests/test_wave_gate.py`. Retaining the baseline means the retry covered a superset of the original delta, not an identical delta, so this limitation should describe the additional changes accurately." | **P3** | none — evidence-record prose, not served to callers | shares DC-5's pair (a record claiming more precision than the facts support; authority: the run directories' own start-head values) — 2nd instance, here in the evidence index rather than a disposition | Standard — outside the blocking classes; source label P3 | PENDING-SHA | `fixed` — the limitation now states what actually happened: the failed round wrote no anchor, so the baseline correctly stayed put, and the retry therefore reviewed a SUPERSET — the original delta plus the commit that archived the failed run and added its allowlist entry. Both were reviewed; nothing in the original delta went unreviewed, which is the fact the record has to support. Verified against the two run directories' recorded start-heads rather than from memory. |
| AR3-01 | L3 §6 architect implementation review, evaluation 3, run dir `cdx-gate-review.L2LDFW`, gate-attest `turn.status: completed`, `parsedVerdict: ISSUES FOUND`, reviewed tree `3c72393`, prompt sha256 `da4ecd49…`, plan sha256 `dabc2011…` | “AR2-01 residue: `integration_spec` bypasses deep ProcessIR reparsing. The direct intent reparses units, but the integration-spec arm returns the caller-owned spec unchanged at workflow.py:414, unlike workflow.py:455. A mutated nested IR produced a raw ValidationError containing the injected value. This remains a secrets/error-contract defect.” | **Critical** (source label) | **secrets/security** · machine-served schemas/contracts | same pair as AR2-01/CX13-01 — **3rd instance**. The AR2-01 sweep enumerated the MECHANISM (a warning-enabled dump) and never asked the ARM question: which intent arms hand the compiler a caller-owned nested IR? A sweep-scope defect, not a new class | **Critical** — anchor: secrets/security regardless of label; source label Critical | PENDING-SHA | `fixed` — the integration_spec arm reparses its units through the same `_reparsed_unit` helper, reparse-only and never sorting (the caller authored this spec and the semantic-hash payload dumps it wholesale, so reordering would move the hash). The sweep is now stated by ARM — direct, recipe/composer, integration_spec — which is the question the earlier one skipped. Reachability bound recorded rather than implied: both wire routes are already value-free, so the leak needs an in-process typed caller, the same threat model under which AR2-01 was tiered Critical. Witness mutates a unit the caller still holds and asserts no warning and no served text renders it; measured RED with the reparse removed. |
| AR3-02 | same round | “AR2-02 residue: request-decidable semantic failures still occur after dependency writes. The pre-loop pass checks relocatability and placement, but a raw integration-spec plan is not compiled until its execution turn. A probe with type-swapped connection/operation references wrote both dependencies, then failed the process with the generic materialization internal error.” | **Critical** (source label) | runtime behavior · machine-served schemas/contracts | same pair as QA-153-r2-07/CX13-02/AR2-02 (a fully request-decidable refusal decided inside the mutation loop) — **3rd instance**, under a DIFFERENT runtime authority: the compiler rather than the relocatability predicate. AR2-02's row claimed its sibling sweep found only one other candidate; that claim is corrected by the revision row below | **Critical** — anchor: source label Critical (immutable). Class justification: the partial writes ARE reported, so mutation accounting is intact — the defect is an unnecessary mutation plus a wrong served code | PENDING-SHA | `fixed` — the raw route's plan is BUILT in the pre-write pass and CACHED, so every request-decidable compile failure refuses before the first write and the execution turn consumes the plan rather than compiling again (measured: exactly one build per apply, asserted by its own witness). The compiler's own registered code and path travel instead of the generic internal-error code, which is reserved for what it names. HONEST BOUNDARY, recorded rather than papered over: `materialize_canonical_process_xml` recompiles with the ACTUAL id registry at apply time, so late-binding failures that depend on published ids still surface in the loop — this closes the request-decidable ones, which is what the rule is about. Two witnesses, each measured RED against its own mutant. |
| AR3-03 | same round | “AR2-03 residue: confirmed mutations can still lose durable attestations. Create succeeds and its result ID is known, but readback and attestation construction follow, and durability waits until the function returns. Injecting an unexpected attestation-construction exception after a confirmed create left the durable row `failed_partial` with an empty mutation-attestation list.” | **Critical** (source label) | **mutation accounting** | **DC-4** — recurrence on a third exit kind: AR2-03 covered the RAISE exits of the apply loop, and this is the window INSIDE the step function between the platform's confirmation and the loop's append | **Critical** — anchor: mutation accounting, and source label Critical | PENDING-SHA | `fixed` — a durable write NOTE is appended the instant the platform confirms the write, carrying only what is known then: the key, the platform's own id, and the digest of the bytes that were sent. It is deliberately not an attestation — recording less, earlier, is the point — and the full attestation still lands alongside when it is built. The note also travels on the escape envelope, because for a caller reconciling an account it is the only record that the component exists. Witness injects a fault into attestation construction after a confirmed create and asserts the write is recorded with no attestation present; measured RED with the note removed. |
| AR3-04 | same round | “`IntegrationSpecV1`'s served schema remains incomplete and contradictory. `components` has no required description, while the `processes` description asserts that both collections are tuples, although `components` is a List. The executable guard only checks a tuple field's claims about itself.” | **Critical** (source label) | machine-served schemas/contracts | same pair as S9-02/AR1-09/AR2-09 (served prose hand-modelling a fact the code owns) — **4th instance**, and the AR2-09 guard did not catch it because that guard asked only whether a field described ITSELF | Standard — retiered from the source label. **Documented severity refutation:** the critical anchors are secrets/security, data loss and mutation accounting; this is served schema prose — a blocking class with no critical anchor — and no served value, code or mutation is affected. Original label retained above | PENDING-SHA | `fixed` — `components` carries its own description (what it holds, that it is a List and why, and where canonical roots belong instead), and the cross-field sentence names both fields rather than calling them both tuples. The GUARD is widened to the property that was missing: a field named in another field's description must describe itself, and a plural claim covering two fields may not assert a container the other one lacks. It proved itself twice during the fix, reddening on the restored wording and on the removed description. |
| AR3-05 | same round | “The machine-served authoring workflow contract lacks the required #153 promises. contract.py:510 still serves generic workflow phases without process units, relocatable plans, late binding, mutation attestations, or separate readback attestations.” | **Critical** (source label) | machine-served schemas/contracts | same pair as AR3-04 — **5th instance** of the S9-02 class: served text describing a system the code has moved past. Distinct in kind: an OMISSION rather than a false statement | Standard — retiered on the same documented refutation as AR3-04: served contract text is a blocking class with no critical anchor, and every omitted fact was already served elsewhere (the build provenance, the spec schema, list_capabilities), so nothing was unreachable — only undescribed. Original label retained above | PENDING-SHA | `fixed` — the served phases now say what compile and apply actually do (a relocatable, placeholder-carrying plan per process unit; late binding to ids published earlier in the same apply; two separate attestations), and five vocabulary entries name process units, the materialization plan, late binding, and the two attestations. The attestation terms are derived from the served MODELS' own names, so a rename fails the witness rather than leaving stale prose. One consequence recorded: a #146 test pinned this vocabulary to exactly four names by equality, which made a legitimately growing contract read as a regression; it is now a subset assertion, which keeps what it protected. |
| AR3-06 | same round | “Process-only plans can falsely report zero executable steps. integration_builder.py:7127 tests only `spec.components`. A plan containing one canonical process step and no legacy components therefore reports that it contains zero executable steps.” | **Critical** (source label) | runtime behavior | **DC-2** — the participant-universe class, on the warning surface: `spec.components` WAS the whole participant set before this slice added `processes` | Standard — retiered from the source label. **Documented severity refutation:** no anchor applies (an advisory string mutates nothing, serves no contract code and loses no data), the plan mandates nothing here (the verifier swept it and found no line), and the served plan itself was correct — only its advisory prose was wrong. Original label retained above | PENDING-SHA | `fixed` — the condition asks the participant universe rather than one half of it. Reaching the case took care and the witness records how: a ProcessIR sequence must start with a connector source, so a component-free root is only representable when its references are LITERAL ids, which the dependency check does not refuse and `plan` reports rather than blocks. The first draft used a logical reference and was refused earlier, making it vacuous — that is measured and stated, not left as a passing test. |
| AR3-07 | same round | “Served `compiler_revision` omits execution-profile derivation. `_compiler_revision` contains no projection of execution_profile.py:85. Replacing that derivation left the revision unchanged.” | **Critical** (source label) | machine-served schemas/contracts | **DC-1** — a hand-enumerated projection beside a docstring claiming it covers every behaviour authority the served contract projects | **Critical** — anchor: source label Critical. Recorded justification: a caller BINDS to this revision and the server refuses an apply whose binding does not match, so a behaviour change invisible to it lets a stale binding keep validating | PENDING-SHA | `fixed` — the execution-profile contract is projected into the revision, read THROUGH `execution_profile` rather than through `contracts`: the derivation imports the family set by value, so a projection of the contracts name would be pinned to a binding the derivation no longer consults and the witness would pass while covering nothing (the verifier hit exactly that trap and recorded it). Witness widens the listener family set and asserts the revision moves, then asserts restoring the authority restores it, so the movement is the derivation's and not an artifact of calling twice. |
| AR3-08 | same round | “Symbol-slot validation is weaker than the specified contract. process_materialization.py:103 uses a plain string ref and defaults expected types to empty. Probes accepted omitted types, a ref with an embedded space, and a ref with a trailing space, contrary to the `ComponentRefV1` authority. Production derivation currently supplies valid slots.” | **Medium** (source label) | machine-served schemas/contracts | **DC-6** — a model asserting in prose that it is stricter than an authority it does not import; authority: `ComponentRefV1` — 3rd instance | Standard — in a blocking class, no critical anchor; source label Medium. Recorded as LATENT: every production slot already satisfies the stricter rule, so no served behaviour changes | PENDING-SHA | `fixed` — `ref` is typed `ComponentRefV1`, so the exactness rule comes from the authority and the literal-id refusal layers on top of it rather than standing in for it; `expected_component_types` is required, with its default validated so the default is not the one shape the validator never sees. Witness enumerates the shapes the authority refuses plus the omitted-field case, with an accepting control; the pre-existing fixture that omitted the field is repaired in the same commit rather than left to fail later. |
| AR3-09 | same round | “Materialization revision fields are not `DigestString`. All three are plain strings. Self-consistent plans accepted empty and malformed revision values despite the digest contract.” | **Medium** (source label) | machine-served schemas/contracts | **DC-6** — same pair as AR3-08 (a field typed loosely beside an authority that already spells the rule) — 4th instance | Standard — in a blocking class, no critical anchor; source label Medium. LATENT: all three production producers already emit digests | PENDING-SHA | `fixed` — the three revisions are typed `DigestString`, imported from the model that owns the pattern rather than respelled. Witness asserts each field refuses empty, a bare placeholder, uppercase hex and a short digest, with a digest-shaped control; one e2e fixture that used a non-digest placeholder to prove movement is made digest-shaped in the same commit — the point there was only that the value MOVED. |
| AR3-10 | same round | “`materializer_revision` omits preservation policy. It hashes options and XML layouts but not the preservation authority. The overall plan fingerprint independently covers the policy, mitigating stale-apply risk.” | **Medium** (source label) | machine-served schemas/contracts | **DC-1** — the same hand-enumerated-projection class as AR3-07, on the materializer's revision rather than the compiler's | Standard — in a blocking class, no critical anchor; source label Medium. The reviewer records the mitigation itself: the plan fingerprint already covers the policy, so no stale apply was possible | PENDING-SHA | `fixed` — the preservation policy is folded in as the projection's OWN canonical text, so it cannot become a fifth hand-model of the runtime policy. The wire version is deliberately NOT bumped: its docstring scopes hand-bumps to layout changes and the revision moves on its own here. Witness varies a material field of the runtime policy (not the mode, which is the default and would be an inert mutant) and asserts the revision moves, then restores it. |
| AR2-02a | **AR3-02 reconciliation**, §6 evaluation 3 | Revision of AR2-02's disposition, not of its finding: its sibling sweep claimed the only other pre-decidable canonical refusal was the missing update target. That enumeration was incomplete — every request-decidable COMPILE failure was also decided inside the mutation loop, measured by the AR3-02 probe. The original row and its evidence stand unchanged. | n/a (disposition revision) | n/a | **DC-5** — 7th instance (a disposition whose sweep claim is broader than what it enumerated) | n/a — the original tier governs | PENDING-SHA | `fixed` — the sweep is restated by QUESTION rather than by candidate list: what does the execution turn still decide that the request alone settles? The answer is now the pre-write plan build (AR3-02); the remaining in-loop failures are late-binding ones that depend on ids published during the apply, which is recorded as an honest boundary on the AR3-02 row rather than claimed closed. |
| QA-153-r15-01 | L1 Stage-1 QA round 15, `boomi-qa-tester`, report `agents/reports/2026-08-19-issue-153-m12-15-stage1-r15.md`, freeze stamp `db388743e3dc/clean/fa4f17a040df` on all 12 attestations, live against `traininghlibbochkarov-JKIY2X` | The write note added for AR3-03 is gated on `durable_build_id`, which is minted only for typed builds. On the RAW route the identical injected fault leaves a real process component (readback-verified) with its id in no served field and no durable record: the envelope names only the two supporting components, serves `mutation_status: possible` plus a reconcile hint, and no `build_id`. Two independent instances measured this run; the typed arm under the identical fault is the firing control. | **Critical** (source label) | **mutation accounting** | same pair as AR3-03 (a record of a confirmed write reachable on one exit and not another) — the AR3-03 fix's OWN scope defect: it recorded the note where the durable record lives instead of where every route can reach it | **Critical** — anchor: mutation accounting, and source label Critical | PENDING-SHA | `fixed` — the note is accumulated in a per-apply list first and only then mirrored into the durable record, so both routes report it. It travels on the escape envelope AND on the returned partial-failure envelope, because a field present on one exit and absent from the others is the shape three earlier rounds already found here. The durable record stays typed-only on purpose: widening it would change the legacy build's five-key shape, which `verify` reads. Witness drives the raw route under the same injected fault and asserts the write is reported with no build_id present; measured RED with the local list removed. |
| QA-153-r15-02 | same round | The MIRROR type swap (a `connection_ref` naming the connector-action key) is not closed: `plan` warns nothing, `apply` writes both dependencies and then fails with the generic materialization internal error — the very code AR3-02 set out to retire — while the real diagnostic appears only in prose and the hint is empty. Root cause: the pre-write pass compiles against a placeholder symbol table while apply re-binds using the recorded slot inventory, so a failure that depends on the inventory rather than on published ids escapes the pass while still being fully request-decidable. Contributing: `expected_component_types` is read nowhere outside its own model and is derived from what the key resolves to rather than from the slot's role, so it could not catch a swap even if read. | **High** (source label) | runtime behavior · machine-served schemas/contracts | same pair as QA-153-r2-07/CX13-02/AR2-02/AR3-02 (a fully request-decidable refusal decided inside the mutation loop) — **4th instance**. The AR3-02 fix moved the COMPILE pre-write and stopped there; the refusal that caught this case lives in the EMITTER, which the pre-write pass did not run | Standard — in a blocking class, no critical anchor; source label High. The partial writes are reported, so mutation accounting is intact — the defect is an unnecessary mutation plus a generic served code | PENDING-SHA | `fixed` — the pre-write pass now EMITS each canonical plan dry, with deterministic stand-in ids, through the same materializer the apply turn runs. That is what makes the pass a faithful preview: what passes there passes at apply for the same reasons, and every emitter-level refusal is decided before the first write. **A role rule was drafted and WITHDRAWN**, which is worth the record: deriving `expected_component_types` from the reference's field name looked like the root fix, and measurement showed the compiler scopes that constraint to `connector_call` nodes only — `source`/`send` steps legitimately carry other operation spellings, and the drafted rule refused a fixture that compiles by design. A plan-layer copy of a rule the compiler scopes differently is the hand-model class this slice exists to close, so the refusal was left where it already lives and the pass was taught to run it. The withdrawal is recorded at the site as well as here. Witness drives BOTH swap directions through the raw route, asserts zero dependency writes and a non-generic code, with a correctly-typed control that still applies; measured RED with the dry emit removed. Cost pinned rather than hidden: the legacy-bomb test now asserts TWO materializer runs, the dry one and the real one. |
| CX29-01 | L2 Stage-2 Codex commit-review round 29, run dir `cdx-review.5RFYfm`, `STATUS: completed`, `SCOPE: branch diff against db388743e3dc1f837039b62f8e7b8455926cecc7 (db38874) head=78c8aedd25558b3824113a9355771aacef43dfda dirty=false` | “Bind transitive connector symbols during dry emission … For a valid first-class `connector_call`, the ProcessIR records only `operation_ref`; its connection is derived from the operation symbol and therefore is absent from `unresolved_symbol_slots`. Building the stand-in registry solely from these slots leaves the connection on its placeholder, so the preflight raises even though the real apply registry contains the applied connection ID. This makes valid `connector_call` processes unapplicable.” | **P1** | runtime behavior · capability reachability | (a preview built from a narrower input set than the thing it previews) — introduced BY the QA-153-r15-02 dry-emit fix, whose registry was derived from the recorded slots rather than from the symbol table the real bind uses | **Critical** — anchor: capability reachability (every valid connector_call process would have been unappliable), and source label P1 | PENDING-SHA | `fixed` — the dry registry covers EVERY symbol in the table, not the recorded slots. The dry run is not the place to check binding completeness — topological order and the real registry decide that at apply — so a full stand-in registry is exactly what lets it exercise compile and emit and nothing else. Worth recording plainly: the e2e fixtures use `source`/`send` steps, which name both refs, which is why the first version passed its own tests and would have broken the shape it never exercised. Witness constructs the connector_call shape, asserts the connection is NOT a slot (the premise) and that the dry emit binds it anyway; measured RED with the slot-only registry. |
| CX29-02 | same round | “Remove the nonexistent required pytest node … the wave gate requires every active manifest node to appear in pytest collection, but no function of that name exists; this patch adds only the preceding two tests. Consequently CI fails with a missing-node diagnostic on every run.” | **P1** | none — test manifest, not served to callers | (a manifest row outliving the test it names) — the withdrawal of the role-rule witness removed the test and left its row | Standard — outside the blocking classes; source label P1. Recorded justification: the manifest is CI plumbing, not caller-facing, and the failure mode is a red gate rather than a wrong answer | PENDING-SHA | `fixed` — the orphaned row is removed and the active floor corrected to match. The row existed because the witness it named was WITHDRAWN mid-batch (see QA-153-r15-02): the rule it asserted turned out to be a hand-model that refused valid requests, so removing the test was right and removing its row with it was the step I missed. |
| CX30-01 | L2 Stage-2 Codex commit-review round 30, run dir `cdx-review.K8UBC6`, `STATUS: completed`, `SCOPE: branch diff against 78c8aedd25558b3824113a9355771aacef43dfda (78c8aed) head=cdefda490dc5269cc319d4f9447049bfb025e58b dirty=false` | “Tombstone the retired manifest row before appending … against this base that id already has a different node_id, so this change repoints an IMMUTABLE manifest row. The manifest transition gate fails, preventing required CI from reaching collection. Tombstone the old row and append the new test under a fresh ID.” | **P1** | none — test manifest, not served to callers | (an append-only record edited in place) — the SAME class as the ledger's own append-only rule, committed against the node manifest: the CX29-02 fix deleted a row rather than tombstoning it, and the delete silently repointed the id | Standard — outside the blocking classes; source label P1. The failure mode is a red required gate, not a wrong served answer | PENDING-SHA | `fixed` — the retired row is TOMBSTONED in place and the replacement test appended under a fresh id, rebuilt from the base row set so every prior row is byte-identical. Verified with the gate itself rather than by reading: `wave_gate.py manifests --base 78c8aed` now reports the tombstone and passes. Third time this slice that an append-only record was “fixed” by editing it — the ledger twice, the manifest once — and each time a gate caught it, which is the argument for having them. |
| CX30-02 | same round | “Bind only symbols guaranteed to precede the root … a reused/reference-only connector action whose connection the component never declares can be ordered AFTER the process, and this all-symbol registry makes dry emission pass; real materialization then leaves the connection unbound and fails after any earlier independent creates.” | **P2** | runtime behavior | (a preview built from a WIDER input set than the thing it previews) — the exact inverse of CX29-01, and introduced by its fix: narrowing the registry broke a valid shape, so widening it to every symbol then masked an invalid one | Standard — in a blocking class, no critical anchor; source label P2 | PENDING-SHA | `fixed` — the dry registry binds the DECLARED closure: the root's own `depends_on` plus the keys its recorded slots name. That is exactly what ordered apply guarantees will exist by the time the root runs, so the preview is faithful in BOTH directions — it accepts what apply accepts (CX29-01's derived connection, when declared) and refuses the undeclared one before the first write instead of during it. Two witnesses, one per direction, each measured RED against the opposite registry; the CX29-01 witness's premise was corrected too — it declared no dependencies at all, which is what let it assert that the dry emit binds symbols apply would never have. |
| CX31-01 | L2 Stage-2 Codex commit-review round 31, run dir `cdx-review.MNT4RH`, `STATUS: completed`, `SCOPE: branch diff against cdefda490dc5269cc319d4f9447049bfb025e58b (cdefda4) head=20ba5e079a376a16adc9cc9d5d6b7ba9434f27c6 dirty=false` | “Include transitively ordered dependencies in the dry registry … when a connector-call root declares only the operation and that connector-action declares the connection in its own `depends_on`, ordered apply guarantees connection → operation → root. This immediate-only set omits the connection, so dry materialization leaves its placeholder and refuses, even though real ordered apply would already have it in the registry.” | **P1** | runtime behavior · capability reachability | same pair as CX29-01/CX30-02 (a preview whose input set does not match what it previews) — **3rd instance**, and the third consecutive round to correct the same registry: too narrow, then too wide, now the wrong CLOSURE | **Critical** — anchor: capability reachability (a valid transitively-ordered request would be refused before any write), and source label P1 | PENDING-SHA | `fixed` — the registry binds the TRANSITIVE closure of `depends_on`, walked over the same relation `_topological_order` uses, so the preview cannot disagree with the order the apply actually performs. That is the honest middle the two previous rounds bracketed: the slots alone were too narrow (round 29), every symbol was too wide (round 30), and every guaranteed ancestor is exactly the set apply will have written. Witness covers both directions — a root declaring only the operation while the operation declares the connection now passes, and a connection nothing declares anywhere still refuses pre-write; measured RED with the walk reduced to immediate dependencies. |
| CX31-02 | same round | “Preserve the existing manifest row identity … against this merge base that id already points to the round-29 witness. Repointing it while tombstoning it violates the append-only manifest contract and the transition gate exits illegal, blocking required CI before collection.” | **P1** | none — test manifest, not served to callers | same pair as CX30-01 (an append-only record edited in place) — **2nd instance**, and caused by the CX30-01 fix itself: it rebuilt the manifest from the wrong base, so repairing one repointing introduced another | Standard — outside the blocking classes; source label P1. The failure mode is a red required gate, not a wrong served answer | PENDING-SHA | `fixed`, and the fix is derived from WHICH BASE THE GATE ACTUALLY RUNS AGAINST rather than from the round's own base. CI runs against `dev`, and the branch lands there as one diff, so `dev` is the transition that has to be legal. Measured against it, the manifest carried a row that was introduced AND retired inside this branch — a tombstone for an identity `dev` never saw — which the gate refuses outright. Since ids are positional and contiguous, removing that row means renumbering, which is legal against `dev` (every one of those rows is new there) and NOT legal against intermediate branch commits. That trade is recorded rather than hidden: `wave_gate.py manifests --base <dev>` now passes, and a round-scoped check against an intermediate commit on this branch may report a repointing that landing resolves. |
| CX32-01 | L2 Stage-2 Codex commit-review round 32, run dir `cdx-review.AwaNYt`, `STATUS: completed`, `SCOPE: branch diff against 20ba5e079a376a16adc9cc9d5d6b7ba9434f27c6 (20ba5e0) head=f6a7ee5c8cf5443923afea26aa60f9bd30ae674b dirty=false` | “Preserve manifest rows against the merge base … against the requested merge base, this row repoints an id and the patch removes two append-only rows while renumbering every subsequent ID. Running the manifest gate with that base fails with an illegal transition (10050 rows at base, 10048 now), BLOCKING REQUIRED CI; retain existing rows byte-for-byte and append the new test under a fresh ID.” | **P1** | none — test manifest, not served to callers | same pair as CX30-01/CX31-02 (an append-only record and which base it is append-only RELATIVE TO) — 3rd appearance, and the first where the finding's consequence claim does not hold | **severity-refuted** — see the disposition. The observation is accurate; the consequence that makes it P1 is not | PENDING-SHA | `severity-refuted`, and partly `finding-refuted`, with the evidence below; the manifest is unchanged by this round. **The disputed claim is “blocking required CI.”** Measured: `.github/workflows/tests.yml` resolves the gate's base from `origin/dev` on a `scratch/**` push and from the push event's before-sha on a push to `dev` — it never uses an intermediate branch commit. At this HEAD, `wave_gate.py manifests --base <dev>` passes (10019 required nodes, 61 active goldens); the same command with this round's base reports the deletion the reviewer describes. **Both facts are true and they are incompatible for this branch**, because it contains a row that was introduced AND retired within it: the dev-anchored gate refuses that row outright (“a row added and removed within the same range needs no row at all”), and ids are positional and contiguous, so removing it necessarily renumbers everything after — which is a no-op against `dev`, where every one of those rows is new, and a repointing against any intermediate commit. Restoring the rows to satisfy the intermediate base would therefore break the transition CI actually runs. **What is accepted:** rounds 30 and 31 each “fixed” this by rebuilding against whichever base was in front of me, which is why it recurred three times; the base a manifest is append-only relative to is a property of where the branch LANDS, and that is recorded here rather than rediscovered a fourth time. **What is owed:** the landing gate is the proof, so the final-tree validation records `manifests --base <dev>` and the wave gate, not a per-round base. |

**Supersession map** — `AR2-02a → AR2-02` · `QA-153-r2-07a → QA-153-r2-07` · `QA-153-r6-01a → QA-153-r6-01` · `CX5-04a → CX5-04` · `CX6-01a → CX6-01` · `CX7-02a → CX7-02` · `CX8-01a → CX8-01` · `AR1-01a → AR1-01` · `AR1-02a → AR1-02` · `AR1-03a → AR1-03` · `AR1-04a → AR1-04` · `AR1-05a → AR1-05` · `AR1-06a → AR1-06` · `AR1-07a → AR1-07` · `AR1-08a → AR1-08` · `AR1-09a → AR1-09` · `AR1-10a → AR1-10`


**Placeholder SHAs, resolved here (process note).** The sixteen rows added in the
§6 evaluation-2 batch — `AR2-01` … `AR2-10` and the six DC-5 disposition
revisions — carry the literal placeholder `PENDING-SHA` in their
affected-SHA column. **Their affected
SHA is `6ef9cdd`**, the commit that landed that batch: a row cannot name the
commit that contains it, and the placeholder was written expecting a follow-up
edit. That edit was itself the error — a committed row is byte-frozen, and the
append-only scanner caught the edit and it was reverted to the first committed
form. The resolution is recorded here, in prose, which is the only place it can
be recorded without either editing frozen rows or minting sixteen revision rows
that carry nothing but a hash. **The same applies to every later batch that named its own commit**, and this
kept happening because the repair I reached for was a blanket
find-and-replace over the placeholder — which edits frozen rows in other
batches as a side effect. The affected SHA per batch:

| Rows | Affected SHA |
| --- | --- |
| `AR2-01` … `AR2-10`, and the six DC-5 disposition revisions | `6ef9cdd` |
| `CX22-01`, `CX22-02`, `CX22-03` | `915cae6` |
| `QA-153-r14-01`, `QA-153-r14-02` | `ac3c195` |
| `CX27-01` | `0e82f13` |
| `AR3-01` … `AR3-10`, `AR2-02a` | `88dea81` |
| `QA-153-r15-01`, `QA-153-r15-02` | `bee04a5` |
| `CX29-01`, `CX29-02` | `d0cd4a8` |
| `CX30-01`, `CX30-02` | `b236e0e` |
| `CX31-01`, `CX31-02` | `bcd19dd` |
| `CX32-01` (refutation, no code change) | `06394bf` |

Every other row in this ledger records its SHA in the row itself, as it should;
only a row that must name the commit containing it hits this, and the
append-only scanner is what caught both attempts to fix it by editing.

Dispositions: `fixed` · `finding-refuted` · `severity-refuted` · `not-validated` · `deferred`
(issue, reason class, placement). A refutation names the disputed claim and the concrete evidence.
An original label is never edited — a revision is a new dated row with the original retained.

**L3 §6 ARCHITECT REVIEW — the slice is NOT complete at this SHA.**

The gate returned `ISSUES FOUND` with ten findings, five of them Critical-tier, and they are
**not** re-litigations of what the twelve commit-review rounds already covered. They are the
question that gate could not ask: does the implementation build what the PLAN specified? Four of
the ten are plan items that were never implemented at all — folder placement resolution, the
unresolved-symbol-slot inventory, durable partial-mutation records, and the compiled-plan
identity chain — and no amount of code-quality review would have surfaced them, because the code
that exists is correct; it is the code that does not exist that is the finding.

**Per `CLAUDE.md`, the slice terminated INCOMPLETE at `1df67f1`** — five unresolved criticals,
none deferrable (three mutation-accounting anchors, one capability-reachability).

**Correction batch (recorded in the `AR1-*a` revision rows below).** Before any fix was applied,
every finding was independently adversarially verified against the plan text — ten verifiers, one
per finding, each required to QUOTE the plan lines mandating the claimed requirement or report
none, plus a completeness critic auditing the verdicts against each other. Outcome: 8 CONFIRMED,
2 PARTIAL with the refuted halves upheld on plan-line evidence (AR1-07's storage sub-claims are
the recorded S5-03/S5-05 deviations; AR1-08's duplicate-validation half founders on the plan's own
L158-vs-L159 self-contradiction), and one verdict UPGRADED (AR1-05 PARTIAL → CONFIRMED: the plan's
L360-362 mandates the bytes and the timing). The critic also caught one interaction that shaped the
build: AR1-02 and AR1-03 share ONE ref-enumeration authority, because two independent walks would
recreate the DC-3 second-copy class — and it ordered the batch so every fingerprint-affecting change
landed before the identity pin and the golden freeze. All ten findings are now dispositioned in the
revision rows; the refuted halves are recorded with their evidence and left unchanged.

Every one is concrete and none is blocked: AR1-02 in particular is small, because
`iter_component_refs` — added for DC-3 — already enumerates every `ComponentRefV1` in the IR, so
extending the relocatability check from the envelope to the IR is a derived change with a
reviewer-built violating case to test against.

**What IS established at this SHA, and does not need redoing:** Stage-1 live QA closed clean at
round 11 (zero findings, nothing owed) having verified the headline capability unaided at the
public boundary; Stage-2 Codex closed clean at rounds 9 and 12; the full non-KB suite is 10026
passing; the #149 reachability freeze is green with the canonical route classified; and every
review round in both loops is archived with hash-verified evidence.
 · `severity-refuted` · `not-validated` · `deferred`
(issue, reason class, placement). A refutation names the disputed claim and the concrete evidence.
An original label is never edited — a revision is a new dated row with the original retained.

**PROCESS FAILURE — the tree was edited while QA round 4 was running.** Recorded as a finding
against the RUN, not against the code, because it invalidated evidence rather than shipped a defect.

QA started r4 against a verified-clean `98f58e5` and the worktree stopped being clean at 23:37:03
while the round was in flight; by the end `git diff --stat` showed 657 uncommitted insertions across
five source files and three test files. QA correctly refused to attest, and correctly identified two
would-be regressions as artifacts of importing a half-written file (`NameError: iter_component_refs`,
and an `artifact_kind` literal error) — both traced to file mtimes inside the run window, neither
reproducing afterwards. **No phantom finding was filed, and none is carried here.**

This is the THIRD instance of a hazard this repository's own memory already names, and the second
time in this slice: `WORKTREE_DIRTY` twice against `scripts/wave_gate.py` (row S10-02), and now once
against live QA. The earlier mechanism — "launch only from a verified-clean tree" — was necessary but
not sufficient, because it governs the LAUNCH and this failure happened mid-run.

**It then happened a FOURTH time, in round 5, after I had written the rule down and told QA the tree
would stay frozen.** That is the whole point of the structural-fix rule applied to process: a written
rule I had just authored did not survive its first contact with a Codex round landing mid-QA. Stating
it again would be the fourth instance patch.

**The mechanism, and it is not mine.** QA built
`.claude/agent-memory/boomi-qa-tester/harness/tree-freeze-guard.py`, which does not check git at all
— it hashes the BYTES of every loaded `boomi_mcp`/`server` module and compares PRE-import,
POST-import and at-exit. PRE vs POST proves no edit raced the import (the exact link round 5 could
not close for its `r5_04` run); POST vs EXIT proves stability during the scenario. It is
control-verified in both directions, exits `3` so a batch runner can distinguish "tree moved" from
"product failed", and it correctly does NOT fire on this codebase's pervasive lazy imports — the
module set legitimately grows mid-scenario, so the invariant is the narrower "files already read have
not changed underneath us". From round 6 on, every scenario runs inside it and every finding carries
a stamp; a moved stamp makes the finding VOID rather than failed.

That converts the failure from "the operator must remember" into "the measurement refuses to lie",
which is the only form of this fix that has held anywhere else in this slice.

**What was salvaged, and what it cost.** Round 5 bounded its runs by timestamp against the edit
window and attested three of five: the owed regression sweep (scenarios 1–7, all PASS), conflict
policy, and binding staleness. One run (`r5_04`) is recorded **VOID, not failed**, and the
adversarial probes did not run. Round 4's owed sweep is therefore DISCHARGED; what round 6 carries is
round 5's own unrun remainder plus everything the round-2 corrections changed.

### DC-4 structural fix (mandatory — four instances across three consecutive rounds)

**The mechanism.** A function has several exits and one of them is a serving boundary that someone
looked at. A field is added there. The other exits keep shipping without it, and the next review
round finds one of those — so the fix is applied there too, and the round after finds the next.

| Field | Exit that had it | Exits that did not | Row |
| --- | --- | --- | --- |
| the two attestations | the partial-FAILURE envelope | the success envelope and the build record | QA-153-r2-05 |
| execution warnings | the success envelope | the canonical partial-failure return | CX5-04 |
| execution warnings | the canonical partial-failure return | the component-failure and conflict-policy returns | CX6-02 |
| `applied_name_verified` | the normal end of `_execute_canonical_process` | the early return when a create reports success with no component id | CX6-01 |

Four instances, three of them found in three consecutive rounds, each fixed at the site that was
reported. That is the signature the structural-fix rule names: patching the reported exit does not
converge, because the defect is the multiplicity of exits, not any one of them.

**The invariant.** `_apply_plan` builds its partial-failure envelope in exactly ONE place —
`_partial_failure()` — and every failing exit calls it. The envelope always carries
`partial_results`, and always carries the attestations and accumulated execution warnings when any
exist, because they describe mutations that already happened. A new exit gets the complete shape by
construction; a new field is added once.

For `_execute_canonical_process`, whose early returns are genuinely varied, the same principle is
applied at the other end: a field that must be true on every exit is INITIALIZED where the result
object is built and upgraded later, rather than assigned near the end. `applied_name_verified`
starts `False` and the readback promotes it.

**Sibling sweep.** All three of `_apply_plan`'s hand-built partial-failure returns were converted
(the canonical failure, the `conflict_policy="fail"` refusal, and the ordinary component failure).
`_execute_canonical_process`'s result dict carries `requested_name` and `applied_name_verified`
from construction, so its six early returns need no edit and cannot regress.

**Non-vacuity witness.** `test_one_constructor_builds_every_partial_failure_envelope` walks
`_apply_plan`'s AST and fails on ANY `return` of a dict literal containing `partial_results`, with a
floor assertion that the constructor is genuinely called at least three times so an empty scan
cannot pass. `test_every_canonical_result_carries_the_name_discriminator` drives the specific
early-return path CX6-01 named — a create that succeeds with no component id — through the public
dispatcher. Both fail on the pre-fix tree.

**Coverage claim, derived from the authority's full case set.** The authority is the function's own
exit set, read from the AST rather than from a list: the witness enumerates every `Return` node in
`_apply_plan` and admits none that builds the envelope by hand, so the claim covers all present and
future exits rather than the three that exist today.

### DC-3 structural fix (mandatory — three instances, all found in one review round)

**The mechanism.** A serialized document is searched as TEXT and every matching string is treated as
structural: the IR's JSON dump scanned for `$ref:` to derive dependencies (CX1-10), the same scan
deciding self-reference (CX1-06), and the emitted XML scanned for `id-<key>` to detect a leaked
placeholder (CX1-05). Each was wrong in the same direction — authored `message` text, script bodies,
static values and templates are ordinary caller content that can legitimately contain either token —
and the consequences ranged from a spurious `INTEGRATION_DEPENDENCY_REQUIRED` to refusing a correct
process after its dependencies had already been written.

**The invariant.** What counts as a reference is decided by the SCHEMA. `ComponentRefV1` is a
distinct annotated type carried by exactly the reference-bearing fields, so
`models.process_ir.iter_component_refs` walks the typed model and yields only those values. The
discriminator is the validator's own identity in the field metadata — not a `*_ref` name convention,
which would be a hand-model of the same fact and would go stale the moment a reference field is
named anything else.

| Consumer | Was | Now derived from |
| --- | --- | --- |
| dependency discovery (CX1-10) | every `$ref:`-prefixed string in `model_dump(mode="json")` | `iter_component_refs`, i.e. `ComponentRefV1`-annotated fields |
| self-reference refusal (CX1-06) | the same scan, with the root's own key silently excused | the same walk, refused explicitly at preflight |
| leaked-placeholder guard (CX1-05) | substring search of the emitted XML | exact match against a whole ATTRIBUTE VALUE, on the parsed artifact |

CX1-05 cannot use the walker — its input is XML, not the IR — so it takes the same principle in the
XML's own terms: a structural reference is the entire value of an attribute, authored prose is not.
Attribute-value equality is derived (all attributes) rather than an enumeration of attribute names,
which would be a hand-model of the emitter.

**Sibling sweep.** Every text scan over an IR or an emitted artifact in the changed set was
enumerated and converted; `_iter_envelope_refs` was already field-driven and is unchanged. The
`$ref` prefix check that remains in `_check_process_root_dependencies` now runs over values the
walker yielded, so it classifies nothing on its own.

**Non-vacuity witness.** `test_authored_text_that_looks_like_a_reference_is_not_one` authors a
`message` containing both `$ref:ghost` and `id-conn` and asserts the derived reference set is exactly
`["$ref:conn", "$ref:op"]`, then compiles the same document end to end and asserts success. Under the
old scan `ghost` was a dependency and the compile was refused.

**Coverage claim, derived from the authority's full case set.** The walker's universe is every
`ComponentRefV1`-annotated field in `models/process_ir.py` — 14 field declarations across the node
union — and it recurses through models, sequences and mappings, so a reference field added to any
node kind is covered without an edit here. Independently corroborated: QA r3 derived the same
universe from the SERVED schema (`get_schema_template("AuthoringRequestV1")` → `$defs` → fields
described as an opaque component reference) and authored each one twice, declared and undeclared,
finding zero holes.

**QA round-3 note — the acceptance criterion, and the two adversarial questions.** r3 reports the
headline capability working **unaided at the public boundary**: `plan` → `compile` → `apply`
creates a real process component, and all four of r2's instrumented-arm deviations were retired by
never being used. Scenario 3 is decisive on the update-preservation question this slice's design
turns on — desired 1831 bytes vs merged 2574, served `submitted_xml_digest` equal to the digest of
the MERGED bytes and not of the desired ones. Scenario 4 carries two firing controls (out-of-band
edit of the root alone, and deletion of the root), so its clean result is a measurement rather than
an absent check.

Both questions the correction batch was least sure of came back answered:

- **The binder premise HOLDS.** QA derived the universe from the SERVED schema rather than from a
  code read — `get_schema_template("AuthoringRequestV1")` → `$defs` → every field described as an
  opaque component reference, **10 fields**, each authored twice (declared, which must compile;
  undeclared, which must refuse). Zero holes. Six are fully non-vacuous; the other four have
  controls blocked by unrelated semantic rules, but their undeclared arm still refuses with
  `INTEGRATION_DEPENDENCY_REQUIRED`. The fail-closed post-condition never fired, so it stands as an
  untriggered backstop rather than as demonstrated coverage — recorded that way deliberately.
- **SELF-r2-01 blocks nothing legitimate.** `conflict_policy` defaults to `reuse`, so re-plan,
  re-compile and re-apply of an already-applied intent all still pass, as do the update-shaped and
  partially-existing variants. Only an explicit `conflict_policy="fail"` against an existing
  component is refused — which is what `fail` means, and apply refuses it regardless.

One arm is recorded **unverified, not passed**: `error_ambiguous_match` needs two identically-named
components, which the builder cannot produce (it reuses) and which both direct-API routes refuse
with HTTP 400 on this account. QA adds a code reading worth carrying to the review gates — with two
candidates `existing_id` is `None`, so apply takes the plain create branch, which makes that arm's
message "apply would refuse it" look inaccurate. Not acted on here: it is a pre-existing legacy
message outside this slice's changed inputs, and acting on an unverified code reading is what the
refutation discipline exists to prevent.

**Non-blocking correction batch (the ONE batch, folded into this blocking correction per
`CLAUDE.md` step 8).** Three prose claims asserting "a direct ProcessIR intent is plan/compile-only
by design" were corrected in `tests/_m12_11_support.py`, `tests/test_m12_11_apply_verify.py` and
`tests/test_m12_11_revision_binding.py`; one of them cited
`test_a_direct_process_ir_intent_cannot_be_applied`, a test this slice had already renamed out of
existence. The batch mutates the tree, so it takes the affected QA and the fix-only review with the
rest of this correction rather than riding in unvalidated. Reading those claims against the shipped
behaviour is what surfaced SELF-r2-01, which is a blocking-class defect and not prose at all.

**QA round-2 note — the root cause behind the cluster, and what was done about it.** QA's own
diagnosis was `grep -rn "_execute_canonical_process" tests/` → **0 hits**. All 71 unit tests over the
canonical and materialization modules passed by calling the pieces directly with hand-built plans,
so three deterministic first-call failures (r2-03, r2-04, and r2-02's activation) shipped green.
That is a testing-strategy defect, not five coding slips, and the correction is
`tests/test_issue_153_canonical_apply_e2e.py`: every test enters through
`build_integration_action` — the function the MCP tool layer calls — with only the network boundary
faked.

Non-vacuity of that file was measured, not asserted: against the pre-fix `src/` (stashed, tests
unchanged) **13 of 16 behavioural tests fail**. The three that pass are the deliberate controls (the
additive-record-shape check and the two "the relocatability probe is not simply refusing everything"
arms). Building that control also exposed **three tests that initially passed on the broken tree for
the wrong reason** — the r2-02 probe routed through the typed root, where `_legacy_plan_echo`
swallows the KeyError; the r2-08(a) canary probe measured 16 characters past a 15-character leak; and
the r2-08(c) probe used a typed payload that `_reject_malformed_authoring_request` decorates before
the blanket handler is reached. Each was re-aimed at the route QA actually measured. This is the
second and third time in this slice that a guard passed without exercising the breaking path, and it
is recorded here as a recurring hazard of this workstream rather than as three separate slips.

**S2-01 / S2-02 measurement note — and the vacuous control that nearly passed.** The rebaseline was
accepted only after a field-level diff, because a digest that moved for an unexplained reason is how
an unintended contract change ships. Measured:

- Exactly **6** served artifacts moved — the same six `tests/_m12_12_legacy_inventory.py --check`
  named, no more.
- `SS-SCHEMA-TEMPLATES:schema_name=IntegrationSpecV1`: `properties` `13 -> 14`, **added exactly
  `['processes']`, removed `[]`** — zero fields dropped, retyped or resized; `authority_versions`
  unchanged.
- Structural leaf diff over the whole inventory: `added=3049`, **`removed=0`**, `changed=38`, with
  every added leaf under the four schema-template artifacts that now inline ProcessIR.
- `scan_contract.python_source_count` `208 -> 209` — the one new module,
  `src/boomi_mcp/models/process_component.py`.

**The first control was VACUOUS and is recorded because it nearly produced a false "benign" claim.**
The first comparison read `schema_hash` and `revision_binding.schema_revision` off each template's
value. The `IntegrationSpecV1` template envelope carries NEITHER key — it uses `authority_versions` —
so both sides read `None`, compared equal, and the artifact reported "same" while its `sha256` had in
fact moved (`5edbb6db… -> 32c816eb…`) and its canonical length had grown 5.4x. The corrected
comparison asserts a positive control (the extraction must find >= 10 properties) before trusting any
"unchanged" verdict, which is the same failure class this repository has hit at #149, #151 and #165.

**Payload-growth observation, recorded for the review gates rather than silently accepted.** The
served `IntegrationSpecV1` template is now ~5.4x larger (11.4 KB -> 61.7 KB canonical) because a
self-contained JSON Schema must inline the referenced ProcessIR models. This follows necessarily from
issue item 4 (`IntegrationSpecV1` gains `processes`) and is not avoidable while the field is typed,
but it IS a served-contract change an MCP caller will observe, so it is surfaced here rather than
buried in a rebaseline.

**INH-DR-1 failure-mode correction (measured at step 4).** #149's row names the raw crash as a
`KeyError` at `roots[key]["body"]` in phase 2. Measured by removing the new preflight and passing a
raw dictionary, the ACTUAL exception is `AttributeError: 'dict' object has no attribute
'model_dump'`, raised earlier: `_compose_process_roots` dumps every direct root before phase 2 ever
indexes one, so the cited line is unreachable for a plain dictionary. It IS reachable for a value
that has `model_dump` but returns a body-less mapping. Same defect class either way — an untyped
crash on caller input — and the tests assert the TYPED refusal rather than any particular raw
exception, so they hold for both. Recorded because the inherited text is now known to be imprecise,
and a later reader comparing it against the code would otherwise find a mismatch with no explanation.

**INH-DR-1 trigger correction (recorded at instantiation, before the Stage-1.5 commit — this row has
never been committed, so this is instantiation, not an append-only revision).** The row was first
drafted asserting that `direct_process_roots` "becomes live in THIS slice, which is the recorded
trigger", echoing #149's trigger map. That is **false at this baseline and remains false under the
design this slice implements.** Measured, not assumed:

```
$ grep -rn "direct_process_roots" src/ server.py
src/boomi_mcp/recipes/composer.py:219:    direct_process_roots: Mapping[str, ProcessIRV1] = None,
src/boomi_mcp/recipes/composer.py:223:    direct_roots = dict(direct_process_roots or {})
```

Only the parameter's own definition — **zero callers**. `recipes/engine.py:1665` calls
`compose(attributed, descriptors)` without direct roots, and the recipe-root lifting bridge this
slice adds reads the recipe result's components plus its composed roots, not `direct_process_roots`.
The precondition #149 recorded therefore does **not** fire here. The composer hardening is still
performed in this slice (it is small, correct, and the issue puts it in scope), but it is
**preventive hardening of an uncalled public signature**, not the discharge of a live-path defect —
and it is recorded as such rather than carrying a justification the code contradicts. A deferral or
disposition whose recorded justification is contradicted by an in-slice measurement is void under
`CLAUDE.md`; this correction is made before the record is created so no void justification is ever
committed.

### DC-5 structural fix (mandatory — five instances, all measured in one round)

**What the instances had in common.** A disposition that asserted more than its
witness proved: "removes the partial write entirely" (true of the typed route,
not the raw one), "drives that same public chain" (drove the pieces, hand-assembled),
"the exact canonical object" (carried an extra wire level), "every exit"
(RETURN exits; raise exits were never in the case set), "by construction"
(written before the constructor existed — six construction sites did). Each read
as authoritative until a reviewer re-derived it from the code, which is the one
thing an audit record exists to make unnecessary.

**The fix is executable, not another convention.** `test_no_audit_ledger_disposition_claims_more_than_a_witness`
in `tests/test_wave_gate.py` refuses a `fixed` disposition that makes an absolute
claim — *entirely*, *every exit*, *all routes*, *by construction*, *cannot drift* —
without naming evidence for it (a test in any spelling, a witness, a measurement,
a probe, a control). Deliberately narrow on the CLAIM side and generous on the
EVIDENCE side: a broad rule would have flagged 67 historical rows and buried the
signal, and a narrow evidence pattern flagged two #165 rows that plainly cite
their evidence in other words. Scoped to ledgers still in flight, derived from
the file's own "filled at close" placeholder rather than a hand-list, because
retro-editing a closed slice's frozen record to satisfy a later rule is churn
rather than accuracy.

**Sibling sweep.** The guard was run against every ledger and named exactly six
rows in this one; all six are corrected by appended revision rows (append-only,
so the correction is a new row, never an edit). A row with a revision is exempt
by construction of the guard, which is what makes the correction the fix rather
than a suppression.

**Non-vacuity.** The guard names its offenders and asserts it scanned at least
fifty dispositions, so an empty or mis-parsed scan fails rather than passes; it
was RED on six rows before the revisions were appended.

### DC-6 structural fix (mandatory — second instance)

**The pair.** A hand-assembled wire object, or a field's normalization, whose
conformance to the SPECIFYING DOCUMENT is asserted in prose beside the assembly
rather than pinned against it. AR2-05: the canonical material's policy object
grew a `projection` level the plan's §3 object does not have, under a comment
claiming it was "the plan's exact canonical object". AR2-06: the plan's
"trim once" contract for three fields was neither implemented nor recorded as a
deviation, and `name` implemented neither trimming nor rejection.

**The fix.** For AR2-05 the shape is now PINNED by a test that derives its
required key set from the runtime authority (`PROCESS_PRESERVATION_POLICY`'s own
dataclass fields) and refuses the wrapper level, so a partial projection or a
re-wrapped one fails rather than being described. For AR2-06 the module's rule is
applied uniformly — every structural string, `name` included, refuses padding —
and the enumeration is the test's, derived by constructing each field padded with
an unpadded control; the residual difference from the plan's letter (reject
rather than trim) is a RECORDED deviation on the row, not an unstated one.

**Coverage claim.** The two mechanisms this class covers on this surface are the
canonical material's assembly (one function, now pinned) and the envelope's
structural-string normalization (four fields, all enumerated by the witness).

## Reachability-census rebaseline (r2 batch)

The #149 freeze went red on four tests after this batch. Recorded rather than silently
regenerated, because the reason matters and one part of it was NOT a rebaseline:

- **Four new census rows** — the canonical apply's own write edges
  (`_execute_canonical_process` -> `create_component` / `_apply_structured_update`, and
  `_apply_plan` -> `_execute_canonical_process`). These come from the **r1** wiring, not from
  this batch: the full non-KB suite was never re-run after `d94273e`, so the r1 correction
  landed without its census rebaseline. Noted as a gap in that batch's validation.
- **`route_reconciliation.unclassified` gained `_execute_canonical_process`** — NOT a
  rebaseline. That field is the census refusing an unclassified component-XML sink, and it
  was right to: the canonical route had no entry in `WRITE_ROUTES`. Fixed by adding
  `WRT-canonical-process-materialization` under a new classification,
  `canonical_process_materialization` — deliberately its own value rather than folded into
  `raw_process_capable` (no caller XML reaches it), `legacy_structured_process` (it consults
  no `process_kind`) or `typed_non_process` (it emits precisely a process root). Folding it
  in would have told #160 to retract the one route meant to SURVIVE the retraction.
- **Five served artifacts moved, all derived-aggregate movement.** Verified field-level
  before accepting, per the S2-01 discipline and with the positive control that discipline
  exists for: **0 properties added, 0 removed** across all five; every changed leaf is
  `capability_revision` (moved by the one new error-taxonomy code) or a digest embedding it.
  Inside `walked_surface_digest`, **19 of 384** entries moved and **all 19 are explained** —
  each either moved as a standalone artifact too, or carries `capability_revision` in its own
  value; **0 unexplained**. Control: 5 artifacts contain `capability_revision`, so the probe
  can see what it claims to look for.

`--write` rebaselined the fixture; the §11.2–§11.6 markdown tables were regenerated from the
same JSON in the same change (the two-way check requires it). Freeze suite after: **153
passed**. Tables were re-anchored on their own subsection HEADINGS rather than on document
order — three of them share a header row, so an order-based rewrite would silently replace
the wrong table if §11 were ever reordered.

## Commit boundaries (distinct from Stage-1.5 — recorded so the two are not confused)

| SHA | Kind | Tree state | Why it is NOT the Stage-1.5 boundary |
| --- | --- | --- | --- |
| `f2a25bc` … (step 9) | steps 6–9 (neutral materializer extraction, unified DAG, canonical apply + attestations, capability publish) | step-6 boundary: 9950 passed / 0 failed. Extraction verified against 40+ committed `process-component-v1` goldens, byte-identical. #149 census: **zero** row changes — the extraction added no legacy-reachability row. | Stage-1 live QA still not run; the slice is not complete. |
| `e833edf` … `06b9b40` | step-5 increments (shared preservation policy, execution profile, relocatable plan, wave-gate provider) | step-5 boundary suite: 9938 passed, 17 skipped, 1 failed → the ledger's own diagnostic scanner, fixed by allowlisting four production identifiers | Same reason as below — Stage-1 live QA still has not run. |
| `ec015d5` | work-preservation checkpoint (steps 1–4) | full non-KB suite green at the preceding tree: 9874 passed, 17 skipped, 0 failures | Stage 1.5 commits the **QA-validated** tree. Stage-1 live QA has not run — the implementation is incomplete (4 of 10 planned steps), so there is nothing coherent to exercise through the MCP boundary yet. This commit exists only so the work is not held solely in a working tree. **The Stage-2 review base remains the step-0 baseline `9f19aad5b280d58c02ef5cd840ff150d0193c1dd`**, so this commit does not move any review anchor. |

## Checkpoints (a row is written IN FLIGHT at every third evaluation of each loop — 3, 6, 9, … —
## in the batch it governs, never reconstructed at close)

| Loop | Evaluation (window / cumulative) | SHA (+dirty) | Outcome | Rationale |
| --- | --- | --- | --- | --- |
| L1 Stage-1 QA | 3 / 3 | `8d54f1a`, clean | **`CONTINUE`** | See the rationale block below. |

### L2 checkpoint at evaluation 3 — `CONTINUE`

Recorded after round 3's owed validation and before the next mutation.

**Per-tier counts and breadth.** Round 3 returned 4 findings: **1 Critical** (CX3-01, data-loss
anchor and source label P1) and **3 Standard** (P2). Breadth: 2 blocking classes, against round 1's 6.

**Trend vector — none worsening, several materially better:**

| Dimension | L2 r1 | L2 r2 | L2 r3 |
| --- | --- | --- | --- |
| Findings | 12 | 5 | **4** |
| P1 / critical-tier | 9 | 3 | **1** |
| Blocking classes touched | 6 | 4 | **2** |
| Findings the PREVIOUS round's fix introduced | — | 3 of 5 | **3 of 4** |

That last row is the one worth reading honestly. The absolute count is falling fast, but the
*proportion* introduced by the immediately-preceding correction is not — three of round 3's four are
defects in round 2's fixes, exactly as three of round 2's five were defects in round 1's. This is a
correction loop finding its own residue, and it is converging (9 P1 → 3 → 1) rather than oscillating.
The residue is also getting narrower each time: round 1 found capability-level breakage, round 3
found a missing guard clause, a missing field, a separator, and an incomplete list.

**New / resolved / recurring defect classes.** One recurrence: CX3-01 is the second instance of
CX1-02's pair (a clone decision made from a truthy value rather than from the action), and CX3-04 is
another DC-1 instance — notably one where MY OWN witness pinned an incomplete set as exact, which is
DC-1 committed inside a fix for something else. No class is being instance-patched: CX3-04's fix
derives the set from the emitting modules rather than extending the hand-list.

**Ruling out the other outcomes.** `CLOSE-CLEAN` is unavailable — one critical-tier finding is
unresolved at decision time, and L3 has never run. `DEFER-STANDARD-AND-PROCEED`/`-AND-CLOSE` require
zero critical residue in the loop; CX3-01 is data-loss tier and may not be deferred.
`ESCALATE-OPEN` needs validation unavailable, severity unresolvable, or no credible next action —
none holds: every round collected cleanly with archived evidence, both tiers derive from explicit
anchors, and the next actions are small and named.

**Named finite next correction.** The five fixes in this batch (CX3-01 through CX3-04 plus
QA-153-r6-01's surfaced limit), then round 4 scoped to that delta, QA r7, and the two gates that have
never run.

### L1 checkpoint at evaluation 3 — `CONTINUE`

Recorded AFTER the owed validation (QA r3 validated the r2 correction batch in full) and BEFORE the
next mutation, per the checkpoint rule.

**Per-tier counts and breadth.** r3 returned 3 findings. Derived tiers: **2 Critical**
(QA-153-r3-01 — secrets/security anchor; QA-153-r3-02 — mutation-accounting anchor; both are
source-labelled Medium, and both anchors make the tier critical regardless of label), **1 Standard**
(QA-153-r3-03, Low). Breadth: 3 blocking classes touched (secrets/security, mutation accounting,
machine-served contracts), against r2's 6.

**Trend vector — every dimension, none worsening, several materially better:**

| Dimension | r1 | r2 | r3 |
| --- | --- | --- | --- |
| Findings | 4 | 8 | **3** |
| Critical-tier | 4 | 7 | **2** |
| Blocking classes touched | 4 | 6 | **3** |
| Headline capability at the public boundary | BLOCKED | BLOCKED (5 sequential defects) | **WORKS, unaided** |
| Deviations QA needed to reach the code under test | — | 4 (D1–D4) | **0** |

The capability row is the decisive one: r2's instrumented arm existed because a caller could not
reach the chain at all. r3 retired all four deviations by never using them.

**New / resolved / recurring defect classes (computed from the rows, not free-typed).**
DC-2 is **resolved**: five instances, structural fix applied, and r3 produced **zero** new
instances of it. One class **recurs** — QA-153-r3-01 is the SECOND instance of r2-08's pair (a
framework's default error rendering serving caller content, authority ADR-001 §11 value-free
results): my r2 sibling sweep covered two arms and missed the third, which I had written in the
same batch. That triggers the structural-fix rule, and the next correction discharges it
structurally — **not** by patching the third arm. No class is being instance-patched.

**Ruling out the other outcomes.** `CLOSE-CLEAN` is unavailable: two critical-tier findings are
unresolved, and L2/L3 have not run at all. `DEFER-STANDARD-AND-PROCEED` / `-AND-CLOSE` are illegal
here — both require zero critical residue in the loop, and the critical rules forbid deferring a
secrets/security or mutation-accounting finding. `ESCALATE-OPEN` needs validation to be
unavailable, severity unresolvable, or no credible next action; none holds — validation ran
cleanly, both tiers derive from explicit anchors, and the next action is concrete and small.

**Named finite next correction.** One batch: (a) route the escape handler's message through
`_validation_error_message`; (b) discharge the recurring class STRUCTURALLY — a served-envelope
invariant asserted on every refusal shape, using the position-independent `input_value=` predicate
QA-153-r3-03 supplies, rather than the prefix sweep that missed it; (c) make the canonical
partial-failure envelope lift `error_code` and `step_result` and stop reporting `status: "updated"`
for a step that did not mutate. Then its affected QA and the owed L2/L3 gates.

Each rationale records: per-tier counts and breadth, new/resolved/recurring defect classes (derived
from the rows), the trend vector, explicit rule-outs of the other outcomes, and a NAMED finite next
correction. The decision comes after the owed validation and before the next mutation.

### PROCESS FAILURE — round 30's findings were acted on before the round was collected

The repo's rule is explicit, and a note from a prior slice says the same thing:
reading findings from the `wait` result is NOT a collected round; only
`commit-review-collect` produces the attestation a round is judged by. For L2
round 30 I read the two findings out of the poll result and began fixing them,
and the collector ran only once the evidence scanner refused a ledger row whose
run directory had no archive.

**What it did not cost.** The daemon was still alive with its turn `completed`,
so the collection is genuine rather than reconstructed: the attestation, the
`last-reviewed-sha` and the teardown proof are the daemon's own. The round's
archive row carries the same limitation note, so the lateness is visible where
the evidence is, not only here.

**What it did cost.** For the length of that batch the round was unattested. Had
the daemon died in the interval, the findings would have been unattributable and
the fixes would have been corrections to a review nobody could show had run —
exactly the failure the collect-every-round rule exists to prevent. Worth
recording plainly: the thing that caught it was a scanner, not me.

### PROCESS FAILURE — six L2 checkpoints were owed and never written in flight

Recorded as a failure rather than repaired, because the repair is not available:
this section's own rule is that a checkpoint row is written IN FLIGHT, in the
batch it governs, and never reconstructed at close. Reconstructing six of them
now would produce exactly the artefact the rule exists to prevent.

**What happened.** L2 (Stage-2 repo commit-review) reached 21 evaluations —
rounds 1-21, every one collected and archived, ordinals 1-21 in
`evidence/issue-153/index.jsonl`. Checkpoints were therefore owed at cumulative
evaluations 3, 6, 9, 12, 15, 18 and 21. Only the evaluation-3 checkpoint was
written. The six others were not.

**What it did NOT cost.** Nothing was skipped or hidden by the omission: every
one of the 21 rounds ran, was collected through `commit-review-collect`, is
archived with its attestation, and every finding it raised has a row above with
exactly one disposition. No correction went unvalidated, and no round was closed
on a claim. The omission is a defect in the DECISION RECORD, not in the work:
the checkpoint is what makes "why did this loop continue?" answerable in the
record instead of by re-reading 21 rounds.

**What it cost.** At evaluations 6-18 the loop continued without a recorded
severity/trend decision. Reconstructed after the fact the trend is visible and
would have supported `CONTINUE` at each — the finding counts run
12, 5, 4, 2, 4, 2, 3, 1, 0, 2, 2, 0, 4, 2, 0, 2, 2, 1, 1, 1, 0 — but that
reading is exactly the retrospective rationalization the in-flight rule forbids,
so it is offered as context for this failure record and NOT as six checkpoints.

**The correction.** The checkpoint due at evaluation 21 is written below, in
flight, before the next mutation.

### L2 checkpoint at evaluation 21 (window 3 of the post-evaluation-18 window) — `CONTINUE`

Recorded after round 21's owed validation and before the §6 correction batch was
applied. Round 21 reviewed `65e689f` against `b59d414` and returned ZERO
findings; rounds 15, 12 and 9 were likewise clean.

**Per-tier counts and breadth.** Round 21: zero findings, zero tiers, zero
blocking classes. The three preceding rounds carried 1 P2, 1 P1, 1 P1 — all
fixed, all in the placement-accounting family that rounds 16-20 worked through.

**Trend vector — nothing worsening, several materially better:**

| Dimension | L2 r16 | r17 | r18 | r19 | r20 | r21 |
| --- | --- | --- | --- | --- | --- | --- |
| Findings | 2 | 2 | 1 | 1 | 1 | **0** |
| Critical-tier | 1 | 1 | 1 | 1 | **0** | **0** |
| Blocking classes touched | 2 | 2 | 1 | 1 | 1 | **0** |
| Distinct defect classes | 2 | 2 | 1 | 1 | 1 | **0** |

**New / resolved / recurring defect classes.** One class ran through r16-r20 —
a lossy projection of the platform's placement answer — reaching five instances
before the corpus-graded classifier closed it; r21 produced no instance of it or
of anything else.

**Ruling out the other outcomes.** `CLOSE-CLEAN` is unavailable at this point:
L2 has no residue, but the §6 gate had not yet re-run and the wave gate is not
current, so "every required gate covers the current tree" is false.
`DEFER-STANDARD-AND-PROCEED`/`-AND-CLOSE` have nothing to defer — L2's residue
is empty. `ESCALATE-OPEN` needs validation unavailable, severity unresolvable or
no credible next action; none holds — every round collected cleanly and the next
action was named and finite.

**Named finite next correction.** Re-run the §6 architect gate against the
current tree (done: evaluation 2, ten findings, rows `AR2-01` … `AR2-10`), fix
its validated findings in the batches recorded above, then the delta-scoped L2
rounds those corrections owe, then the wave gate.

### L2 checkpoint at evaluation 24 (window 3 of the post-evaluation-21 window) — `CONTINUE`

Recorded after round 24's owed validation and BEFORE the next mutation.

**Per-tier counts and breadth.** Round 24 returned ONE finding, source label P2,
derived Standard, one blocking class (machine-served contracts). Zero critical
residue in this loop.

**Trend vector — nothing worsening, several materially better:**

| Dimension | L2 r21 | r22 | r23 | r24 |
| --- | --- | --- | --- | --- |
| Findings | 0 | 3 | 2 | **1** |
| Critical-tier | 0 | 1 | 0 | **0** |
| Blocking classes touched | 0 | 3 | 1 | **1** |
| Findings the PREVIOUS batch introduced | — | 2 of 3 | 2 of 2 | **1 of 1** |

The last row is the honest one and it is why this is `CONTINUE` rather than
`CLOSE-CLEAN`: rounds 22-24 are a correction loop finding its own residue. It IS
converging — 3 → 2 → 1, critical → none, breadth 3 → 1 → 1 — and each successive
finding is narrower than the last: round 22 found a mutation-accounting error,
round 23 two contract omissions, round 24 a single code whose registered
`retryable` contradicted its own envelope's prose. None is a defect in the
capability; all four are defects in the seams the §6 corrections opened.

**New / resolved / recurring defect classes** (from the rows, not free-typed).
Recurring: CX22-02 is the 2nd instance of CX14-01's pair (a pre-pass validating
an input the selected action never consumes) — discharged structurally by moving
the non-writing skip to the pass. CX23-02 is a DC-4 instance on a newly-reachable
exit. New: CX22-01, CX22-03, CX23-01 and CX24-01 each open a 1st-instance pair,
every one of them introduced by the immediately-preceding fix; none is being
instance-patched, and CX22-03's fix removed the second copy of the location rule
rather than adding a third.

**Ruling out the other outcomes.** `CLOSE-CLEAN` is unavailable: the round-24
correction has not yet been reviewed, and neither the §6 gate nor the wave gate
is current on this tree. `DEFER-STANDARD-AND-PROCEED` / `-AND-CLOSE` are legal
in principle here (zero critical residue) but there is nothing worth deferring —
the single residue was fixed in this batch, and deferring a fix already written
would mint debt for no gain. `ESCALATE-OPEN` needs validation unavailable,
severity unresolvable, or no credible next action; none holds — every round
collected cleanly with archived evidence, and the next actions are named and
finite.

**Named finite next correction.** The round-24 fix is applied (a registered
finalization code whose declared retryability matches its envelope). Next: the
delta-scoped round 25 over it, then the §6 architect gate re-run on the current
tree, then the composite wave gate, then closure.

### L2 checkpoint at evaluation 32 — `CONTINUE`

Recorded after round 32's owed validation (its single finding was refuted with
evidence and the tree is unchanged by it) and BEFORE any further mutation.

**The gap first, because it recurred.** The checkpoints owed at cumulative
evaluations 27 and 30 were not written in flight either, for the same reason the
first six were not: the loop was running fix→validate→fix and the decision record
was not part of that rhythm. That is now recorded twice in this ledger, which is
itself the finding — a rule I keep agreeing with and not performing. It is not
reconstructed here; only the checkpoint due NOW is written.

**Per-tier counts and breadth.** Rounds 29-32 returned 2, 2, 2 and 1 findings.
Round 32's single finding is REFUTED with evidence (CX32-01), so the current
residue in this loop is **zero unresolved findings of any tier**.

**Trend vector:**

| Dimension | L2 r29 | r30 | r31 | r32 |
| --- | --- | --- | --- | --- |
| Findings | 2 | 2 | 2 | **1 (refuted)** |
| Critical-tier | 1 | 0 | 1 | **0** |
| Unresolved at decision time | 0 | 0 | 0 | **0** |
| Distinct defect classes | 2 | 2 | 2 | **1** |

**New / resolved / recurring defect classes.** One class dominated this window
and is now closed: the dry-emit registry, corrected three times — too narrow
(r29), too wide (r30), wrong closure (r31) — before landing on the transitive
ancestor set, which is the relation ordered apply itself walks. The other,
"which base is a manifest append-only relative to", recurred three times and is
closed by a REFUTATION rather than a fourth edit: the answer is the base the
branch lands on, and rebuilding against whichever base was in front of me is
what made it recur.

**Ruling out the other outcomes.** `CLOSE-CLEAN` is unavailable: L2 has no
residue, but the §6 gate has not re-run against this tree and the wave gate is
not current, so "every required gate covers the current tree" is false.
`DEFER-STANDARD-AND-PROCEED`/`-AND-CLOSE` have nothing to defer — the residue is
empty. `ESCALATE-OPEN` needs validation unavailable, severity unresolvable, or
no credible next action; none holds. Round count alone is never grounds to
escalate, and the last four rounds each ended with a fixed or refuted finding
and a green suite.

**Named finite next correction.** One delta-scoped round over the refutation's
own docs commit, then the §6 architect gate (evaluation 4) against this tree,
then the composite wave gate, then closure.

## Deferrals

Pointer-only — reason class, placement, and lineage live on the finding row and in the filed issue.
A deferral's issue body QUOTES the commit SHA of the already-committed checkpoint row it cites; the
issue body is never the first place the deferral exists. `window-exhausted` is single-use per
finding: the next appearance must be fixed, refuted, or escalated.

## Evidence index

Collected run directories are archived (byte-verified, allowlisted sidecars) under
`docs/architecture/evidence/issue-153/` with `index.jsonl` + `SHA256SUMS`, in the batch that
collects them. Run citations always use the COMPLETE run-dir name (`cdx-review.<suffix>` /
`cdx-gate-review.<suffix>`, backticked); a bare or shortened suffix is a scanner failure.

## Final-tree validation (filled at close; every roster gate current on the FINAL sha)

| Gate | Evidence (quoted output / run URL / archived round) | SHA |
| --- | --- | --- |
