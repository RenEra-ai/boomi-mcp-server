# Audit ledger — issue #158 (M12.20 Canonical WSS listener entry and materialization, T7)

Audit record for the completion workflow (`CLAUDE.md`, amended 2026-08-12 / -08-14; standing
rules in `docs/architecture/COMPLETION_WORKFLOW_RULES.md`).

Plan of record: `.codex/plans/issue-158.md` — the attested Codex architect plan, attestation
`.codex/plans/issue-158.attest.json` (message sha256
`f28242a0766fed357e8ac86d1f3215fc3408e09d980bd723ed0c448b7d3c14c8`, thread
`01a08cc1-3f8f-7b71-8306-41496c06b40d`, turn token 1, gate `architect`, collected
`ok:true`/`stopped:true`, teardown `confirmed`). Where the plan and this ledger disagree, the
correction is recorded here with its evidence and the plan is NOT silently re-authored.

## Stage-1 step 0 — baseline

| Field | Value |
| --- | --- |
| Issue | #158 — M12.20 Canonical WSS listener entry and materialization (T7) |
| Step-0 baseline (`$BASELINE`) | `3a8469e109cbf414aff1f9db35649ef06071fb66` |
| Branch | `codex/issue-158` |
| Branch point | `dev` @ `3a8469e` (closing commit of #157) |
| Baseline suite | 12026 passed, 19 skipped (12045 collected) — full non-KB, local `.venv` 3.12, `PYTHONPATH=src`, 976 s, run to completion BEFORE any edit, exit 0 |
| Baseline manifests | 12045 required nodes (`tests/fixtures/wave_gate/test_nodes.jsonl`), 76 active goldens (`tests/fixtures/wave_gate/goldens.jsonl`) |
| Pristine oracle tree | `git archive 3a8469e` extracted outside the repository (scratchpad) with the repo `.venv` symlinked in; the two pre-flip SOAP-target anchors are rendered from THAT extraction, never from the working tree |
| Slice kind | behaviour-affecting (new IR node kind, emitter registration, entry-shape invariants, routing-gate flips, deployment recognizers, served-contract changes) |

The collected count 12045 equals the recorded floor, so the floor is current on the branch point.

### Environment corrections recorded at step 0

1. **The first architect gate session never produced a plan.** Both turns of session
   `01a08cb7-6a39-7a42-956a-122783b8c3d6` (primary brief, then the one in-session retry) ended
   `failed` within a minute with an empty message. The Codex app-server log for that session reads
   "You've hit your usage limit … try again at Sep 14th, 2026 9:42 PM", and a direct CLI probe on a
   second model returned the same text, so the limit was account-wide rather than model-specific.
   The round was collected with `--outcome failed` (refused `declared_failed`, daemon `stopped:true`)
   and nothing from it is used. The owner restored Codex capacity the same day; a one-token probe
   confirmed it before a fresh session was started. The fresh session's primary turn certified.
2. **Gate sessions run under a Codex home with no MCP servers and no plugins.** On #157 an architect
   plan turn parked on an MCP tool-approval elicitation that a headless driver cannot answer. The
   architect brief therefore told Codex that MCP tools are unavailable and to ground the plan in the
   committed goldens, the evidence archive and the source. The repo's own Stage-2 commit review is
   unaffected by this choice and runs under the default home per `CLAUDE.md` §5b.

## Loop roster (enumerated in advance, before the first correction)

Per `CLAUDE.md` the roster is fixed before any correction is applied. A gate not on this list cannot
mint a loop mid-run; adding one is itself a recorded checkpoint decision.

| # | Logical loop | Authority | Scope |
| --- | --- | --- | --- |
| 1 | Stage-1 QA | `boomi-qa-tester`, live through the public MCP tool boundary | listener deploy + inbound payload per distinct listener family (bare WSS and API Service routes), canonical-root deployment-target selection, the named negative probes, and the scheduled-root controls |
| 2 | Stage-2 repo commit review | detached Codex review per `CLAUDE.md` §5b–5e | initial: `--base $BASELINE`; then each fix delta |
| 3 | Architect implementation review | `/codex-issue` §6 gate (`--gate review`) | implementation vs `.codex/plans/issue-158.md`; **capped at 3 evaluations** by `docs/architecture/COMPLETION_WORKFLOW_RULES.md` |
| 4 | Composite wave gate | `scripts/wave_gate.py` | full suite, golden manifest, determinism, wave-delta review, one live scenario per changed capability class |
| 5 | Terminal correction loop | only if a final non-blocking batch mutates the tree | that batch |

The pre-flip SOAP-target anchor capture below is **evidence provisioning**, not a QA evaluation:
it runs from the pristine extraction of the baseline tree with no source change and debits no loop.

## Pre-flip legacy-oracle anchors (captured before either routing gate changed)

The issue's hard constraint: capture the two missing SOAP-target listener anchors BEFORE the flip,
while the legacy renderer still emits them. After the flip no independent producer exists.

Archive: `docs/architecture/evidence/issue-158/preflip/` — the capture script, both raw inputs, both
output byte streams and a manifest carrying the baseline SHA, the interpreter version and each
output's digest. Each form was rendered twice — through `SyncPipelineBuilder.build`, which routes a
listener chain to the legacy arm at the baseline, and through `ProcessFlowBuilder.build` on the
lowered config — and the capture refuses unless the two agree byte-for-byte. They agreed.

| Chain | Stages | Component name | Bytes | sha256 |
| --- | --- | --- | --- | --- |
| `listener_soap_send` | listen → SOAP send | Sync Listener Soap Send Golden | 1562 | `a678f4831b3f5071b421649b8881cbd81227bc9e1a2bbc5b932796a1ddb766a7` |
| `listener_map_soap_send` | listen → map → SOAP send | Sync Listener Map Soap Send Golden | 1810 | `b9c50541da707cfcdd0bec2cf68aaa700626a7709c7f8fe139e68c73b77e16c0` |

Provenance class, per the `CLAUDE.md` Stage-1 fixture rule: **legacy-oracle parity capture** from a
tree causally independent of the implementation under test.

## Plan corrections (recorded before the first correction is applied)

Claude's implementation plan is `.codex/plans/issue-158.claude.md`. It verifies the architect plan
against the tree at `3a8469e`; the corrections below OVERRIDE the architect plan, each re-verified in
the main thread before adoption. The architect plan is not silently re-authored.

| # | Architect said | Actually | Evidence (re-verified) |
| --- | --- | --- | --- |
| C1 | only the registry absence pin moves | the registry COUNT pin moves too (19 to 20 keys) | `tests/test_process_emitter_registry.py` `test_registry_covers_discriminator_exactly` |
| C2 | the listed retirements are complete | the execution-profile authority test also asserts the old import; kept by name, body rewritten | `tests/test_process_execution_profile.py` `test_the_rule_uses_the_compilers_own_authority_not_a_second_copy` |
| C3 | the revision oracle keeps working | it read the family set off the execution-profile module, and its stand-in graphs carry no edges; the profile path therefore classifies from the entry node alone (`classify_entry`), and emission uses the strict derivation | `src/boomi_mcp/authoring/contract.py` `_execution_profile_behaviour_oracle` |
| C4 | admit existing linear processing | `flow_control` is in the root linear vocabulary but the legacy listener guard refuses it, and the root `exception` terminal was never an audited listener form; both are refused | `LISTENER_EXCLUDED_ROOT_KINDS` in `src/boomi_mcp/models/process_ir.py` |
| C5 | outbound calls ride `connector_call` | the REST `connector_call` allowlist is GET/PATCH only, so REST POST, database Send and SOAP targets are reachable from a listener only through the endpoint form, which every anchor has | `CONNECTOR_CALL_CAPABILITIES_V1` |
| C6 | no pointer work needed | the discriminator-tag strip list is hand-kept and needed `listener`, or the forbidden-connection pointer names a path that does not exist | `_DISCRIMINATOR_TAGS` |
| C7 | projection entries follow | a node kind with no authoring facts takes the WHOLE served contract down (KeyError), measured as roughly 146 failures | `_node_entries` in `src/boomi_mcp/authoring/process_ir_projection.py` |
| C8 | the evidence commit may carry the goldens | adding golden files without manifest rows turns two golden-directory tests red, so the evidence commit carries the bytes ONLY under the evidence archive | commit `d688714` |
| C9 | the legacy inventory becomes empty | the golden inventory test SPLIT by the listener prefix; the split itself is removed | `tests/test_process_emitter_parity.py` `test_sync_corpus_anchors_every_committed_sync_golden` |
| C10 | route the refused spellings to legacy | the legacy body would mis-shape the two unaccepted listener spellings as ordinary sources, so the gate admits every lowered core and the adapter refuses those two | `_sync_pipeline_is_canonical`, `_listener_disposition` |
| C11 | inbound facts are available | the symbol table never carried a request profile and has no input-type field; a new optional symbol field carries the input type, with a structured-config fallback when apply has no snapshot | `ComponentSymbolV1.input_document_type`, `_listener_inbound_facts` |
| C12 | exclude reference-only processes | excluding them with no fallback regresses a build whose only process is a reference; when no authored root exists the reference-only processes stay eligible | `_deployment_root_candidates` |
| C13 | the "adapter not reached" pin is at one line | it is a different test at a neighbouring line; the cited line is the archetype pre-lowering test, which stays | `tests/test_sync_pipeline_adapter_cutover.py` |
| C14 | cited line numbers | several drifted after #156 and #157 | re-derived per file in `.codex/plans/issue-158.claude.md` |

## Design decisions recorded during implementation

| # | Decision | Why |
| --- | --- | --- |
| D1 | No `connector_action.wss.listen` entry is served (the architect plan asked for one) | That entry type is served as "a callable connector family/action pair; the registry is a closed allowlist". A WSS Listen operation is NOT callable, so publishing it under that type would make the served type definition false. Its family and action are served by `node.listener`, `capability.listener_entry` and the `semantic_rule.listener.*` entries instead, and the connector-action refusal's cited diagnostic entry names the listener node's selector. Revised after QA-158-r1-01: the first version of this justification rested on `GATED_CONNECTOR_CALL_REASONS`, a table nothing serves, and was measured false. |
| D2 | The pre-flip capture script is committed as a `.py` file in the evidence archive (the implementation plan preferred text) | It is the exact invocation that produced the bytes; issue-155 set the precedent of a script in the archive, and every scanner passes with it. |
| D3 | The legacy process-flow `$ref` role check is unchanged | It already routes on the WSS builder's own resolver; the canonical counterpart of the WSS operation-only `$ref` dispatch is the compiler's listener-entry resolution (`validate_listener_entry`). |
| D4 | The reused-listener confirmation runs on a REAL run only | A dry run reads nothing from the account by contract. The check fails closed: a reused listener whose start cannot be read, or starts on another operation, refuses before any package exists (`BUILD_LISTENER_ENTRY_MISMATCH`). |
| D5 | The execution profile no longer consults any symbol | The entry node alone decides. The five #153 revision witnesses keep their node ids and are rewritten onto the new authority: symbol-driven mutants must move the served revision, faithful reimplementations must not, and the table-size whitelist moves to the one family lookup left on this path, the listener entry's `listener_operation_symbol`. |
| D6 | A submitted raw WSS operation XML with no Listen element is now refused | The WSS family's verb location is measured (the element's presence), so a WSS document naming no action settles nothing — the same rule every other known-verb family already has. A served behaviour change, covered by live QA. |
| D7 | The compile-path parse-code allowance retires `PROCESS_IR_SCHEMA_INVALID_CARDINALITY` | The compiler now emits it statically (listener position), which is exactly the case the allowance's stale-entry check requires retiring. |

## Findings ledger

One row per raw finding. Columns: source ID · verbatim summary · source gate / run dir /
attestation · original severity label · blocking class · defect class · derived tier + anchor ·
affected SHA/delta · disposition.

| ID | Summary (verbatim) | Source / run dir | Orig. label | Blocking class | Defect class | Tier (anchor) | Affected SHA | Disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SELF-158-r1-01 | "the recorded `spec[\"processes\"]` is a tuple, and orchestration requires a list" — every build `build_integration` apply records, legacy builds included, was refused by `orchestrate_deploy` with `BUILD_REGISTRY_ENTRY_MALFORMED` ("spec has a malformed process list."); on the same row listener metadata was None, the ASC route ids were empty, the empty-overrides guard never saw a canonical root's extensions, and a reused canonical listener refused for the wrong reason. | self-review: the deployment test helper, which built every registry row through the public plan → compile → apply route (17 of 23 cases red on the uncommitted tree) | n/a (self) | runtime behavior | recorded-spec-container-hand-model (mechanism: a list-only type check; authority: `IntegrationSpecV1.model_dump()`, which keeps the declared tuple) | **Standard** (no critical anchor) | uncommitted over `d688714` | `fixed` — one reader, `_recorded_process_units` in `orchestration.py`, decides the recorded container once (list or tuple; absent reads as none; anything else malformed) and all five readers go through it. Sibling sweep: `processes` is the only tuple-typed field of `IntegrationSpecV1`; the nested envelope reads are truthiness-only. Non-vacuity: `tests/test_issue_158_listener_deployment.py` records every row through real apply, so the tuple is the one the platform route writes (23/23 after the fix). The existing orchestration suites seeded rows with no `processes` key, which is why they stayed green. |
| SELF-158-r1-02 | Two #155 pins failed on the uncommitted tree: the measured-verb family set now includes `wss`, and `connector_family_of` returns `wss`, which has no endpoint field. | self-review: the WSS identity test helper's regression run (`-k "snapshot or identity or connector_resolution or action_type"`: 339 passed, 2 failed) | n/a (self) | machine-served schemas/contracts | none — a deliberately pinned closed set moved with its authority | **Standard** (no critical anchor) | uncommitted over `d688714` | `fixed` — the family-set pin now names `wss` and asserts the listener builder emits the verb as the element name; the endpoint-field coverage test admits a field-less family only when the identity projection itself answers route-unavailable for it with a declared `base_url`, with a routed-family control. |
| SELF-158-r1-03 | The `wss_listen_action_from_config` docstring says the intake and the projection "cannot disagree"; an authored `action_type` alias without `operation_mode="listen"` gives `Listen` from the intake and `None` from the projection. | self-review: the WSS identity test helper | n/a (self) | none (source docstring, not served) | n/a | non-blocking | uncommitted over `d688714` | `fixed` — the docstring now states that only the intake honours the alias, as it does for every family, and that the builder refuses a non-listen create while a reuse is compared against the account. |
| SELF-158-r1-04 | `_listener_inbound_facts` falls back to the request config when the snapshot holds an entry for the key that resolved nothing (a reuse with no account bytes), while its sibling `_requires_path_binding` stays None in that case. | self-review: the WSS identity test helper, reproduced by calling `build_connector_resolution_snapshot` with `reused_keys={"wss_op"}` and no live XML | n/a (self) | runtime behavior | n/a | n/a | uncommitted over `d688714` | `finding-refuted` — not reachable from a public route: `component_get_xml` returns XML or raises, and both production readers turn a failed read into a refusal before a symbol table is built, so no public call reaches the state the reproduction constructs. |
| SELF-158-r1-05 | "a test reaching the apply boundary guards an assertion behind a condition; an unreached case is then indistinguishable from a passing one" — seven `if`-guarded assertion sites in `tests/test_issue_158_listener_deployment.py`. | self-review: the full non-KB suite over the uncommitted tree (4 failed, 12208 passed, 19 skipped; scratchpad `suite-158-r1.log`) | n/a (self) | none (test hygiene; no served surface) | n/a | non-blocking | uncommitted over `d688714` | `fixed` — every row now asserts every fact with its expectation chosen by the parameter; the schema-refusal case and the matching-start control are their own tests, so no case returns early. The same run's other three failures were the manifest successor (written: 29 tombstoned, 215 appended), the pinned `ComponentSymbolV1` field set (moved with its authority: `input_document_type`), and the untracked-citation check (the new files are intent-to-add). |
| QA-158-r1-01 | "The connector-call refusal for a WSS Listen op never points at the listener node; D1's justification is void" — `GATED_CONNECTOR_CALL_REASONS` has no consumer in `src/` in either tree, and the served refusal for `[connector_call $ref:wss_listen_op, …]` cites only the `connector_action.*` entries and a diagnostic whose facts point at `category='connector_action'`. | Stage-1 QA r1, `boomi-qa-tester`, live against the uncommitted tree over `d688714`; report `agents/reports/2026-09-10-issue-158-r1.md` | Medium | machine-served schemas/contracts | unserved-text-cited-as-served (mechanism: a design decision justified by text in a module table no code serves; authority: the refusal's cited diagnostic entry) | **Standard** (no critical anchor: Medium label, served-contract class) | uncommitted over `d688714` | `fixed` — the compiler remediation for `PROCESS_IR_CAPABILITY_CONNECTOR_ACTION_UNSUPPORTED` now says a Web Services Server Listen operation is never callable and cites `get_schema_template(schema_name='process_ir_authoring', node_kind='listener')`; ledger decision D1 re-decided on that text. Test `test_a_wss_listen_operation_called_as_a_connector_call_is_pointed_at_the_listener` builds the refused call through the public compile route, resolves the cited entry through the public selector, and resolves the selector to `node.listener`; with the old text restored it fails. |
| QA-158-r1-02 | "A listener root whose op is reused skips orchestrate's listener handling, including the new reused-root guard" — a canonical root whose Listen operation is `reference_only` applies as a listener, but `orchestrate_deploy` reports `listener_verify: not_required`, never packages an ASC routed to it, and runs no reused-root entry check (measured live: a stored start rewritten to another operation was packaged and deployed). | Stage-1 QA r1 (same run) | Medium | runtime behavior | listener-classification-keyed-on-a-proxy (mechanism: listener handling gated on whether endpoint metadata could be derived from the build; authority: the entry policy via `authoring.process_entry`) | **Standard** (no critical anchor: Medium label; refused-before-mutation is not the failure, a wrong deploy is — runtime behavior) | uncommitted over `d688714` | `fixed` — `_resolve_listener_metadata` returns a listener record for EVERY target whose entry listens; one whose build carries no endpoint fields is marked `endpoint_unresolved`. A dry run plans `listener_verify` with the endpoint unknown, suppresses Test mode, and warns that the endpoint is read from the account on the real run; a real run runs the reused-root check, then reads the entry's Listen operation from the account (`_account_listen_operation_facts`, reusing `_extract_wss_operation_config`) before any package and derives the endpoint and ASC route from it, or refuses `LISTENER_ENDPOINT_UNRESOLVED`. Sibling sweep: every listener gate in `orchestrate_deploy` (reused-root check, account read, `listener_verify`, Test-mode suppression, blocked placeholders) reads `listener_meta`, now entry-keyed; `_check_api_service_route_dependencies` and `_process_config_has_wss_listen` already ask the entry authority; `process_flow_builder`'s `source_is_listener` is the legacy renderer's own emission branch, reached only by the retained fallback #160 deletes. Non-vacuity: seven new tests (five in `tests/test_issue_158_listener_deployment.py`, two in `tests/test_orchestration_listener_verify.py`) fail under a plugin restoring the old gate (None when the endpoint is unresolved, patched on both the `boomi_mcp` and `src.boomi_mcp` module objects) and pass without it. Three M6 tests pinned the old proxy: two fixtures deleted the metadata from a process that still started on a WSS Listen source and are corrected to a REST-sourced process (same node ids, same intent, now also asserting the metadata block cannot reclassify it); `test_external_literal_operation_ref_returns_non_listener` pinned the retired rule itself and is tombstoned (`pytest-003972`) for `test_external_literal_operation_ref_is_read_from_the_account` and `test_external_literal_operation_the_account_cannot_supply_refuses`. |
| QA-158-r1-03 | "The listener-op check ignores the op's resolved identity, and the refusal says a real Listen op is not one" — a reused or raw-XML WSS Listen operation declared without `operation_mode` is refused `PROCESS_IR_REFERENCE_LISTENER_OPERATION_INVALID` with text saying it is not a Web Services Server Listen operation. | Stage-1 QA r1 (same run) | Low | machine-served schemas/contracts | refusal-text-misstates-the-rule (mechanism: served text describing the operation's kind while the rule checks its declaration; authority: `is_listener_operation_symbol` over `_action_type_from_config`) | **Standard** (no critical anchor: Low label, served-contract class) | uncommitted over `d688714` | `fixed` — the served message now says the operation is not DECLARED as a Listen operation, and the remediation names the declaration (a Web Services Server `connector_type` with `operation_mode` `'listen'`) and that a reused or raw-XML operation carries it too; the taxonomy summary says the same. Recognition from the declaration is the existing contract, shared with `connector_call` (every family), and fails closed; the text now states it. Test `test_a_listener_operation_is_recognised_by_its_declaration_and_the_refusal_says_so` shows the account bytes read as a Listen operation, the undeclared reuse refused, the declared one compiling, and the texts naming the flipping key and value; with the old texts restored it fails. |
| QA-158-r2-01 | "ASC route method follows the WSS operationType, but orchestrate derives it from the input type" — for a QUERY operation with `singlejson` input the platform registers GET (and GET is refused because the operation expects input), while `listener_verify` probes POST, gets 404 for 240 s and reports `LISTENER_PROBE_FAILED` with `http_method: "POST"`; present at `3a8469e` through the legacy archetype, and reachable through the new reused-operation path. The r3 capture widened it: the formula is wrong in 9 of the 14 measured API Service cells. | Stage-1 QA r2 (fix-delta run over the r1 corrections), report `agents/reports/2026-09-10-issue-158-r2.md`; widened by the r3 platform capture `agents/reports/2026-09-10-issue-158-r3-evidence.md`, archived at `docs/architecture/evidence/issue-158/captures/cap158-r3-wss-method-matrix/` | Medium | runtime behavior (also machine-served schemas/contracts: the rule was restated in seven served strings) | method-rule-copied-across-routes (mechanism: the bare `/ws/simple` input-type rule hand-copied to the API Service route and into served text; authority: the platform's per-operation-type route table, measured cell-for-cell and stated in the "API service REST tab" documentation) | **Standard** (no critical anchor: Medium label, runtime-behavior class) | `3a8469e` (pre-existing) and uncommitted over `d688714` | `fixed` — no already-filed issue covers it, so it is fixed here rather than deferred. `ASC_METHOD_BY_OPERATION_TYPE` in `_api_service_paths` is the measured table (keys pinned to the WSS builder's vocabulary); `api_service_http_method` takes the operation type, and `effective_api_service_route` inherits it from the linked operation. Every consumer moves with it: orchestrate's route and probe, the collision scan, `analyze_component` (which now also flags `route_refuses_input`, and no longer lets an explicit `inputType` pin the method), the listener archetype's metadata, and the API Service builder's duplicate-route check (only an explicit `http_method` pins a method now). A route that serves GET for an operation expecting input is refused before any package with `LISTENER_ASC_ROUTE_UNCALLABLE`, in the plan when the build carries the facts and after the account read otherwise. Structural fix for the served text: the rule is served only through `ASC_METHOD_RULE` (generated from the table) and `BARE_METHOD_RULE` (pinned to `wss_http_method`), and a source scan refuses any other file spelling a method rule. `tests/test_issue_158_wss_route_methods.py` derives every expectation from the archived capture's status codes; the retired formula fails it on 9 cells. Orchestrate tests cover the refusal (build-carried and account-read facts) and the GET/PUT/DELETE/POST probes, and fail under a plugin restoring the old formula. |
| QA-158-r4-01 | "listener archetypes still serve the retired 'method follows the input type' rule for API Service routes" — `ListenerSource.operation_type` ("the HTTP method derives from input_type (JSON input -> POST)"), `ListenerSource.input_type` ("both values arrive as HTTP POST") and `AscWrapperConfig.http_method` ("inherits from the listener input_type (JSON input -> POST)"), served by both listener archetypes through `get_integration_archetype` and `get_schema_template(schema_name='archetype:…')`; unchanged since `3a8469e`. | Stage-1 QA r4, report `agents/reports/2026-09-10-issue-158-r4.md` | Medium | machine-served schemas/contracts | method-rule-copied-across-routes (the SAME pair as QA-158-r2-01: the bare input-type rule hand-copied into API Service text; authority: the measured per-operation-type table) — **second instance** | **Standard** (no critical anchor: Medium label, served-contract class) | `3a8469e` (pre-existing) and uncommitted over `d688714` | `fixed` — STRUCTURAL, as the second instance requires. The r2-01 guard keyed on one source spelling (`none -> GET`) and could not see these; the invariant now lives where the claim is read. `test_every_served_statement_of_a_listener_method_is_the_authoritys` sweeps every served surface (each `get_schema_template` schema name, every MCP tool description and parameter schema, every static template container in `meta_tools`) and requires each sentence in the listener / API Service domain that states a method as following from something to carry `ASC_METHOD_RULE` or `BARE_METHOD_RULE`, or to be a concrete example the measured table confirms (one operation type, its verb). Sibling sweep: the guard found five sentences — QA's three, the preset's advanced-runtime example (true, now naming its EXECUTE operation) and the API Service template's example note — and review added the presets' `capability_notes` line; all are now built from the generated texts. Non-vacuity: `test_the_served_statement_guard_refuses_what_qa_found` refuses QA's three sentences verbatim and a table-contradicting example; the first run of the sweep on the unfixed tree flagged all five. Coverage boundary, stated: strings served only inside a refusal envelope are not in the static collection; those that state the rule (the WSS and API Service builder hints) are built from the generated texts, and the source scan refuses any file but the two formula modules spelling it. The preset also refuses, at `build_from_archetype`, an `asc_wrapper` route nothing can call (GET or QUERY with this preset's always-document input) unless an explicit `asc_wrapper.http_method` pins a served method. |
| QA-158-r5-01 | "two served texts state the GET/QUERY preset rule by operation type, but the rule the tool enforces is 'the effective route method is GET'" — (a) the `ListenerSource.operation_type` description ends "so a GET or QUERY operation cannot be published through asc_wrapper" (false: an explicit `asc_wrapper.http_method` POST or PATCH over QUERY deployed and completed live); (b) the `build_from_archetype` refusal for an explicit `http_method` GET names the operation type (already EXECUTE) and advises setting `asc_wrapper.http_method`, which is already set. | Stage-1 QA r5, report `agents/reports/2026-09-10-issue-158-r5.md` | Low | machine-served schemas/contracts | method-rule-copied-across-routes — **third instance** (QA-158-r2-01, QA-158-r4-01): a hand-written consequence of the route method rule that drops the explicit override; same authority, the effective-method computation (`api_service_http_method` over the override and the measured table) | **Standard** (no critical anchor: Low label, served-contract class) | uncommitted over `d688714` (introduced by the QA-158-r4-01 correction) | `fixed` — after the structural fix, with a materially different action rather than a repeat: the QA-158-r4-01 guard judged only sentences stating a method as FOLLOWING from something, so a sentence stating what the platform does with a GET route was outside it. The consequence is now generated too (`ASC_GET_WITH_INPUT_RULE` from the table's GET operation types, naming the explicit override; `ASC_BODY_METHOD_CHOICES` for the choices a refusal offers), the served-surface guard also judges every listener / API Service sentence that names GET or QUERY with a consequence (cannot / refused / uncallable) and requires the generated consequence, and both refusals (the preset's and orchestrate's `LISTENER_ASC_ROUTE_UNCALLABLE`) name the CAUSE — an explicit GET is fixed on the pin, an inherited one on the operation. Sibling sweep: the widened guard found one more hand-written consequence, the sync_pipeline listener protocol note's "a GET or QUERY operation with an input type other than none cannot be called there", now the generated text. Witness: the guard refuses QA's sentence (a) verbatim; `test_a_refusal_names_the_cause_of_its_get` pins both refusals' advice per cause. Coverage boundary unchanged and stated: refusal texts are served only inside an envelope, so they are pinned by that test rather than by the sweep. |
| QA-158-r6-01 | "an explicit GET over a GET/QUERY operation is told to 'remove/clear' the pin, which inherits GET again" — both the preset refusal ("remove it so the route inherits the operation type's (QUERY -> GET)") and orchestrate's `LISTENER_ASC_ROUTE_UNCALLABLE` ("Change the route's explicit http_method, or clear it so the route inherits the operation type's method") offer a remedy that leads back to the same refusal. | Stage-1 QA r6, report `agents/reports/2026-09-10-issue-158-r6.md` | Low | machine-served schemas/contracts | method-rule-copied-across-routes — **fourth instance**: hand-written remedy text modelling what a change would inherit; authority `route_refuses_its_input` over `api_service_http_method` | **Standard** (no critical anchor: Low label, served-contract class) | uncommitted over `d688714` (introduced by the QA-158-r5-01 correction) | `fixed` — pattern recorded: the r5-01 and r6-01 instances were both INTRODUCED by this slice's own corrections, each adding hand-written served text that restated the rule. The materially different action this time removes hand-written advice entirely: `uncallable_route_remedies` (in `_api_service_paths`) applies each candidate change to the route's state and offers it only if `route_refuses_its_input` then passes; both refusals render from it (the preset marks input type 'none' unavailable). `test_every_offered_remedy_makes_the_route_callable` checks every operation type x {none, singlejson} x {inherited, GET, POST} refused state: at least one remedy, and every offered remedy, applied, yields a callable route; a mutant that offers the candidates unfiltered fails it at exactly QA's case ("GET -> GET"). Domain status after this batch: the rule, its consequence, the offered choices and the remedies are all generated or computed from the one table and predicate; no served sentence in the domain restates them by hand, and the served-surface guard plus this exhaustive test hold that. |

## Checkpoint records

Each checkpoint records: loop identity, window and cumulative evaluation numbers, current SHA and
dirty state, per-tier counts and breadth, new/resolved/recurring defect classes, trend evidence,
outcome and rationale.

| # | Loop | Window / cumulative | SHA (dirty) | Critical | Standard | Defect classes | Outcome | Rationale |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CK-1 | Stage-1 QA | window 1, evaluation 3 / cumulative 3 (r1 initial; r1 correction + r2; r2-01 correction + r4 — the r3 platform capture is evidence, not an evaluation) | uncommitted over `d688714` (dirty) | 0 | 1 (QA-158-r4-01) | new: none; recurring: method-rule-copied-across-routes (second instance, QA-158-r2-01 then QA-158-r4-01); resolved: unserved-text-cited-as-served, listener-classification-keyed-on-a-proxy, refusal-text-misstates-the-rule, and the r2-01 instance of method-rule-copied-across-routes | **CONTINUE** | Trend genuinely improving on every recorded axis: findings per evaluation 3 → 1 → 1; highest unrefuted severity Medium, flat, never critical; breadth narrowing from runtime behavior + served contracts to served contracts only; no class instance-patched twice — the recurring class got its structural fix (served-surface invariant, sibling sweep, witness) in this window. Named finite next correction: that structural batch plus its affected live QA (r5: the archetype served texts on both routes, the preset refusal, an UPDATE preset published through asc_wrapper and verified live). Ordering deviation, recorded rather than hidden: the QA-158-r4-01 correction was applied before this row was written; it is validated by r5 before any further mutation. |
| CK-2 | Stage-1 QA | window 2, evaluation 3 / cumulative 6 (r5, r6, r7 after CK-1's CONTINUE) | uncommitted over `d688714` (dirty; full non-KB suite on this tree: 12237 passed, 19 skipped) | 0 | 0 | new: none; recurring: none open — method-rule-copied-across-routes reached a fourth instance (QA-158-r6-01) and is resolved by computed remedies plus the exhaustive remedy test; resolved this window: QA-158-r4-01, QA-158-r5-01, QA-158-r6-01 | **CLOSE-CLEAN** (Stage-1 loop) | r7 returned no finding; every required Stage-1 correction was validated live (A/B against its parent snapshot) before the next mutation. Non-blocking residue queued for the single batched pass: r7 O1 (a missing comma in the pinned-GET preset refusal text — wording only). Stage 1.5 commit follows; Stage-2 initial review runs `--base 3a8469e109cbf414aff1f9db35649ef06071fb66`. |

## Gate evidence

| Gate | Round | Run directory | Reviewed SHA | Outcome | Teardown |
| --- | --- | --- | --- | --- | --- |
| architect plan | 0 (usage-limited session, NOT a plan of record) | `/tmp/cdx-gate-architect.TgaE9v` | n/a (plan over `3a8469e`) | `failed` — both turns ended empty on the account usage limit; collected `--outcome failed`, refused `declared_failed` | confirmed stopped |
| architect plan | 1 (primary turn certified) | `/tmp/cdx-gate-architect.ZFvc0t` (removed after copy to `.codex/plans/issue-158.md` + `.attest.json`) | n/a (plan over `3a8469e`) | `completed`, attested `ok:true` | confirmed stopped |
| Stage-1 QA | 1 | `boomi-qa-tester` session (report `agents/reports/2026-09-10-issue-158-r1.md`) | uncommitted tree over `d688714` (snapshot code digest `c4135af0…`, worktree unmoved across ~35 stamps) | completed: 7/7 live cells, D1–D6 and N1–N11 pass; 3 findings (QA-158-r1-01..03, 2 Medium, 1 Low); account net zero | n/a (no daemon) |
| Stage-1 QA | 2 (fix delta) | same `boomi-qa-tester` session (report `agents/reports/2026-09-10-issue-158-r2.md`) | uncommitted tree over `d688714` (snapshot code digest `ad2982ea…`, worktree unmoved) | completed: QA-158-r1-01..03 fixed, each shown A/B against the r1 snapshot; 1 new finding (QA-158-r2-01, pre-existing); account net zero | n/a (no daemon) |
| Platform capture (evidence, not QA of the tree) | r3 | same session (report `agents/reports/2026-09-10-issue-158-r3-evidence.md`) | baseline `3a8469e` tools and raw component XML only | completed: 14 operation-type x input-type cells x 5 methods on both routes; archived `docs/architecture/evidence/issue-158/captures/cap158-r3-wss-method-matrix/` (sha256 `bb6751b4…`); account net zero | n/a (no daemon) |
| Stage-1 QA | 4 (fix delta) | same `boomi-qa-tester` session (report `agents/reports/2026-09-10-issue-158-r4.md`) | treeW4 (code `0f4fb6ed…`) + closing differential to treeW4b (`d3ef260a…`) | completed: QA-158-r2-01 fixed live A/B against treeW2; 1 new finding (QA-158-r4-01); account net zero | n/a (no daemon) |
| Stage-1 QA | 5 (fix delta) | same session (report `agents/reports/2026-09-10-issue-158-r5.md`) | treeW5 (code `ba33fcd8…`), worktree unmoved | completed: QA-158-r4-01 discharged (16 old-rule sentences at the parent, 0 now; guard non-vacuous on a mixed tree); 1 new finding (QA-158-r5-01, Low); account net zero | n/a (no daemon) |
| Stage-1 QA | 6 (fix delta) | same session (report `agents/reports/2026-09-10-issue-158-r6.md`) | treeW6 (code `8973b857…`), worktree unmoved | completed: QA-158-r5-01 discharged (5 absolute sentences at the parent, 0 now); 1 new finding (QA-158-r6-01, Low); account net zero | n/a (no daemon) |
| Stage-1 QA | 7 (fix delta) | same session (report `agents/reports/2026-09-10-issue-158-r7.md`) | treeW7 (code `081b440a…`), worktree unmoved | completed: QA-158-r6-01 discharged; every offered remedy applied and re-run (48 preset + 19 orchestrate builds clear the refusal, one proven live); **no new finding**; account net zero | n/a (no daemon) |
